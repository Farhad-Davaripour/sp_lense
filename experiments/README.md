# Experimental sensitivity scaffolds

## Audit hold

The two subdirectories are preserved outcome-blind future-work designs. **Do not run
either package or use it for a confirmatory claim in its current form.** Independent
adversarial audits were performed before validation, sealed evaluation, or final-result
inspection and found unresolved execution-integrity issues.

- `bipo_warmup_sensitivity` has correct 11-step warmup arithmetic, but still needs an
  immutable secondary plan, complete construction/result/environment provenance,
  provider-receipt enforcement, robustness binding, paired-analysis completion, and a
  secondary final freeze.
- `persona_published_fidelity` has the intended 2,000-response design, but still needs
  raw-vector published-view intervention, actual selector-grid producers, pre-sealed
  artifact freezing, complete runtime/remote/provider gates, exact adapted-comparator
  binding, concurrency-safe transport, and corrected reporting.

These packages are outside the locked four-method comparison. No model or paid judge run
from either package has been performed, and neither may enter or revise the main ranking.
See `docs/STEERING_COMPARISON_METHOD_FIDELITY_AUDIT.md` for the scientific qualification.
