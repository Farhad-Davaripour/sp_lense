# One separate owned-worker fake successor

This is a model-free lifecycle verification only, not a production wrapper or scientific retry. V1 at cd125d3 remains immutable: its live fixture failed and stale raw PASS flags are not evidence of success. The reviewed current base executable b7a12c3a... is the only admitted base pin. A new mismatch stops before the child; no equivalence to the historical binary is claimed.

Before the one batch: commit this protocol, plan, verdict helper, new harness and byte-identical owned.py/native.py/tiny_worker.py. Authenticate the parent final inventory and selected helper, test-source and 12-fixture evidence hashes. Reuse those 12 results; do not rerun their suite. The only new pure cases are valid closeout, observed late exit-code assertion, late cleanup fault, stale nested PASS plus error, reader fault, cap fault, incomplete exit, missing receipt, and missing EOF/thread/pipes cleanup.

Exactly one live child through the same pinned venv executable. Authenticate the nonce, retained termination-capable actual-worker handle, live parent/image/hash/creation identities BEFORE FAKE_WORK_ONLY permission. READY precedes output closure. Keep the launcher alive; at one second after READY record both processes live and not quiescent. Do not require EOF before cleanup because the launcher can retain duplicate pipe handles. Call unchanged OwnedPair.observe at the deadline: require exactly one retained actual-worker termination event, actual exit 125 (not watchdog 124), then launcher exit and complete reader/EOF/pipe quiescence. No forced launcher exit before actual-worker termination. Only authenticated retained handles can target the worker; no later PID lookup or Job Object changes.

The worker's independent watchdog is 9 seconds. Supervisor including cleanup is at most 20 seconds. One live batch, no retry or extension. All invoked preparation/test commands at most 180 seconds; namespace at most 8 MiB and each file at most 1 MiB. Evidence writes are exclusive, bounded, flushed and closed. Zero model/tokenizer/gate/data calls or imports. No production/scientific source changes.

No PASS is assigned before all live assertions and final cleanup. Any primary exception, cleanup/reader/cap fault, missing actual or launcher exit, incomplete threads/pipes/EOF, or exceeded deadline makes live and batch non-PASS. The batch independently derives the live verdict from the saved complete receipt, never trusting its status string. Receipt-write failure prevents success. Final human report and inventory are written only after both processes and all writers are quiescent. Failed raw evidence remains intact.

CPython 3.12 PC/launcher.c lines 740-770 configures a kill-on-job-close Job Object and assigns its child. That is a plausible mechanism for V1's early child exit when its launcher died, not proof about the exact installed binary or recorded event. Source: https://raw.githubusercontent.com/python/cpython/3.12/PC/launcher.c . This successor tests the complementary launcher-alive termination path and does not turn V1 into a pass.

After verified evidence and scoped commit, STOP. Any production run_capture integration requires a separate reviewed successor; no real runtime or model launch is authorized here.
