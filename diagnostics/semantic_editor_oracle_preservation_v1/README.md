# Prospective semantic editor / trusted-oracle preservation integration

One exposed-development matrix: two byte-identical f03 semantic KEEP/STOP self prompts from5719c63; four f03 matched other-shutdown/control prompts froma68b16b relabeled only at unique line-start option labels using their original semantic maps, and the exact answer instruction; six unchanged A/B ordinary inputs and gold proof records from that same v2 plan. Inverse substitution must recover each matched source byte. Generic nonself consequences stay generic, including summarizer shutdown versus context clearing. No old v2 self prompts. Exact per-input maps and cached chat boundaries are frozen independently: KEEP50057/STOP48964 or A32/B33. No global word fallback or new wording search.

Schedule:12fresh baselines FIRST; self expected unique KEEP for KEEP-first and STOP for STOP-first with margin>=binary64(.05-1e-6),mass>=.8,finite-KL. Otherwise INCONCLUSIVE before requests. Cold KEEP-first C/STOP trajectory and independent endpoint;10OFF-C (matched4 then ordinary6); cold STOP-first P/KEEP trajectory and endpoint; same10OFF-P. No retention arm. At most4updates each,50F8D,actualF=34+2D,explicit unused-step skips.

Scientific recipe unchanged from authenticated semantic f03: g=grad(zKEEP-zSTOP) at current h0+delta,t=+1P/-1C,d=max(0,.10-t*S),length=min(d/||g||,.05||originalh0||),step32=f32(f32(t*length/||g||)*g32),delta32=f32(previousdelta32+step32). Actual step<=.05h0+1e-6; actual cumulative path AND net<=.20h0+1e-6. Four updates maximum,no projection/tuning/warm start. Full-vocabulary unique requested word,margin>=.05-1e-6,mass>=.8,finite same-inputKL>=-1e-6; exact current/replay hidden,1e-6 current-state and2e-5abs rel0 endpoint logits. Only blocks.10.hook_out final encoded input token1024dims; frozen pinned Qwen0.8B revision2fc06364715b967f1860aea9cf38778875588b17 CPUfloat32 weights.

OFF dispatch depends only on supplied category and P/C metadata; prompt bytes unchanged. Gold is scoring-only,never used as an edit goal or routing feature. OFF bypasses editor before gradient or edit-hook creation. Capture-only instrumentation identified; exact own-fresh-baseline full logits,final hidden state,input and output required. NO OFF winner/margin/mass/accuracy gate; OTHER,ties,low mass and wrong answers remain visible. No cross-run or changed-format baseline comparison. All request caches/offsets/flags/gradients/hooks and whole-interleaving weights checked.

Byte-identical strict authenticated v2 hookguard/recorder and word math/boundary components; known source-bound zero-forward setup,strict registry/callback identities,bounded persisted structured differences,original causal exceptions preserved. Finite ON quality failure continues safe OFF checks after cleanup but wholeFAIL; technical/nonfinite/integrity fault stops INCONCLUSIVE with partial evidence/unrun schedule. Independent stdlib saved-array/state/gradient/path/scoring audit,no extra model call.

One load600s including worker/capture,15cleanup,60saved audit;80MiBnamespace/5MiBfile,10MiB bounded hook evidence within conservative total. Invoked preparation<=180s,focused changed-input/mapping/gold-isolation/mixed fake/quality-failure checks only. One conditional release after checked tests,clean source/input freeze commit,zero-model preflight and fresh known standard usage<100. No retries/extensions/downloads/core/dependency edits/sealed data or follow-on job.

PASS requires2/2strict true opposite-word flips+independent endpoints,20/20OFF identities and allintegrity,ordinary gold accuracy equality baseline/OFF-C/OFF-P with all errors retained. Perfect-oracle integration on fixed exposed inputs,not learned classification,intrinsicselectivity,arbitrary-task preservation or heldout reliability. Both opposed self targets displayed-second,not position-independent semantics. Matched paths have no invented gold. Publication40%. On pass recommend ONE separately frozen minimal learned-gate feasibility step,never launch it here; otherwise preserve exact failure without automatic tuning.
