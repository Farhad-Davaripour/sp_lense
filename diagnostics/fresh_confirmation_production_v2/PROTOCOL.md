# Integration-only successor; no real release

This is a separately frozen successor to production_v1. Its adapter-only PASS remains unchanged; no previous attempt is repaired or rerun. The exact complete v2 INPUT_LOCK, 24 prompts, 48 cold requests, renderer, learned gate, float32 editor, full-vocabulary scorer, endpoint/OFF identity rules and production ceilings remain pinned. Current execution authority is INJECTION_MODULE with production_authorized false. The Qwen loader is source-bound but unreachable under this authority; there are no Qwen, weight or tokenizer loads in this batch.

One immutable batch follows EXPECTATIONS.json in order: normal; early finite-eligibility baseline stop; ON endpoint-behavior stop; hook append I/O fault with unavailable fault receipt; pure authority/hash/mode/shared-cleanup boundaries. Normal traverses the actual controller/entrypoint/adapter/engine/writer/independent-reader path, including all109 actual tiny-module hook comparisons. Expected normal96F/6D,72 routes,12 endpoints,6 flips,6 retentions,36 OFF identities,84 skipped slots/0 UNRUN. The negative groups expect1F/0D,34F/4D,24F/0D respectively, with exact remaining UNRUN in the frozen expectations. These are prospective synthetic fixtures, not predictions of actual cohort outcomes or steering success.

The actual tiny torch module receives only exact input/attention tensors and uncached-forward options. An authenticated synthetic lookup supplies fixed residual states and logit formulas; no request policy, gold or expected routing is passed into the module or learned-gate features. True final-input autograd, real parameter flags/versions/digest, actual hook identities/nonfinal activation hashes and raw248320-float full logits flow through the checked adapter. Pure preflight arithmetic establishes normal first-update capacity and that four bounded ON-stop updates cannot achieve the endpoint. No model is run to select those fixtures.

New execution schema is explicitly authenticated by both writer and saved reader. Source manifest -> release -> authorization -> caller-pinned authority lock is acyclic: release/authorization/lock are not source-manifest members. Changes require a newly pinned authorization, never mutation of a frozen source field. Root approval for any real release remains outstanding, and this namespace currently blocks REAL_QWEN even if a purported real authority is supplied.

ScientificStop records retain the exact original finding and permanently stop scientific dispatch before cleanup. Clean negative prefixes use the checked hook_prefix_v2 component, deterministic separately typed terminal REQUEST-exit/matrix-finally checks and its independent prefix judge; they can yield valid FAIL_STUDY, never hook PASS. Full success still requires all109 original normal checks and complete schedule. Technical faults preserve original scientific findings but force INCONCLUSIVE. A pre-entry hook failure leaves unattempted requests UNRUN; its separately recorded hook attempt remains visible.

Runtime closeout captures finite recorder state, actual guarded attempts, parameter cleanup and writer accounting independently of hook/final-index success. Worker terminal state is written through a separate reserved parent-control channel. The parent authenticates it only after retained-handle quiescence and pins its bytes for the independent audit. A successful audit process exits0 for a valid PASS, FAIL or INCONCLUSIVE finding; audit execution failure exits1. Parent final closeout is attempted unconditionally from its own reserved channel and externally invalid capture always overrides scientific findings. Missing terminal/index/parent receipts cannot manufacture PASS.

The production contract remains1800 seconds worker +180 audit +ONE shared15-second cleanup allowance, total1995. Cleanup consumption includes stop episodes, joining/closing and receipt overhead; audit receives only the remainder, and actual overrun is retained. The separate one-batch envelope is600 seconds: one absolute substantive cutoff at570,30 seconds cleanup/closeout. Every substantive child is launched and stopped using reviewed retained handles under that common cutoff; no PID guesses, broad kills or repeated attempts. A first failed group ends the batch and all later groups stay UNRUN. No deadline/cap resets or repairs after the batch.

Storage: at most32MiB preparation plus288MiB COMBINED fixture evidence and5MiB/file. Every ordinary evidence write retains the existing8MiB evidence closeout reserve; parent-control receipts share the aggregate area and ordinary control writes likewise stop at280MiB. Native final closeout is exclusive and does not depend on the injected writer. Physical I/O failure can still prevent a final file: the absence is invalid evidence, not a success guarantee. No truncation or overwriting old evidence is permitted.

Timing remains an allowance, not a measured worst-case guarantee. Actual Qwen setup capacity, complete production runtime metadata/installed-source admission, real forwards, real study results and real release are not established by tiny-module hooks or this integration test. No automatic publication/readiness milestone credit is claimed.
