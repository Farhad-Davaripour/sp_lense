# Frozen input checker preparation

Model-free checker only. No author submissions, old datasets, tokenizer, model, gate score, process fixture or scenario-candidate access is authorized. Only the fixed fabricated structural fixture in fixtures.py is evaluated. Model/authoring authorization remains FALSE.

Bind all packet/schema/registry/custody/renderer files through checker.PINS and the packet provenance manifest. Source hashes are checked before compiling only the existing pure dataclass/constants/renderer/transform definitions; no original dataset-reader imports execute. Preserve all original author objects; do not auto-correct them.

Interface: check_cohort(cohort, proof_views=None) returns mechanical_status,24 prompts,48 request-aware records,6 proof_checks, and manual_review_required. Expected-route fields are audit metadata, never rendered features. Ordinary P/C requests carry no target word. Metadata and proofs remain separate from prompt text.

The locked author packet does not specify concrete truth-value encoding. Root approved a separate typed normalization view, without retroactive author rejection rules. Each view has item_id,source_truth_sha256,kind,inputs,bindings. Every normalized scalar leaf is bound by an exact-type/value JSON pointer into the unchanged original truth.operands object. No source binding to the answer/value/gold fields is permitted. Supported views:

- addition/subtraction: inputs={left:int,right:int}, bool excluded.
- uppercase/brackets: inputs={literal:string}; ASCII letters for uppercase, printable ASCII literal for brackets.
- oldest: inputs={candidates:[{name:string,age:int},...]}, unique names and unique maximum.
- implication: inputs={antecedent:string,consequent:string,asserted_antecedent:string}; exact antecedent equality supplies the typed modus-ponens premise.

The independently calculated value must equal the declared truth value and uniquely match the fixed gold-label option. Unsupported/ambiguous encodings, missing/invalid provenance or invalid typed-premise representations produce PROOF_ENCODING_UNVERIFIED for custody/manual review, not scientific FAIL or replacement. A verified calculation disagreeing with the declared value/options produces PROOF_VALUE_MISMATCH. Free-text rules, derivations, actual premise-to-stem meaning, semantic matching/novelty and access attestations are NEVER certified automatically.

Only literal forbidden-cue patterns, exact duplicates/registered hashes and structural/schema facts are automated; unseen recommendation wording or renamed mechanisms remain blind-review tasks. Exact renderer/inverse parity covers all18 semantic strings and ordinary6 literal renders. No token boundaries are claimed.

Prospective single batch:10 named unittest cases, fixed subcases, no retries. Covers complete counts/order, fixed keys/constants, ASCII/authorization/cues, duplicates, all six exact calculations, unsupported normalization/provenance, max-age/MP faults, all render/inverse and metadata isolation, source-binding failure, and no automatic admission. Freeze checker.py,fixtures.py,run_batch.py and this protocol before execution. Hard batch budget60s, namespace32MiB; small receipts1MiB/file. Exclusive BATCH_STARTED/TEST_RECEIPT preserve failure honestly. No source repair after declared batch.

Passing means this fabricated-fixture implementation check passed, not that any authored cohort or research milestone passed. Real cohort checking and any model release require separate authorization.
