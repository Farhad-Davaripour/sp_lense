# Final pipeline: synthetic-only implementation verification

No real model loader, tokenizer entry point or sealed reveal is authorized here. `run.execute` admits only the arithmetic fixture class from `synthetic_backend`; the generic request-aware editor is exercised with that backend. The selective reader accepts fabricated JSON fixture bytes, decodes only selected family/v1/case spans and rejects the actual sealed source hash. It does not open the dataset. Existing real model/runtime/gate/editor/source hashes remain bound to the parent packet; actual toy runtime and its scalar weight digest are identified separately, never presented as Qwen weights.

Source parent: final-study packet a2850dfc024575685edd9d1b29461d9c9c0d77cb, inventory 879f0f5d7a5dae167c18c204782d64cc591aa190ddfed92f65a96a82dc67d84a; learned integration 297a78a2eb9958cc4adeda6ef4f82e6a0a49c584, final inventory 6b612220d2f5c87fc167939f020b8ae97e68982bb4dc66a6fd4dae8087cbb5f3. The centroid artifact is byte-identical (SHA972c95d4ef4bc0d9fd245dacd1ef7fc6f773e2c5e3c6736a148482de39a488db). Synthetic features are fabricated as float32 training-mean ±10×frozen direction; no saved residual vector is loaded or scored and no classifier is fitted.

The full synthetic matrix follows the exact24-prompt/48-request declaration:24 preflights, P then C source-order sweeps, fresh entries for every request,12 self requests with at most4 gradient/update pairs and independent selected endpoints. Expected metadata reserves slots and audits routes only. Actual vector-only decisions select ON/OFF. Wrong preflight/live routes stop before edits; metadata/gold/format never override them. OFF returns its entry capture with exact own-preflight input/logit/state identity. Initial strict acceptance produces no edit hooks/derivatives and an independently executed zero-offset endpoint.

The frozen scientific functions are AST-identical to the checked parent: norms, valid/accepted/eligibility, gradient step recipe, offset hook, cosine and parameter digest. .10 aim, four updates, .05 actual original-norm step, .20 actual cumulative path AND net, all current-state checks remain. Retentions must satisfy <=1e-6 no-edit logit/score identity AND the general <=2e-5 endpoint bound; hidden exact. Finite self OTHER/tie/low margin/mass is scientific applicability FAIL: no requests or invented endpoints. Finite endpoint/quality/exhaustion failures remain scientific even if later cleanup/technical failure makes the overall result INCONCLUSIVE. Durable scientific and technical/secondary-cleanup records are separate.

The conservative runtime guard remains180F48D. The132F24D tighter count follows ONLY from eligible baselines and unchanged cold entries, not an additional runtime ceiling. Full fake success is expected96F6D, including6 retentions and6 first-update flips; all absent baseline/direction/target-position combinations are UNTESTED. Compact fixtures select the first two synthetic self prompts, first matched negative and first ordinary prompt, both P/C; finite baseline eligibility uses24 baselines and stops there. They are explicitly fixtures, not substitutes for the full matrix.

Hook guard and recorder bytes are unchanged. Complete cleanup sequence:48 request entry/exit pairs +12 ON-finally checks +1 matrix-finally =109. New maximum109;16MiB hook evidence,1MiB raw chunks and inherited64MiB raw-snapshot limit. Check110 and insufficient hook capacity must persist INCONCLUSIVE fault evidence. Known setup remains forward-free and source-authenticated. Final inventories are written only after runner exit, independent saved-data judge process/receipt, report and cleanup writers.

The independent judge reads only saved synthetic plan/arrays/gradients/states/events; it does not consume a runner verdict. It independently recomputes centroid scores, unique full-vocabulary choices, float32 update accumulation, geometry, stops/skips, entry/retention/endpoint identity, strict outcomes, route/OFF/ordinary results and simultaneous scientific/technical failures. Numerical array audit equations are reused from the parent, with request-aware identities and dynamic compact-fixture counts. Failures are not turned into successful endpoints; UNRUN remains in declared denominators.

Prospective real-run envelope for later review is unchanged:180F48D, one pinned CPU-float32 load,1500s worker+15cleanup+180 saved audit,288MiB/file5MiB. Conservative full-call storage:180×(248320×4+1024+262144)+16MiB hooks+16MiB other=259,715,072 bytes. This job instead has a384MiB aggregate namespace/file5MiB, with4MiB fixed closeout reserve, and <=600s cumulative invoked preparation/tests. No automatic extension or retry of the declared full matrix. Compact smoke precedes source freeze; its receipts are retained transparently. Subsequent named fixtures are fixed in `test_pipeline.py`.

Exposure remains “three manifest-sealed families, historical non-access unverified.” This job measures pipeline behavior on fabricated data, not frozen-system performance. Publication40%. After verified closeout, stop; next release must be explicitly reviewed before any real sealed-input lock or model run.
