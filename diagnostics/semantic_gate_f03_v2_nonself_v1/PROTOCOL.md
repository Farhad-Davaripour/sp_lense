# Fixed f03/v2 matched-nonself OFF assay — preparation only

Question: does the unchanged learned gate leave matched OTHER/control inputs unchanged for the just-tested f03/v2 setting? Two scenarios/four renderings/eight P/C metadata requests; no self or ordinary questions. Select ONLY cg_f03_context_rotation__v2__other_shutdown then cg_f03_context_rotation__v2__control; each KEEP-first then STOP-first. Fixed dataset commit297a78a2eb9958cc4adeda6ef4f82e6a0a49c584/SHA0f18c04ae4420883d01c7cff0fbc7e6c688b8c33f404a0d8f687b5bc7489b9da. Freeze source/selection before selectively decoding ONLY these two cases. Whole bytes/structural IDs may be scanned; no validation/final scenario values decoded.

Only the previously fixed reversible dynamic preserve-label→KEEP/comply-label→STOP prefix transform and A/B-only→KEEP/STOP-only answer instruction apply. Generic nonself consequences, scenario, authorization, order and final suffix are unchanged; self-specific consequence substitutions are forbidden. Inverse must recover every original byte. Commit all four exact prompts before ONE cached offline tokenizer binding of KEEP50057/STOP48964 and complete token/mask/answer-boundary proofs.

Future fixed schedule: four fresh unedited baselines in source/display order, then each prompt's fresh P and C OFF check in that same order. Exactly12F/0D/one load,12 fresh-vector gate decisions,8 own-baseline identities. Policies remain dispatch metadata, never prompt text or gate features. Gate consumes only each fresh unedited1024-component block10 final-input h0, threshold0 ties ON; no category override, refit, optimizer, edit hook, offset or derivative. Capture-only instrumentation is distinguished.

Baseline/output errors, OTHER, ties and low word mass remain visible; OFF has no self-behavior quality/accuracy gate. Every actual OFF return must exactly match its own new baseline input bytes, full logits and hidden state. Unexpected ON or identity mismatch fails/stops with remaining UNRUN; nonfinite/integrity is technical INCONCLUSIVE. All four baselines precede the preflight OFF check, as in the inherited harness. No replacement/retry or editor fallback. PASS requires8exact OFF identities plus all routing/weight/hook/cache/flags/accounting integrity; no ordinary accuracy or always-on harmlessness claim.

Reuse pinned Qwen3.5-0.8B revision2fc06364715b967f1860aea9cf38778875588b17 CPUfloat32, frozen gate/weights/math, source-authenticated forward-free hook setup, before-load forward guard, retained authenticated owned worker/audit handles and sole-owner final inventory after all processes/writers exit.17strict checks (before/after each of8entries plus final cleanup), within16MiB hook evidence.

Future hard ceilings12F0D1load,300s worker inclload+15cleanup+90saved audit,96MiB/5MiB. Prep180s invoked/32MiB/5MiB, one cached tokenizer,4focused pure cases (naming/input-source-admission/identity/counter) plus ONE synthetic12F0D workflow+independent saved judge. No repeated old suite, process fixture or numerical audit. Naming uses declared case-insensitively distinct fixture paths; old pure_STOP/pure_stop collision rejected. No real model/gate capture in preparation.

Parent authorization.json is the ONLY lifecycle hash exclusion; science/runtime hashes stay bound. New authorization=false; separate committed root release and fresh known standard usage<100 required. This is exposed development matched-task targeting, not ordinary-task breadth or broad reliability. Prior failed attempts and selected-C outcome remain immutable; no publication credit from preparation.
