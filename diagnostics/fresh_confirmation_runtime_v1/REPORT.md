# Actual-input-bound candidate: fake execution only

Result: `PASS_CANDIDATE_FAKE_ONLY`. Exactly one prospectively locked success-only fake batch ran through `controller.py`, the candidate entrypoint, fixed schedule, actual filesystem recording, and independent saved-evidence reconstruction. No retry, failure-suite repetition, actual model load, tokenizer load, or real forward occurred. This is a component/workflow result, not confirmation-study evidence or publication-milestone credit. Production authorization remains false.

## Prospective bindings

The source/input preparation was committed before dispatch at `5c9deadf789710700a1d9408bb214dff1e5362d3`; its 21 files were checked for raw working/Git byte equality before dispatch. `SOURCE_FREEZE.json` binds 20 source/input files totaling 345,517 bytes; all 20 remained unchanged after the batch.

- Source-freeze SHA-256: `f4a6b7791a452f20c5a69640585183f108930e18875596a487e7ae2a30c799be`.
- Bound-plan SHA-256: `7bf942482de628a18d532a55ab087b947dbb90a0dadec685d8d3b2a07581dfdd`.
- Complete v2 input commit: `01de65cc93b128d605ee1d66b8ccaebc6f6eda40`.
- Complete v2 INPUT_LOCK SHA-256: `d008fb7c53b974694e76afa9ef7db3f4f2d4443eed8a2c83ba06ea6e83987b22`.
- Exact admitted inputs SHA-256: `f62399de185341ff9380318f390b17c271de7480b0ce7ca1c22c85e5e6a94785`.

The initial prospective protocol's dependency-pending language describes the required preparation gate, not the state at execution: `DEPENDENCY.json`, `PREPARATION_RECEIPT.json`, and `PREBATCH_COMMIT.json` document that the committed complete v2 lock was authenticated before freeze and dispatch. Its 24 complete token proofs join the unchanged 24 prompts and 48 cold requests; maximum input length is 150, below the unchanged 160-token limit. The candidate has no encode fallback. O06 remains explicitly manual-review-bound with machine proof encoding unverified; it has not been silently recertified.

The fixed fake fixture is keyed by exact input-tensor hashes. The backend receives token IDs, attention mask, execution phase, and edit offset, not prompt metadata, categories, requested policy, expected routes, or gold. Audit/schedule metadata does not enter the frozen learned gate's fresh-state-only feature interface. Synthetic fresh states and outcomes are deliberately prescribed fixture behavior, not predictions of this real-text cohort.

## Measured synthetic reconstruction

The independent saved reader and judge returned `PASS_SYNTHETIC_ONLY` and complete I/O, with no scientific or technical failures:

| Quantity | Observed |
| --- | ---: |
| Baselines / cold requests | 24 / 48 |
| Fake forwards / derivatives / fake backend loads | 96 / 6 / 1 |
| Routes / self endpoints | 72 / 12 |
| Actual synthetic flips / already-correct retentions | 6 / 6 |
| Exact OFF identities | 36 |
| Schedule cells DONE / SKIPPED / UNRUN | 96 / 84 / 0 |
| Requests DONE / UNRUN | 48 / 0 |

Synthetic ordinary accuracy was 3/6 at baseline, P, and C, with the same synthetic errors and exact OFF identities. These are fixture checks only. Pinned numerical gate/editor/scoring functions and endpoint/OFF predicates were reused; independent predicate parity and the minimal input-binding adaptations are recorded in preparation receipts. No scientific threshold, prompt wording, candidate selection, optimization recipe, or old verdict was changed.

## Timing, ownership, and storage

The controller reported 109.140 seconds including the independent audit; `FINAL_RETURN.json` records 109.125 seconds after saving the test report. Both are below the prospective 170-second substantive cutoff and 180-second controller ceiling. The separate cleanup envelope was 195 seconds from the original start, without deadline reset. Both authenticated retained process handles produced signaled, successfully queried exit-code-zero proofs; pipes closed, capture threads joined, and ownership capture was quiescent with no capture or cleanup faults. This run exercised the retained-handle controller with fake work, not a real model process. The inherited saved judge's `real_supervisors_executed: false` refers to its lack of a real-model supervisor claim; authoritative fake process evidence is in `TEST_REPORT.json`.

The completed raw capture contains 100,429,041 bytes, including its 127,366-byte closeout index. Index SHA-256: `8285e3ece9c58c4032a52974e39f2fe8372fc10261a20e448c1d01d944a054b7`. It leaves 8,261,242 bytes in the writer's reserved closeout allocation. The largest file is a complete 993,280-byte little-endian float32 full-logit vector. The final namespace membership, preparation/log receipts, byte totals, and hashes are recorded in `FINAL_INVENTORY.json`; its own bytes are excluded from its member list and included separately when checking the namespace ceiling. Limits remain 32 MiB preparation, 288 MiB combined fake evidence, 320 MiB namespace, and 5 MiB per file.

## Remaining boundary

The production ceiling remains 180 forwards, 48 derivatives, one load, 1800 seconds worker, 15 seconds cleanup, and 180 seconds audit. Those production timings are allowances, not a measured real-model worst-case guarantee. Real-model hook tensor shapes/capacity, real backend integration through this candidate, and final real-run admission/release remain outstanding. Fake hooks and successful fake process ownership cannot establish future real hook capacity or all real-model supervisor behavior. Three new families cannot establish broad reliability; this synthetic test supplies no new real steering result, natural P-first flip evidence, reusable direction, or general preservation claim. Stop for root review before any real-model release.
