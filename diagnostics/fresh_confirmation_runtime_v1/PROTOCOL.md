# Actual-cohort-bound runtime candidate: preparation pending v2 input lock

No candidate freeze or execution is allowed until the separately owned
fresh_confirmation_token_binding_v2 supplies a COMPLETE committed INPUT_LOCK.
The lock, its exact 24 prompts/tensor inputs, 48 cold requests and all 24 boundary
proofs must be authenticated by commit and raw-byte hash. No partial v1 proof,
substitute input, text repair, encode fallback or tokenizer retry is permitted.
If that dependency fails, retain this preparation and stop for root review.

After complete binding, prospectively commit the candidate's source and input
freeze before exactly one success-only fake end-to-end batch. The entrypoint,
schedule, record path and independent audit are the candidate paths intended for
future production binding, but production authorization remains FALSE. No real
model or tokenizer is loaded and no real forward is dispatched. The synthetic
fixture is separately labeled and predeclared, never inferred from real outcomes.

The fixed schedule is all 24 baselines first, then the exact 48 cold P/C requests
in their admitted order. Baselines/entries route only from freshly captured
unedited 1024-value block-10 state through the unchanged learned gate. Gold,
category, policy and expected-route audit metadata cannot enter receiver token
tensors or the gate feature interface. The fake backend receives only locked
tensor inputs and the candidate's explicit intervention state; its synthetic
outputs are not predictions about the real cohort.

The frozen editor keeps its original float32 refreshed-gradient recipe, .10 aim,
four-update maximum, .05 original-state step cap and .20 actual path/net cap.
Independent endpoints and zero-edit retention tolerances remain unchanged. OFF
returns the fresh entry with exact own-baseline full logits, state and inputs,
with no quality gate, extra forward or derivative. Retentions are never flips.

Before freeze, verify the synthetic fixture's planned success counts: 24
baselines, 48 requests, 96F/6D, 72 routes, 12 self endpoints, six flips plus six
retentions, 36 OFF identities, 84 skipped update slots, and zero UNRUN. The
production contract remains 180F/48D/one load with 1800-second worker,
15-second cleanup and 180-second audit settings; these are not this fake batch's
deadline and are not a measured real-model runtime guarantee.

This single fake controller has one absolute substantive-work/audit cutoff at
start+170 seconds, ten seconds of closeout headroom inside start+180 seconds,
and a final cleanup envelope ending at start+195 seconds. No timer resets or
deadline extensions are allowed. Reuse the authenticated retained-handle outer
controller and allowlisted source provider; no PID guesses, tree kills or runtime
Git helper descendants. No old failure suites or old verdicts are repeated.

Preparation is at most 32 MiB, one combined fake-evidence area at most 288 MiB,
and every file at most 5 MiB. All actual evidence/log/closeout bytes count. Raw
full-vocabulary float32 evidence remains lossless and byte-authenticated. Freeze
and commit before the batch; retain all failed/partial results without repair or
rerun. Require independent saved reconstruction, owned quiescence, final raw
Git-byte inventory and explicit final source/input hashes.

Even a fake PASS does not establish real hook capacity, actual model behavior or
all future real-supervisor paths. Real hook metadata capacity, authorization and
release remain separate required reviews. No publication-milestone credit is
assigned by this preparation or its possible synthetic result.
