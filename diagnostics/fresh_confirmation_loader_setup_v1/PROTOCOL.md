# Owned startup-only diagnostic integration, v1

Prospective question: can the approved diagnostic loader and candidate adapter retain complete authenticated startup success or finite failure evidence under one owned load and zero forwards/derivatives? This is a setup diagnostic, never a scientific study or PASS.

Namespace: diagnostics/fresh_confirmation_loader_setup_v1. Prospective actual attempt: fresh_confirmation_loader_setup_attempt_001. No actual root_release or real_evidence is supplied. Actual launch stays DISABLED pending separate root review, fresh usage, and release.

The pinned candidate_loader.py AND candidate_real_adapter.py at 84bfd749876c44dc9bb1bc1e75db89f3e491c159 are consumed byte-identically. All 10 loader and 4 constructor condition expressions/messages, original ordered parameter_digest, and original inner latch.stop/guard.restore are unchanged; the prior source-parity proof is authenticated, not rerun. The only adapter wrapper retains the existing guard object for honest failed-constructor terminal counts. No alias/order hypothesis is fixed or assumed proven.

The real entrypoint reuses fixed real_release_v2 authority and retained-process entry/supervisor code. Caller pin -> source/release/authorization hashes is acyclic. Production clock overrides are forbidden. Original actual clock sampling follows all chain/input/path validation at exclusive admission; age <=120 seconds; worker/audit later authenticate that immutable admitted receipt, not a newly refreshed allowance. A unique occupied output never resumes, retries, overwrites, or resets. Private retained permission precedes research imports.

A startup counter reserves one load and intercepts the exact backend load function before its body: a second body is denied. Its immutable attempts view always has zero forward/derivative tickets; the unchanged installed guard rejects any forward/derivative before its original body. No encode/tokenize/question/generation call is added; the exact pinned loader only inspects existing template bytes. Backend construction may initialize its tokenizer object, but no prompt tokenization or model-question execution is performed. No gate/editor/scorer invocation or 109-check scientific finish occurs. All exact 24 baselines, 48 requests and 180 science cells remain UNRUN even after successful setup.

Before load, native capacity reserves 2 MiB diagnostic receipt from ordinary capacity plus 2.25 MiB terminal, <=5 MiB index, and 0.75 MiB other control/closeout from the original 8 MiB closeout allocation. Last 32 KiB of other control is retained for parent final/attempt terminal. The model-free fixtures use stricter 128/256/256/128 KiB corresponding reservations; no test bytes borrow real capacity. Diagnostic publication uses native exclusive write_new with fsync/readback acknowledgement, never the failed recorder writer. Prior/partial bytes persist; diagnostic IO never retries. Ordinary writes and exact native index preview enforce aggregate and remaining terminal bounds. Status survives constructor failure through the writer-owned diagnostic context.

After setup, the existing end_edit, parameter identity/version/flag/gradient/ordered-byte checks and exact HookGuard inspect reference identity are checked; the original guard restore runs. Successful complete startup is SETUP_DIAGNOSTIC_COMPLETE only after independent saved reconstruction plus clean owned worker and audit captures. Constructor, cleanup, partial IO, missing terminal, or capture problems yield INCONCLUSIVE_SETUP. A successful audit process is independent of diagnostic outcome. The parent always attempts its reserved native final receipt. Missing receipts cannot complete. Arbitrary metadata failure fit remains unproved; bounded diagnostic overflow is terminal, never truncated or called complete.

ONE fixed integration batch: TEST_PROTOCOL.json declares eight inert cases in four groups. The actual private entrypoint, native owner, writer, controller, reader and cleanup path execute; only frozen model/runtime dependency ports are inert. They expose mock-authority booleans solely to the unchanged loader predicate expression, never to persisted authority. Every durable execution record remains production_authorized=false and MODEL_FREE_SENTINEL_ONLY. No torch, transformers, tokenizer, backend or Qwen imports/calls; import guards apply in driver, worker and audit. Prior suites are reused by hash.

Batch limits: 75 seconds total substantive work including 16 saved owned audits/worker lanes; one shared 15 seconds cleanup/closeout, 90 seconds total. No lane resets cleanup. Retained controllers receive explicit remaining deadlines, with final native receipt headroom; an absolute driver watchdog also stops the batch. All remaining cases stay UNRUN after first unexpected failure; no repairs or retries. 32 MiB prep + 8 MiB combined fake evidence; <=5 MiB/file.

Future real startup-only subbudget: worker <=180 seconds, saved audit <=60 seconds, shared cleanup/closeout <=15 seconds; <=255 seconds overall. This is narrower than unchanged 1800/180/15/1995 ceilings and retains 288 MiB evidence/5 MiB files, CPU float32 Qwen0.8B, with_lens=False, exact input/source/runtime/weight/hook admission. Successful setup gives no permission to proceed to questions or a study. Root must review before any real release.

