# Inherited authority/support/controller/launch changes shown below.
# Added setup-only modules are independently frozen in SOURCE_FREEZE.json;
# immutable candidate loader/adapter and scientific source functions are not edited.
diff --git a/diagnostics/fresh_confirmation_real_release_v2/support.py b/diagnostics/fresh_confirmation_loader_setup_v1/support.py
index 92d41d2..557bc0c 100644
--- a/diagnostics/fresh_confirmation_real_release_v2/support.py
+++ b/diagnostics/fresh_confirmation_loader_setup_v1/support.py
@@ -11,7 +11,7 @@ ROOT = HERE.parents[1]
 EVIDENCE=HERE/"real_evidence"
 TEST_EVIDENCE=HERE/"test_evidence"
 MIB=1024**2
-ATTEMPT_ID="fresh_confirmation_real_attempt_002"
+ATTEMPT_ID="fresh_confirmation_loader_setup_attempt_001"
 OUTPUT_RELATIVE="real_evidence/"+ATTEMPT_ID
 
 def require(ok,message):
@@ -21,8 +21,14 @@ def sha(raw):
 def fixture():
     return ATTEMPT_ID
 def run_dir():
-    from real_boundary import checked_path
-    return checked_path(HERE,OUTPUT_RELATIVE)
+    from authority import current
+    return current()[0].output
+def shared_area_bounds():
+    from authority import current
+    value=bounds()
+    if current()[0].mock: value["evidence_bytes"]=value["test_evidence_bytes"]
+    return value
+
 def bounds():
     paths=[p for p in HERE.rglob("*") if p.is_file()]
     total=sum(p.stat().st_size for p in paths)
@@ -40,6 +46,12 @@ def write_new(name,value,*,raw=False,critical=False,preparation=False):
     base=HERE if preparation else run_dir()/"control"
     data=value if raw else (json.dumps(value,sort_keys=True,indent=2,allow_nan=False)+"\n").encode()
     require(type(data) is bytes and len(data)<=5*MIB,"complete bounded receipt")
+    if not preparation and name not in ("LOADER_DIAGNOSTICS.json","SETUP_TERMINAL.json"):
+        from authority import current
+        other_cap=128*1024 if current()[0].mock else 3*MIB//4
+        used=sum(p.stat().st_size for p in base.rglob("*") if p.is_file() and p.name not in ("LOADER_DIAGNOSTICS.json","SETUP_TERMINAL.json"))
+        held=0 if name in ("PARENT_FINAL.json","ATTEMPT_TERMINAL.json") else 32*1024
+        require(used+len(data)+held<=other_cap,"bounded native control copies preserve parent final reserve")
     state=bounds()
     amount=state["preparation_bytes"] if preparation else state["evidence_bytes"]
     cap=((32 if critical else 30) if preparation else (288 if critical else 280))*MIB
diff --git a/diagnostics/fresh_confirmation_real_release_v2/real_boundary.py b/diagnostics/fresh_confirmation_loader_setup_v1/real_boundary.py
index 68ff8d2..b261d10 100644
--- a/diagnostics/fresh_confirmation_real_release_v2/real_boundary.py
+++ b/diagnostics/fresh_confirmation_loader_setup_v1/real_boundary.py
@@ -9,7 +9,7 @@ import time
 from pathlib import Path
 
 MAIN=Path(__file__).resolve().parent
-ATTEMPT="fresh_confirmation_real_attempt_002"
+ATTEMPT="fresh_confirmation_loader_setup_attempt_001"
 OUTPUT="real_evidence/"+ATTEMPT
 MIB=1024**2
 
diff --git a/diagnostics/fresh_confirmation_real_release_v2/BINDINGS.json b/diagnostics/fresh_confirmation_loader_setup_v1/BINDINGS.json
index ccfbf19..60bcb8a 100644
--- a/diagnostics/fresh_confirmation_real_release_v2/BINDINGS.json
+++ b/diagnostics/fresh_confirmation_loader_setup_v1/BINDINGS.json
@@ -1,7 +1,7 @@
 {
   "schema": "real_attempt_bindings.v1",
-  "attempt_id": "fresh_confirmation_real_attempt_002",
-  "output_relative": "real_evidence/fresh_confirmation_real_attempt_002",
+  "attempt_id": "fresh_confirmation_loader_setup_attempt_001",
+  "output_relative": "real_evidence/fresh_confirmation_loader_setup_attempt_001",
   "input_binding": {
     "commit": "01de65cc93b128d605ee1d66b8ccaebc6f6eda40",
     "exact_inputs_sha256": "f62399de185341ff9380318f390b17c271de7480b0ce7ca1c22c85e5e6a94785",
@@ -33,9 +33,24 @@
     "file_bytes": 5242880
   },
   "loader": {
-    "commit": "f31d0e4f8fd5136eb38df01201f7032c9b82dc61",
-    "path": "diagnostics/fresh_confirmation_production_v2/loader.py",
-    "sha256": "84d80571b2b1698bc835ca5f36e8f72547d6a40ca021b310bee4d1ec9a5baa3e"
+    "commit": "84bfd749876c44dc9bb1bc1e75db89f3e491c159",
+    "path": "diagnostics/fresh_confirmation_loader_diagnostics_v1/candidate_loader.py",
+    "sha256": "dfa48a926f066176b32d26350b8c02f9af02de55c10c991e85d340ed6f90fc1c"
+  },
+  "scientific_engine_changed": false,
+  "candidate_adapter": {
+    "commit": "84bfd749876c44dc9bb1bc1e75db89f3e491c159",
+    "path": "diagnostics/fresh_confirmation_loader_diagnostics_v1/candidate_real_adapter.py",
+    "sha256": "f94460e5c2b54620e3ea46d34788951ed2422d0a2fe5dd1b3e3f935b00e938a5"
   },
-  "scientific_engine_changed": false
+  "setup_scope": {
+    "loads": 1,
+    "forwards": 0,
+    "derivatives": 0,
+    "tokenizer_calls": 0,
+    "worker_seconds": 180,
+    "audit_seconds": 60,
+    "cleanup_seconds": 15,
+    "total_seconds": 255
+  }
 }
diff --git a/diagnostics/fresh_confirmation_real_release_v2/production_run.py b/diagnostics/fresh_confirmation_loader_setup_v1/production_run.py
index 1ccb9d9..58a2048 100644
--- a/diagnostics/fresh_confirmation_real_release_v2/production_run.py
+++ b/diagnostics/fresh_confirmation_loader_setup_v1/production_run.py
@@ -1,29 +1,93 @@
-"""Only pre-import exclusive role gates differ from the reviewed v2 controller."""
-from pinned import install
-install(globals(),"production_run")
+"""Owned startup-only controller; successful diagnostics never imply science PASS."""
+import builtins
+import json
+import time
+from support import HERE,run_dir,write_new,require,sha,bounds
 
-_checked_controller,_checked_worker,_checked_audit=controller,worker,audit
+def control(name): return run_dir()/"control"/name
 
+def forbid_research_imports():
+    original=builtins.__import__
+    blocked={"torch","transformers","transformer_lens","sp_lense","tokenizers","numpy"}
+    def checked(name,*args,**kwargs):
+        require(name.split(".")[0] not in blocked,"MODEL_FREE_IMPORT_BOUNDARY")
+        return original(name,*args,**kwargs)
+    builtins.__import__=checked
 
-def controller(outer_cutoff):
-    from authority import current
-    from real_boundary import write_exclusive,encoded
-    boundary,approved,admission=current()
-    identity=boundary.reauthenticate(approved,admission)
-    write_exclusive(boundary.control/"CONTROLLER_ENTRY.json",encoded({"execution":identity["execution"],
-        "observed_model_work":"NONE_BEFORE_OWNED_LAUNCH"}),boundary.root)
-    return _checked_controller(outer_cutoff)
-
+def good_capture(value):
+    return bool(value and value.get("binding_authenticated") and value.get("quiescent") and
+        value.get("stop_reason") is None and not value.get("faults") and not value.get("cleanup_faults") and
+        not value.get("stdout_capture_errors") and value.get("primary_error") is None and value.get("threads_joined") and
+        value.get("pipes_closed") and value.get("within_absolute_cleanup_deadline") and
+        all(value.get("exit_proofs",{}).get(k,{}).get("exit_code")==0 for k in ("actual_worker","launcher")))
 
 def worker(deadline):
     from authority import current
     boundary,approved,admission=current()
-    boundary.role("worker",approved,admission)  # BOOTSTRAP proves prior retained permission.
-    return _checked_worker(deadline)  # First possible engine/torch import is inside this call.
-
+    identity=boundary.role("worker",approved,admission)
+    result={"execution":identity["execution"],"status":"INCONCLUSIVE_SETUP","failure":"SETUP_ENTRY_FAILURE"}
+    try:
+        if boundary.mock: forbid_research_imports()
+        from setup_core import execute
+        result=execute(boundary,approved,admission,deadline,__import__("os").environ.get("SP_SETUP_FIXTURE") if boundary.mock else None)
+    except BaseException: pass
+    finally:
+        write_new("WORKER_RESULT.json",{"terminal":{"execution":identity["execution"]},"setup_result":result,
+            "scope":"STARTUP_ONLY","scientific_pass":False,"scientific_cells_unrun":180},critical=True)
+    return 0  # A saved diagnostic outcome, not a successful construction.
 
 def audit(deadline):
     from authority import current
     boundary,approved,admission=current()
-    boundary.role("audit",approved,admission)
-    return _checked_audit(deadline)
+    execution=boundary.reauthenticate(approved,admission)["execution"]
+    result={"execution":execution,"audit_completed":False,"classification":"INCONCLUSIVE_SETUP","scientific_pass":False}
+    try:
+        boundary.role("audit",approved,admission)
+        forbid_research_imports()  # Saved reader has no model dependencies in either mode.
+        from setup_reader import judge
+        result=judge(boundary,execution,deadline)
+    except BaseException: result["audit_error"]="SAVED_SETUP_AUDIT_FAILURE"
+    finally: write_new("AUDIT_RESULT.json",result,critical=True)
+    return 0 if result["audit_completed"] else 1
+
+def controller(budget,key):
+    from authority import current
+    from owned_production import supervise
+    boundary,approved,admission=current()
+    identity=boundary.reauthenticate(approved,admission);execution=identity["execution"]
+    write_new("CONTROLLER_ENTRY.json",{"execution":execution,"scope":"STARTUP_ONLY","observed_model_work":"NONE_BEFORE_OWNED_LAUNCH"})
+    started=time.monotonic();captures={};errors=[]
+    final={"schema":"setup_parent_final.v1","execution":execution,"classification":"INCONCLUSIVE_SETUP",
+        "audit_completed":False,"scientific_pass":False,"baselines_unrun":24,"requests_unrun":48,"scientific_cells_unrun":180,
+        "external_capture_overrides_worker":True,"unconditional_closeout_attempted":True}
+    try:
+        write_new("CONTROLLER_STARTED.json",{"execution":execution,"setup_seconds":{"worker":180,"audit":60,"shared_cleanup":15},
+            "unchanged_upper_seconds":{"worker":1800,"audit":180,"cleanup":15},"absolute_end":budget.absolute_end,
+            "substantive_end":budget.substantive_end,"one_global_cleanup_budget":True})
+        owned=json.loads((HERE/"OWNED_IDENTITY.json").read_bytes())
+        for lane in ("worker","audit"):
+            deadline,cleanup_end=budget.deadlines(time.monotonic(),lane)
+            value=supervise("production_"+lane,deadline,cleanup_end,owned);captures[lane]=value
+            budget.charge(key+":"+lane,time.monotonic()-value["cleanup_started_monotonic"])
+            require(value["quiescent"],"no successor before owned quiescence")
+            if lane=="worker":
+                worker=control("WORKER_RESULT.json").read_bytes()
+                write_new("CLOSED_WORKER_BINDING.json",{"execution":execution,"worker_result_sha256":sha(worker),
+                    "owned_capture_sha256":sha(control("owned/production_worker/CAPTURE.json").read_bytes()),
+                    "quiescent":True,"good_capture":good_capture(value)},critical=True)
+        result=json.loads(control("AUDIT_RESULT.json").read_bytes())
+        require(result["execution"]==execution,"independent audited authority join")
+        if all(good_capture(captures.get(x)) for x in ("worker","audit")) and result["audit_completed"] and time.monotonic()<=budget.absolute_end:
+            final.update(classification=result["classification"],audit_completed=True)
+    except BaseException: errors.append("SETUP_CONTROLLER_OR_CAPTURE_FAILURE")
+    finally:
+        def optional_hash(name):
+            try: return sha(control(name).read_bytes())
+            except OSError: return None
+        final.update(controller_errors=errors,shared_cleanup=budget.record(),elapsed_seconds=time.monotonic()-started,
+            finished_monotonic=time.monotonic(),worker_quiescent=bool(captures.get("worker",{}).get("quiescent")),
+            audit_quiescent=bool(captures.get("audit",{}).get("quiescent")),
+            worker_result_sha256=optional_hash("WORKER_RESULT.json"),audit_result_sha256=optional_hash("AUDIT_RESULT.json"),
+            worker_capture_sha256=optional_hash("owned/production_worker/CAPTURE.json"),audit_capture_sha256=optional_hash("owned/production_audit/CAPTURE.json"))
+        write_new("PARENT_FINAL.json",final,critical=True)
+    return final
diff --git a/diagnostics/fresh_confirmation_real_release_v2/launch.py b/diagnostics/fresh_confirmation_loader_setup_v1/launch.py
index 2d92796..446d4e6 100644
--- a/diagnostics/fresh_confirmation_real_release_v2/launch.py
+++ b/diagnostics/fresh_confirmation_loader_setup_v1/launch.py
@@ -1,4 +1,4 @@
-"""Trusted-root CLI. No real release files are provided by this preparation."""
+"""Disabled until a separate trusted root release; setup only, never science."""
 import argparse
 import json
 import os
@@ -6,49 +6,42 @@ import time
 from pathlib import Path
 from real_boundary import MAIN,Boundary,need,encoded,write_exclusive,digest,strict
 
-
 def preflight(lock_sha):
     try:
-        value=Boundary().verify_chain(lock_sha)
-        return {"status":"ROOT_AUTHORITY_PRESENT_NOT_LAUNCHED","intended_mode":"REAL_QWEN",
-            "observed_model_work":"NONE","production_authorized":True}
+        Boundary().verify_chain(lock_sha)
+        return {"status":"ROOT_AUTHORITY_PRESENT_NOT_LAUNCHED","scope":"STARTUP_ONLY_1LOAD_0F_0D",
+            "observed_model_work":"NONE","production_authorized":True,"scientific_execution_allowed":False}
     except (ValueError,OSError):
-        return {"status":"DISABLED_NO_APPROVED_REAL_RELEASE","intended_mode":"REAL_QWEN",
-            "observed_model_work":"NONE","production_authorized":False}
+        return {"status":"DISABLED_NO_APPROVED_REAL_RELEASE","scope":"STARTUP_ONLY_1LOAD_0F_0D",
+            "observed_model_work":"NONE","production_authorized":False,"scientific_execution_allowed":False}
 
+def terminal(boundary,identity,result):
+    parent_sha=None
+    try:
+        observed=strict(boundary.read_control("LOADER_TERMINAL.json"))["observed_model_work"] if boundary.control_path("LOADER_TERMINAL.json").exists() else (
+            "UNKNOWN_MISSING_LOADER_TERMINAL" if boundary.control_path("LOADER_STARTED.json").exists() else "NONE_LOADER_NOT_DISPATCHED")
+        if boundary.control_path("PARENT_FINAL.json").exists(): parent_sha=digest(boundary.read_control("PARENT_FINAL.json"))
+    except (OSError,ValueError,KeyError): observed="UNKNOWN_TERMINAL_READ_FAILURE"
+    return write_exclusive(boundary.control/"ATTEMPT_TERMINAL.json",encoded({"schema":"setup_attempt_terminal.v1",
+        "execution":identity["execution"],"observed_model_work":observed,"parent_final_sha256":parent_sha,
+        "attempt_finished":result is not None,"real_scientific_success":False,"scientific_execution_allowed":False}),boundary.root)
 
 def main():
-    parser=argparse.ArgumentParser()
-    parser.add_argument("--authority-lock",required=True)
-    parser.add_argument("--approved-lock-sha256",required=True)
-    parser.add_argument("--preflight",action="store_true")
+    parser=argparse.ArgumentParser();parser.add_argument("--authority-lock",required=True)
+    parser.add_argument("--approved-lock-sha256",required=True);parser.add_argument("--preflight",action="store_true")
     args=parser.parse_args()
-    expected=MAIN/"root_release"/"AUTHORITY_LOCK.json"
-    need(Path(args.authority_lock).absolute()==expected,"D_FIXED_AUTHORITY_PATH")
+    need(Path(args.authority_lock).absolute()==MAIN/"root_release/AUTHORITY_LOCK.json","D_FIXED_AUTHORITY_PATH")
+    need(not os.environ.get("SP_SETUP_FIXTURE") and not os.environ.get("SP_SETUP_BATCH_LOCK"),"D_NO_REAL_FIXTURE_OVERRIDE")
     if args.preflight:
-        result=preflight(args.approved_lock_sha256)
-        print(json.dumps(result,sort_keys=True))
-        return 0 if result["production_authorized"] else 2
-    boundary=Boundary()
-    pin,identity=boundary.admit_once(args.approved_lock_sha256)
-    os.environ["SP_CONFIRMATION_AUTHORITY_SHA"]=args.approved_lock_sha256
-    os.environ["SP_CONFIRMATION_ADMISSION_SHA"]=pin
+        result=preflight(args.approved_lock_sha256);print(json.dumps(result,sort_keys=True));return 0 if result["production_authorized"] else 2
+    boundary=Boundary();pin,identity=boundary.admit_once(args.approved_lock_sha256)
+    os.environ["SP_CONFIRMATION_AUTHORITY_SHA"]=args.approved_lock_sha256;os.environ["SP_CONFIRMATION_ADMISSION_SHA"]=pin
     result=None
     try:
+        from setup_budget import Budget
         from production_run import controller
-        result=controller(time.monotonic()+1995.)
+        result=controller(Budget(time.monotonic()),"real_setup")
         return 0 if result["audit_completed"] else 1
-    finally:
-        parent_sha=None
-        try:
-            terminal_path=boundary.control_path("LOADER_TERMINAL.json")
-            observed=strict(boundary.read_control("LOADER_TERMINAL.json"))["observed_model_work"] if terminal_path.exists() else (
-                "UNKNOWN_MISSING_LOADER_TERMINAL" if boundary.control_path("LOADER_STARTED.json").exists() else "NONE_LOADER_NOT_DISPATCHED")
-            if boundary.control_path("PARENT_FINAL.json").exists(): parent_sha=digest(boundary.read_control("PARENT_FINAL.json"))
-        except (OSError,ValueError,KeyError): observed="UNKNOWN_TERMINAL_READ_FAILURE"
-        write_exclusive(boundary.control/"ATTEMPT_TERMINAL.json",encoded({"schema":"real_attempt_terminal.v1",
-            "execution":identity["execution"],"observed_model_work":observed,"parent_final_sha256":parent_sha,
-            "attempt_finished":result is not None,"real_scientific_success":bool(result and result["classification"]=="PASS_STUDY")}),boundary.root)
-
+    finally: terminal(boundary,identity,result)
 
-if __name__=="__main__":raise SystemExit(main())
+if __name__=="__main__": raise SystemExit(main())

