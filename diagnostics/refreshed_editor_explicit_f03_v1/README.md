# Unchanged refreshed editor: explicit f03/v1 replication

One development replication on cg_f03_context_rotation/v1/self_shutdown. Selection is deterministically the first discovery family not already tested with the refreshed editor (f01 and f02 were tested). F03 was exposed to other methods; this is not untouched confirmation or broad generalization.

Original four mapping/display records come from certified_descent_comply_v1 preregistration at8832d9c490aebd944d3172b1c5471ae77961f700, authenticated through inventory SHA f727503381c49fc844463457d49202ec9977cacb6ca8c784866c6d229d9e2393. Only the two previously approved explicit option-consequence substitutions are applied. Inverse substitution recovers every original byte. The context-rotation narrative, action identities and authorization remain intact; no policy line. Original source metadata and records are frozen. No exact transformed prompt appears in the three old refreshed-gradient preregistrations or the explicit-f01 editor freeze.

Editor.py, run.py and score.py are authenticated byte-for-byte copies from4ed956e5b68b6eb8ecce682ab5c1b3fc9ea0c9e5 / inventory2a03a3eec7ddd593fcdda6bc4a6175395338e6cb60b89bde48ec64c172ae48fb. Only namespace/input/provenance and necessary selection assertions differ. Local cached tokenizer computation independently records each f03 full token sequence and joint A32/B33 answer boundary before freeze; no f01 alignment is reused. Final encoded INPUT token remains the sole block10 edit location.

All four eligible baselines first; per rendering, independent no-edit retention, cold opposed trajectory with up to four gradient/update pairs, then independent selected-final-endpoint replay. SAME .10 linear aim, .05-1e-6 observed acceptance, .05 actual-step and .20 actual-path/net caps, original state/parameter/quality/eligibility checks and strict full-vocabulary gates. Oracle supplies intended action AND letter mapping. Explicit conditional skips, no padding or rescue. Maximum44 forwards/16 derivatives; one unchanged pinned Qwen3.5-0.8B CPU float32 load. Worker600s including load, cleanup15s, independent saved audit60s. Namespace64MiB/file5MiB; conservative bound61,543,972 bytes. Preparation invoked commands<=180s, only changed-input tests and one minimum-size fake wiring check. Fresh standard usage available and<100; no credits/reset, retry or extension.

Raw logits, gradients, states, offsets, actual geometry, conditional accounting and independent replay are retained and independently verified without new model calls. Report all eight outcomes and four joint pairs; true flips versus no-edit retentions, semantic versus physical directions and zero-opportunity UNTESTED. One prompt-specific algorithm, not a shared vector, learned gate, autonomous semantics, motive, broad reliability or ordinary-task preservation. Historical evidence untouched. Publication40%. Stop after this matrix. If complete PASS, recommend one tiny ordinary/matched-other oracle-targeting test of the same algorithm, without launching it; otherwise retain exact failures and no recipe/family changes.
