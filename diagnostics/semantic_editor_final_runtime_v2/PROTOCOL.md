# Admission and final-closeout successor (no model authorization)

This v2 preserves the reviewed v1 scientific editor, classifier, independent numerical judge, hook guard, complete input IDs/masks and production plan byte-for-byte. Only usage admission, the authoritative post-process verdict join, audit-time external source reauthentication, and their namespace/provenance bindings change. Parent v1 at b184f36461d84d7e0fb090db0789825551942705 remains immutable. This is implementation verification, not new scientific evidence.

Before the single pure batch, freeze and commit every new helper/test/protocol plus copied scientific/input source. Test the actual admission functions with in-memory synthetic root documents; the on-disk authorization stays false and no approved release or real_attempt is created. No model/backend/process traversal, tokenizer, gate scores, fitting or extraction. Test commands are bounded to 180 invoked seconds and this preparation namespace to 32 MiB / 5 MiB per file. Reuse authenticated parent tests, not their outcomes as real evidence.

## Usage invariant

Accept the observed direct payload, MCP JSON text blocks, structuredContent, or agreeing recognized views. Prefer rateLimitsByLimitId.codex; otherwise use standard legacy rateLimits. Ignore other limit IDs, including Spark. Use the highest available primary/secondary usedPercent, with null windows unavailable, not zero. Reject missing/error/ambiguous/nonfinite/out-of-range/malformed data, JSON duplicate keys, conflicting recognized views, and explicit standard exhaustion/spend/rate-limit flags. Preferred numerical windows supersede legacy numerical windows, but explicit standard legacy blocking flags still veto. The declared percentage must equal the derived maximum exactly and be below 100; 90–99 is allowed. Root pins the full receipt bytes indirectly through the canonical receipt hash and exact committed release hash. Receipt age must be 0–120 seconds. Root authority is not established by a caller-supplied hash.

## Final-closeout invariant

After exact-cohort authentication, the production judge rechecks every recorded installed/external helper/runtime/environment hash before saved judging. It does not refresh launch usage or call production admission at final audit. The saved independent judge writes judge_results.json, judge_report.md and its completed judge_receipt.json; these are intermediate until both external process captures are complete_valid, exited zero, EOF observed, all owned writers joined/quiescent and fault-free. A malformed/missing completed receipt, external worker/audit fault, nonzero exit, deadline, logging or cleanup fault makes final_closeout.json and the sole final REPORT.md INCONCLUSIVE, retaining the raw PASS/FAIL, scientific findings and raw technical/cleanup/UNRUN artifacts. Final inventory is written only after both process groups/writers and final receipt/report are finished. No stale judge PASS is authoritative.

## Unchanged future assessment

Exactly 24 inputs, 48 P/C requests, 72 fresh routes, 12 self endpoints, 36 OFF checks; conservative 180 forwards / 48 derivatives, one pinned CPU-float32 load, 1500 seconds worker + 15 cleanup + 180 saved audit, 288 MiB attempt / 5 MiB file, 109 strict hook checks / 16 MiB hook records. The inherited conservative record bound is 259,715,072 bytes, including 16 MiB for sources, two bounded logs and closeout; these small additional receipts fit the existing closeout reserve. This preparation's 32 MiB allowance is separate from the future attempt. 132F/24D remains a conditional derivation, never the cap. No method, threshold, target-map, cohort or input changes. Exposure remains three manifest-sealed families with historical non-access unverified. No blind independent model replay is added: the independent judge uses saved raw data and the already-frozen per-request endpoint forwards.

Only a separately reviewed, exact committed root release may later enable production. See RELEASE_SCHEMA.md. This job stops after pure tests, source/result commits and authenticated inventory; no launch is authorized here.
