# One model-free real-release boundary batch

This namespace changes only authority, entrypoint/role gates and output-root binding. It executes the reviewed production_v2 engine, independent judge, real adapter, numerical predicates, strict hook guard, prefix component, recorder and retained-handle controller through immutable source wrappers. The assembled engine and judge SHA256 values must remain83259d692a740155a32a2bd5c204dad42c6ec17e411bd57f45c86f3d07919332 and e2d7d6d740dc1bd9904b8361ce5a73a26d15db2b48f5260728bc3e1f1d01f4a6. No96-forward integration rerun is authorized.

Prospective real attempt: fresh_confirmation_real_attempt_001. Its only real output is real_evidence/fresh_confirmation_real_attempt_001 within this namespace. Relative identity must match exactly, including case; traversal, alternate roots, symlinks/reparse points and hardlinked saved files are rejected. One exclusive directory/admission receipt reserves the attempt before imports or owned substantive work. Controller, worker and loader dispatch each have additional exclusive receipts. Failure/partial admission leaves the reservation occupied; no overwrite, reset, retry or resume is offered.

The trusted root caller supplies the exact independently approved authority-lock SHA on the command line. Source manifest -> root release -> authorization -> caller-pinned lock is acyclic. These future authority files are absent under root_release; the current disabled-state document is not an authorization. A later root release creates new files, without mutating SOURCE_FREEZE or old attempt evidence. It must pin exact source/input/runtime/output/resource values and an actual usage-tool receipt. Standard Codex usage is recomputed using the unchanged pinned parser; missing, malformed, altered, exhausted or stale (>120 seconds) receipt prevents first admission. Worker/writer/audit reauthenticate that same immutable admitted receipt and its original freshness, not a new mid-run quota observation.

Owned bootstrap permission still precedes any model-related import. The new worker gate checks that bootstrap execution identity/current PID and creates an exclusive worker-entry record before invoking the unchanged worker. The loader gate requires the exact writer output root and reserved1load/0F/0D state before its exclusive dispatch. The immutable pinned loader remains CPUfloat32 Qwen3.5-0.8B, local/offline, with_lens=False, with exact runtime/weight/source and complete hook setup admission before the first forward. Counters180F/48D/1load/160tokens,109 hooks,1800s worker/180s audit/one shared15s cleanup and288MiB evidence remain unchanged.

Authority records intended REAL_QWEN mode but never asserts that model work happened. Dedicated loader-start/terminal records distinguish no dispatch, a returned pinned real adapter, and unknown partial work after a failed load. Missing loader/worker terminal evidence is fail-closed for audit. Sentinels use a separate MODEL_FREE_SENTINEL_ONLY scope and actual production authorization false; production Boundary rejects that scope. Tests create no real root_release files and cannot import the loader.

Fixed groups are in TEST_PROTOCOL.json: one coherent mock-approved chain reaches exactly one sentinel; seven mode/source/input/runtime/output semantic denials; eight usage/authority denials plus disabled preflight; six duplicate/reuse/permission/missing-terminal denials with prior bytes preserved; shared worker/writer/audit identity and unchanged resource ceilings, including an audit far after initial usage freshness. TEST_INPUTS and caller-pinned TEST_INPUT_LOCK freeze exact mock data before the batch. Test roots are separately contained under test_evidence and model observations remain SENTINEL_ONLY, not real ownership or scientific outcomes.

One batch only:75 seconds substantive plus15 reserved closeout,90 total. A monotonic cutoff stops subsequent stages; an absolute hard watchdog bounds failure. A failed group stays failed and all later groups remain UNRUN; no repair/retry or cap reset. At most32MiB preparation plus32MiB combined test evidence,5MiB/file. A forbidden-import guard covers torch, Transformers, model backends, real adapters and loader modules. No Qwen/model/tokenizer/torch/backend calls or imports, no ordinary-task outcomes, no repeated integration suite.

The preparation pass initially found an undeclared read-only support/authority source pin and a local JSON newline representation difference. Both were resolved before source freeze: the missing exact committed pins were added, and the local runtime file is byte-pinned with a separate exact semantic equality check against the immutable v2 runtime reference. No scientific/runtime value changed; no batch/model work occurred during preparation.

PASS means only this authority/output boundary passed pure/sentinel checks. Root read-only review and a subsequent explicit real release remain required. No real study result, actual Qwen loading, real hook capacity, timing guarantee or publication milestone is established.
