# Complete hook-evidence capacity audit

One saved-only <=30-second pure batch, no model/tokenizer/constructor imports, runtime edits, prompt/outcome reads or new science. Source universe is exactly the hook_evidence files authenticated by the real-attempt inventory at b0ff4e7070e0205849f0777a5d71c2bffe898d5d, its guard/recorder/JSON serializer, and the unchanged semantic_confirmation_resource_v1 contract. Read metadata and code only; do not open other raw-attempt files. Four fixed checks are frozen before execution.

1. Authenticate working and committed bytes, reconstruct all saved compressed setup-before/reference/setup-changes chunks and exact compact JSON, and compare setup differences using only the AST-selected pure differences function. No model-aware source module is imported.
2. Profile actual aliases/identities, integer digit widths, escaped strings and raw serialized lengths. A20-digit identity sensitivity allowance is conditional on unsigned64 values and unchanged structure/strings, not a bound on arbitrary Python keys or future hook metadata. Never replace, normalize or rewrite original identity evidence.
3. Verify a source-compatible difference record includes arbitrary callable qualname strings. Use small fixed1024/2048-character examples and a cardinality calculation at the existing64MiB raw-value limit. Hexadecimal strings alone provide16^n distinct valid compact-JSON failure records; all byte strings of length<=16MiB number less than256^(16MiB+1). If the former exceeds the latter, no lossless codec can fit every source-permitted complete failure into the full allowance—even before setup/check/fault overhead. This proves lack of an unconditional guarantee, not an observed model failure or an estimate of likely metadata.
4. Reserve all109 records at4096B,64KiB fault closeout, two setup/source JSON records at64KiB each, and snapshot manifests at64KiB each. Report a deliberately loose gross guard-demand envelope for3 normal values,1 mutually exclusive setup-failure value and up to109 check-failure values:64 raw1MiB chunks/value, each encoded file<=5MiB if accepted. This is a guard ceiling, not a compression theorem or reachable schedule. A fixed seeded1MiB codec probe demonstrates positive compression overhead; measured recompression ratios never become future size assumptions.

Capacity PASS requires a conservative complete source-derived bound, including all failure metadata and serialization overhead, below16MiB. Otherwise verdict is UNVERIFIED even if reconstruction tests pass. Existing aggregate/per-file guards prevent oversized writing but cannot establish complete evidence will fit. Minimum future admission predicate must account for exact live setup/reference AND a proven finite failure-difference envelope, all109 records/manifests and fault reserve before any forward. Merely measuring setup is insufficient when future strings/cardinalities are unbounded. No new envelope, truncation, normalization, evidence omission or capacity change is authorized.

Namespace32MiB/per-file5MiB; outputs are compact source pins, calculation, receipts and report, not duplicated snapshots. An absent guessed native-extension path is preserved in PREPARATION_OBSERVATION.json; no compressor source identity is inferred from that failed pre-freeze metadata lookup. The sole declared batch remains prospective until source commit. On failure preserve and stop without rerun. Production remains unauthorized.
