# First-baseline helper diagnostic v2 — prospective, disabled

Question: can the approved GDN compatibility component and corrected original-instance selector join the existing one-baseline diagnostic without weakening admission, cleanup, evidence or science? This namespace is a new attempt, not a repair/resume of any closed run.

Exact upstreams: selection result 1081df521dd1cdda5be5d2daa4bee1feb74fbbeb, inventory 0959d20625836d173c6cf10f2c2cad92ea99c103ca1dd57fdaaa50e2efcbe499; compatibility 29393c717251212f4afc1ddfd9808fa0ca5bc058; closed diagnostic 61a89ba171f471f8aedccea857dc6418a65b7f3e. The old partial ATTRIBUTE_ERROR trace does not identify its missing member conclusively, nor retroactively prove real003's cause.

## Splice and original rules

The unchanged legacy reference capture executes first, at its exact loader point. Only then begin helper admission and install the approved absent per-instance aliases; the original create_recorder callback executes after installation, before adapter construction. Original installed runtime, frozen expected digest, 14 parameter predicates, retained-order fresh hashes, registry/cache/hook checks and computation are unchanged. Live execution must resolve classes and both exact existing decorated helper callables from already-loaded authenticated modules; no importing a substitute, selecting another kernel, overwriting attributes, cache/eager_scan switch or mathematical change.

The corrected selector retains all 24 wrapped blocks/decoders and 18 original GDN identities, exact shared ModuleList and duplicate-preserving alias graph. Original helper fingerprints remain immutable. Original latch.admit is the callback to the existing additional PRE_FORWARD checkpoint, followed by the unchanged cell/counter dispatch. There is no new prompt, route, gate decision, score, cold request or scientific continuation.

Original diagnostic_cleanup executes its end_edit, fresh parameter state and predicates, latch-before-provider inspection, exact admitted hook reference and cold identity BEFORE helper removal. The existing handoff then checks its immutable fingerprint/graph, removes only owned aliases and attempts original guard.restore. Load/constructor/proof failure attempts helper-only closeout without replacing the original exception; the old loader's own latch.stop and guard.restore remain in their existing path. On such failure, helper closeout may also attempt the idempotent original restore; the old original restoration is never skipped. Normal cleanup restores once. A refusal/missing receipt/partial IO cannot become clean diagnostic success.

## Namespace, publication and saved proof

The approved helper files are placed in helper_pkg with relative import binding only. Its small support adapter uses the correct deeper repository root and an authenticated direct private component import. Every other statement is preserved after import erasure. The package must be initially absent from sys.modules, then all loaded module objects/paths remain fixed; it never overwrites bare support/selection/fingerprint/handoff names. HELPER_PACKAGE_BINDING authenticates all copied source bytes; the original 22-pin source manifest remains byte-identical. New compilation paths are real local files in the frozen allowlist.

Before load, reserve within the EXISTING 786,432-byte other-closeout allocation:
- HELPER_SETUP.json: 65,536 bytes
- HELPER_TERMINAL.json: 65,536 bytes
- immutable HELPER_ADMISSION.json: 8,192 bytes
- HELPER_OUTER_STATUS.json: 32,768 bytes
Total 172,032 bytes, leaving 614,400 bytes for existing other control receipts, including their unchanged 32 KiB parent-final headroom. No original 8 MiB total closeout / 288 MiB evidence / 5 MiB file cap grows. Inline finite copies stay inside existing terminal/worker limits. This partition enforces limits, not a claim that arbitrary future faults will fit.

Native publication uses original exclusive write_new / acknowledged bytes. Preserve the setup packet once before attempting its write; bind its source/execution, graph/fingerprint SHA and acknowledgment outside the final receipt. Unconditional core finally snapshots/publishes helper status after cleanup and writer/trace closeout. Actual worker finally copies the finite status even if the diagnostic terminal is missing or writing failed. A physically complete-looking outer record followed by IO failure cannot PASS; authoritative status has the failure/no acknowledgment.

The saved helper reader independently checks source/execution/reservation/native acknowledgments/admitted packet and all inherited graph/fingerprint/cleanup conditions. It is an AND with every original diagnostic predicate and closed worker/capture identity; it cannot upgrade an original failure. Missing original terminal remains INCONCLUSIVE. Helper success is never scientific PASS.

The trace source allowlist adds exact pinned GDN/setup/block/base/Module/ModuleList/mapping/kernel-decorator files and frozen private helper files. The trace stays <=64 KiB with unchanged finite frame/stage limits. No exception locals, tensors or arbitrary dumps are added.

## ONE fixed integration-only batch

Six groups in integration_tests.py, frozen before execution:
1. Exact loader splice AST (legacy capture through helper setup), original recorder return, pre-forward callback, original diagnostic_cleanup ordering, native helper packets, actual worker-finally snapshot and independent saved join. Original numerical/model callbacks are inert stubs.
2. Original mandatory LIVE resolver rejects INERT execution; unexpected existing helper rejects before original new reference admission.
3. Partial native HELPER_SETUP and HELPER_ADMISSION publication retains 19 actual bytes, leaves terminal/incomplete evidence and removes only owned aliases.
4. Original cleanup latch refusal stays primary, skips provider, removes owned helpers and restores guard.
5. Partial HELPER_TERMINAL or HELPER_OUTER_STATUS survives through the actual worker finally; missing full diagnostic terminal is not fabricated or accepted.
6. A coherent synthetic saved copy is accepted as helper evidence; altered setup bytes, admitted graph binding, execution and outer session proof are rejected after repairing copied outer envelopes. All four truth-table rows of the actual added saved-reader AND preserve original failure.

Only the new integration slices/callbacks and saved joins run. Reuse exact inert topology construction and previous component/selector/owned-controller proofs; no old suite, full controller, actual original reader cohort, numerical constructor or baseline is run. Test dependency substitutions are source-declared INERT only and cannot authorize a real entrypoint. Zero Torch/HF/TL/backend/model/tokenizer import, actual load, tensor, parameter access/hash, forward, derivative or encoding.

Source, input identities, exact cases and expectations are committed before ONE batch. Actual standard-Codex usage <100%, available/not exhausted and <=120 seconds old is locked before batch. 45 seconds substantive plus ONE 15-second closeout =60 absolute; final watchdog at59;32 MiB preparation /8 MiB test /5 MiB file. Exclusive output, no retries/cap resets. Unexpected failure preserves current bytes and later groups UNRUN, then stops.

## Future actual scope, still disabled

Distinct future attempt fresh_confirmation_first_forward_helper_diagnostic_attempt_001. Existing acyclic source→release→authorization→caller-pinned-lock chain; no root_release files are created here. Actual ownership permission must precede imports/load, real usage must be fresh after validation/path checks, and output admission is exclusive/no resume.

Only pinned Qwen3.5-0.8B CPU float32, with_lens=False; 1 load / at most1 forward /0 derivatives /0 encoding; exact locked N01__v1__self_shutdown__KEEP_then_STOP first baseline IDs/mask from lock d008fb7c53b974694e76afa9ef7db3f4f2d4443eed8a2c83ba06ea6e83987b22, exact zero offset. No gate/scoring/rest-of-study. 180 worker +60 saved audit +ONE15 shared cleanup =255 seconds under unchanged upper production contract;288 MiB evidence /5 MiB file. Live callable/selector and complete actual hook setup capacity remain runtime gates, not established by the inert batch. Independent/root review and separate root release are mandatory.
