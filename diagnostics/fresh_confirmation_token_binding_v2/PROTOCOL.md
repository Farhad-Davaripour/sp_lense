# Fresh confirmation token binding v2

This is a separately frozen successor to immutable v1 INCONCLUSIVE. The only behavioral correction separates the display-ordered answer alphabet from the canonical token map. Ordinary answer_words must be exactly [A,B]. Semantic words must be two unique strings comprising exactly KEEP and STOP, ordered exactly as the declared KEEP_then_STOP or STOP_then_KEEP layout. Both layouts return the same canonical KEEP=50057, STOP=48964 mapping. No prompt, author truth, proof criterion, tokenizer helper or scientific method changes.

Reuse the exact TEXT_LOCK committed at f15af48aaa54995c593630a67986199bd7a9644d, SHA371562bd76cdc0f105fb1324f1de28fa11eb44bff191c1d06c883cb98a9c456d, and EXACT_INPUTS SHA f62399de185341ff9380318f390b17c271de7480b0ce7ca1c22c85e5e6a94785. Source artifacts are authenticated directly in their committed parent namespace; none are regenerated. The new INPUT_LOCK additionally names the exact input source for downstream consumers. All24 prompts and48 requests remain fixed; five machine-exact ordinary proofs plus manually supported O06 remain unchanged.

Freeze all new source and bindings before one <=10-second pure batch: (1) both display orders/canonical mapping; (2) exact ordinary order, rejecting reversed/duplicate/word labels; (3) malformed missing/extra/unknown/nonstring/nonlist alphabet; (4) duplicates; (5) layout mismatch/unknown layout. Fixed subcases are in pure_tests.py. Any failure stops this attempt; no repair or retry. A passing receipt bound to the same source freeze is required before tokenization.

Then one separately usage-checked, cached local-only tokenizer load binds all24 prompts from scratch. Reuse the exact inherited boundary helper and all cache/template/package/runtime/source pins. No v1 token proof is used as v2 evidence. Record full IDs, masks, final-input and appended-answer boundary roles, prefix identity and lengths. The160-token maximum is unchanged and checked after all successful boundary records. Source or boundary faults stop immediately and preserve remaining NOT_TOKENIZED IDs. All24 passing proofs and lengths are required for INPUT_LOCK. No model loads, forwards, derivatives, gate scoring or dataset extraction; production remains disabled.

One90-second own-process tokenizer watchdog, no fallback/download/retry. Storage remains32MiB total/5MiB per file, enforced by the inherited exclusive writer. Final inventory is created only after the writer has exited. Namespace/input provenance and the pure-test prerequisite are lifecycle changes only. Read-only verification may authenticate TEXT_LOCK and exact_inputs, verify the24 token artifact hashes, receipt and source freeze without importing tokenizer/model code.
