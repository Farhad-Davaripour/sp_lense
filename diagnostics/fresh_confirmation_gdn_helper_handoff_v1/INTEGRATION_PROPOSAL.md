# Exact next loader/diagnostic splice proposal, not executed

The existing candidate_loader.py remains byte-identical (`eb4ec9a9fd1b6b94a5a10856071351a5e6cdbaca62066048db7a87b62997fac3`). Its unchanged `diagnostics.capture_legacy_before(...)` must remain BEFORE this new handoff. Splice helper selection/installation immediately after that call and wrap only the following unchanged `create_recorder(...)` call as Handoff.setup(original_setup). This keeps strong legacy ordering fixed before any optional bridge materialization and puts both helper attributes into the prospective complete setup/reference state. The original complete-setup and constructor predicates retain their exact expressions/messages and expected digest.

Before calling the splice in a future real candidate, authenticate the exact already-loaded module, selected decorated helper implementation, original GDN class and bridge class from all pinned code files; verify the actual layer layout and complete original/bridge mapping. The existing compatibility LIVE_EXISTING_MODULE gate stays mandatory. This model-free handoff has not executed that live gate, the class binding, or actual HookRecorder setup. Any mismatch is a blocker, never permission to change defaults, choose another kernel, unwrap for dispatch, switch cache/eager_scan/native forward, alter weights, or replace the frozen digest.

Retain the original create_recorder return unchanged. Copy handoff.admission() once into the existing native loader context/outer admission, and expose the handoff as an additional necessary identity check. At the existing first locked baseline's original pre-forward check, invoke Handoff.checkpoint(original_check) so the original latch/guard callback remains first and its exact result survives. Do not authorize a second forward, change the locked IDs/mask, add offset/gradient work or continue the study.

Wrap the already-approved diagnostic_cleanup callback through Handoff.close(original_cleanup). The original callback receives the restoration join and otherwise retains its original call chain. It returns the original cleanup object unchanged on success and propagates the original error on failure; additional helper failures remain terminal. Helper identity checks/removal occur only after the original inspection/cold-identity sequence (or its refusal), inside the finally wrapper that always attempts the unchanged guard.restore. Full/historical scientific finalizers remain untouched. Persist handoff.status() in the existing unconditional outer finally even if native publication fails. Preserve the originally admitted setup packet separately; never derive it from final saved bytes.

The saved diagnostic reader should AND the new saved_reader.judge result with all original loader, parameter, guard, accounting and authoritative capture predicates. Supply expected_setup from the independently authenticated setup/outer binding and controller_status from authoritative closed worker evidence. Never replace or upgrade an original incomplete verdict, and never interpret HANDOFF_DIAGNOSTIC_JOIN_COMPLETE as scientific PASS.

Reserve both <=64 KiB helper receipts within the existing <=2 MiB diagnostic/native receipt and existing closeout allocations before load. Bind exact source/execution/native acknowledgments to existing full inventory/accounting. Re-admit complete actual setup/reference and all existing hook reservations within the unchanged 16 MiB hook/288 MiB total/5 MiB file limits. Actual capacity remains unverified until that prospective admission succeeds. Add the pinned GDN bridge, HF kernel decorator, Qwen3/Qwen3.5 mapping and configuration sources to the bounded trace allowlist, without increasing its 64 KiB cap.

This is a proposed small splice, not a copied controller or actual launch. A future separately reviewed/root-authorized diagnostic still requires a fresh unique attempt/output, actual fresh usage, unchanged retained-process ownership, pinned Qwen3.5-0.8B CPU float32, one load/at most one first locked unedited baseline forward/zero derivatives and encoding, and 180-second worker + 60-second audit + ONE shared 15-second cleanup = 255 seconds. No root authority or executable real-launch command exists in this namespace.
