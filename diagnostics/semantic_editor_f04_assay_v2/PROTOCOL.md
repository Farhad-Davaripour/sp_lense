# F04 cleanup-only successor — no scientific execution authorized

This separately frozen successor tests only sticky owned-process deadline cleanup. V1 at b608415 remains INCONCLUSIVE_PREPARATION_NOT_RELEASED, including its actual raw launcher-termination error. Its error code was not recorded; a possible exit race is not a retrospective exoneration. The original real final assessment's independent eligibility failure and identity fault also remain untouched.

The exact V1 two f04 prompts, input lock, complete token records, 42F/16D plan, learned gate, editor, scientific scoring, hook guard and independent judge are copied byte-for-byte. There is no input selection, tokenization, model load, forward, derivative, real gate score or fit in this job. V1's authenticated complete fake matrix and normal owned worker/audit results are reused, not rerun. V1's fixed self-only exposed-development question, both P/C requests/display orders, .10 aim, four updates, .05 actual step, .20 actual path AND net, original eligibility/fail-stop and endpoint/retention gates are unchanged. No rescue or broader scientific claim.

New invariant: the first stop cause is sticky. Each retained handle has at most one termination attempt, recorded before invoking TerminateProcess. Repeated deadline/poll/wait/terminate/kill observes bounded completion only, preserving every genuine primary and cleanup fault. Never reopen by PID. Only authenticated actual-worker and originally owned launcher handles may be targeted. Termination access-denied error5 is an observed already-exited race ONLY when an immediate GetLastError read records5 and the same still-valid retained handle supplies a successful signaled wait and successful exit-code query. Keep the failed API result and full proof. Live error5, invalid handle, any other error even with later exit, failed/missing wait or query proof remain cleanup failures permanently. No fallback or security exemption.

Exit observations and close faults are recorded before joining evidence writers. Physical quiescence requires both proven process exits, all private/capture/evidence threads and pipes finished. Prior faults survive later quiescence. A deadline is always INCONCLUSIVE execution; the fixture can pass ONLY by correctly verifying that expected technical result, exactly one actual-worker termination, both exits125 rather than watchdog124, no unaccounted cleanup failures, complete bounded I/O evidence, and disabled real authorization.

One batch after source/freeze commit: exactly twelve pure tests declared in cleanup_tests.py, then exactly one fresh non-model fake_deadline child through unchanged entry.py and the actual owned_capture.supervise/run_capture boundary. Deadline1s from supervisor start; child watchdog9s; existing bounded capture cleanup joins retained. No full toy matrix, normal worker/audit, standalone identity suite or other process fixture rerun. No retry or extension if a test fails. Batch/prospective preparation budget180s invoked commands,32MiB namespace,5MiB per file. Conservatively allow24 ownership events and26 evidence writers per capture under existing64/66 caps; no cap increases. The one-second deadline remains measured from supervisor start including handshake, matching V1, not reset on READY.

Future (not released): one frozen Qwen0.8B CPUfloat32 load,42F16D,300s worker+15s cleanup+90s saved judge,96MiB/5MiB,13 strict hook checks. The unchanged record bound86,325,248 bytes includes16MiB hook and16MiB source/log/receipt reserve; this successor does not enlarge the scientific envelope. Current explicitly reviewed base SHA b7a12c3af0b4db44191eec14ea095eba731b7328917f570806183093d19ddca2 and all inherited runtime bindings stay fixed. Old binary equivalence is not asserted; any new mismatch stops.

Microsoft documents TerminateProcess as asynchronous and error5 after termination on an open handle: https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-terminateprocess . That does not justify ignoring error5 without same-handle exit proof.

Freeze code and tests before the single batch. Production authorization remains false. Preserve raw deadline execution INCONCLUSIVE separately from fixture verdict. Final inventory is written after all child processes, I/O and evidence writers plus the final report/receipts finish. Stop with a scoped commit and proposed release schema for independent root review, never launch a scientific run.
