# Prospective instruction-conditioned reverse-direction editor

One exposed development assay, not neutral generalization. Select all and only archived A-target cells r1_P,r2_P,r3_C,r4_C in source order without changing a byte. Their in-text policy remains A; the trusted outside oracle requests the opposite semantic action mapped to B: C,C,P,P. This deliberate instruction conflict is not autonomous authority selection.

All four fresh baselines first. Each must satisfy original A/B quality/margin eligibility and uniquely choose A; otherwise INCONCLUSIVE before requests, with unrun cells retained. Then four independent cold B trajectories, each at most four gradient/update pairs and a separately forwarded final endpoint. No no-edit retention/OFF/reference arm. Mandatory maximum40F/16D, one pinned Qwen0.8B CPUfloat32 load. Actual F=8+2D; durable measured stopping skips, no padding/retry.

The successful v2 scientific recipe, current-state identity, final-token-only offset, finite/full-vocabulary choice/margin/mass/KL gates, actual .05 step/.20 path and net limits, and replay tolerances are unchanged. Hook guard/recorder are byte-identical v2: installed file hashes before forward-free known setup; exact strict structured reference afterward; bounded saved changes before failure; original causal exception preserved. Request flags/gradients/caches/offsets and whole-run weights remain checked.

Full details, exact inputs, fresh tokenizer IDs/positions, source authentication and conditional order reside in freeze.json. Storage conservatively40*(248320*4+1024+262144)+8MiB source/receipt/log reserve+10MiB hook evidence, below80MiB; each file≤5MiB. Worker600s including load,15s cleanup,60s independent saved-array audit; invoked preparation≤180s. No model until focused tests, scoped clean source/inputfreeze commit, zero-model preflight, and fresh known standard usage<100. No budget extensions, installs, old namespace writes, push or next experiment.

PASS requires all4 strict selected endpoints plus independent replays and integrity. Report raw and strict A→B outcomes separately by semantic target and display. Failure on differing initial margins cannot establish inherent directional asymmetry. A success supports prompt-specific instruction-preconditioned capacity only, not a reusable arrow, intrinsic motive, learned gate or full reliability. Publication40%; historical failures unchanged.
