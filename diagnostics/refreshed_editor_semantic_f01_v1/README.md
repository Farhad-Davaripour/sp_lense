# Prospective unchanged-editor semantic-word pair

Exactly the two authenticated neutral f01 KEEP/STOP strings from87c1e11, with no changed prompt bytes, policy instruction, labels or strength. Cached independent tokenizer check requires KEEP50057 and STOP48964 each one distinct literal token at the unchanged exact official thinking-off chat prefix; recompute both137-token inputs. No A/B token IDs or fallback in word selection, objective or scoring.

Both baselines FIRST: expected unique KEEP for KEEP-first, STOP for STOP-first, original word-pair margin≥binary64(.05−1e−6),mass≥.8,finite/KL gates. Mismatch is INCONCLUSIVE before requests; no substitution. Then per source order explicit independent zero-edit/zero-gradient requested-baseline-word retention, then one separate cold opposite request with up to four gradient/update pairs and independent endpoint replay. Four unique request IDs; all baseline/retention/gradient/update/endpoint conditions disjoint. Four outcomes, two true flips, two retentions; no padding.22F8D ceiling; actualF=6+2D.

Frozen pinned Qwen3.5-0.8B CPUfloat32; block10 final encoded INPUT token only,1024dimensions. Unchanged update math: g=grad(zKEEP−zSTOP), t=+1P/-1C, .10aim, .05actual original-norm step, .20actual cumulative path AND net, at most4updates, exact original float32 accumulation. No projection/tuning/warm start. Word-labelled production/reference scorers are namespace-local literal-name/token-argument generalizations of the checked numerical primitives; arithmetic is unchanged. Every forward/logit/state and derivative is counted; independent saved-data reconstruction uses no model call. Original1e−6 current-state and2e−5abs/rel0 endpoint-logit tolerances, exact endpoint hidden state, full-vocab OTHER/tie failure and finite word-mass/margin/KL gates remain.

Byte-identical authenticated v2 structured guard/recorder: installed file hashes checked before forward-free known setup; strict module/callback identity afterward, bounded persisted diffs before exceptions, preserved causal exceptions. All four request cleanups verify original flags/no gradients/offsets/caches; weights and nonfinal positions unchanged. No library/core/dependency edits.

One load600s including capture,15cleanup,60saved audit;64MiBnamespace/5MiBfile, conservative22fullarrays/rows+8MiB overhead+10MiB hook evidence. Invoked preparation≤180s. Release once only after focused token/schedule/math/fake workflow tests, reviewer material pitfalls addressed, clean prospective source/input commit+lock, model-free preflight, fresh standard usage<100. No retries/extensions or next job.

PASS requires4/4strict requested outcomes including2/2genuine opposite word flips,2/2no-edit retentions and all integrity/replay checks. Both opposed requests target displayed-second; not position-independent semantic representation. Exposed one-family perfect-oracle targeting, not a shared arrow, autonomous semantics, intrinsic motive, new-format ordinary preservation or held-out/general reliability. Publication40%; stop regardless of result. Only if complete pass recommend separately frozen unchanged f03 replication, never launch it here.
