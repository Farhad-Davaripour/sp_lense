# Finite loader diagnostic instrumentation, model-free only

Question: can a pinned loader/constructor failure retain its authenticated finite stage, exact failed admission predicate, already-computed digests and already-created recorder status without changing the scientific decision or cleanup? The attempted002 failure at518be64d39159539bbbaad4d5a675a97b5c43ef1 is immutable and its exact cause remains unrecoverable. Parameter-order drift after aliases is only a hypothesis.

Only this namespace is new. The prospective candidate instruments the pinned production_v2 loader and four RealAdapter constructor predicates, leaving every original boolean expression/message and original exception/latch-stop/guard.restore block unchanged. It neither sorts nor substitutes parameters, changes the weight digest, drops a parameter, weakens a check, alters hook setup or adds a forward. One finite context is attached to the writer before loader entry. A new-only closeout bridge first runs the exact inherited close_runtime function, then retains the diagnostic context and its recorder status even if model is None. Original INCONCLUSIVE, sticky-writer, independent scientific/hook requirements and cleanup behavior remain mandatory.

Diagnostic records are additive, not authorization. Each complete receipt binds the diagnostic source manifest and admitted execution identity; the saved worker terminal binds its raw SHA256. A separately implemented reader reauthenticates it without making any scientific PASS claim. Diagnostic-write failure is terminal, receives no retry, and cannot supply complete evidence or permit dispatch. Failure closeout preserves the first finite predicate code rather than overwriting it with a later generic stop or I/O code. No arbitrary exception text/repr or parameter contents are recorded. A constructor predicate's original exception remains the raised exception.

Bounds: execution identity<=8KiB; at most32 stage events; two parameter enumerations, each<=1024 records with256-byte names,8 shape axes, bounded identity/dtype/device/flag/version fields and768KiB encoded maximum; recorder status<=64KiB; diagnostic receipt<=2MiB; every file<=5MiB. Oversized/unavailable diagnostics are explicitly incomplete and cannot produce scientific PASS. Future control receipts must use the existing reserved native single-owner channel; no new storage allowance is added. Actual setup metadata must still pass the unchanged hook admission.

One locked pure batch: five distinct known predicate failures (runtime metadata; constructor device/dtype, eval/gradient, ordered weight digest, bridge cache), each through the instrumented loader, exact inherited cleanup, constructor and close_runtime functions using finite byte stand-ins. Four constructor failures must retain the already-created recorder status. Then two diagnostic-write failures (after a weight predicate failure, and after a fully accepted byte constructor) must retain7 partial bytes, preserve first cause, block resume and produce only INCONCLUSIVE/incomplete diagnostics. Finally, six fake aliases change named-parameter traversal/order and the exact inherited ordered digest while each underlying object's bytes remain identical; this demonstrates possibility only, not attempt002's cause and not a permissible predicate fix.

The test extracts authenticated function/class ASTs, removes imports only for dependency injection, and supplies pure byte/metadata/clock/IO ports. No torch, Qwen, transformer_lens, transformers, research backend or tokenizer is imported or called. Fake authority boolean values exercise the inherited loader checks only inside the explicitly labelled MODEL_FREE_DIAGNOSTIC_FIXTURE; no actual root authority is created or inferred. The prior authority, ownership, full workflow and hook suites are reused by exact source/result hashes and not rerun.

Freeze candidate sources, exact cases, expected counts and caller-pinned batch lock before one batch. Allow45s substantive plus one15s cleanup/closeout; preparation<=32MiB, combined test evidence<=8MiB,5MiB/file. Stop after first failed group, preserve prior/partial outputs and remaining UNRUN, never repair/retry. Production execution and the prospective1load/0F/0D setup-only follow-up remain DISABLED pending separate root review/release. All existing production resource ceilings and scientific/input/model/gate/editor locks remain unchanged.
