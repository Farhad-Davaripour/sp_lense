# One prospective local Qwen setup-only diagnostic — DISABLED

Do not run it from this preparation. No root release, owned launch, model import, weight load, tokenizer call or prompt execution is authorized here.

After independent review of this committed source/test result, root may separately authorize one newly named attempt fresh_confirmation_loader_setup_attempt_001 in a newly locked within-namespace real_evidence output root. It must not reuse attempt002's authority, occupied directory, release files or launcher identity. Reuse the reviewed real_release_v2 acyclic source -> release -> authorization -> trusted caller-lock admission, post-validation120-second actual usage check, exclusive occupation and retained-owned-process permission before research imports. No force/reset/resume flag.

Prospectively bind the instrumented candidate loader/RealAdapter and unchanged pinned production_v2 hook/science sources; inject the existing native critical write_new publisher as writer.loader_diagnostic_publisher for one exclusive LOADER_DIAGNOSTICS.json receipt. Bind the terminal bridge so an already-created raw recorder remains available if the constructor fails. Bind diagnostic source SHA, exact authority/input/runtime/model/installed-source pins, complete output inventory and independent reader to the closed worker/external-capture receipts. This publisher/real setup-only entrypoint and root authority are not integrated or authorized by the pure test.

Fixed operation: reserve exactly one local pinned Qwen3.5-0.8B CPU float32 ResearchBackend.load(with_lens=False), offline, unchanged weights/config. Install the unchanged forward/derivative guard first. Record bounded named-parameter identity/name/shape/dtype/flag/version enumeration immediately before hook setup and from RealAdapter's own captured post-setup list. Retain only the original constructor's already-computed actual ordered digest plus frozen expected digest. Preserve every original authority/runtime/template/hook/device/eval/weight/cache predicate. Do not sort, reorder, rename, deduplicate differently or replace the digest. No parameter contents in diagnostic records.

If a predicate fails, preserve its first finite stage/code and recorder status, then execute the original latch.stop/guard.restore and inherited failure closeout; missing/partial diagnostics stay INCOMPLETE/INCONCLUSIVE. If the adapter returns, record only SETUP_DIAGNOSTIC_COMPLETE, then use existing end_edit/guard.restore cleanup without executing any prompt or scientific hook-check sequence. A completed setup diagnostic is not a scientific PASS. All24 baselines,48 requests and180 scientific cells remain UNRUN in either branch. No automatic second load or scientific continuation, including after a successful setup result.

Compute ceiling for the later attempt:1load,0F,0D,0tokenizer calls and no prompt tensor execution. The existing production contract is not changed:180F/48D/1load/160tokens/109checks remains the upper contract,1800s worker/180s saved audit/shared15s cleanup inside1995s,288MiB evidence,5MiB/file, existing32MiB preparation. This diagnostic uses a stricter0F/0D scope. Reserve the<=2MiB diagnostic control receipt and bounded terminal-copy overhead within existing closeout/aggregate limits before model access; verify actual complete hook setup admission before any return. Timing is an allowance, not a predicted guarantee.

The independent saved audit must authenticate receipt bytes/source/execution and the closed worker status, preserve first-cause and incomplete indicators, reconcile every partial/untracked byte, and report only diagnostic findings. Root must inspect the result before any further proposal. The alias-order pure demonstration is not evidence about this real attempt and cannot authorize a weight-predicate change.
