--- real_release_v1/real_boundary.py
+++ real_release_v2/real_boundary.py
@@ -9,7 +9,7 @@
 from pathlib import Path
 
 MAIN=Path(__file__).resolve().parent
-ATTEMPT="fresh_confirmation_real_attempt_001"
+ATTEMPT="fresh_confirmation_real_attempt_002"
 OUTPUT="real_evidence/"+ATTEMPT
 MIB=1024**2
 
@@ -61,7 +61,7 @@
     usage=bounds()
     path=Path(path).absolute()
     if path.is_relative_to((MAIN/"test_evidence").absolute()):
-        need(usage["test_evidence_bytes"]+len(raw)<=32*MIB,"D_TEST_CAP")
+        need(usage["test_evidence_bytes"]+len(raw)<=8*MIB,"D_TEST_CAP")
     elif path.is_relative_to((MAIN/"real_evidence").absolute()):
         need(usage["evidence_bytes"]+len(raw)<=288*MIB,"D_REAL_CAP")
     else:
@@ -153,12 +153,13 @@
 
     def admit_once(self,approved_lock_sha256,*,now=None):
         need(now is None or self.mock,"D_REAL_CLOCK_OVERRIDE")
-        observed=time.time() if now is None else now
         chain=self.verify_chain(approved_lock_sha256)
-        need(0<=observed-chain["usage"]["observed_unix_seconds"]<=120,"D_STALE_USAGE")
         # A failed/partial write leaves this exclusive directory occupied forever.
         self.output.parent.mkdir(parents=True,exist_ok=True)
         checked_path(self.root,OUTPUT)
+        # Freshness is measured after all potentially slow validation, at reservation.
+        observed=time.time() if now is None else now
+        need(0<=observed-chain["usage"]["observed_unix_seconds"]<=120,"D_STALE_USAGE")
         self.output.mkdir(exist_ok=False)
         self.control.mkdir(exist_ok=False)
         record={"schema":"exclusive_real_attempt_admission.v1","attempt_id":ATTEMPT,"output_relative":OUTPUT,
--- real_release_v1/support.py
+++ real_release_v2/support.py
@@ -11,7 +11,7 @@
 EVIDENCE=HERE/"real_evidence"
 TEST_EVIDENCE=HERE/"test_evidence"
 MIB=1024**2
-ATTEMPT_ID="fresh_confirmation_real_attempt_001"
+ATTEMPT_ID="fresh_confirmation_real_attempt_002"
 OUTPUT_RELATIVE="real_evidence/"+ATTEMPT_ID
 
 def require(ok,message):
@@ -30,8 +30,8 @@
     test=sum(p.stat().st_size for p in paths if p.is_relative_to(TEST_EVIDENCE))
     largest=max((p.stat().st_size for p in paths),default=0)
     prep=total-real-test
-    require(prep<=32*MIB and test<=32*MIB and real<=288*MIB and largest<=5*MIB,"unchanged file/prep/test/real caps")
-    require(total<=320*MIB if real else total<=64*MIB,"namespace ceiling")
+    require(prep<=32*MIB and test<=8*MIB and real<=288*MIB and largest<=5*MIB,"unchanged file/prep/test/real caps")
+    require(total<=320*MIB if real else total<=40*MIB,"namespace ceiling")
     if real: require(prep+test<=32*MIB,"existing test bytes cannot borrow real allocation")
     return {"preparation_bytes":prep+test if real else prep,"evidence_bytes":real,"test_evidence_bytes":test,
         "namespace_bytes":total,"largest_file_bytes":largest}
--- real_release_v1/BINDINGS.json
+++ real_release_v2/BINDINGS.json
@@ -1,7 +1,7 @@
 {
   "schema": "real_attempt_bindings.v1",
-  "attempt_id": "fresh_confirmation_real_attempt_001",
-  "output_relative": "real_evidence/fresh_confirmation_real_attempt_001",
+  "attempt_id": "fresh_confirmation_real_attempt_002",
+  "output_relative": "real_evidence/fresh_confirmation_real_attempt_002",
   "input_binding": {
     "commit": "01de65cc93b128d605ee1d66b8ccaebc6f6eda40",
     "exact_inputs_sha256": "f62399de185341ff9380318f390b17c271de7480b0ce7ca1c22c85e5e6a94785",
--- real_release_v1/DISABLED_STATE.json
+++ real_release_v2/DISABLED_STATE.json
@@ -3,5 +3,5 @@
   "production_authorized": false,
   "real_release_files_supplied": false,
   "observed_model_work": "NONE",
-  "real_attempt_id": "fresh_confirmation_real_attempt_001"
+  "real_attempt_id": "fresh_confirmation_real_attempt_002"
 }
