# Narrow real-release freshness successor

This prospective model-free successor changes only the boundary namespace/attempt identity and the placement of the initial receipt-age measurement. It preserves real_release_v1 source e490fd32ed999cb0000a78250d1d20363a1b371b, result d002d14604eb91da0877ea866c6338f05143f367, and its limited five-group PASS unchanged. The independent freshness blocker remains part of that historical result.

After chain/input validation and output-parent/path checking, admission samples the actual clock and enforces an age in [0,120] seconds immediately before the exclusive attempt-directory reservation. The reservation is the admission event. Later durable receipt writes and worker/audit authentication validate the original admission timestamp; they do not create a new freshness window. Existing partial-write occupation and no-reset/no-resume semantics are unchanged. Production caller clock overrides remain forbidden. No new post-reservation freshness gate or receipt schema is introduced.

The only future real attempt is fresh_confirmation_real_attempt_002 under this namespace's real_evidence directory. No root_release files or real_evidence directory are created by preparation or this test. Real execution remains disabled until a separate root-reviewed release.

One frozen regression batch, not the old five-group or 96-forward suites, uses pure mock authority chains and a test-only replacement of real_boundary.time. Every tested admission calls admit_once without the now override, exercising the actual production clock-sampling branch. The helper advances time during real chain or path validation; no model-aware imports or calls are permitted. Intended REAL_QWEN mode in a mock chain never represents observed real work.

Four ordered groups: (1) separate verification-delay and path-delay cases start at receipt age119, finish validation at121, and must raise D_STALE_USAGE before attempt reservation, admission receipt or loader/sentinel dispatch; (2) timely validation starts at110, finishes chain at118 and path at119.5, records119.5, reaches one pure sentinel, then reauthenticates unchanged worker/writer/audit identity at age9000; (3) a true production Boundary rejects explicit clock override before any chain verification; (4) the actual launch.py --preflight path returns DISABLED_NO_APPROVED_REAL_RELEASE with no real authority/output and no loader/import work. Every remaining group is UNRUN after the first failure, with no repair/retry.

The single batch allows45 seconds substantive work and15 seconds reserved cleanup/closeout, with a process-ending hard timer before60 seconds. Preparation <=32MiB, all test evidence <=8MiB, per-file <=5MiB. Production ceilings remain exactly180F/48D/1load/160tokens/109checks,288MiB evidence,1800s worker/180s audit/15s shared cleanup,1995s total. All engine/judge/loader/scoring/identity/hooks and input-bound records are reused unchanged by exact hashes. No full workflow, new scientific evidence, real ownership exercise or real hook capacity claim is made by this pure regression.
