Model-free oracle hook lifecycle diagnosis

The fixed prospective scope and ordered cases are in PLAN.json. The only forward operations allowed are tiny CPU arithmetic fixtures built from actual installed bridge/HookPoint classes, never Qwen or another language model. No derivative computation. Preserve any failure; no diagnostic retry or real-model release is implied.

Source-grounded candidate: materialize exactly BlockBridge's verified pre-LN infrastructure before taking a strict structured callback/module baseline. Validate the exact additions instead of ignoring new modules or callbacks. After setup, require complete callback identity/order, permanent metadata, registry and module-graph identity. v1's exact historical mismatch was not recorded; a reproduced mechanism establishes plausibility only.
