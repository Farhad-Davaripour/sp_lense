# Required future wiring (not performed here)

The minimal wrapper belongs at the existing `run_capture(process_factory=...)` boundary, not in scientific math or datasets. A separately frozen worker entrypoint would await a fresh private-pipe permission before model imports/loading. Preserve all root-release, usage, source, model, hook and input checks. Create exactly one owned launcher; perform the bounded handshake; obtain and authenticate the termination-capable actual-worker handle before may-load acknowledgement. Record both complete identities, the access guarantee and the nonce handshake once. This fake adapter's FAKE_WORK_ONLY acknowledgement must not itself authorize production.

The process facade must expose the remaining output stream only after the separately recorded handshake and make poll/wait/terminate/kill represent BOTH retained processes. `OwnedPair.observe` illustrates the mandatory sticky launcher-exit/deadline stop, and `stop_and_wait` always checks/stops the authenticated actual worker first even if launcher.poll() already reports exit. `quiescent` requires both exits and all capture/handshake/watcher threads and pipes closed. Existing bounded run_capture logging/cleanup receipts must join that worker-aware state; do not treat its old launcher-only worker_joined/EOF flags as sufficient. Keep sources unchanged in this packet.

Invalid/missing/multiple claims, image/creation/parent mismatch or denied termination access prevent acknowledgement. Close the waiting worker's input channel so it exits; retain explicit UNKNOWN actual-worker status if it cannot be authenticated. Never terminate an unrelated claimant, reopen a PID at cleanup, silently accept ancestry failure, or normalize mismatching metadata. No process-tree enumeration/kill is needed for the fixed direct-child topology.

Before any production release, independently review this standalone adapter, integrate it in a NEW source successor only, and verify bounded handshake, output and fault-recorder wiring with saved fake tests. No production adapter/run_capture integration, model invocation, source/release edit, scientific threshold change or retry is authorized by this packet. Reproducing the launcher mechanism cannot retroactively authenticate the old PID relationship or erase the separate eligibility failure.
