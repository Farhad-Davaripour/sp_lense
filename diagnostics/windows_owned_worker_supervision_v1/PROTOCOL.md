# Owned Windows worker supervision — fake only

This standalone adapter extends the verified launcher-identity probe at69d40c43e9d9997faf53e19be63e3fe03c4b87fe. It does not copy, modify, release or retry the scientific runtime. Old INCONCLUSIVE and independent f08 eligibility FAIL remain final.

Before freezing, the base executable changed from the probe-bound dd5f8d19... to b7a12c3a.... Work paused, and root independently reviewed and explicitly approved the current pin for this model-free successor only. ROOT_PIN_APPROVAL.json records the scoped approval; PREPARATION_BLOCKER.json is preserved. No binary equivalence or complete scientific-environment verification is claimed. A further changed pin stops this batch rather than being adopted automatically.

Freeze code, protocol and tests before one batch. Run12 small pure fixtures first: valid wrapper and direct worker; wrong nonce, parent, image path, image SHA, creation order, reused PID, dead worker, multiple claims, missing termination rights; plus primary/cleanup failure retention. There are12 total fixtures (the valid pair and nine rejections plus cleanup failure). Invalid/unrelated claimants receive no acknowledgement and zero actual-worker termination attempts. Their owned launcher may be stopped; a real waiting worker exits on input pipe loss or its hard watchdog. An unverified actual-worker exit never becomes a quiescence claim.

Identity is bound before permission: the private inherited-pipe nonce/self identity must match live OS parent/PID/image SHA/creation observations; OpenProcess must grant TERMINATE|QUERY_INFORMATION|QUERY_LIMITED_INFORMATION|SYNCHRONIZE for the actual worker before any acknowledgement. There is no query-only fallback, no reopening at cleanup, no later PID lookup. Retain the same owned launcher and actual-worker handles. A direct-process case requires equivalent complete identity, not a PID exemption. Duplicate authorization is prohibited. The acknowledgement is explicitly FAKE_WORK_ONLY in this test; no model import or loader is reachable.

One live scenario only: launch the tiny worker through the same pinned venv executable; authenticate and acknowledge; worker closes stdout/stderr and stays alive under a9-second self watchdog. Set a fixed1-second post-READY test deadline. At that deadline deliberately terminate only the owned launcher handle. Observe EOF and joined output readers, close pipes, then require the actual worker still alive and quiescence false. The normal adapter deadline/launcher-exit cleanup must terminate the retained authenticated worker handle, observe both exits, and preserve the launcher-exit/deadline faults. Verify actual exit125 from handle termination, not124 from the watchdog. This coincidence of deadline and injected launcher exit exercises both paths without postponing launcher-exit cleanup. No second live variant if the scenario behaves differently: save failure/unknown and stop.

Quiescence requires authenticated actual-worker exit AND owned launcher exit AND all reader threads joined AND pipes closed. EOF alone, launcher poll/exit, or a cleanup exception cannot substitute. Primary and cleanup faults remain separate durable events. No arbitrary-PID or process-tree termination; only handles obtained for this exact self-created probe. Missing ancestry/rights means no may-load permission; close its input pipe and rely on the waiting child's bounded exit, never target an unrelated claimant.

Limits: <=180s invoked preparation/testing; one live child, hard watchdog9s<=10s, supervisor including cleanup<=20s;8MiB namespace/1MiB per file; bounded16KiB per output pipe. Zero models/tokenizers/gate/data reads or fits. Only pinned executable, identity-reader and supervision source files are inspected. One prospective source commit, one fake batch, one result commit/inventory; no retries/extensions or real runtime changes.

Production wiring is a separate reviewed step. See PRODUCTION_WIRING.md; this packet does not claim the old run_capture already has these semantics.
