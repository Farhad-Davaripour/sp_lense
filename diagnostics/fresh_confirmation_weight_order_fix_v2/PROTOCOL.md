# Prospective reader-only optimization successor

Question: are the SAME mandatory saved weight-proof conditions enforced at actual Python optimization0,1,2?

Predecessor diagnostics/fresh_confirmation_weight_order_fix_v1 source ef4be24715df7fdab2470a89c583a29a39d0a27e/result e7a689b9c795573c58c72e693579c9acb10ac953 remains immutable; inventory9547f0a48b481d9955fa428e9b01e97d108d542882377fe808bad748c6808013. Its11byte-standin cases passed, but independent review blocked real release because weight_reader mandatory asserts vanish under optimization. The retained-order correction otherwise passed review. This successor neither reruns nor repairs those old outcomes.

## Exact source scope

Replace each weight_reader.py assert CONDITION with need(CONDITION). need raises a finite ValueError on false; the existing reader catches it and returns BINDING_INCOMPLETE. All condition ASTs, branch order and classification results remain identical. No broad reader rewrite or new scientific predicate. Source parity checks the exact AST transformation including the sole helper, and rejects any remaining reader/test assert.

Twenty-seven inherited files are byte-identical, including retained-order loader/adapter/helpers, full native setup reader, controllers, hook/IO/counting components and original14predicate/digest/current registry/cleanup checks. Only successor namespace/attempt constants, matching bindings, source manifest and reader-only tests/docs are otherwise new. Every content digest still uses the complete strong pre-hook tuple; this assignment never accesses or hashes parameters.

New future attempt fresh_confirmation_weight_order_fix_attempt_002. Existing1load0F0D0encoding,180worker60auditONE15sharedcleanup255total,288MiBevidence/5MiBfile/native2MiB remain unchanged. No real root_release supplied. Complete real setup/cleanup proof and later root review/authority remain necessary; no scientific questions or automatic continuation.

## Single saved-evidence regression

Only six pinned saved FAKE files are read: native/terminal pairs from v1 alias_reordered, wrong_initial, mutation_before. No parameter access/hashing, constructors, models/tokenizers/backends/torch imports, forwards, derivatives or encoding. Rebinding source/execution identities is explicitly recorded as SYNTHETIC_SAVED_FAKE_TRANSFORMATION_ONLY, never real authority or real evidence. No raw dataset or author inputs are read.

TEST_PROTOCOL.json fixes optimization groups0,1,2 and10cases per group: valid binding; wrong receipt hash; inner source,execution,phase,multiplicity,byte-boundary and digest tampering; preserved initial-fingerprint-mismatch and constructor-fingerprint-mismatch classifications. Inner tampering repairs the outer native hash and duplicated terminal proof. Digest tampering also repairs the already-computed-digest duplicate, so the mismatch-classification guard must still reject it. Multiplicity tampering repairs occurrence counters so the identity-multiplicity comparison is actually reached. Fixed expected failed-need source lines prove rejection happened at the intended mandatory condition.

Each group is a direct pinned stdlib interpreter process, with -B -E plus no optimization flag/-O/-OO. Explicit require(sys.flags.optimize==requested) verifies actual optimization. A finite observer wraps need only to record its failing caller line and then invokes the original need; no condition is removed or substituted. All test expectations use explicit checks, never assert. Native/terminal/transform/results are exclusive acknowledged new saved files with hashes and bounded readback.

One45s substantive deadline covers all three child groups and every saved reader; ONE15s aggregate retained-child cleanup/final closeout inside60s. No deadline reset or retry. The exact direct interpreter is pinned by path/hash; Popen retains its process handle, no PID rediscovery/tree kill/venv-wrapper child ambiguity. Handles/pipes must close and children must be quiescent. A59.5s parent terminal timer bounds the batch; incomplete groups/remaining suffix stayUNRUN. This tiny pure-child ownership check does not replace or certify the full inherited real supervisor.

32MiBpreparation/8MiBcombinedtestevidence/5MiBfile. Tool-derived actual standard-Codex usage must be available/nonexhausted/<100 and<=120s old at start. Source/test/input lock and exact expectations are committed before one batch. Failure means immutable receipt/no repair or rerun. Success is PASS_SAVED_READER_OPTIMIZATION_ONLY, never scientific PASS.

## Release boundary

FUTURE_ROOT_RELEASE.md gives the exact prospective command for fresh_confirmation_weight_order_fix_attempt_002; old consumed authority/output may not be reused. Root must independently review this successor, supply a separate acyclic source→release→authorization→caller-pinned lock and fresh actual usage, then authorize at most one setup-only attempt. Admission after slow checks/path validation retains its120s requirement; audit reauthenticates the original immutable admission. This preparation does not authorize a real load, LoRA, steering or logit study. All24/48/180scientific cells stayUNRUN.
