# Preflight scientific-failure join: narrow synthetic repair

The only evaluator change delays the existing wrong-route exception until finite
self eligibility failures have also been recorded. Route-only precedence remains
RoutingMismatch; eligibility-only precedence remains EligibilityError. No update,
acceptance, gate, gradient, hook lifecycle or independent-judge equations change.

This namespace owns its copied sources and evidence only. Parent pipeline db7c3c05
and its 693-entry inventory remain immutable. Parent dependencies are authenticated
before copying; new source hashes are frozen before exactly one combined synthetic
fixture plus two minimal ordering regressions. All use two fabricated prompts,
not final cohort text, model residual captures, a model loader or a tokenizer.

Expected verification PASS means the synthetic experiment correctly reports FAIL:
both preflight failures survive, zero derivatives/edit registrations occur, and all
40 reserved request slots stay UNRUN. The unchanged independent judge must agree.

Limits: 180 seconds invoked preparation/tests, 64MiB namespace, 5MiB per file.
No retry/full matrix, git mutation, sealed reveal, real model or outside write.
