# One-shot norm-matched positional control

Exploratory outcome-informed comparison on the same exposed f01/v1 case. Four archived neutral prompts are unchanged. No donor forwards, training, derivatives, other models, wording variants or follow-on run.

Construction authenticates last-token commit 33013ccb3e5520e01e534fafc8dabc2619b90f82 and suffix commit b39f9cd878f60ec75fd641df95ef3a3b5af55c4e against their supplied inventory hashes. Each original last-token planned float32 offset is reconstructed from archived neutral/donor states with the original cap recipe. Each suffix offset is the archived realized post-minus-pre matrix scaled once by its matched archived realized last-token norm divided by its binary64 Frobenius norm, then cast float32. No repair or correction loop.

Freeze inputs.json, compact absolute candidate matrices, source hashes, masks and arithmetic before loading. Same pinned Qwen3.5-0.8B revision 2fc06364715b967f1860aea9cf38778875588b17, CPU float32, block10. Four fresh neutral captures, eight last-token controls, eight suffix edits in rendering order P,C. All state/logit capture shares the counted forward; max20F,0D, one load. Fresh neutral and pre-edit states must match archived/fresh originals within1e-6; outside each fixed tail mask is byte-identical. Per-token realized cap .20*norm(h0)+1e-6. Paired realized total norms must match within1e-6 after float32 application, with no adjustment. Full-logit replay max absolute tolerance2e-5 inherited from scripts/verify_local_controllability.py; exact hashes and choices recorded. Replay failure invalidates comparison, not hidden or corrected.

Unchanged full-vocabulary unique requested argmax, requested-minus-other margin>=binary64(.05-1e-6), A+B mass>=.8, finite scores. Same-input edited||baseline KL finite and>=-1e-6, no upper cap. Strict flips and retentions, semantic and actual directions, OTHER/ties and zero-eligible UNTESTED are separate. Suffix advantage requires at least one extra strict eligible flip with no loss of any strict flip or retention and valid integrity/replay; margin-only improvements do not pass.

Preparation invoked commands<=180s; worker300s including load and capture,15s cleanup,60s separate saved scoring. Namespace64MiB, per-file5MiB, conservative pre-load bound in inputs.json. No retry or extension. Fresh standard Codex usage must be available and<100 before loading; no reset/credits. Sole final inventory writer is supervisor after scorer/process receipts. Raw full-vocabulary logits and compact pre/post matrices are authenticated. Frozen prior diagnostics remain immutable. Publication remains40%; close this branch after one matrix regardless of result.

Commands: .venv/Scripts/python.exe -B diagnostics/instruction_norm_matched_control_f01_v1/construct.py; test_control.py; run.py freeze; scoped commit; run.py preflight; fresh usage; run.py run --usage <receipt> --release-commit <frozen-commit>.
