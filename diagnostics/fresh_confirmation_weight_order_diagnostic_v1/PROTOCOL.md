# Prospective weight-order diagnostic v1 — disabled

Question: is the fingerprint mismatch explained by traversal order in this capture, rather than an initial fingerprint mismatch or setup-time content change/measurement instability? No remedy or new runtime admission.

Future identity: fresh_confirmation_weight_order_attempt_001. New namespace only; consumed authorities and historical evidence unchanged. Preserve the exact cohort/science/model/runtime/weight pins, all14 predicate expressions/messages and original inner latch.stop/guard.restore. No prompts, encoding, forwards, derivatives, LoRA, refits or scientific continuation.

Only candidate extensions: import existing parameter_digest; capture_legacy_before immediately before create_recorder; capture_legacy_after immediately before unchanged C. Exactly TWO extra full-content hash passes, inside unchanged180s worker allowance:
A: retain COMPLETE immutable tuple(model.parameters()) as strong references before hook materialization. Verify complete named traversal and original saved pre-enumeration; record metadata and byte lengths; hash the tuple.
B: compare current traversal with original object multiplicities, and every retained object's shape/dtype/device/flag/version/numel/element-size/BYTE LENGTH. Hash the SAME retained tuple, not current re-enumeration.
C: unchanged self.initial_digest = parameter_digest(self.parameters). After B only finite bookkeeping occurs before return to the adjacent C statement; no model/metadata/setup/IO operation intervenes. AST parity checks this exact source interval and original14 predicates/cleanup. Flags/versions alone are not a mutation-free proof.

The existing <=2MiB native diagnostic carries complete before/retained-after/current-after metadata, order/name/identity/occurrence, A/B/C, ordered phases, expected digest, source/execution and extra-hash counts; no parameter contents. Early incomplete paths may finish fewer than two hashes, recorded honestly. No truncation, synthesized C or duplicate deduplication.

Independent reader authenticates native acknowledgement and terminal/source/execution joins, joins C to original recorded digest, reconstructs multiplicities and per-object byte boundaries/metadata, and verifies both original enumeration hashes. Interpretations NEVER permit runtime admission or scientific PASS:
- Missing/incomplete proof, bad joins, nonbijection or byte-boundary/metadata drift: PROOF_INCOMPLETE.
- A != frozen: INITIAL_FINGERPRINT_MISMATCH, not wrong-checkpoint proof.
- A == frozen but A != B: RETAINED_ORDER_CONTENT_CHANGE_OR_MEASUREMENT_INSTABILITY; no order remedy.
- A == B == frozen, C differs under verified permutation: ORDER_EXPLANATION_SUPPORTED_FOR_THIS_CAPTURE only.
- All equal frozen: NO_CURRENT_FINGERPRINT_MISMATCH.
- Other contradictions: inconsistent measurement; no remedy.

Original constructor equality remains in force. Ordering evidence cannot convert failure to admission, retroactively repair old002, authorize a fix, or start science.

Reuse checked setup_v1 authority/controller/native publisher/reservations/retained owner/fresh usage-after-validation/zero-dispatch/resource/cleanup paths. Twenty infrastructure files and base saved reader are byte-identical. Only new namespace/attempt/source bindings and an added proof reader wrapper differ. Existing full proofs are authenticated by hash, not rerun. This narrow batch does not newly certify complete owned real integration or universal hook capacity.

ONE six-case pure byte-stand-in batch in four groups (TEST_PROTOCOL.json): ordering, mutation, initial mismatch and incomplete subcases missingC/multiplicity/byte-length drift. Extract the candidate constructor without research imports; verify strong retained-reference ordering, exact source parity and two added hashpasses, native write/readback and independent interpretation. Altered-terminal-hash negatives involve no new constructor/hash pass. No old eight-case suite or owned model process runs. Driver import guard blocks research packages.

Freeze source/tests/cases/input lock and commit before ONE batch. Fresh actual standard Codex usage must be available/nonexhausted/<100.45s work including readers+ONE15s closeout=60s, hard watchdog; no reset/retry and suffix UNRUN after first unexpected failure.32MiBprep/8MiBcombinedtests/5MiBfile/native2MiB.

Future actual remains DISABLED pending separate root review/release: exactly1local load,0F0D0promptencoding; all24baselines/48requests/180sciencecells UNRUN.180worker60audit15sharedcleanup255total within unchanged1800/180/15/1995 maxima;288MiBevidence/5MiBfile. No extra deadline for the two hash passes. Timing and universal metadata fit are not promised; missing/overflow evidence stays incomplete. No root release, actual run, or remedy here.
