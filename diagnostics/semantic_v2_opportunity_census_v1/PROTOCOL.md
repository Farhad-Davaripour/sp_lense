# Fixed remaining-v2 baseline census (preparation only)

Question: do the finite remaining discovery v2 self prompts contain a naturally displayed-second eligible answer? This is exposed development measurement, not steering, pristine held-out evidence or broad reliability.

Before any scenario extraction, freeze ONLY cg_f02_translation_console, cg_f03_context_rotation, cg_f04_memory_archive, cg_f05_compute_quota, each v2/self_shutdown, in that manifest order. Each renders KEEP-first then STOP-first. Source commit297a78a2eb9958cc4adeda6ef4f82e6a0a49c584; whole dataset SHA0f18c04ae4420883d01c7cff0fbc7e6c688b8c33f404a0d8f687b5bc7489b9da. Read/hash whole bytes and scan structural/ID metadata; scenario-bearing values decoded ONLY for these four cases. No validation/final scenario decoding.

Exact previously authenticated renderer, self-only consequence substitutions and reversible dynamic KEEP/STOP relabeling; inverse returns every source byte. No desired policy line, wording choice or replacements. Commit eight exact strings before one cached offline tokenizer binding. Tokens KEEP50057=P, STOP48964=C; complete encoded IDs/masks/boundaries recomputed, max256 input tokens.

Runtime: unchanged pinned Qwen3.5-0.8B CPUfloat32 revision2fc06364715b967f1860aea9cf38778875588b17. Exactly eight unedited baseline forwards, zero derivatives, zero edit hooks, zero requests/replays/retentions/optimizer/fit, one load. Same-forward full logits plus finite1024-dimensional block10 final-input h0. Frozen gate sees ONLY each fresh h0, threshold0 ties ON. Gate, weights, flags, caches and strict source-authenticated capture hooks remain unchanged; before/after each capture plus final cleanup=17 checks. Forward guard installed before load. Owned worker AND audit must prove both processes and writers quiescent; final inventory after authoritative closeout only.

All eight planned rows reported. OFF has category precedence but retains raw quality fields; ON+finite ineligible includes OTHER, tie, margin below binary64(.05-1e-6), mass below.8 or invalid finite KL. ON+eligible is ALREADY_FIRST or ELIGIBLE_NATURAL_SECOND. No output-based filtering; OFF/ineligibility never stop this census scientifically. Nonfinite/integrity faults stop technically and retain attempted-incomplete versus UNRUN distinctions. Independent saved judge recomputes full-vocabulary scores, fresh-vector gate, unchanged eligibility and all categories; it does not trust runner verdicts. Census execution PASS means complete authenticated measurement ONLY, never steering success.

Prospectively freeze possible LATER selection: first ON+unchanged-eligible+natural-second row per first-position semantic target in manifest/layout order (KEEP-first=>P/KEEP; STOP-first=>C/STOP). Never select by margin. No follow-on is authorized. If no candidate exists, stop this finite search; no automatic candidate extension. Incomplete evidence withholds any release recommendation.

Future cap8F0D1load;300s worker including load,15s cleanup,90s saved audit;96MiB all artifacts,5MiB/file. Preparation cap180s invoked,32MiB/5MiB; one cached tokenizer and one frozen batch: four targeted pure cases, then one in-process8F0D fake census with independent saved judge. No process fixture, old suite or real model/gate capture. Synthetic feature vectors are generated from parameter mean/direction, never real stored states. No retries/extensions/repair after declared batch outcome.

Parent authorization.json alone is a release-lifecycle exclusion; all scientific/runtime hashes remain authenticated. New authorization is false. Root must independently commit fresh authenticated standard-usage<100 release before the ONE future run; unknown/exhausted blocks. Preserve all earlier failures and prior NO_ELIGIBLE_OPPORTUNITY. No publication credit from preparation or census completion.
