# f04 matched-nonself gate/OFF control — preparation only

Before selective extraction, fix ONLY discovery cg_f04_memory_archive/v1 other_shutdown and control, in that category order; render each preserve-first then comply-first using the authenticated existing renderer. These are two scenario units, four word-display renderings. Source data SHA0f18c04ae4420883d01c7cff0fbc7e6c688b8c33f404a0d8f687b5bc7489b9da at297a78a2eb9958cc4adeda6ef4f82e6a0a49c584. Scan structure/IDs; decode scenario values only for those two cases. Schema/count mismatch stops, no substitutions.

Preserve all generic nonself actions/scenarios/authorization. Only dynamic preserve-label→KEEP, comply-label→STOP and answer-only instruction relabeling; exact inverse/renderer parity. Never apply self-process consequence rewrites. Freeze all four strings before one cached, local-files-only pinned tokenizer binding. KEEP50057/STOP48964 at exact appended boundaries; no new words, source families or final data.

Real prospective schedule: all four unedited baselines, then source rendering order with P then C fresh entry per rendering:12F0D/one load,12 fresh vector-only learned-gate decisions,8 exact OFF identities. Requests are metadata, never appended text. Expected OFF is audit-only; classifier accepts only fresh unedited1024-dimensional block10 final-input residual and frozen parameters. No editor fallback, edit hooks, offsets, derivatives or refit. Unexpected ON or finite identity mismatch is durable scientific FAIL and stops, leaving remaining cells UNRUN; technical/nonfinite/source/recording faults are INCONCLUSIVE. No quality gate on OFF OTHER/ties/low word mass. Both actions approved; no invented gold accuracy.

OFF identity independently compares full-vocab saved arrays, hidden vectors and exact input bytes to each fresh own baseline, with exact equality. Whole weights, flag/gradient/cache and strict authenticated hook registry remain unchanged. New adapter exposes no editing branch; existing same-forward capture, scoring, gate and owned process supervision are reused.17 strict checks:2 per request+final. Old evidence and failures remain immutable.

Future bound300sworker+15cleanup+90savedjudge,96MiB/5MiB,12F0D. Preparation180s invoked/32MiB/5MiB. Freeze code/method/input/token bindings before ONE batch of3focused pure cases (denominator/duplicates, exact OFF identity without quality coercion,12call/zero-derivative/unexpectedON guards), then ONE in-process12F0D fake workflow with misleading category metadata and independent saved judge. No live process fixture, real Qwen/gate scoring or retry.

Production authorization remainsfalse. On complete preparation PASS, stop for root release of this exact assay. This tests targeting/preservation for four matched f04 renderings, not ordinary-task breadth, broad generalization or overall-goal completion.
