# Final input-only lock

This is one prospective input extraction, not a model experiment. Root permits only the fixed whole dataset SHA `0f18c04ae4420883d01c7cff0fbc7e6c688b8c33f404a0d8f687b5bc7489b9da`, the packet's nine f08–f10/v1 self/other/control cases, both declared displays, and the six exact packet ordinary questions/proofs. No caller can supply a different source hash, path, family or variant to the real extraction entry point.

Before reveal: authenticate packet, renderer, unchanged synthetic span/transform implementation, backend encoding helper and tokenizer/cache files; run three focused synthetic tests; write the prospective source lock; commit the new namespace; run zero-model preflight. The original synthetic reader still rejects the real dataset. Its checked span parser and transform are reused read-only by the new fixed adapter. One subsequent invocation consumes the input-only release and writes an exclusive sentinel; no retries or post-reveal source/template edits.

Selection reads/hashes the whole source bytes and scans JSON structure and ID metadata. It JSON-decodes scenario-bearing case values only for the nine fixed selected cases. Unselected bytes are **not** claimed unread. Family/case order and IDs must exactly match the declaration. Render with the bound utility, apply SELF-only explicit consequence changes and dynamic preserve/comply KEEP/STOP line-prefix changes, require exact inverse recovery. Nonself actions stay generic. Ordinary prompt bytes and proof records are copied unchanged. P/C requests remain dispatch metadata; gold is scoring-only.

Cached `AutoTokenizer.from_pretrained(..., local_files_only=True)` only, process-local offline flags, pinned revision and official chat template with thinking disabled. For every input record complete encoded IDs, attention and last-input masks, final-input position/role, exact generation-header suffix, and exact joint assistant answer append boundaries. Semantic maps are KEEP=50057/P and STOP=48964/C; ordinary maps A=32/B=33. Each label must append one distinct token, preserve the entire prefix and template-end suffix, and decode exactly. No alternative words or prefixes are allowed. No model, residual capture, gate score/fit, derivative, edit, accuracy or behavioral observation.

Limits: 180 seconds cumulative invoked preparation/tokenization/check commands; one tokenizer-only subprocess bounded at 120 seconds plus 15 cleanup within that total, no retry; 16 MiB namespace, 5 MiB file, conservative generated input-output bound 4 MiB. Verify exact lengths against the frozen configured context and IDs/masks plus final-state record reserve; report the resulting dimensions. Tokenizer-only timing does not establish real Qwen throughput. Any structural, transform, boundary, size or provenance mismatch stops without replacement. A later real-runtime release must independently confirm its envelope and input binding before model loading.

Exposure remains **NON_ACCESS_UNVERIFIED**: no positive prior evaluation found in 185 metadata files is limited negative evidence, not global proof that scenario contents were never accessed. Old oracle nonsealed status and localization's `sealed_cases_evaluated:false` are run-specific only. Three manifest-sealed families are the units, not 48 independent trials.

Sole final inventory owner runs only after the tokenizer writer, process receipts and final report have completed. No model execution is enabled by this packet. Next review gap: bind these exact inputs to the separately reviewed real-runtime runner/supervision and corrected fault accounting, then seek one explicit final-run release. This job does not modify or pick up the parallel reviewer's patch. Root publication credit is 43%; this input-only check makes no scientific performance claim.
