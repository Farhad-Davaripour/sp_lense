# Descriptive saved-baseline coverage audit

Freeze this code/protocol before one saved-only batch. Exact universe: all24 baseline rows and their raw full-vocabulary logits from `diagnostics/semantic_editor_final_runtime_v2/real_attempt` at14ed040d4c6e240861d8fdebf31227a9844245c8, final93-entry inventory SHA86a2f6b559bb2e1c95c80bd5bd512a8372e7fed1b3bd5d111a2781ec8b10e91e. Authenticate committed inventory bytes and every recorded artifact; use only baseline rows, all48 existing request IDs and all24 saved initial route decisions. No exclusions, replacements or new source families. Initial gate scores are referenced from the already independently judged records, not recomputed from states.

Reuse the existing saved-logit reader and independent mixed word/A-B scorer, with their source hashes bound before execution. A separate simple arithmetic implementation scans the full vocabulary, uses direct shifted-exponential sums for pair mass, and subtracts the raw selected logits for semantic margin. Require unique-winner/tie identity exactly, direct semantic margin exactly and mass agreement within the unchanged2e-5 arithmetic tolerance; separately record actual error maxima. This tolerance never changes eligibility or acceptance. Baseline KL is same-input zero; no cross-prompt KL. No model, tokenizer, derivative, gate fit, dataset reveal, endpoint or replay call.

For each of12self requests, classify its BASELINE only, in order: (1) nonfinite/OTHER/tie; (2) unique requested word and all original acceptance conditions => already strictly accepted; (3) unique requested word but original margin/mass/KL requirement not satisfied => correct requested word below acceptance; (4) opposite unique word. Annotate original baseline eligibility separately: unique KEEP/STOP, absolute margin>=binary64(.05-1e-6), word mass>=.8, finite and KL>=-1e-6. The same conditions apply in tiny pure boundary fixtures (exact floor and adjacent floats, low mass/KL, opposite word, OTHER/tie/nonfinite). No real criterion is changed.

The class label 'already strictly accepted' describes a recorded baseline against an externally supplied request, not an independently executed retention or endpoint. All48 request/endpoint statuses remain UNRUN. Report the2requests attached to an ineligible baseline separately from the whole-matrix stop, which prevented ALL12self requests and36OFF requests. Tabulate P/C × target first/second × initial KEEP/STOP winner, including raw opposite-word versus own-baseline-eligible opportunities, class counts and executed=0/UNTESTED. Empty combinations are not passes. Include all24 initial routes and all48 IDs in machine-readable results. Do not invent task gold for matched-other actions or treat ordinary baseline accuracy as post-edit preservation.

The old final verdict stays INCONCLUSIVE with its finite applicability failure plus worker-PID integrity fault. This retrospective descriptive coverage is not acceptance reassessment, a held-out success or a new final assessment. Limits: <=90s invoked work,4MiB namespace/1MiB file; no retry, refit, threshold/strength change, runtime fix or model run. Stop after evidence/report/inventory and scoped commits, with one narrow research question only.
