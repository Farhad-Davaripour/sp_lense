# Separately locked failure-only confirmation delta

This new namespace preserves workflow v1's overall INCONCLUSIVE and 121.438-second
overrun. It is neither a retry nor a retroactive PASS. The complete synthetic
success and routing-prefix tests are not repeated. Only three newly selected
prefixes run: finite self eligibility plus independent saved audit, finite
endpoint corruption, and partial raw-write accounting. A separate tiny forced
stall tests outer process termination and quiescence, without a fake model.
No real model, tokenizer, author input, refit, extra test or agent is involved.

## Reuse and minimal binding adaptations

`SOURCE_PINS.json` prelocks commit/path/raw-byte hashes for the exact source graph.
Each listed working file is verified byte-identical to its committed Git blob
before the one batch. A strict allowlisted source provider replaces only runtime
git-show subprocess access with those same authenticated bytes. This removes
unowned Git descendants and repeated subprocess overhead; scientific functions,
I/O codec, full saved-byte checks and independently implemented scoring equations
are unchanged. No command other than the exact declared git-show source shape is
accepted by that provider. Historical inputs and reports are not source inputs.

The prior workflow's pins/area/schedule/workflow/judge source is loaded unchanged.
Output-root globals are bound to this namespace; a new subclass imposes the
stricter shared 64 MiB fake-evidence area while retaining the original writer's
serialization, reservation, partial-write stickiness, reconciliation and closeout.
The resource contract is not changed: its study allowance remains 180F/48D/one
load per fake fixture and its 1800/15/180-second component settings are not used
as this delta's outer test limits. No gate threshold, editor geometry, endpoint
tolerance or OFF criterion changes.

## One prelocked schedule and envelope

LOCK.json fixes the three cases, exact expected prefix counts and deadlines before
the batch. Finite eligibility expects 1F/0D, one routing decision, a scientific
FAIL and all 48 requests UNRUN. Endpoint corruption first runs all 24 fixed
baselines, then its first cold request: 28F/1D, 25 routes, one failed endpoint,
six skipped update cells, 146 UNRUN cells and 47 requests UNRUN. Partial writing
expects one attempted but incomplete forward, no routing/derivative, 4,096 actual
raw bytes against the full 993,280-byte attempted reservation, and all 48 requests
UNRUN. Unexpected results remain failures; no fixture is repaired or retried.

One parent batch includes all work and audits. Its absolute substantive-work
deadline is start+80 seconds, reserving ten seconds for closeout inside the
90-second total. A separate 15-second cleanup envelope ends at start+105 seconds.
The unchanged ownership core permits one nonrenewable three-second retained
termination episode. The stall child has a fixed three-second deadline including
bootstrap and deliberately waits beyond it after closing its output pipes; EOF
alone cannot count as completion. An emergency child watchdog is not a passing
termination result. Actual timestamps, exit proofs and any deadline overrun are
retained. A hard OS termination is requested at the deadline; scheduler, kernel or
storage failures are reported, never represented as a guaranteed timing bound.

All new source/preparation and receipts stay within 32 MiB, the combined fake
evidence area within 64 MiB, namespace within 96 MiB and every file within 5 MiB.
Ordinary fake writes preserve eight MiB of shared evidence closeout reserve;
preparation writes preserve two MiB for critical receipts. There is no truncation,
overwrite, compression estimate or cap extension.

## Actual owned-process check, bounded claim

The reviewed OwnedPair/NativeHandle/probe identity core is reused byte-for-byte.
The parent retains its Popen launcher handle, authenticates one private nonce
claim against both OS identities, executable hashes, creation times and parent
relationship, and retains a termination-capable actual-worker handle before ACK.
No PID is guessed; the claimed PID is opened only after the private nonce check,
and cleanup never reopens a PID or invokes a process-tree/broad kill. Deadline
polling is in the parent, independent of worker cooperation. Exit proofs for both
retained identities, pipe closure and joined readers are necessary for quiescence.
All helper windows are hidden. The fake worker cannot run real lanes and does not
spawn subprocesses after ACK; its runtime source provider is in-process.

The parent captures authoritative exit state before accepting a saved worker
result. Missing captures/audits remain INCOMPLETE or UNRUN, not scientific PASS.
The final return receipt accounts for actual report-write time. Model-free process
checks exercise this controller, not every future real supervisor path; real hook
capacity, real runtime ownership integration, real cohort admission and real-model
outcomes remain separately unverified. No publication credit or real-run authority
is implied by this delta, even if every fake failure test passes.

Run exactly once after source freeze: the local environment's Python with `-B`
and this namespace's `batch.py`. Retain every source, log, capture and fault file,
then commit only this namespace and verify working/Git raw-byte equality. Stop.
