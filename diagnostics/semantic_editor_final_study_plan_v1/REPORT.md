Final-study planning packet: checked, awaiting review

The prospective cohort is fixed at 18 semantic prompts (all f08-f10/v1 categories, both word-display orders) plus six new ordinary A/B questions. Both P/C requests are declared for all 24 prompts. Exact ordinary bytes and scoring-only proofs are frozen; semantic content remains unopened. The source whole-file digest and committed frozen system are authenticated.

The schedule contains 180 uniquely identified maximum forward slots, 48 derivative slots, 72 fresh routing decisions, 12 independent self endpoints and 36 OFF identities. Eligible paired baselines necessarily give six no-edit retentions, reducing the realizable ceiling to 132F/24D. Four focused pure-fake tests passed; an additional narrow import/direction-table check passed. No model, tokenizer, gate fit, new gate score or sealed content decode/render occurred.

Proposed later limits: 1,500 s worker + 15 s cleanup + 180 s saved audit, 288 MiB namespace / 5 MiB file. The conservative capture/storage calculation is 259,715,072 bytes. These are review proposals, not execution authority.

Explicit gaps remain: request-aware two-policy/retention orchestration, selective sealed input locking, the complete independent final judge, and metadata-based historical exposure review. Finite self eligibility failures are proposed as scientific FAIL, requiring review of this change in classification; original eligibility thresholds remain unchanged. No pristine-heldout claim is made. Publication remains 40%.

Next: one model-free implementation and independent review of the schedule/retention/final-judge adaptations on synthetic data. No sealed reveal or model run should precede separate release. Stop here.
