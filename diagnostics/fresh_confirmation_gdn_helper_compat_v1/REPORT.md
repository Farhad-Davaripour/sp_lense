# Result: PASS_INERT_COMPONENT_ONLY

The one prospectively frozen six-group batch passed in 0.23399999970570207 seconds of substantive work; saved closeout completed at 0.25 seconds, using 0.016000000294297934 seconds of the single closeout allowance. The actual command exited 0 after 0.5154282 seconds. All six groups completed, no group remains UNRUN, and no retry was performed. Actual standard-Codex usage was 12% before the batch. Limits remained 45 seconds work + one 15-second closeout, 32 MiB preparation, 8 MiB evidence and 5 MiB/file.

There were zero Torch/HF/TransformerLens/backend/tokenizer imports, model loads or constructor calls, actual parameter accesses/hashes, forwards, derivatives and encodings. Inert ordinary Python objects and callbacks are explicitly marked synthetic. No installed package, historical attempt, loader, model, gate, editor, prompt, threshold, numerical function or scientific/control rule was edited. Real authority is absent.

## What the fixed evidence establishes

- The exact installed bridge attribute-load ASTs fail for both absent helper names on an inert original instance. The preserved actual first-forward trace remains partial: its exact missing member is a source-supported inference, not a recorded fact or a proof of historical real003's cause.
- The convolution wrapper renames only `x` to `hidden_states`, accepts only neutral `seq_idx=None`, passes every numerical argument object unchanged and returns the exact target result. An inert execution of the exact HF decorator AST shows why the original unmapped x call fails. The recurrence helper retains the exact target object, original arguments/defaults and result, without implicit method self-binding.
- Existing instance/class helpers, unexpected type/phase/multiplicity, altered source bytes, wrong expected callable, injected/live relabel, and later module-target replacement are rejected without overwriting unexpected attributes.
- Before-write and after-write setter failures on the second inert instance roll back all earlier/partial owned additions. A repeated installation is denied. Unexpected helper replacement and deletion failure remain terminal/incomplete, preserve the first cause and never grant scientific PASS.

The four external raw code hashes are authenticated. Saved SOURCE_AST_EVIDENCE.json binds the bridge call ASTs at lines 181/228, constructor assignments, signatures and unchanged numerical-function AST hashes. The actual numerical bodies were not executed: fixture bodies were deliberately replaced with inert capture callbacks; optional imports and the outer kernel decorator were also inert substitutions. Their interface evidence is not numerical-equivalence or real-forward evidence.

## Exact new source boundary

Only this new namespace was authored. `source_contract.py` authenticates and parses/compiles code without importing package modules. `compat.py` supplies the bounded target admission and transactional per-instance installation. `compat_tests.py` is the one fixed inert batch; it uses explicit, non-assert expectations and exclusive acknowledged saved receipts. Protocol, source pins, freeze, usage, batch lock, command closeout and results accompany these files. There is no scientific runner or executable real-launch command in this component.

The live admission branch requires the existing exact registered HF module/class/callables and source/default identities; it accepts only the source-pinned wrapper when that wrapper has already selected its own original HF Torch implementation. It does not force that selection, unwrap for execution, choose a different kernel or import optional packages. That live branch has NOT been exercised. Different outer wrappers or selected implementations remain rejected/unverified, even if an inert signature test succeeds.

## Pins and remaining work

- Source/test freeze commit: `e069480b846382d1965fd53a706d4421683f022f`.
- Source manifest SHA: `5c016871ef19485002259e3637c3221b8ef61375991d77a9cc0bfc7a1e175eaf`.
- Batch lock commit: `796acfebcbd0af90a466b249290573fa0b747993`.
- Batch lock SHA: `06fa73b5b724b7eb60126c2a829c68d40cefa891f006b4c386f9d9dd4783cde9`.
- Batch report SHA: `6dd468b2cc907ce1e5e5fd296c5b6b4568cea59b4847741ced0ee93eda86678a`.
- Closed first-forward evidence commit: `61a89ba171f471f8aedccea857dc6418a65b7f3e`; inventory SHA `9c44f878284fb4d3322aa27f69ec73d11881bcfa01445302850cfce329f7b950`.

The 11 raw test-evidence files total 105,872 bytes; the largest is the deliberately altered source fixture (88,325 bytes). FINAL_INVENTORY.json records every namespace file except itself and the exact final byte totals.

The smallest next proposal is the narrowly reviewed placement in INTEGRATION_PROPOSAL.md: authenticate actual already-loaded callables and a complete set of original GDN instances, install the two transparent helpers before NEW complete hook/setup/reference admission, and bind/inspect their exact identities alongside all existing checks. Source/runtime callable admission, complete live instance enumeration, prospective hook-reference capacity, authoritative terminal/cleanup joins and any real forward remain unverified. They require separate integration approval; any actual diagnostic additionally requires a fresh root-authorized attempt and existing one-load/at-most-one-forward/zero-derivative/zero-encoding limits. This result neither launches that work nor supplies scientific success.
