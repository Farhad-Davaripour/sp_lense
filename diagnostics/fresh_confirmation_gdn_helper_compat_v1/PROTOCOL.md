# Prospective GDN helper compatibility component

One question: can a transparent per-instance interface binding supply the two source-demonstrated missing helper interfaces without changing numerical functions or guards? This is a component-only, inert-model-free task. No controller successor, real release, loader invocation, numerical execution, or scientific outcome is authorized.

## Fixed evidence and scope

Closed first-forward attempt 001 is preserved at commit `61a89ba171f471f8aedccea857dc6418a65b7f3e`; its full inventory SHA is `9c44f878284fb4d3322aa27f69ec73d11881bcfa01445302850cfce329f7b950`. Its acknowledged trace SHA is `bdbdd584c1ccf564bc2fc02a13ab2be5da1bf38ce166f07d465a40ea98eb3bd7`. The first recorded stage/category is BRIDGE_DISPATCH / ATTRIBUTE_ERROR, with a partial frame list. The exact missing member is inferred from source compatibility, not recorded in that trace; no claim about historical real003's exact cause is permitted.

Four exact external code files are pinned in SOURCE_PINS.json and source_contract.py: installed HF modeling_qwen3_5.py, TransformerLens GatedDeltaNet bridge, HF hub_kernels.py decorator, and Qwen3.5 architecture mapping. Read/AST/compile access does not import these packages or execute their module bodies. Installed files remain untouched.

The bridge reads `hf.causal_conv1d_fn` at line 180 and calls it at line 181 with `x`, `weight`, `bias`, `activation`, and neutral `seq_idx=None`. The HF constructor assigns neither helper, while its existing module-level causal function expects `hidden_states`. The bridge's recurrence call at line 228 expects `hf.chunk_gated_delta_rule`; the HF module provides decorated `torch_chunk_gated_delta_rule`. The frozen AST checks these interfaces, original call expressions and unchanged numerical-function source hashes.

## Component contract

Targets authenticates all four source byte hashes. It binds an exact original class and the exact already-admitted module-level callable pair by object identity. INERT_FIXTURE is explicit and cannot be relabeled as a live module. LIVE_EXISTING_MODULE never imports or loads: it requires the caller's already-loaded, registered exact HF module, original class code, target source/code/defaults, unchanged module-global identities, and the exact source-pinned decorator wrapper with its already-selected original Torch implementation. Unknown outer kernel wrappers, alternate implementations, incompatible code/defaults or source/callable drift are rejected; the component never switches to a fallback or changes the existing decorated target. Actual live admission remains unverified by this batch.

Installation takes a unique, bounded list of labelled original instances (1..24), requires the pre-hook-reference phase, and prechecks that both names are absent from instance, class, and dynamic lookup. Existing None values, existing functions, subclasses, duplicate instances, or unexpected attributes are not overwritten. It installs ordinary per-instance function attributes, never bound methods or class patches.

The convolution adapter accepts exactly the bridge's keyword signature, requires active installation and `seq_idx is None`, renames only `x` to `hidden_states`, omits that documented neutral metadata, and forwards the other four numerical argument objects unchanged to the exact existing decorated target. It returns the exact target result. It does not unwrap for dispatch, alter defaults, perform tensor operations, or introduce a generic fallback. Recurrence installs the exact module-level callable itself; positional/keyword arguments, defaults, return identity and lack of implicit self-binding remain intact.

Every mutation is recorded before its setter is called, including a setter that writes then raises. Failure triggers reverse rollback of only owned exact helper objects, preserving unexpected replacements. A failed installation is terminal and cannot be retried. Incomplete deletion/identity cleanup remains FAILED with rollback_complete false; it never grants scientific PASS or real authorization. Snapshot checks retain other instance dictionary value identities, not weight contents. These checks do not replace the existing model parameter, registry, cache, hook, latch, or ownership predicates.

## One fixed inert batch

Six groups, in this exact order:

1. `old_missing_helpers`: both original bridge attribute-load ASTs raise AttributeError on an inert original object, without invoking callbacks.
2. `transparent_argument_mapping`: two inert instances; exact argument/return identities for both helpers, neutral-only seq_idx, unchanged recurrence object/no self-binding, clean inspection/removal. Exact decorator AST plus inert fallback demonstrates the old unmapped x call is rejected.
3. `existing_or_unexpected_instance`: existing None/function/class helper, subclass, duplicate instance and post-reference phase are rejected without overwriting state.
4. `source_and_callable_rejection`: altered source bytes, wrong expected callable, injected/live relabel, and changed recurrence module binding are rejected before helper use.
5. `partial_install_rollback`: a second-instance setter fails before or after writing; every earlier/partial owned addition is removed, original inert state restored, and another install attempt denied.
6. `closeout_identity_failure`: unexpected helper replacement is preserved and closeout fails; a separate partial-install/delete-failure fixture remains terminal and incomplete without replacing first cause.

All test expectations use explicit checks, not removable assertions. Fixtures retain the actual HF signatures, but replace numerical bodies with inert capture callbacks. The exact source decorator AST runs only with an inert absent-package importer and identity outer decorator; no HF/Torch numerical function, model class, actual parameters, tokenizer or backend is invoked. These synthetic transformations are recorded in every case receipt. They prove interface forwarding and failure handling, not model/kernel numerical equivalence or an end-to-end real forward.

Before the single batch, commit sources, this protocol, cases and source freeze; then commit the caller-pinned batch lock plus a fresh actual standard-Codex usage receipt. Require available usage below 100%, no exhaustion, age <=120 seconds. The substantive deadline is 45 seconds, one shared closeout allowance is 15 seconds, absolute envelope 60 seconds; a 59-second hard-exit watchdog preserves any existing bytes if normal closeout cannot finish. No child processes are spawned by the batch, and no deadline is reset. Preparation <=32 MiB, combined test evidence <=8 MiB, any file <=5 MiB; receipts have explicit finite fields and no arbitrary exception text. The test directory is exclusive and may not be reused. The first unexpected failure leaves the current group incomplete and later groups UNRUN, retains all bytes and ends this attempt without repair or rerun. Missing terminal means INCONCLUSIVE, never PASS.

PASS_INERT_COMPONENT_ONLY requires all six fixed groups, exact source/lock authentication, saved receipt byte readback, zero forbidden imports/work, and timing/storage compliance. It confers no scientific or real execution authority.
