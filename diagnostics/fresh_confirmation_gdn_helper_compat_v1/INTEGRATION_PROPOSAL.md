# Smallest prospective integration placement (not implemented or released)

After the unchanged pinned local loader returns its newly loaded original HF instance, but before HookRecorder setup/reference admission, resolve every intended original GDN instance through the frozen Qwen3.5 bridge mapping. Verify completeness, exact original class, object identity/multiplicity and unchanged parameters/current registry using the existing predicates. Retain the legacy pre-hook parameter order exactly as previously reviewed. This component does not choose a prompt or an alternative model path.

Pass the already-loaded module and exact already-observed callable objects to Targets in LIVE_EXISTING_MODULE mode. Reauthenticate the four code pins, live code/defaults and currently selected decorator implementation. Unknown/different callable topology is a blocker, not permission to unwrap, force a Torch fallback, download a kernel, switch eager_scan/cache/native forward, or accept an observed replacement digest. The live-module admission branch has not been exercised in this model-free component batch.

Install both helpers transactionally on only the confirmed original GDN instances before prospective complete setup/reference capture. Preserve the original guard/latch and all 14 predicates, ordered content digest, parameter flags/versions, registry/cache/hook identities and cleanup. Bind finite component status, original instance identities and both helper callable identities/source pins into the admitted setup receipt. Inspect those exact identities at each existing identity-check point as additional necessary conditions; never substitute for or shorten existing checks. Route every component error into the unchanged irreversible dispatch stop and original failure precedence. On setup failure, roll back only exact owned additions and always run the original guard restoration. Preserve a rollback failure as incomplete rather than masking the primary failure.

Adding helpers changes live object metadata, so old hook/reference receipts cannot be reused as admission for the modified instance. A new integration must capture the full prospective setup/reference and fit the existing allocations; the component itself supplies no hook-capacity or real-runtime guarantee. Extend the existing bounded trace allowlist with the exact GDN bridge, architecture mapping and decorator source pins to improve future finite source attribution without enlarging the trace cap.

The next approval should cover only this integration handoff and its added identity/provenance joins. Do not copy a whole controller or authorize a model attempt as part of this component. Any later real diagnostic needs a new attempt/output, separate root release, fresh usage, retained-process ownership and the previously reviewed first-locked-unedited-baseline-only boundary: Qwen3.5-0.8B CPU float32, one load, at most one forward, zero derivatives/encoding, no continuation; 180-second worker + 60-second saved audit + one shared 15-second cleanup = 255 seconds, 288 MiB evidence and 5 MiB/file. No future real command is executable from this namespace; real authority remains absent.
