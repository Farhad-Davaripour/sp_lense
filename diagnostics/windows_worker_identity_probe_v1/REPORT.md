# Windows worker identity: launcher/child split confirmed

One source-locked trivial probe reproduced the mechanism with the exact pinned venv executable. The owned Popen handle identified launcher PID31860, image `.venv/Scripts/python.exe` (SHA256 `038b13f7d1f8bd011290eb0044fd1c133746082929dbef567d1a566cf25598df`). Its actual Python child was PID6804, OS parent31860, image the pinned base interpreter (SHA256 `dd5f8d19f6755d6491ee7c4bef2fe35ddd521334cc3ca3ed8fc93ebcadf135d0`). The child still reported `sys.executable` as the venv path. Therefore that Python-level path does not identify the OS process image, and direct Popen-PID versus os.getpid equality is not valid for this launcher configuration.

All 11 prospective relationship checks passed: private-pipe nonce, both live handles, child/parent PID consistency, owned wrapper parent, image paths/hashes, creation order and Python self-reports. Both exited zero with EOF; reader joined, no cleanup error or termination attempt. Probe elapsed0.235s; complete invoked command1.2524802s. Exactly one owned trivial child was launched; zero model loads/forwards/derivatives/tokenizers/gate scores. Preparation/verification commands stayed well below90s; no datasets, activations or real-attempt files were accessed except its recorded PID/nonce metadata. All old files remain unchanged.

The inspected local `Lib/venv/__init__.py` lines282–310 describe Windows launcher copies, and `.venv/pyvenv.cfg` binds the separate base interpreter. Those source/config hashes and the distinct installed launcher-template hash are in plan.json. The template and venv executables are not asserted byte-identical.

Historical implication: the prior owned PID26176 / claimed PID13360 mismatch is consistent with the now-observed launcher mechanism. Its live ancestry was not recorded then, so this probe does not retroactively authenticate those PIDs or erase the INCONCLUSIVE verdict. The independent f08 eligibility failure (0.03303 margin below0.049999) remains final and would still prevent joint success.

Minimal proposed change for a separately reviewed successor: authenticate a live pre-load private-pipe nonce handshake, query and retain the reported actual Python handle, and bind its direct OS parent, image/hash and creation identity to the owned launcher handle. Preserve the equivalent strict direct-process case. Reject wrong/missing/multiple claims or unavailable ancestry; never replace PID equality with a blanket whitelist. Retain owned-handle cleanup and every existing scientific/integrity gate. Required pure regressions are enumerated in proposed_binding.json. No production runtime was changed and no model retry is authorized.

Source commit `b6ed206d5cab757276d31ae7d76b71654dbb1f27`; freeze `73822a1b8a7628439b1716f298b715aad53416538ef450d58d21f409d708082d`. This is process-diagnostic evidence only, not behavioral/publication success. Next single step is review of this proposed binding for a future source successor, not another scientific run.
