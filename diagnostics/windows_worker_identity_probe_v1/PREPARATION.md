Preparation used only local metadata/source reads. One PowerShell metadata-formatting command failed to parse because foreach was piped directly; it was corrected to assign the foreach result before formatting. This happened before source freeze and before any probe launch; no child or scientific operation ran. No model/runtime code was edited. All subsequent probe errors, if any, are preserved without retry.
