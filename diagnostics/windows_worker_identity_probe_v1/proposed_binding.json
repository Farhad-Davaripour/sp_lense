{
  "status": "PROPOSAL_ONLY_NO_PRODUCTION_IMPLEMENTATION",
  "candidate": "Authenticate actual Python identity against the live owned launcher before model loading",
  "allowed_observed_topology": "driver -> owned pinned venv launcher -> one directly parented pinned base Python worker",
  "direct_process_alternative": "Claimed PID equals owned handle PID AND image/hash/creation/nonce identity are independently verified; not a blanket exemption",
  "required_checks": [
    "Use a fresh private inherited-pipe challenge/acknowledgement before any model imports/loading",
    "Validate complete single claim and nonce before query/synchronize-only OpenProcess on the claimed PID",
    "Retain live owned launcher and actual Python handles; query PID/parent PID/image/creation time from handles, not repr or sys.executable alone",
    "Require child OS parent PID equal owned launcher PID, wrapper creation no later than child creation, and both pinned image paths/hashes",
    "Require Python PID/PPID/base executable claims agree with OS observations",
    "Preserve verified owned-handle cleanup and actual-child exit observation; no arbitrary-PID or tree termination",
    "Missing OS access, dead/reused handle, wrong ancestry, extra claimant or incomplete nonce handshake fails closed",
    "Leave root release, usage, source/model/gate/input pins, budgets and all scientific gates unchanged"
  ],
  "required_small_pure_regressions_before_any_successor_release": [
    {"fixture": "valid observed launcher/direct-child topology", "expected": "accept"},
    {"fixture": "valid independently bound direct-process topology", "expected": "accept"},
    {"fixture": "wrong nonce or multiple claims", "expected": "reject before model loading"},
    {"fixture": "wrong parent or unrelated PID", "expected": "reject before model loading"},
    {"fixture": "wrong OS image path/hash, including sys.executable-only match", "expected": "reject"},
    {"fixture": "creation-time mismatch or dead/reused PID", "expected": "reject"},
    {"fixture": "missing receipt or unavailable OS relationship", "expected": "reject"},
    {"fixture": "cleanup fault after a valid claim", "expected": "preserve primary evidence and classify technical failure"}
  ],
  "regressions_status": "Prospective requirements only; no additional child or production traversal was run in this diagnostic",
  "historical_result": "Immutable INCONCLUSIVE; prior live ancestry unavailable",
  "independent_scientific_failure": "f08 STOP-first self baseline margin0.0330295563 below0.049999 remains final",
  "model_retry_authorized": false
}
