# Typed scientific-stop hook prefix, prospective v2

This separately named engineering successor preserves hook_failclosed_v1 at 5c8ff02c617c693ad643851ce00988cbf6f513dc unchanged. It is not a scientific criterion change, shortened successful run, model result or authorization.

A valid finite scientific stop must not be passed to the full-run hook finish path and turned into an invented hook failure. This component adds SCIENTIFIC_STOP, permanently forbidding further forwards/derivatives. Its finite reason is one of ROUTING, FINITE_ELIGIBILITY or ENDPOINT_BEHAVIOR; the exact original scientific finding is bound by its SHA256, with the consumed schedule prefix and completed normal-check prefix captured at the first stop.

All 109 normal labels and all planned schedule IDs (180 in the full study) remain declared. Consumed schedule slots may be DONE or explicitly SKIPPED as independently established by the controller ledger; nothing after the stop is silently consumed. Every remaining slot remains UNRUN. Normal checks not performed remain unperformed normal slots even when an identically named terminal safety check is separately measured.

## Separate terminal cleanup

After the scientific latch is set, the only permitted hook inspection is the deterministic terminal cleanup sequence: the active REQUEST-exit if its REQUEST-entry has no completed normal exit, then matrix-finally. A preflight stop needs matrix-finally only. If a completed normal matrix-finally is already present, there is no duplicate terminal check. No caller-selected shorter sequence is accepted.

Clean terminal checks have their own typed stream, never increment the normal-check prefix and never count as model dispatch. The combined completed normal and terminal inspections remain within the existing 109 physical-record allocation. The writer authenticates complete setup, executed-prefix bytes, finite stop record, terminal checks and prefix index. A clean negative result is SCIENTIFIC_STOP_COMPLETE_PREFIX with permits_pass:false. Its existence cannot satisfy the full-run hook PASS predicate.

Any identity/serialization/IO/admission fault makes evidence technically INCOMPLETE, including a fault during negative cleanup or closeout. The original scientific code/finding/cursor remain available in a finite non-IO status() regardless of index or fault-receipt failure. Partial bytes are retained. The controller must persist this live status in its independent final path and bind it to external closeout; it must not reconstruct clean status from an index. If that channel also fails, missing completion prevents PASS and execution remains INCONCLUSIVE.

Normal admit/inspect/finish, all serializers/caps/fault behavior, and the full saved judge are inherited directly from the pinned v1 module. Full normal hook completion still requires all 109 checks and full schedule consumption. No latch reset, forward after stop, cap increase, re-fit or relaxed scientific predicate exists.

## One fixed three-group stdlib batch

1. Clean valid request-prefix stop and zero-check preflight stop, deterministic terminal cleanup, independent saved-prefix verification; verify method-identity inheritance and authenticate/reuse prior six-group/109 proof without rerunning it.
2. Negative cleanup identity mismatch; fully written prefix index followed by IO failure plus lost fault receipt; partial initial scientific-stop receipt plus lost fault receipt. Each retains the scientific finding and finite obtainable/persistable controller status. No arbitrary exception str/repr.
3. Self-consistent claimed normal-count/UNRUN/terminal-label forgeries rejected by independent saved judge; reset/admit and resumed-dispatch attempts rejected with zero calls; missing live controller status rejected.

Only fabricated metadata is used. Freeze and commit source before one batch: <=60 seconds, <=32 MiB preparation plus <=32 MiB combined fixtures, <=5 MiB/file, zero model/tokenizer calls. Failures are immutable; no retry or extension. Existing 16 MiB hook/288 MiB aggregate/64 KiB fault/64 KiB normal index reservations remain unchanged. Negative artifacts are allocation-checked against actual remaining capacity; inability to store them is technical INCOMPLETE, never falsely complete.

The production adapter remains a separate root-reviewed integration. No real authorization is supplied.
