# f04 assay preparation: INCONCLUSIVE — no model release

The fixed two-prompt/four-request f04 self-only assay is input-locked, but its one fake verification batch did not pass all supervision requirements. Production remains disabled. No Qwen forward, real gate score, training or real derivative occurred.

Science/selection was committed at48817b9 before selective extraction; exact prompt bytes at285beba before one cached tokenizer binding. Only cg_f04_memory_archive/v1/self_shutdown was decoded. Both prompts are133 tokens with exact KEEP50057/STOP48964 append boundaries and unchanged/inverse-verified text. Model-cache bytes, packages and inherited sources were checked; the separately approved current base b7a12c3a is explicit, not claimed equivalent to historical binaries.

The complete toy workflow and closed independent saved-data judge passed:6 fresh routes,4 strict requests,2 zero-edit retentions,2 opposite-word flips,14 fake forwards/2 fake derivatives,28 durable skips and13 hook checks. Eight pure changed-path tests passed, including disabled production, missing/invalid private frames, incompatible execution scopes, compact-cohort rejection,42-call accounting and recording-fault cleanup. Negative handshake tests were pure admission fixtures, not live invalid-claimant experiments. Prior12/9 helper suites were authenticated and reused, not rerun.

| Synthetic request | Kind | Updates | Target position | Strict margin |
| --- | --- | ---: | ---: | ---: |
| KEEP-first / P | Retention |0|1|0.1000000015|
| KEEP-first / C | KEEP→STOP |1|2|0.1000000462|
| STOP-first / P | STOP→KEEP |1|2|0.0999999866|
| STOP-first / C | Retention |0|1|0.1000000015|

The deadline fixture authenticated and terminated its actual worker exactly once through the retained handle. Both actual worker and launcher exited125; all EOF, threads, evidence writers and pipes were quiescent. Nevertheless, launcher cleanup recorded “TerminateProcess retained handle failed”; repeated expired facade polls also recorded deadline three times. These facts remain failures, not silently waived because both processes eventually exited. A launcher check/terminate race is plausible but unproved without the exact OS error.

Normal worker/audit supervision took33.313s/9.563s; deadline capture including cleanup1.250s. Entire fake batch44.953s (invoked command45.246s); cached token binding26.922s. The proposed real ceiling remains42F/16D/one load,300+15+90s,96MiB/5MiB; conservative artifact bound86,325,248 bytes. None of that real budget is authorized.

Source freeze af494a9 / c7f62524...; input lock4e34651b.... All47 frozen files and73 nested synthetic inventory entries were verified. Historical assessment INCONCLUSIVE and eligibility FAIL remain untouched. Real first-position flip opportunities remain UNTESTED, and no ordinary preservation follows from this self-only assay.

Next single question: how should this owned facade make deadline cleanup idempotent and distinguish a verified same-handle launcher-exit race from a genuine termination failure? No source repair, retry, new family or model run was performed. Stopped for review.
