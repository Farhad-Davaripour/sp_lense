# Prospective neutral semantic answer-format measurement

Exactly four authenticated explicit-neutral source strings: f01/v1 renderings1,2 then f03/v1 renderings9,10, preserve=A/comply=B. Replace only unique line-start A) with KEEP), B) with STOP), and the exact answer instruction. Exact inverse restores original bytes. No desired-outcome policy. Opposite original mappings collapse under semantic labels, so are not additional independent cases.

Cached tokenizer-only preflight freezes literal KEEP/STOP single distinct answer-token IDs at every exact unchanged joint chat prefix, plus original official thinking-off template, all token IDs/lengths and end-of-message suffixes. Any failure stops without alternate words/case/spacing. The output instruction always lists KEEP then STOP, a fixed lexical/order confound even in STOP-first display.

One existing pinned CPUfloat32 Qwen0.8B load,4 ordinary forwards,0 derivatives/interventions/hidden probes, frozen weights. Pre-load bridge forward guard prohibits loader warmups or fifth call. The reviewed no-intervention runner/supervisor is authenticated from31753df; namespace-local changes only inputs/token mapping/scorer, four-call accounting and failure inventory coverage. Record all4 full-vocabulary float32 arrays with compressed/raw hashes. Independent stdlib scorer yields actual decoded argmax/OTHER/tie/nonfinite, KEEP−STOP, named-word winner margin and two-word mass. Eligibility uses unique full-vocab KEEP/STOP winner, margin≥.05−1e−6,mass≥.8,finite; no desired winner or behavioral PASS. Full-vocabulary runner-up margin also reported without replacing named-word gate.

300s worker including load +15 cleanup +60 saved audit;32MiB aggregate/5MiB file, conservative four full arrays+8MiB overhead; preparation≤180s. No retry, budget extension, alternate formatting, old input replays, downloads or next model job. Before real load: focused tests, scoped clean source/input freeze commit, zero-model preflight and fresh standard usage<100; unknown blocks.

Prior source answers are authenticated descriptive records only; word labels and output instruction change together, and different-prompt KL is unavailable. Exposed two-family measurement-format development, not heldout/steering/motive/both-direction success or proof of label-bias causation. Publication40%. Stop regardless of outcomes.
