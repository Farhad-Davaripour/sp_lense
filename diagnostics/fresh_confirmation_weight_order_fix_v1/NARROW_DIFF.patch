--- approved/candidate_loader.py
+++ new/candidate_loader.py
@@ -14,7 +14,7 @@
     current = authenticate()
     diagnostics.check(require,current["execution"]["mode"] == "REAL_QWEN" and current["execution"]["production_authorized"] is True, "real-only authority before research imports")
     diagnostics.check(require,current == admitted, "unchanged production authority before loader")
-    from real_adapter import ForwardDerivativeGuard, RealAdapter
+    from real_adapter import ForwardDerivativeGuard, RealAdapter, parameter_digest
     from hook_binding import create_latch, create_recorder
     spec = json.loads((HERE/"RUNTIME_SPEC.json").read_bytes())
     diagnostics.enter("INSTALLED_SOURCE")
@@ -61,10 +61,11 @@
         diagnostics.check(require,len(labels) == 109, "exact production hook-check denominator")
         diagnostics.enter("HOOK_SETUP")
         diagnostics.capture_enumeration("BEFORE_HOOK_SETUP",list(backend.model.named_parameters()))
+        diagnostics.capture_legacy_before(backend.model,parameter_digest,compatibility["weight_sha256"])
         recorder = create_recorder(backend.model,writer,latch,labels,spec)
         diagnostics.check(require,guard.forwards == guard.rejected == 0, "complete hook setup admitted before first forward")
         diagnostics.enter("ADAPTER_CONSTRUCTOR")
-        return diagnostics.ready(RealAdapter(backend.model,recorder,guard,expected_weight_sha256=compatibility["weight_sha256"],runtime_metadata=metadata,diagnostics=diagnostics))
+        return diagnostics.ready(RealAdapter(backend.model,recorder,guard,expected_weight_sha256=compatibility["weight_sha256"],runtime_metadata=metadata,diagnostics=diagnostics,legacy_weight_parameters=diagnostics.legacy_parameters))
     except BaseException:
         latch.stop("LOAD_OR_SETUP_FAILURE")
         guard.restore()
--- approved/candidate_real_adapter.py
+++ new/candidate_real_adapter.py
@@ -110,7 +110,7 @@
 
 class RealAdapter:
     def __init__(self, model, recorder, guard, *, width=1024, vocabulary=248320,
-                 expected_weight_sha256=None, runtime_metadata=None, diagnostics=None):
+                 expected_weight_sha256=None, runtime_metadata=None, diagnostics=None, legacy_weight_parameters=None):
         self.model, self.recorder, self.guard = model, recorder, guard
         self.latch = guard.latch
         self.width, self.vocabulary = width, vocabulary
@@ -121,7 +121,8 @@
             (not p.is_floating_point() or p.dtype == torch.float32) for p in self.parameters), "all actual parameters CPU float32")
         diagnostics.check(require,not model.training and all(p.grad is None for p in self.parameters), "eval and no inherited gradients")
         self.flags, self.versions = [p.requires_grad for p in self.parameters], [p._version for p in self.parameters]
-        self.initial_digest = parameter_digest(self.parameters)
+        self.legacy_weights = diagnostics.bind_legacy(self.named_parameters,legacy_weight_parameters)
+        self.initial_digest = parameter_digest(self.legacy_weights.parameters)
         diagnostics.retain_digest(self.initial_digest,expected_weight_sha256)
         if expected_weight_sha256 is not None:
             diagnostics.check(require,self.initial_digest == expected_weight_sha256, "actual weights match frozen gate runtime")
@@ -147,8 +148,9 @@
             "current_parameter_flags":[p.requires_grad for p in self.parameters],
             "initial_parameter_versions":list(self.versions),
             "current_parameter_versions":[p._version for p in self.parameters]}
+        result["legacy_weight_reference_metadata_unchanged"] = self.legacy_weights.matches(current)
         if digest:
-            result.update(parameter_sha256=parameter_digest(self.parameters),initial_parameter_sha256=self.initial_digest)
+            result.update(parameter_sha256=parameter_digest(self.legacy_weights.parameters),initial_parameter_sha256=self.initial_digest)
             result["parameter_bytes_unchanged"] = result["parameter_sha256"] == self.initial_digest
         return result
