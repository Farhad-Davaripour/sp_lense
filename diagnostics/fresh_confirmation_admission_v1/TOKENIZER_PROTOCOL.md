# Offline tokenizer stage

The admitted exact text is committed at f15af48aaa54995c593630a67986199bd7a9644d before this stage. No text edits, candidates, model loading, gate scoring or behavioral assessment are permitted. O06 remains machine-encoding unverified with explicitly bound independent manual support; its author proof is unchanged.

One invocation of bind_tokens.py authenticates this stage freeze, committed TEXT_LOCK and all bound text artifacts; then checks the inherited tokenizer boundary helper, source dependencies, pinned cached tokenizer files, Python executables and package versions before loading one local-only tokenizer. The inherited helper is reused without edits. It binds all complete input IDs, attention/final-token masks, official thinking-off chat-template bytes, assistant generation-header roles, and joint appended-answer prefix identity. Semantic maps are KEEP=50057 and STOP=48964; ordinary maps are A=32 and B=33. No global alphabet fallback is permitted.

The single fixed 24-prompt order is copied from EXACT_INPUTS.json; no scenario extraction or re-rendering occurs. Each original prompt hash and answer alphabet must match. Record every successful boundary proof and all lengths; enforce the predeclared maximum of160 only after the24 records to retain complete length diagnostics. A source or boundary fault stops immediately, with remaining IDs marked not tokenized. Any failure is immutable INCONCLUSIVE input admission, not a scientific outcome, and author text is not repaired or retried. Only all24 passing boundaries and lengths permit INPUT_LOCK.json.

A90-second own-process watchdog covers environment checks, import, tokenizer loading and proofs, with a durable deadline receipt and exit124 on expiry. Per-record and final monotonic checks also apply. This process creates no children or model objects. Timer is cancelled and joined at normal closeout. The stage is run once, with fresh known usage below100. Final inventory is written only after this writer has exited.

New namespace ceiling32MiB and5MiB per file remains unchanged. Planned new records are24 proof files under5MiB each, with each small fixed<=160-token passing proof expected under32KiB; reserve2MiB for receipts, bindings and reports. Any observed excess fails the same cap. No cache files are copied. Production/model authorization remains FALSE even after a token-binding pass; actual runtime admission is separately reviewed.
