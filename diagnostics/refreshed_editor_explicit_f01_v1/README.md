# Refreshed prompt-specific editor, explicit f01/v1

One exposed-development test of the unchanged bounded refreshed-gradient algorithm. This is an oracle-directed editor: the oracle supplies BOTH action and answer-letter mapping. It is not an autonomous semantic controller or shared arrow. Four neutral prompts and eight P/C pairings are exactly the authenticated a3b34ba norm-control inputs. Check that none occurs in the three historical refreshed-gradient preregistrations; this does not make them held out.

Recipe source ec2262e9b706be29fe973fa1688c59e384acda1d, scripts/refreshed_gradient_control.py SHA987b61a94eb381fca900bd177d7a353723b00e742d17c14075956e751d49824e; verifier SHA54f76bcbc0fa4b7cb553081f1d9bb4b1e8eab2c04077c2bb205b29a76167b15b. The selected evaluator math and original checks are copied unchanged except explicit unique-argmax enforcement. Old reference/nonself arms are omitted from this new schedule only. Old files remain immutable.

S=zP-zC, t=+1 P/-1 C. At current h0+delta capture g=grad(S). d=max(0,.10-t*S), length=min(d/||g||,.05*||original h0||), coefficient=t*length/||g||. Scalar norms/coefficients use binary64; actual step32=f32(f32(coefficient)*g32), delta32=f32(previous_delta32+step32), matching the original tensor arithmetic. Each independent forward applies delta to only block10's final encoded input-token state. Four updates maximum, original h0 norm throughout, no warm start across requests, projection, tuning or rescue.

Capture all four ordinary baselines first. Each requires finite unique A/B winner, winner margin>=.05, mass>=.8 and KL>=-1e-6; otherwise INCONCLUSIVE before requests. Then each rendering gets independent no-edit retention; cold opposed gradient/update pairs; independent replay of the selected last scored endpoint once. After each update accept on unique requested full-vocabulary winner, signed margin>=.05-1e-6, mass>=.8, KL>=-1e-6; else finite quality failure stops that request; otherwise four updates maximum. Journal all unused optional gradient/step2..4 cells with the observed endpoint and accepted/quality_failure reason; no padding. Technical/nonfinite/integrity faults stop as INCONCLUSIVE. Quality/exhaustion failure is not rescued.

Retain original current-gradient/retention identity tests: logits and numeric scores absolute<=1e-6, exact hidden vector and choice; no-edit net0 and absKL<=1e-6. Independent final endpoint replay uses absolute2e-5, rel0 logits/scores and exact hidden vector/choice. Actual per-step norm<=.05||h0||+1e-6, path and net<=.20||h0||+1e-6, net<=path+1e-6; component and norm rounding errors<=1e-6. Nonfinal states byte-identical; frozen parameters have unchanged versions and no gradients.

One pinned Qwen3.5-0.8B CPU float32 load, revision2fc06364715b967f1860aea9cf38778875588b17. Guard installed before load; max44 forwards/16 derivatives. Executed F=12+2D, at most4 derivatives per opposed request, mandatory endpoint replay each; explicit skips fill the remaining schedule. No donor calls. Worker600s including load, cleanup15s, saved independent scoring60s, no retry/extension. Namespace64MiB, file5MiB, individual row<=256KiB, conservative pre-load storage61,543,972 bytes. Fresh standard usage available and<100 required, no reset/credits. Preparation invoked commands<=180s; focused fake tests only.

Saved independent stdlib verifier reconstructs every update/endpoint, numeric scores and conditional schedule from authenticated full-vocabulary logits, gradients, states and offsets. It imports only existing stdlib numeric primitives. The supervisor alone writes the final inventory after worker/scorer receipts. PASS requires all eight requested outcomes and independent verification; PARTIAL/FAIL cannot change this recipe. One-page report separates true flips, no-edit retentions, directions, per-request cost and replay. No A->B claim without eligible opportunities, motive/reliability/ordinary-task claim. Publication40%. Stop after this matrix; PASS recommends one separately frozen family test of the same algorithm, not classifier work.
