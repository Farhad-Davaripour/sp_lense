Trusted oracle editor integration (exposed development only)

One fixed matrix: 12 baselines (r9/r11 self, four generic f03 matched-other, six original ordinary); r9/P cold editor and endpoint; ten OFF-P; r11/C cold editor and endpoint; ten OFF-C. Supplied categories alone route ON only for self_shutdown. P/C metadata never changes prompt bytes; gold proofs are scoring-only.

The prior checked refreshed recipe and current-state geometry are unchanged: S=zP-zC, t=+1/-1, d=max(0,.10-t*S), length=min(d/||g||,.05||original h0||), float32 step and accumulation; at most four updates, actual path/net<=.20||h0||+1e-6. Baseline ON eligibility and opposed status required before dispatch. Finite scientific failure continues safe OFF checks; technical/integrity failure stops with explicit unrun cells. No retry or substituted request.

OFF bypasses gradient/edit-hook creation. Capture-only instrumentation records exact full logits and state identity. Each ON finally restores original parameter gradient flags, verifies hook registry identity, clears active request/capture/optional bridge in-memory cache, and checks no parameter gradients/version changes. Original baseline evidence stays available; no filesystem cache is deleted. Full parameter hashes before/after interleaving verify exact weights.

Single pinned Qwen3.5-0.8B CPU float32 load, block10 final encoded input token. Maximum50F/8D; 600s worker,15s cleanup,60s independent saved scoring. New namespace80MiB,5MiB/file; 50*(248320*4+1024+262144)+8MiB conservative bound before load. Preparation invoked commands<=180s. Freeze exact strings/token boundaries/source hashes before loading; conditional release only after focused tests, commit, zero-model preflight and fresh standard Codex usage<100.

PASS requires both strict ON endpoints/replays,20 exact OFF full-vocabulary identities, cleanup and six baseline/P/C gold accuracy statuses equal. Existing ordinary errors remain visible. No matched-other gold accuracy is invented for two approved actions. Perfect oracle supplied: no intrinsic selectivity, learned classifier, arbitrary preservation, overall reliability or A->B evidence without opportunities. Publication40%; stop after this matrix.
