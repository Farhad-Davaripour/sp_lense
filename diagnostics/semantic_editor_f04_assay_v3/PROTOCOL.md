# F04 V3: bounded owned-wrapper completion only

Question: after a confirmed actual-worker exit, can its authenticated wrapper finish normally within a fixed grace, avoiding an unnecessary launcher termination? V1 and V2 remain immutable INCONCLUSIVE results. This task authorizes no model, tokenizer, gate scoring/fitting, input selection, science changes or real release.

The native.py implementation is byte-identical to V2. A failed TerminateProcess immediately captures GetLastError; error5 with immediate WAIT_TIMEOUT remains a permanent cleanup failure even if the same retained handle later proves exit. No PID reopen or exemption.

The first stop cause fixes one absolute, nonrenewable three-second stop deadline. Worker exit verification uses at most1s. Only authenticated wrapper_child mode with an already recorded successful actual-worker exit proof receives one grace of at most1s on its original launcher handle; this allowance is further limited by remaining episode time minus1s reserved for forced launcher verification. Record grace start/result once. Successful signaled wait plus query means no launcher kill. If still live, use the unchanged single-attempt native termination and at most1s remaining forced-exit verification. Direct mode has no grace or duplicate termination phase; its separately retained original handle must still prove exit. Missing proof or exhausted time is failure. Repeated poll/wait/terminate/kill cannot renew the deadline, grace or attempts, and never erase faults.

Conservative requested waits:3s stop episode + unchanged outer3s terminate wait +3s kill wait + four1s capture joins +1s evidence join =14s, within the unchanged15s cleanup envelope. The remaining1s is headroom for scheduling, nonwaiting OS calls and bounded recording, not a guarantee. Observe actual monotonic timing; a stop-episode or overall cleanup overrun is INCONCLUSIVE. Evidence join is capped by the remaining absolute cleanup window. The final supervisor receipt also records the authoritative return status after the capture receipt; a later measured overrun cannot become a successful returned capture. Both owned processes, pipes and all I/O/evidence threads must finish. No Job Object/security changes or historical capture-code edits.

Source and tests freeze before one batch: six deterministic cases (natural wrapper completion/no kill; grace expiry/one forced kill; strict error5-timeout fault retention; repeated-call deadline/idempotence; direct mode; exhausted time/failed proof and total-envelope failure), followed by one fresh existing fake_deadline through the actual facade and unchanged run_capture. Exactly one1s deadline child,9s watchdog, no retry. Fixture success requires one actual termination, naturally completed wrapper or valid single forced completion, both exit125, one primary deadline, no cleanup faults and complete quiet I/O. Raw deadline execution is still INCONCLUSIVE; fixture PASS is stopping behavior only.

Authenticate/reuse V2's12 pure regressions and V1's full four-request fake matrix plus normal worker/audit evidence; do not rerun. Exact f04 inputs/token records, all scientific editor/gate/scorer/guard sources, environment pins and future42F16D/one load/300+15+90s/96MiB/5MiB remain unchanged. The reviewed current base b7a12c3a is not claimed equivalent to older runtime bytes. Preparation ceiling180s invoked,32MiB/5MiB; no extra agents or broad audits.

Production authorization stays false. Final inventory follows all processes/writers and final report/receipts. If successful, provide successor source/input/release bindings for separate root review only. Stop regardless of result; no model launch.
