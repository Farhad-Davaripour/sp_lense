# Final runtime adapter: preparation only

No real model release exists. This job permits fake backends/processes only, <=600s cumulative invoked work,384MiB namespace/5MiB file. No real gate scoring, fitting, tokenizer rerun or dataset re-extraction. Existing fixed inputs are read from authenticated committed artifacts. The reviewed preflight fault join was committed byte-exact first; editor/gradient math and strict guard/recorder remain unchanged.

Production admission independently binds final input commit1438e20, its inventory and exact input lock; pipeline db7c3c05; packet a2850df; repaired editor SHA2e9be8be…; all copied source hashes, frozen gate parameters, installed hook/runtime sources and cached tokenizer assets. The plan has exactly24 prompts,48 P/C requests,72 fresh routes,12 self endpoints,36 OFF checks,180F/48D. The real judge compares the full frozen plan and every original prompt/token proof independently against committed input sources before reading any outcomes. Compact, substituted or synthetic evidence cannot be admitted as real evidence.

Production uses locked complete input IDs and boundary proofs, not backend tokenization. Final input masks/roles and max136 tokens are preserved. The unchanged editor computes grad(zKEEP−zSTOP), .10aim, at most4updates, .05actualstep and .20actualpath+net, unique full-vocabulary winner/.05−1e−6margin/.8mass/finite same-inputKL. Cold requests route only from their own unedited state. Initial accepted retentions have0D and an independently executed endpoint with exact hidden state and<=1e−6 logits, in addition to the general<=2e−5 endpoint tolerance. OFF returns its fresh capture and requires exact own-baseline identity without a quality/ordinary-accuracy gate.

Production envelope: one pinned CPUfloat32 Qwen0.8B load,180F48D,1500s including loading,15s cleanup,180s independent saved audit;288MiB total/5MiB file;109 strict hook checks/16MiB hook records.132F24D is only a derived conditional bound. Full logits are993280bytes; per-row bookkeeping reserve262144bytes exceeds complete input IDs/masks plus six final vectors26752bytes. Conservative artifact bound259715072bytes includes16MiB hook records plus16MiB sources, receipts, two bounded4MiB process logs and closeout reserve. The recorder counts all nested logs in its per-attempt cap. No resume/retry/cap extension.

The inherited binary-pipe supervisor owns the exact Popen process handle, never a caller-supplied PID; it retains bounded raw logs, observed EOF, termination/kill attempts and quiescence. Deadlines preserve prior scientific findings, later technical/cleanup faults, and explicit UNRUN accounting. No inventory is allowed with a live/unjoined writer. Worker and saved judge are separately externally bounded. Only the final owner writes an attempt inventory after both processes, their receipts and final report. Scientific finite applicability/endpoint/exhaustion failures remain FAIL; technical/nonfinite/integrity/time/resource faults are INCONCLUSIVE without erasing earlier failures.

`authorization.json` is deliberately disabled. It is a separate root-review release pin, not a scientific parameter, and is excluded from the scientific source hash to avoid circular release hashing. A later root-authorized commit must contain the exact approved release document and its SHA pin. The document binds the earlier prospective source commit/freeze, exact input lock, immutable limits, and a<120s known standard-Codex usage tool receipt<100%. The production command accepts no caller-selected model, cohort, hash, threshold or settings. No such release is supplied or created here.

Commands now: `prepare.py setup`, `prepare.py freeze`, `prepare.py preflight`; one frozen `tests.py` batch only. Later, only after independent root review and explicit committed release pin: `run.py run` (currently rejects before output creation/model imports). Worker-only entry also requires the same admission. The future judge command is `judge.py <exact attempt path>`.

Verification uses one compact20F2D arithmetic traversal, a two-forward combined preflight fault fixture, guard/counter checks and tiny process failures. They remain explicitly SYNTHETIC and never become real success. Reuse prior authenticated full synthetic matrix rather than rerunning it. Exposure remains three manifest-sealed families with historical NON_ACCESS_UNVERIFIED; three families are the units, not48 trials. Publication credit stays44% pending root verification.

Remaining release requirement: independent review of this production adapter/judge and fake receipts, then one separately pinned real-run release. Saved-data judging shares verified mathematical infrastructure; it is not an external lab replication. Real cold-state identity, endpoint replay, routing and final behavior are unmeasured on the final cohort until that authorized run.
