# Complete-normal / terminal-incomplete-fault hook boundary

Prospective pure engineering check, not model evidence or authorization. The root decision at e2943cfc12ab695b9fd8b4932f8a0b287ac2febe is controlling. The earlier capacity audit b4472e719bae9f183d695e23484f77329dbe2c08 remains UNVERIFIED. No existing scientific criterion, source, attempt or cap is changed.

## Invariant and admission

Before model access, the caller authenticates frozen sources, inputs and schemas. Before any forward, this component accepts only complete serialized actual setup-before, reference, setup-changes and source/setup receipts, with a verified zero-forward setup declaration. It independently checks the exact setup difference. The historical guard still owns source authentication, permitted forward-free setup and full identity measurement; this component never whitelists a new module or drops callback identities.

It reserves exact encoded setup bytes/manifests, 109 records of at most 4,096 bytes, an additional bounded final index (65,536 bytes), and an untouched 65,536-byte fault reserve within the existing 16 MiB hook and 288 MiB aggregate caps. The final-index reserve is inside, not added to, those caps. Actual schema-dependent final-index length is checked before admission. Each file is at most 5 MiB; each complete raw snapshot is at most 64 MiB split into 1 MiB chunks. Allocation uses actual codec output, never a historical compression ratio.

Every identity, serialization, admission or IO fault latches dispatch terminal before diagnostic serialization. The first finite code and original in-memory exception survive later failures; arbitrary exception str/repr is never used in a receipt. No cleanup, matching check, another caller, receipt failure or re-admission can restore scientific dispatch. Ordered schedule consumption records the exact remaining suffix, including explicit skips without padding calls.

Complete changed metadata is saved only if its whole encoded allocation fits. Otherwise, it remains explicitly incomplete/unavailable. Existing and partial bytes are retained. A coded receipt is not a substitute for full evidence. Fault-receipt failure still prevents completion.

Normal completion requires all 109 exact checks, fully consumed schedule, exact file/index ownership and authenticated reconstruction. A controller status bound to the final index hash AND complete independent byte verification is required: an index physically written before an IO error is not authoritative. The production final judge must also join externally authenticated controller status and complete-valid, quiescent worker/audit captures. Missing status or failed receipt/external closeout cannot PASS.

## Exactly six frozen pure case groups

1. Complete normal setup/reference reconstruction and 109 checks; parity with the pinned historical pure differences function; missing controller status rejects.
2. Exact-fit versus one extra byte and 109 versus 110 checks; permanent stop.
3. Lossless fitting mismatch, terminal latch observed before codec, preserved exact remaining suffix, no re-entry.
4. Raw snapshot cap, compressed file cap and aggregate overflow; actual fixed-seed codec expansion rejects a raw-only allocation estimate.
5. Partial chunk and manifest writes; fault-receipt failure; partial final index; fully written final index followed by IO failure with no fault receipt. Preserve poison exceptions without invoking str/repr and reject all failed closeouts.
6. Independent rejection of forged check completeness, missing/changed chunks, untracked bytes, incomplete index and even indexed-but-unreferenced bytes.

All fixtures are fabricated metadata. One batch only; 60 seconds, at most 32 MiB combined fixture files and 32 MiB preparation, each file at most 5 MiB. No model/tokenizer imports or calls. No retry or extension. The finite batch failure receipt remains final if any assertion fails.

## Limits

This proves neither universal fault-dump capacity nor actual model setup admission. Runtime admission must still measure exact complete metadata before its first forward. This component cannot itself authenticate an OS capture or assert scientific success. Production authorization remains FALSE; integrated adapter review and separate root release remain necessary.
