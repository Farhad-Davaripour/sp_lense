# PASS — pure first-forward recorder preparation only

The ONE prospectively frozen eight-case inert recorder batch passed. This is engineering evidence for finite stage attribution, saved receipt checks, first-cause preservation and the approved diagnostic cleanup sequence; it is not a real forward, a diagnosis of closed attempt003, or scientific success. Actual disabled-state preflight returned `DISABLED_NO_APPROVED_REAL_RELEASE`. No real authority or real attempt output exists.

Source/test commit: `0877185e64c9383ffa7a3ee885d383d52553170a`. SOURCE_FREEZE SHA256: `4a5385a1f88e01213f97cfd78c463b0d09f3fd8e83b83aef3cb7967423388b2f` (44 source/preparation members). BATCH_LOCK SHA256: `829c9c0578310666df3f40a0dbd3c32a50faad2ef425a641e132f7408ad8dcc8`. BATCH_REPORT SHA256: `0e58467bed50709e9141ccc08dc2c3a4fcbe401f58fe7a45f442e273eec247e0`.

## What changed

Only this new namespace was created. Observational spans distinguish guarded bridge entry/return, selected-hook entry/return, hook-context enter/exit, adapter postconditions, outer adapter return, first logits serialization/publication, dispatched-input identity and separately typed cleanup stages. A bounded first record is never overwritten by outer propagation or later cleanup failures. Finite categories and allowlisted source/hash/line frames are recorded; no arbitrary exception text, locals, tensors, credentials or parameter contents are retained.

The changed adapter SHA256 is `16568905adb54ba95830548ea8b2609910619905340783442faaf00bb91a76ed`. AST erasure of only these spans/context wrappers exactly recovers the complete checked adapter (`992acc44dfaa4bb67591e4eb956ea149ac200a5e3484dc70a422e0f33be8e01c`). All original numeric statements, digest algorithm, retained-order/current-registry checks and predicates are unchanged. The corrected candidate loader, diagnostic helpers, explicit saved weight reader, native loader handoff and reviewed authority/hook/owned-process dependencies remain pinned. Source observation uses code-file hashes only, not weight hashes.

The new standalone entry authenticates the unchanged 24/48 input lock but dispatches only the fixed first `N01__v1__self_shutdown__KEEP_then_STOP__baseline` IDs/mask, original zero offset and first raw logits publication. Its nonrenewable counter allows one load and at most one forward, never derivatives, encoding, requests, scoring, gate decisions or continuation. The full scientific runner and historical sources/verdicts are untouched.

Root approved the standalone cleanup before freeze: original end_edit; fresh retained-order parameter_state(digest=True); original latch admission before unchanged HookGuard inspection and exact admitted-reference comparison; all original parameter predicates plus explicit active_request_empty, wrapper_cache_empty and bridge_cache_empty; unconditional guard.restore in finally. Terminal refusal never reaches the provider. Success is `diagnostic_cleanup_complete` only, with scientific_pass false, full_scientific_finalizer_called false, and 109 normal scientific hook checks UNRUN.

## Fixed batch outcomes

All eight fixed cases passed; no remaining cases are UNRUN. The clean case and the valid base of the saved-tamper case executed each inert cleanup callback once and restored the guard once. Bridge, selected-hook, context-exit and adapter-postcondition failures were attributed to their exact distinct stage. Those failures and the publication failure exercised later terminal-latch refusal: zero provider callbacks, one restoration callback, first cause preserved. The serialization case retained LOGITS_SERIALIZATION as primary while the separate end_edit failure remained secondary and later cleanup stages were not falsely marked executed.

The publication/native-I/O case retained LOGITS_PUBLICATION despite a 19-byte partial trace write; it remained incomplete with no acknowledged trace hash and the independent reader rejected it. This expected negative is a passing test, not a valid complete diagnostic receipt. Five saved-data negatives rejected altered receipt hash, source, execution, event stage and truncated JSON. Inner source/execution/stage mutations repaired the outer receipt hash so rejection did not rely on stale hash alone. Four counter negatives rejected second load, second forward, derivative and a different cell.

Counter snapshots named `actual_load_dispatches` describe the counter's deliberately consumed inert slot in these synthetic fixtures, not actual model loading. Every fixture separately declares synthetic_only and real_model_loads=0. Total actual work: zero Qwen/torch/backend/tokenizer imports or calls, zero adapter constructions or parameter accesses/content hashes, zero actual forwards, derivatives or encodings. No model, weight or tokenization experiment, previous suite or full scientific schedule was run.

The substantive batch elapsed 0.375 seconds. The command exited 0 after 0.5633067 seconds. One closeout used 0.014999999664723873 seconds; the 45-second work + ONE 15-second closeout / 60-second absolute envelope was met. No process was spawned by this in-process batch. Existing owned-process proofs are reused; a newly exercised real controller or quiescent model process is not claimed. No retry, cap reset, source repair or source edit occurred after freeze.

## Bounds, known limits and remaining review

The additional native diagnostic is capped at 64 KiB inside the existing 0.75 MiB other-closeout allocation, with a finite status copy bound to native terminal/external worker capture. A partial/missing/failed acknowledgment cannot become a complete trace. Unknown source frames and incomplete bounded scans remain explicitly partial; they do not establish the ultimate internal exception location. The primary is buffered until the one final trace publication: termination before publication can leave it unknown. No universal arbitrary-error-fit guarantee is claimed.

This batch exercised the actual pure trace/reader/cleanup-order helpers with inert callbacks and parsed the adapter for computation parity. It did not import or execute the instrumented real adapter, run the new complete diagnostic core plus owned controller and full saved-reader integration, test actual Qwen hook capacity, or prove a live first-forward outcome. These remain explicit independent-review/real-release conditions, not implied by this PASS. No extra test or real launch is automatically authorized.

Future attempt remains `fresh_confirmation_first_forward_diagnostic_attempt_001`, separately disabled. Its prospective caps are one load / at most one forward / zero derivatives / zero encoding, worker180 + audit60 + ONE shared cleanup15 = 255 seconds, unchanged 288 MiB evidence / 5 MiB per file. Preparation and combined tests stay within 32 MiB / 8 MiB. The original full-study upper contract and all prior attempts remain unchanged. `FUTURE_ROOT_RELEASE.md` contains the exact new authority chain and one-time command for root consideration only. Stop for independent review; no scientific conclusion is available.
