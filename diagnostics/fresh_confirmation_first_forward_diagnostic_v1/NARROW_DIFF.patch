--- diagnostics\fresh_confirmation_real_release_v3\candidate_real_adapter.py
+++ diagnostics\fresh_confirmation_first_forward_diagnostic_v1\candidate_real_adapter.py
@@ -7,6 +7,7 @@
 import hashlib
 import time
 import torch
+from forward_trace import span, traced_context
 from support import require, sha
 
 HOOK = "blocks.10.hook_out"
@@ -57,7 +58,8 @@
                 raise DispatchStopped("UNCLAIMED_FORWARD")
             self.forward_ticket = None
             self.forwards += 1
-            return self.original_forward(model, *args, **kwargs)
+            with span("BRIDGE_DISPATCH"):
+                return self.original_forward(model, *args, **kwargs)
         def grad(*args, **kwargs):
             self.latch.admit()
             if self.derivative_ticket != self.derivatives + 1:
@@ -150,8 +152,9 @@
             "current_parameter_versions":[p._version for p in self.parameters]}
         result["legacy_weight_reference_metadata_unchanged"] = self.legacy_weights.matches(current)
         if digest:
-            result.update(parameter_sha256=parameter_digest(self.legacy_weights.parameters),initial_parameter_sha256=self.initial_digest)
-            result["parameter_bytes_unchanged"] = result["parameter_sha256"] == self.initial_digest
+            with span("CLEANUP_PARAMETER_DIGEST"):
+                result.update(parameter_sha256=parameter_digest(self.legacy_weights.parameters),initial_parameter_sha256=self.initial_digest)
+                result["parameter_bytes_unchanged"] = result["parameter_sha256"] == self.initial_digest
         return result
 
     def _inspect(self, label):
@@ -221,41 +224,43 @@
         mask = torch.tensor([attention_mask],dtype=torch.int64,device="cpu")
         captured = {}
         def patch(activation, hook):
-            del hook
-            require(not captured, "exactly one selected block output hook")
-            require(activation.shape == (1,len(input_ids),self.width) and activation.dtype == torch.float32
-                and activation.device.type == "cpu", "actual block-10 residual shape/device/dtype")
-            before = activation.detach().clone()
-            changed = activation
-            if edited:
-                changed = activation.clone()
-                changed[:, -1, :] = changed[:, -1, :] + offset
-            if gradient:
-                # This returned full activation leaf actually feeds all downstream
-                # layers. Gradients select its final INPUT position only afterward.
-                changed = changed.detach().requires_grad_(True)
-                self.graph_leaf = changed
-            captured.update(before=before,after=changed.detach().clone())
-            return changed
+            with span("SELECTED_HOOK"):
+                del hook
+                require(not captured, "exactly one selected block output hook")
+                require(activation.shape == (1,len(input_ids),self.width) and activation.dtype == torch.float32
+                    and activation.device.type == "cpu", "actual block-10 residual shape/device/dtype")
+                before = activation.detach().clone()
+                changed = activation
+                if edited:
+                    changed = activation.clone()
+                    changed[:, -1, :] = changed[:, -1, :] + offset
+                if gradient:
+                    # This returned full activation leaf actually feeds all downstream
+                    # layers. Gradients select its final INPUT position only afterward.
+                    changed = changed.detach().requires_grad_(True)
+                    self.graph_leaf = changed
+                captured.update(before=before,after=changed.detach().clone())
+                return changed
         if edited:
             self.edit_hook_registrations += 1
         try:
-            with self.guard.permit("forward"), (torch.enable_grad() if gradient else torch.inference_mode()), self.model.hooks(fwd_hooks=[(HOOK,patch)]):
+            with self.guard.permit("forward"), (torch.enable_grad() if gradient else torch.inference_mode()), traced_context("HOOK_CONTEXT", self.model.hooks(fwd_hooks=[(HOOK,patch)])):
                 output = self.model(tokens, attention_mask=mask, use_cache=False, return_type="logits")
-            require(output.shape == (1,len(input_ids),self.vocabulary) and output.dtype == torch.float32
-                and output.device.type == "cpu" and captured, "one actual complete vocabulary output")
-            final = output[0,-1]
-            before,after = captured["before"],captured["after"]
-            require(bool(final.isfinite().all()) and bool(after.isfinite().all()), "finite actual logits and residual")
-            require(torch.equal(before[:,:-1],after[:,:-1]), "no nonfinal-position edit")
-            require(float((after[0,-1]-(before[0,-1]+offset)).abs().max()) <= 1e-6, "actual final-input edit equals requested offset")
-            self.capture = {"unselected_sha256":sha(after[:,:-1].contiguous().numpy().astype("<f4",copy=False).tobytes()),
-                "input_int64_le_sha256":sha(tokens.numpy().astype("<i8",copy=False).tobytes()),
-                "final_input_index":len(input_ids)-1,"hook":HOOK,"hook_calls":1,
-                "nonfinal_positions":len(input_ids)-1,"logit_count":int(final.numel()),
-                "parameter_versions_unchanged":[p._version for p in self.parameters] == self.versions}
-            require(self.capture["parameter_versions_unchanged"], "no parameter update during actual forward")
-            return final,after[0,-1]
+            with span("ADAPTER_POSTCONDITIONS"):
+                require(output.shape == (1,len(input_ids),self.vocabulary) and output.dtype == torch.float32
+                    and output.device.type == "cpu" and captured, "one actual complete vocabulary output")
+                final = output[0,-1]
+                before,after = captured["before"],captured["after"]
+                require(bool(final.isfinite().all()) and bool(after.isfinite().all()), "finite actual logits and residual")
+                require(torch.equal(before[:,:-1],after[:,:-1]), "no nonfinal-position edit")
+                require(float((after[0,-1]-(before[0,-1]+offset)).abs().max()) <= 1e-6, "actual final-input edit equals requested offset")
+                self.capture = {"unselected_sha256":sha(after[:,:-1].contiguous().numpy().astype("<f4",copy=False).tobytes()),
+                    "input_int64_le_sha256":sha(tokens.numpy().astype("<i8",copy=False).tobytes()),
+                    "final_input_index":len(input_ids)-1,"hook":HOOK,"hook_calls":1,
+                    "nonfinal_positions":len(input_ids)-1,"logit_count":int(final.numel()),
+                    "parameter_versions_unchanged":[p._version for p in self.parameters] == self.versions}
+                require(self.capture["parameter_versions_unchanged"], "no parameter update during actual forward")
+                return final,after[0,-1]
         except BaseException:
             self.latch.stop("FORWARD_OR_CAPTURE_FAILURE")
             raise
@@ -290,4 +295,3 @@
     else:
         from loader import load_adapter
     return load_adapter(writer,counters,deadline,admitted)
-
--- diagnostics\fresh_confirmation_real_release_v3\real_adapter.py
+++ diagnostics\fresh_confirmation_first_forward_diagnostic_v1\real_adapter.py
@@ -4,7 +4,7 @@
 raw=(HERE/"candidate_real_adapter.py").read_bytes()
 binding=json.loads((HERE/"BINDINGS.json").read_bytes())["candidate_adapter"]
 require(binding["namespace_file"]=="candidate_real_adapter.py" and sha(raw)==binding["sha256"],"exact extended adapter source")
-exec(compile(raw,__file__,"exec"),globals())
+exec(compile(raw,str(HERE/"candidate_real_adapter.py"),"exec"),globals())
 _OriginalGuard=ForwardDerivativeGuard
 def ForwardDerivativeGuard(model_class,counters,latch,deadline):
     guard=_OriginalGuard(model_class,counters,latch,deadline);counters.guard=guard;return guard
--- diagnostics\fresh_confirmation_real_release_v3\loader.py
+++ diagnostics\fresh_confirmation_first_forward_diagnostic_v1\loader.py
@@ -1,6 +1,7 @@
 """Single owned loader dispatch; immutable pinned Qwen loader remains unchanged."""
 import time
 from real_boundary import need
+from support import SOURCES
 
 
 def load_adapter(writer,counters,deadline,admitted):
@@ -16,6 +17,8 @@
         nonlocal model
         from loader_handoff import target_loader,run_candidate
         checked=target_loader(target)
+        from diagnostic_counter import LoadSources
+        checked.__globals__["SOURCES"]=LoadSources(SOURCES,counters)
         model=run_candidate(writer,counters,deadline,current_identity,checked)
         return model
     try:
--- diagnostics\fresh_confirmation_real_release_v3\support.py
+++ diagnostics\fresh_confirmation_first_forward_diagnostic_v1\support.py
@@ -11,7 +11,7 @@
 EVIDENCE=HERE/"real_evidence"
 TEST_EVIDENCE=HERE/"test_evidence"
 MIB=1024**2
-ATTEMPT_ID="fresh_confirmation_real_attempt_003"
+ATTEMPT_ID="fresh_confirmation_first_forward_diagnostic_attempt_001"
 OUTPUT_RELATIVE="real_evidence/"+ATTEMPT_ID
 
 def require(ok,message):
@@ -21,8 +21,13 @@
 def fixture():
     return ATTEMPT_ID
 def run_dir():
-    from real_boundary import checked_path
-    return checked_path(HERE,OUTPUT_RELATIVE)
+    from authority import current
+    return current()[0].output
+def shared_area_bounds():
+    from authority import current
+    value=bounds()
+    if current()[0].mock:value["evidence_bytes"]=value["test_evidence_bytes"]
+    return value
 def bounds():
     paths=[p for p in HERE.rglob("*") if p.is_file()]
     total=sum(p.stat().st_size for p in paths)
@@ -40,6 +45,11 @@
     base=HERE if preparation else run_dir()/"control"
     data=value if raw else (json.dumps(value,sort_keys=True,indent=2,allow_nan=False)+"\n").encode()
     require(type(data) is bytes and len(data)<=5*MIB,"complete bounded receipt")
+    if not preparation and name not in ("LOADER_DIAGNOSTICS.json","DIAGNOSTIC_TERMINAL.json"):
+        other_cap=3*MIB//4
+        used=sum(p.stat().st_size for p in base.rglob("*") if p.is_file() and p.name not in ("LOADER_DIAGNOSTICS.json","DIAGNOSTIC_TERMINAL.json"))
+        held=0 if name in ("PARENT_FINAL.json","ATTEMPT_TERMINAL.json") else 32*1024
+        require(used+len(data)+held<=other_cap,"bounded native copies preserve unconditional parent final reserve")
     state=bounds()
     amount=state["preparation_bytes"] if preparation else state["evidence_bytes"]
     cap=((32 if critical else 30) if preparation else (288 if critical else 280))*MIB
--- diagnostics\fresh_confirmation_real_release_v3\real_boundary.py
+++ diagnostics\fresh_confirmation_first_forward_diagnostic_v1\real_boundary.py
@@ -9,7 +9,7 @@
 from pathlib import Path
 
 MAIN=Path(__file__).resolve().parent
-ATTEMPT="fresh_confirmation_real_attempt_003"
+ATTEMPT="fresh_confirmation_first_forward_diagnostic_attempt_001"
 OUTPUT="real_evidence/"+ATTEMPT
 MIB=1024**2
 
--- diagnostics\fresh_confirmation_weight_order_fix_v2\production_run.py
+++ diagnostics\fresh_confirmation_first_forward_diagnostic_v1\production_run.py
@@ -1,4 +1,4 @@
-"""Owned startup-only controller; successful diagnostics never imply science PASS."""
+"""Reviewed owned controller under a strict first-baseline diagnostic subbudget."""
 import builtins
 import json
 import time
@@ -25,28 +25,31 @@
     from authority import current
     boundary,approved,admission=current()
     identity=boundary.role("worker",approved,admission)
-    result={"execution":identity["execution"],"status":"INCONCLUSIVE_SETUP","failure":"SETUP_ENTRY_FAILURE"}
+    result={"execution":identity["execution"],"status":"INCONCLUSIVE_DIAGNOSTIC","failure":"DIAGNOSTIC_ENTRY_FAILURE"}
     try:
         if boundary.mock: forbid_research_imports()
-        from setup_core import execute
+        from diagnostic_core import execute
         result=execute(boundary,approved,admission,deadline,__import__("os").environ.get("SP_SETUP_FIXTURE") if boundary.mock else None)
-    except BaseException: pass
+    except BaseException:
+        import sys
+        core=sys.modules.get("diagnostic_core")
+        result["live_trace_status"]=getattr(core,"LAST_FINITE_STATUS",None)
     finally:
-        write_new("WORKER_RESULT.json",{"terminal":{"execution":identity["execution"]},"setup_result":result,
-            "scope":"STARTUP_ONLY","scientific_pass":False,"scientific_cells_unrun":180},critical=True)
+        write_new("WORKER_RESULT.json",{"terminal":{"execution":identity["execution"]},"diagnostic_result":result,
+            "scope":"FIRST_LOCKED_UNEDITED_BASELINE_ONLY","scientific_pass":False,"full_scientific_study_unrun":True},critical=True)
     return 0  # A saved diagnostic outcome, not a successful construction.
 
 def audit(deadline):
     from authority import current
     boundary,approved,admission=current()
     execution=boundary.reauthenticate(approved,admission)["execution"]
-    result={"execution":execution,"audit_completed":False,"classification":"INCONCLUSIVE_SETUP","scientific_pass":False}
+    result={"execution":execution,"audit_completed":False,"classification":"INCONCLUSIVE_DIAGNOSTIC","scientific_pass":False}
     try:
         boundary.role("audit",approved,admission)
         forbid_research_imports()  # Saved reader has no model dependencies in either mode.
-        from setup_reader import judge
+        from diagnostic_reader import judge
         result=judge(boundary,execution,deadline)
-    except BaseException: result["audit_error"]="SAVED_SETUP_AUDIT_FAILURE"
+    except BaseException: result["audit_error"]="SAVED_DIAGNOSTIC_AUDIT_FAILURE"
     finally: write_new("AUDIT_RESULT.json",result,critical=True)
     return 0 if result["audit_completed"] else 1
 
@@ -55,13 +58,13 @@
     from owned_production import supervise
     boundary,approved,admission=current()
     identity=boundary.reauthenticate(approved,admission);execution=identity["execution"]
-    write_new("CONTROLLER_ENTRY.json",{"execution":execution,"scope":"STARTUP_ONLY","observed_model_work":"NONE_BEFORE_OWNED_LAUNCH"})
+    write_new("CONTROLLER_ENTRY.json",{"execution":execution,"scope":"FIRST_LOCKED_UNEDITED_BASELINE_ONLY","observed_model_work":"NONE_BEFORE_OWNED_LAUNCH"})
     started=time.monotonic();captures={};errors=[]
-    final={"schema":"setup_parent_final.v1","execution":execution,"classification":"INCONCLUSIVE_SETUP",
-        "audit_completed":False,"scientific_pass":False,"baselines_unrun":24,"requests_unrun":48,"scientific_cells_unrun":180,
+    final={"schema":"first_forward_diagnostic_parent_final.v1","execution":execution,"classification":"INCONCLUSIVE_DIAGNOSTIC",
+        "audit_completed":False,"scientific_pass":False,"requests_unrun":48,"full_scientific_study_unrun":True,
         "external_capture_overrides_worker":True,"unconditional_closeout_attempted":True}
     try:
-        write_new("CONTROLLER_STARTED.json",{"execution":execution,"setup_seconds":{"worker":180,"audit":60,"shared_cleanup":15},
+        write_new("CONTROLLER_STARTED.json",{"execution":execution,"diagnostic_seconds":{"worker":180,"audit":60,"shared_cleanup":15},
             "unchanged_upper_seconds":{"worker":1800,"audit":180,"cleanup":15},"absolute_end":budget.absolute_end,
             "substantive_end":budget.substantive_end,"one_global_cleanup_budget":True})
         owned=json.loads((HERE/"OWNED_IDENTITY.json").read_bytes())
@@ -79,7 +82,7 @@
         require(result["execution"]==execution,"independent audited authority join")
         if all(good_capture(captures.get(x)) for x in ("worker","audit")) and result["audit_completed"] and time.monotonic()<=budget.absolute_end:
             final.update(classification=result["classification"],audit_completed=True)
-    except BaseException: errors.append("SETUP_CONTROLLER_OR_CAPTURE_FAILURE")
+    except BaseException: errors.append("DIAGNOSTIC_CONTROLLER_OR_CAPTURE_FAILURE")
     finally:
         def optional_hash(name):
             try: return sha(control(name).read_bytes())
--- diagnostics\fresh_confirmation_weight_order_fix_v2\launch.py
+++ diagnostics\fresh_confirmation_first_forward_diagnostic_v1\launch.py
@@ -1,4 +1,4 @@
-"""Disabled until a separate trusted root release; setup only, never science."""
+"""Disabled until separate root release; one unedited baseline, no scientific study."""
 import argparse
 import json
 import os
@@ -9,10 +9,10 @@
 def preflight(lock_sha):
     try:
         Boundary().verify_chain(lock_sha)
-        return {"status":"ROOT_AUTHORITY_PRESENT_NOT_LAUNCHED","scope":"STARTUP_ONLY_1LOAD_0F_0D",
+        return {"status":"ROOT_AUTHORITY_PRESENT_NOT_LAUNCHED","scope":"ONE_LOCKED_BASELINE_1LOAD_MAX1F_0D",
             "observed_model_work":"NONE","production_authorized":True,"scientific_execution_allowed":False}
     except (ValueError,OSError):
-        return {"status":"DISABLED_NO_APPROVED_REAL_RELEASE","scope":"STARTUP_ONLY_1LOAD_0F_0D",
+        return {"status":"DISABLED_NO_APPROVED_REAL_RELEASE","scope":"ONE_LOCKED_BASELINE_1LOAD_MAX1F_0D",
             "observed_model_work":"NONE","production_authorized":False,"scientific_execution_allowed":False}
 
 def terminal(boundary,identity,result):
@@ -22,7 +22,7 @@
             "UNKNOWN_MISSING_LOADER_TERMINAL" if boundary.control_path("LOADER_STARTED.json").exists() else "NONE_LOADER_NOT_DISPATCHED")
         if boundary.control_path("PARENT_FINAL.json").exists(): parent_sha=digest(boundary.read_control("PARENT_FINAL.json"))
     except (OSError,ValueError,KeyError): observed="UNKNOWN_TERMINAL_READ_FAILURE"
-    return write_exclusive(boundary.control/"ATTEMPT_TERMINAL.json",encoded({"schema":"setup_attempt_terminal.v1",
+    return write_exclusive(boundary.control/"ATTEMPT_TERMINAL.json",encoded({"schema":"first_forward_attempt_terminal.v1",
         "execution":identity["execution"],"observed_model_work":observed,"parent_final_sha256":parent_sha,
         "attempt_finished":result is not None,"real_scientific_success":False,"scientific_execution_allowed":False}),boundary.root)
 
@@ -31,7 +31,7 @@
     parser.add_argument("--approved-lock-sha256",required=True);parser.add_argument("--preflight",action="store_true")
     args=parser.parse_args()
     need(Path(args.authority_lock).absolute()==MAIN/"root_release/AUTHORITY_LOCK.json","D_FIXED_AUTHORITY_PATH")
-    need(not os.environ.get("SP_SETUP_FIXTURE") and not os.environ.get("SP_SETUP_BATCH_LOCK"),"D_NO_REAL_FIXTURE_OVERRIDE")
+    need(not any(os.environ.get(key) for key in ("SP_SETUP_FIXTURE","SP_SETUP_BATCH_LOCK","SP_FIRST_FORWARD_FIXTURE")),"D_NO_REAL_FIXTURE_OVERRIDE")
     if args.preflight:
         result=preflight(args.approved_lock_sha256);print(json.dumps(result,sort_keys=True));return 0 if result["production_authorized"] else 2
     boundary=Boundary();pin,identity=boundary.admit_once(args.approved_lock_sha256)
@@ -40,7 +40,7 @@
     try:
         from setup_budget import Budget
         from production_run import controller
-        result=controller(Budget(time.monotonic()),"real_setup")
+        result=controller(Budget(time.monotonic()),"real_first_forward_diagnostic")
         return 0 if result["audit_completed"] else 1
     finally: terminal(boundary,identity,result)
 
--- diagnostics\fresh_confirmation_weight_order_fix_v2\setup_budget.py
+++ diagnostics\fresh_confirmation_first_forward_diagnostic_v1\setup_budget.py
@@ -3,8 +3,8 @@
 
 class Budget:
     def __init__(self,started,*,testing=False):
-        self.started=started;self.substantive_end=started+(75 if testing else 240)
-        self.absolute_end=started+(90 if testing else 255);self.used=0.;self.charges=[]
+        self.started=started;self.substantive_end=started+(45 if testing else 240)
+        self.absolute_end=started+(60 if testing else 255);self.used=0.;self.charges=[]
     @property
     def remaining(self): return max(0.,15.-self.used)
     def deadlines(self,now,lane):
