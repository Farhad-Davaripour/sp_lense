# Prospective first-forward execution diagnostic — no real authorization

Question: for the exact already-locked first UNEDITED baseline, which instrumented stage and allowlisted source location first fail, and which observed cleanup-stage failures occur later? This is execution diagnosis only. The closed real attempt 003 remains INCONCLUSIVE_STUDY and immutable; its exact exception is not recoverable from its finite fault receipt. No warning or weight-mutation cause is assumed.

Base: packaged real003 commit `3158826bcd180878f0c101bd2dcea2fca40f9ded`, inventory `35c6739ce3786545c33949abe6352438f0199807bd1d776fa28ffed132ff9132`, original source `c36c900374466472412a6f8cf0aec0d161a618b2`. Historical files and verdicts are not edited. The reviewed corrected loader, retained legacy weight tuple/hash algorithm/expected digest, 14 original predicates, hook/source admission and authority/clock/owned-process machinery are reused. AST removal of only the observation wrappers must recover the complete original adapter AST exactly.

## Fixed scope

Future distinct attempt: `fresh_confirmation_first_forward_diagnostic_attempt_001`. Full input lock remains `d008fb7c53b974694e76afa9ef7db3f4f2d4443eed8a2c83ba06ea6e83987b22`. Only `N01__v1__self_shutdown__KEEP_then_STOP__baseline` may consume a forward ticket. The exact first prompt's existing complete input IDs and all-input attention mask are selected; `tokens_01.json` is pinned to `90dde97a7e6a9bcf6103e9004fef64351c2a771a461bd2f71ec0388b04bb7588`. There is no tokenizer fallback, prompt editing, candidate choice or new encoding. The existing 24-input/48-request plan is authenticated solely to preserve its locks, original hook setup and explicit UNRUN suffix; no other prompt executes.

Actual candidate: pinned Qwen3.5-0.8B CPU float32, with_lens=False; one load, at most one guarded forward, zero derivatives or encodings. The offset is the original `torch.zeros(1024)`. Adapter numeric statements and predicates remain identical; the first full-vocabulary byte serialization is the original `z.detach().numpy().astype("<f4", copy=False).tobytes()` followed by the unchanged raw writer. No gate decision, score, gradient, P/C request, eligibility tuning, additional baseline or continuation runs. A clean diagnostic capture is not a steering/scientific PASS.

## Observation and unknowns

Stages are fixed in `forward_trace.py`: load handoff; outer adapter; guarded bridge dispatch; selected hook; hook-context enter/exit; adapter postconditions; logits serialization/publication; dispatched-input proof; capture clearing; individually typed cleanup stages and writer closeout. Entry/return/raise edges distinguish a begun operation from a returned operation. No completed recorder checks does not imply that no activation hook ran.

The innermost first exception captures a write-once in-memory primary record. Later cleanup failures are separate bounded records; propagation through nested cleanup spans is not claimed to prove distinct underlying exceptions. Only finite exception categories and up to eight allowlisted source/hash/line locations are included: never arbitrary exception text, locals, activation/logit values, credentials or weight contents. Source locations are authenticated to frozen candidate/repository/installed-source pins. Unallowlisted locations, more than eight frames or an incomplete bounded scan are explicitly marked partial. A known stage with partial frames does not establish the ultimate internal source location. Native publication occurs once after attempted cleanup/index closeout, with exact acknowledgment; interruption before publication leaves trace information unknown, not reconstructed. No incomplete trace may be interpreted as complete.

The complete additional native trace is at most 64 KiB, carved from the existing 0.75 MiB other-closeout allocation. At most 96 events, 16 secondary cleanup records, eight saved frames per finding and a 512-frame scan are permitted. Any limit or diagnostic I/O failure is sticky/incomplete and trips the original latch; no field is silently truncated and represented as complete. A finite non-I/O status is copied into the authenticated terminal, or the outer worker fallback if terminal publication fails. A written index cannot override failed trace publication or absent status. Saved interpretation requires exact source/execution/hash/length joins and explicit checks, never removable assertions.

## Diagnostic closeout boundary

Root approved the standalone diagnostic cleanup before freeze. It uses the reviewed startup-style sequence, not a fictitious complete 109-check scientific finalizer: original end_edit; fresh original retained-order parameter_state(digest=True); original latch admission before the unchanged HookGuard.inspect provider and exact admitted-reference equality; unchanged flag/version/identity/byte predicates and explicit active_request_empty, wrapper_cache_empty, bridge_cache_empty checks; original guard.restore unconditionally attempted in finally. If the latch is terminal, admission rejects before provider inspection and that stage is recorded. No latch is cleared or bypassed. The full scientific runner and its cleanup are not changed. Success is labeled diagnostic_cleanup_complete only, with scientific_pass false, full_scientific_finalizer_called false and all 109 normal hook checks UNRUN. Cleanup stages are reported separately from owned-process quiescence.

## One model-free batch

Before running, commit source/test locks, exact case list and expectations. One fresh actual standard-Codex usage receipt under 100%, no more than 120 seconds old at batch admission, is mandatory. Missing/exhausted usage stops work. Eight fixed inert cases only:

1. Clean path: all applicable callback stages return, shared one-baseline diagnostic cleanup executes every cold-state/identity step and restoration once, native trace readback verifies.
2. Bridge failure: first stage BRIDGE_DISPATCH; the subsequent terminal-latch cleanup refusal prevents any provider inspection and does not replace the first cause. The other execution-failure cases exercise the same refusal, except the explicit earlier end_edit failure in case 6.
3. Selected-hook failure: first stage SELECTED_HOOK, not its outer bridge.
4. Context-exit failure: first stage HOOK_CONTEXT_EXIT.
5. Adapter postcondition failure: first stage ADAPTER_POSTCONDITIONS.
6. Serialization failure followed by cleanup failure: primary LOGITS_SERIALIZATION is preserved; cleanup remains secondary.
7. Publication failure followed by partial native diagnostic I/O: primary LOGITS_PUBLICATION survives, receipt is INCOMPLETE and cannot verify.
8. Saved tamper/truncation and hard-counter negatives: valid clean trace verifies; altered receipt hash, source, execution or event stage (inner cases repair outer hash), and truncated bytes reject; second load/forward, derivative and wrong-cell tickets reject.

The expectation code is explicit and survives Python optimization. These callbacks are not tensors, models, hooks, parameters or real logits. They never import or construct the actual adapter, backend, torch or tokenizer. The source parity check parses AST only. No earlier constructor, optimization, full workflow or owned-process suite is repeated. This batch cannot prove real forward behavior, live hook capacity, actual controller execution or complete real diagnostic reader integration.

Limits: 45 seconds substantive work plus ONE 15-second cleanup/closeout, 60 seconds absolute, one batch with no retries or deadline/cap resets. A watchdog terminates at 59 seconds to preserve the absolute boundary. Stop at the first fixed-case failure and retain its evidence plus all remaining UNRUN cases. Preparation at most 32 MiB; combined test evidence at most 8 MiB; every file at most 5 MiB. No actual model/parameter access or content hashing, forwards, derivatives or encoding is allowed in this batch.

## Future real release, still disabled

Separate root review and release are mandatory. The new strict diagnostic envelope is worker 180 seconds + saved audit 60 seconds + ONE shared cleanup 15 seconds = 255 seconds; the unchanged upper scientific contract remains 180F/48D/1load/160tokens, 1800+180+15=1995 seconds and 288 MiB/5 MiB. The new counter imposes the stricter 1load/max1F/0D bound. Actual setup/source/weight/hook admission before the first forward remains required. Reserved native loader proof is at most 2 MiB; terminal 2.25 MiB, index 5 MiB and other closeout 0.75 MiB remain inside the existing 8 MiB closeout reserve. Unknown/overflow/missing final evidence stays inconclusive. This plan makes no universal arbitrary-error-dump or timing guarantee. No real release files or real attempt output are created by preparation.
