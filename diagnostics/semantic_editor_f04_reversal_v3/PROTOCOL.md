# V3 finite-sequence iteration correction — production disabled

Only two expressions in start_states.check_h0/check_replay change: iterate each of the two score/state sequences directly instead of concatenating them. Lists and float arrays are accepted without converting, truncating or hiding nonfinite values. Length1024/full vocabulary248320, exact offset/hidden, raw hash/length/finite containment,1e-6 h0 and2e-5 logit tolerances are unchanged. The V1/V2 INCONCLUSIVE preparations remain immutable.

Exactly three frozen pure cases: (1) equal list/array score and h0 pairs in both orders (all four cross-orders); (2) NaN and positiveInf in either left/right h0 or logit sequence reject (eight fixed subcases); (3) h0/logit wrong length, h0 difference>1e-6, logit difference>2e-5, changed offset or changed hidden state reject (six fixed subcases). Authenticate/reuse V1 six pure and V2 two path cases, with no reruns.

Then exactly ONE unchanged in-process full two-request synthetic workflow and independent saved-data judge: expected14F2D,4 correct routes,2 first-target recoveries,12 skips0UNRUN,9 strict cleanup checks. Separate exclusive starting_arrays/logits and full_recovery roots are retained. No live child fixture, real model/tokenizer/gate scoring/fitting or new inputs. Do not repair or weaken expectations after outcome.

The exact two authenticated original f04 endpoints, unchanged input/tokens, original h0, carried old+new path/net, gradient recipe, gate, thresholds and all runtime sources remain bound. Future ceiling26F8D/one load,300+15+90s/96MiB/5MiB. Preparation180s invoked/32MiB/5MiB. Real authorization remains false.

PASS here means complete synthetic preparation/checker evidence only. The future question is constructed-start recovery to first position; the original unedited model preferred first. No natural-baseline/position-independent reliability, ordinary-preservation, new model success or overall-goal claim. Stop for root's separate release review.
