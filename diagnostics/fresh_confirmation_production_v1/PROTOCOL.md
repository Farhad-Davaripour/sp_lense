# Production adapter preparation, not a model release

One new namespace only. Authorization remains FALSE. The exact complete v2 input lock, 24 admitted prompts, 48 cold requests, learned gate, float32 edit equations, full-vocabulary scorers, endpoint tolerances, and OFF identity predicates remain unchanged. There is no tokenizer fallback, prompt search, refit, or real-model execution.

Before the single adapter-only batch, bind the COMPLETE committed `fresh_confirmation_hook_failclosed_v1` interface and root's decision at `e2943cfc12ab695b9fd8b4932f8a0b287ac2febe`. Preserve the old capacity UNVERIFIED result. Actual complete setup/reference/changes and all 109 normal check reserves must be admitted before any future first forward. Arbitrary error details need not fit, but every mismatch or incomplete-evidence condition permanently closes dispatch and prevents PASS.

The injected tensor module is not Qwen and loads no weights or tokenizer. Its small layer-10 hook interface exercises these fixed checks only: exact zero-edit replay; nonzero last-input edit with measured unchanged nonfinal hashes; current-state downstream autograd and full-vocabulary endpoint identity; complete actual parameter flags/versions/identities/digest and hook/cache cleanup; recorder-overflow propagation with a permanently closed dispatch latch. Also statically compile the source delta, reject disabled production admission, and check a guarded unclaimed forward without executing its body. No full cohort rehearsal or old failure suite is rerun.

Exactly one prospectively source/input-locked, committed test batch. Work cutoff 45 seconds; complete outer controller and cleanup envelope 60 seconds from one start. At most 32 MiB preparation and 32 MiB combined injection evidence, 5 MiB per file. Failed or incomplete stages remain recorded; no retry or repair after the batch. Usage must be available and below 100 percent immediately before dispatch.

The disabled production controller takes worker=1800, cleanup=15, audit=180 seconds and 180F/48D/1load ceilings from the unchanged resource contract, not old 300/90 defaults. A real process, actual complete model setup admission, runtime package/parameter compatibility, resource/timing behavior and final external release are not established by this small injection test. Any future real release requires independent root review; this preparation itself authorizes none.
