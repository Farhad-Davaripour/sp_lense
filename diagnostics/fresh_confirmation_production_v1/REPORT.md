# Disabled production adapter: bounded injection result

`PASS_ADAPTER_ONLY`: the single frozen tensor-module batch completed all five selected groups in 36.500 seconds at the controller (31.579 seconds for worker checks). The work cutoff was 45 seconds and the absolute controller/cleanup envelope was 60 seconds. Both authenticated retained handles yielded signaled exit-code-zero proofs; capture threads joined, pipes closed, and quiescence was established with no ownership/capture/cleanup fault. No retry or post-batch source repair occurred.

This is an adapter/component result, not a real study result, complete production hook proof, real-run release, or milestone credit. Three small injected torch modules were constructed; only the normal fixture executed five module forwards and one autograd derivative. No Qwen constructor, model-weight load, tokenizer load/call, real forward, full-cohort rehearsal, or historical failure-suite rerun occurred. Production authorization remains FALSE.

## Prospective bindings and exact delta

Source/input preparation was committed before dispatch at `562588f9307faceaf5aa1b5c964b8dd0eedf835a`. All 30 committed files had identical raw working/Git bytes before execution. `SOURCE_FREEZE.json` pins 29 members, 331,901 bytes, with SHA-256 `b7f0faefb27b52e6825322de2ef0ee87a0e1bd02085fb72ad7902ee031a9fb92`. Fresh standard Codex usage was 15 percent before dispatch.

The unchanged complete v2 INPUT_LOCK is at `01de65cc93b128d605ee1d66b8ccaebc6f6eda40`, SHA-256 `d008fb7c53b974694e76afa9ef7db3f4f2d4443eed8a2c83ba06ea6e83987b22`. The production candidate still binds exactly 24 prompts / 48 cold requests / 180 conditional cells, with maximum actual input length 150 under the frozen 160-token ceiling. Bound-plan SHA-256 is `448ce8dfb2a0fa18eb56cd900b913c56b2bbb8e24ec942560adaf444a9749011`. No input, label order, selection, wording, tokenizer fallback or gold proof was changed; O06 retains its existing manual-proof distinction.

The 73 committed source/input pins include the checked actual-input runtime at `2e11335f1036532b6f79fb2927dd9616c8a09915`, original scientific functions at `1d4cc39eba5fb7f987649a05f71247061f005d02`, raw writer/reader and resource contract, and the complete hook successor at `5c8ff02c617c693ad643851ce00988cbf6f513dc`. Hook implementation SHA-256: `f86ab31d463e9be3b25f8e2be0a45a50600da0b54e61d89189c58fd068f80484`. Root's prospective fault decision at `e2943cfc12ab695b9fd8b4932f8a0b287ac2febe` is bound. The old universal fault-capacity result remains UNVERIFIED.

`EXACT_ENGINE_DELTA.patch` records the generated engine/judge delta. The prepared engine SHA-256 is `12bbdad293ba349a2d04a8c0b5e5bc97eb186906e658dfd19169b86887558db5`; judge SHA-256 is `baf99592c49fc8c21ba713ea3fdf4429dbf5e4988097e31acf159ad7fd3891ee`. Preparation verifies unchanged original scientific definitions and exact AST equality of independent numeric/acceptance predicates.

The new binding replaces the dummy weight, literal nonfinal hash, and fake active/cache assumptions with actual named-parameter identities, complete flag/version arrays, full parameter-byte digests, measured nonfinal activation bytes, bridge-cache checks, and exact unchanged HookGuard snapshots. A returned layer-10 full-activation leaf feeds the true downstream graph; autograd selects the last INPUT position. Only token tensors, mask, operational phase and residual offset reach the model interface. Gate features remain fresh unedited residuals; category, gold, expected route and policy are audit/controller fields, not model output formulas or gate inputs. The pre-load bridge guard and autograd tickets reject any unclaimed call before its original body executes.

The bounded writer adaptation preserves raw float32 vectors, exclusive/append semantics, reconciliation, per-category/file/aggregate caps and native failure closeout. It uses finite coded errors and the hook component's shared permanent latch. All hook files use that same writer owner. Saved hook judgment requires both full byte reconstruction and the recorder's live controller status; physical index completeness alone is insufficient. Production worker/audit captures remain an additional required final condition.

## What the small batch established

| Selected check | Measured result |
| --- | --- |
| Disabled authority and exact source binding | Rejected production before loader; compiled exact prepared delta |
| Zero/nonzero/current-gradient adapter | 5F / 1D; exact full-vocabulary zero-edit replay and selected endpoint; current gradient 0.625, distinct from baseline 0.375 |
| Parameters and cold cleanup | Mixed original flags `[true,false]` restored; versions `[0,0]` unchanged; no gradients; digest/identities/hook/cache checks pass |
| Digest beyond version flags | A deliberate post-capture fixture `.data` mutation left versions unchanged but changed the measured whole-parameter digest |
| Overflow propagation and unclaimed dispatch | Overflow latched H_CAPACITY; correctly reserved later F/D permits rejected, actual F/D remained zero, `never_forward` remained UNRUN; separate unclaimed forward rejected before body |

Every executed normal capture measured nonfinal SHA-256 `b943a2362ca1b8ed9f011d2fd4c31f9a328c806c5c2d9547df40be3a3f45025d`. Its actual 248,320-value raw vectors are each 993,280 bytes, with byte identities independently authenticated by the saved I/O reader. The normal physical capture is 5,026,158 bytes, index SHA-256 `4124426b150f34dbcab5b8c8b922375e1d1eddefea44df8ca5a1a6f359c22b6c`.

The tiny normal fixture intentionally executes only four of its required 109 hook checks. The other 105 remain UNRUN and no complete hook index is claimed: recorder status remains ADMITTED with `permits_pass:false`. Complete 109-check component reconstruction is evidence from the separately committed hook suite, not from this adapter prefix.

The overflow fixture injects an aggregate accounting value one byte over the ceiling after complete tiny setup. It is not an observed real-model overflow. Its status is TERMINAL, primary H_CAPACITY, `receipt_failed:true`, `index_sha256:null`, `permits_pass:false`, and the exact one-cell schedule remains UNRUN. Its 12,222-byte physical writer capture is COMPLETE because the bytes actually written are intact; that does not mean hook or scientific completeness. The missing fault receipt cannot authorize a PASS. The authoritative finite controller status is retained in `ADAPTER_TEST_REPORT.json` and the closed owned batch report.

Combined injection evidence is 5,038,380 bytes; largest file is 993,280 bytes. `FINAL_INVENTORY.json` records final preparation/receipt/log membership and hashes, excluding only itself. All final caps include its separately measured bytes: 32 MiB preparation, 32 MiB combined injection evidence, and 5 MiB per file.

## Remaining boundary

The production loader, real worker/audit lanes, and final admission/release path were prepared but NOT executed. A real run still needs independent root review, a separately authorized successor release, fresh usage and bounded-schema admission, actual pinned Qwen CPU-float32 runtime/parameter compatibility, and complete actual model hook setup/reference/change admission before its first forward. The tiny torch module cannot establish those model-specific conditions, complete production hook evidence, or real-model success/failure-path timing and behavior.

Production limits remain 180F / 48D / one load and worker 1800 seconds, audit 180 seconds, with one 15-second cleanup allowance in a maximum 1995-second envelope. These are enforced allowances in the disabled controller, not measured worst-case completion guarantees. The small test did not execute the full production scientific schedule or its saved scientific final adjudication. Root review of that integrated source and its early-stop/closeout paths remains required; no complete final-study pipeline claim follows from this result.

No new steering reliability, natural P-first flip, reusable direction, semantic specificity or broad preservation claim follows. Stop for root review before any real-model access.
