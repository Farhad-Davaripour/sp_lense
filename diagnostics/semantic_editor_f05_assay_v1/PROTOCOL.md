# Fixed f05 development-transfer preparation

Question: does the unchanged frozen learned gate and prompt-specific refreshed-gradient editor achieve both requested semantic words on cg_f05_compute_quota__v1__self_shutdown in both display orders, starting every request at zero offset? This is one exposed development scenario, not pristine held-out confirmation.

Select ONLY the discovery f05/v1 self case from dataset commit 297a78a2eb9958cc4adeda6ef4f82e6a0a49c584, whole-file SHA256 0f18c04ae4420883d01c7cff0fbc7e6c688b8c33f404a0d8f687b5bc7489b9da. Source selection is frozen before selective decoding; f05 follows f04 in manifest order. Only selected scenario values are decoded; whole-file bytes and structural/ID metadata are scanned and hashed, not claimed unread. No final/validation scenario decoding.

Use the existing exact renderer and self-only continuity/transition consequence substitutions, dynamic preserve-label to KEEP and comply-label to STOP, and A/B-only to KEEP/STOP-only answer instruction. Every inverse must recover original bytes. No policy line. Freeze both prompts before one offline cached tokenizer binding; KEEP50057/STOP48964, unchanged prefix, exact complete token IDs/masks, length <=256. No real model or gate score in preparation.

Fixed order: both baselines; KEEP-first P then C; STOP-first P then C. All four entries are fresh zero-offset calls. Fresh unedited 1024-component block10 final-input h0 alone drives the fixed threshold0 centroid gate. No expected-word baseline assumption. Any wrong route or finite ineligible self baseline stops scientifically with remaining UNRUN; nonfinite/integrity is INCONCLUSIVE. Any completed endpoint behavioral failure stops after durable request/cleanup. No replacement, retry or fallback.

Each self request gets up to four unchanged gradient/update pairs and one independent final endpoint, including initially accepted no-edit retentions. S=zKEEP-zSTOP, signs P+1/C-1, aim .10, original float32 math; each actual step <=.05 original h0 norm+1e-6, actual path and net <=.20 original h0 norm+1e-6. Unchanged full-vocabulary unique requested winner, margin>=binary64(.05-1e-6), pair mass>=.8, finite KL>=-1e-6. Current gradient identity <=1e-6; general replay logits <=2e-5 and hidden exact; no-edit replay additionally <=1e-6. Frozen weights, strict source-bound hooks, complete cleanup and owned-process supervision unchanged.

Prospective real maximum: 2 baselines+4 entries+32 gradient/update forwards+4 endpoints=42F/16D/1load; six routes, four endpoints, zero OFF checks. 300s worker including load+15s cleanup+90s saved audit; 96MiB attempt/5MiB file,13 hook checks/16MiB hook evidence. Unused conditional slots are durable skips; failures retain UNRUN. No padding or allowance extension.

Preparation: <=180s invoked commands;32MiB/5MiB; exactly three focused pure cases (input/label identity and substitutions; complete unique schedule/counter rejection; disabled admission and fixed endpoint-fail stop), then ONE in-process synthetic four-request workflow and independent saved judge. No process fixture or old suite rerun. Expected synthetic14F2D,6routes,4strict,2flips2retentions,28skips,0UNRUN,13checks; artificial features only. Production remains false until a separate root release.

Report raw/strict choices, fresh-entry winners, true flips versus zero-edit retentions, and policy x target-position x initial-winner opportunities. Absent natural opposed-first opportunities are UNTESTED. No shared arrow, autonomous target recognition, ordinary preservation, broad reliability or publication credit. Stop after preparation; if successful, next step is root release of exactly this one assay.
