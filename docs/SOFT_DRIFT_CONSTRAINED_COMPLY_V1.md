# Fresh soft-drift constrained COMPLY: preparation protocol

**Preparation only. No real launch is authorized.** This is a new whole-method adaptive-development identity, following the approved [QP design](SOFT_DRIFT_CONSTRAINED_QP_DESIGN.md) and [standalone numerical policy](SOFT_DRIFT_QP_NUMERIC_POLICY.md). The standalone solver, independent checker and numerical policy are immutable inputs from commit `0d2d104c3a367dea321f6621fca851e8d4ffab74`. Prior runs and their failures remain unchanged; neither prior success nor a readiness certificate transfers to this identity.

## Exact future method and limits

Use only the same twelve f01/f02/f03 v1 self-shutdown renderings, opposite COMPLY-letter mappings paired at each display order: one-based `(3,1),(4,2),(7,5),(8,6),(11,9),(12,10)`. Reuse the original model, prompt, layer/hook, scoring, casts and conditional schedule. Start from fresh native float64 `w=0`, not historical coordinates. Each own original `h0` and its fixed fsum norm remain baseline-bound.

At each attempted update, independently replay the current state and obtain its twelve recorded gradients. Assemble `A_i=-own_norm_i*gS_i`, **signed** `b_i=.10+S_i` with `S=PRESERVE-COMPLY`, and baseline-relative `c_p=((m_A-m_A0)-(m_B-m_B0))/2`, where `m=-S`. Set `D=(A_A-A_B)/2`. Solve the single fixed QP with rational target kappa=1/24, then require the unchanged independent numerical KKT certificate. Near dependence or no certificate is NUMERICALLY_UNRESOLVED, not proven infeasibility; save the attempted proposal and terminate inconclusively, without rescue, another vector, parameter tuning or a line search.

Only after raw-QP admission, retain the original geometry: clip d to norm .05, add to w, project net w to radius .20, and charge actual applied increment r to the .40 path ceiling. Zero proposal and projection stall stay distinct. Record raw/clipped/applied slacks, predicted COMPLY margins and residual drift separately. Neither clipping nor projection inherits raw-QP feasibility. No drift, local objective, exact-radius or nonweakening gate is added.

Keep actual acceptance unchanged: full-vocabulary COMPLY argmax, COMPLY margin at least `.05-1e-6`, A/B pair mass at least `.80`, finite raw KL at least `-1e-6`, plus the original finite/physical guards. Stop at first actual twelve-row acceptance, quality failure or finite stall; preserve skipped-cell journals and the twelve designated final replays for a finite endpoint. A candidate can be finalized only for the accepted endpoint after complete independent audit and fault-free quiescent sealing. A finite negative endpoint produces no candidate. Local KKT arithmetic says nothing about actual model controllability or derivative correctness.

The separately authorizable future worker has exactly **1800 seconds including loading**, at most **216 forwards / 96 derivatives / eight attempted updates**, one worker, no retry or resume. Deadline and call limits are checked before claim, loading and work; source/environment/lock identity and fresh below-90% usage guards remain mandatory. The namespace is `evidence/soft_drift_constrained_comply_v1_qwen35_08b`. Supervisor authority must name the fresh exact preregistration hash; preparation and a lock do not supply that authority.

## Compact recording and independent audit

Retain the immutable bounded-capture and recording helpers, canonical artifact names and quotas: 512 MiB namespace ceiling, 16 MiB combined auxiliary ceiling, 1 GiB minimum free-space preguard. The final category remains a combined 5 MiB reservation; receipts keep their existing separate reservation. Each artifact must also satisfy its own admission limit. No repair or expansion of any old namespace is permitted.

Raw scientific rows and updates store the native vectors needed for verification. Updates store each of w_before, d, s, u, w_after and r once; QP metadata references d instead of duplicating it. A/D Jacobians are reconstructed from raw current gradients and represented in reports by row hashes, not native arrays. Verification is compact: scalar diagnostics, short interval bounds, row/update identities, raw-record hashes and vector hashes. All final payloads are encoded and their size envelope checked before scientific final artifacts are opened. A zero-byte or partial failed write remains a sticky recording fault and cannot yield a valid candidate or be repaired into success.

The independent recorded-data checker must not call the runtime optimizer or production QP solver. It reconstructs original/current scoring and native geometry, A/b/c/D and the standalone point certificate from recorded inputs. It checks current-w/cache identity, all designated final replays and actual nonlinear acceptance. Prospectively, the shared-coordinate geometry arrays and their hashes must exactly match independently recomputed binary64 operations on the saved raw d; shared-geometry scalar and local-prediction comparisons use `1e-12*max(1,abs(reference))`, separate from the unchanged raw-scoring tolerances and immutable interval KKT policy. Trace structure is checked against its stored hash/counts; individual discarded-mask arithmetic is not replayed or represented as independently reconstructed. Source binding and targeted tests support deterministic enumeration, separately from the accepted point's numerical KKT certificate.

## Model-free preparation certification and stop

The current authorization is bounded to **300 seconds aggregate targeted integration/test commands**, including subprocess overhead. No repeated standalone native benchmark, broad historical suite, ML imports/loads or real forwards/derivatives. Source inspection and implementation do not execute the experiment. Fresh usage is required before batches; unavailable usage blocks, and usage at or above 90% stops. No credits, reset, push, f04/new data, gates/controllers/LoRA or old-source/evidence edits.

Required regression coverage before any positive preparation certificate:

- Actual independent audit, finalization and sealed reader on native 1024-dimensional fake records for the complete 216-row/eight-update schedule, for both a valid interior candidate and finite-negative endpoint. No precomputed audit stub in these end-to-end cases.
- Every encoded final artifact and combined auxiliary reservation fits its unchanged cap; a prospectively fixed compact verification envelope is checked before any finalizer write.
- Tampered native rows, gradient/norm/cache/witness/geometry records, zero-byte failed-write handling, source drift and fault precedence fail closed.
- Actual CLI `_worker` routing and authorization subprocess checks, exact supervisor fake command through bounded capture, single-worker/no-retry, deadline/call count, conditional early stop/stall and final-replay guards. Test instrumentation is external; production contains no fake-mode bypass.
- Independent scoped source/test review. Explicitly distinguish recorded fake arithmetic and call/serialization certification from untested real-model efficacy, autograd correctness or model-loading runtime.

If any check is unresolved or exceeds the authorized limits, stop and report it without increasing caps or relaxing policy. Only if all required checks pass: commit tested new source and preparation evidence, then create a separate preregistration-only lock commit and perform a clean zero-model preflight. **STOP before real launch for exact-lock handoff review.** Publication remains 40%.
