# Three-family COMPLY preparation closeout

**Disposition: preparation incomplete — STORAGE_AFFORDABILITY_NOT_CERTIFIED.**
Numerical implementation and focused synthetic checks are complete. Do not
preregister or run this candidate yet. The new evidence namespace remains
absent; no lock, RUN_STARTED, WORKER_CLAIM, candidate or real arrays were created.

Zero tokenizer loads, zero model loads, **0 real forwards /0 real derivatives**.
One fixed synthetic benchmark only; synthetic tensor tests are software checks,
not model measurements or scientific success. Publication readiness remains40%.

## What was prepared

The new config binds twelve complete v1/self_shutdown renderings from the
first three manifest-ordered discovery families f01/f02/f03. Four fixed
semantic-mapping/display layouts per family are THREE situations, not twelve.
F03 becomes training for THIS candidate; its old transfer history is unchanged.
F04 remains exposed development outside fit, not pristine heldout.
Broader coverage is a hypothesis; old C already passed f03.

Historical files remain unmodified. Isolated wrappers retain the original
COMPLY objective/signs (including negative RHS), fresh zero common vector,
own-original norms, float64/float32 cast sequence, projection/stop/cap rules,
raw full-vocabulary acceptance and80-digit independent selected KKT audit.
Only allowed constraint/row/call/time bounds and truthful scope metadata change.
No new regularizer, nonweakening guard, selector, fallback or controller.

Proposed future schedule, NOT executed:12 baselines +
8*(12 gradient forwards+12 scored edits)+12 final replays =216F/96D,
192 optional cells,8 updates, one worker/attempt,1200s INCLUDING loading.
This is neither a runtime prediction nor an extension of an old attempt.

## Checks actually executed

- Solver tests:33 passed, first run1.47s; final changed-path recheck1.36s.
- Three-family tests:28 passed in one run,6.84s.
- Total DISTINCT focused tests:61. Pytest plugin autoload disabled.
- Existing pytest cache-permission warning only; no permission repair.
- Ruff passed for new solver/benchmark/plan/runtime/checker/tests.
- Import-only adaptation checks: parent modules and methods unmodified;
  Session AST differs only in two progress strings; increment bytecode/constants
  unchanged; evaluate binds the new increment; supervisor timeout1200s.
- Text/schedule independently rebuilt:12 rows/3 situations,216 cells,
  96 derivative cells,192 optional cells. No model/tokenizer import needed.
- Scope/layout tamper, common-vector/own-norm geometry, all8 updates,
  acceptance/quality/zero/stall/skip branches, cap/no-retry, actual OTHER argmax,
  nonfinite/raw tamper and final replay failures are covered.
- Missing/negative preparation certificate blocks freeze/preflight BEFORE
  inherited namespace/claim/load actions in focused tests.
- Independent KKT tests reject vector, negative dual, stationarity, RHS,
  constraint-sign and metric corruption. All4096 masks, exact ties,
  redundancy/rank-floor, infeasible and marginal cases covered.
- No broad historical suites, real model execution, or repeated benchmark.

## Fixed synthetic affordability benchmark

Source declaration preceded its sole run: four native1024/12-row systems,
20s external cap per case,90s whole benchmark, one attempt/case, no retries.
Whole benchmark elapsed13.82799999974668s. Every case accounted for4096 masks;
16,384 total. No torch/transformers imports in any benchmark worker.
The complete returned JSON values are preserved in
shared_comply_crossed_three_family_v1_preparation.json (serialization normalized).

|Fixed case|Solver s|External s|Journal bytes|Selected mask / status|
|---|---:|---:|---:|---|
|Positive orthogonal|12.782000000122935|12.984999999869615|1761631|4095 / KKT_ESTIMATE_ONLY|
|Negative RHS, zero optimum|0.10999999986961484|0.29600000008940697|617635|0 / KKT_ESTIMATE_ONLY|
|Redundant rank-deficient|0.07800000021234155|0.26599999982863665|506201|1 / KKT_ESTIMATE_ONLY|
|Opposing infeasible|0.07799999974668026|0.2809999999590218|506309|null / NUMERICALLY_UNRESOLVED|

The opposing case is mathematically constructed as infeasible, but the
algorithm correctly reports unresolved and issues NO infeasibility certificate.
The orthogonal solution's norm0.25495097567963926 is an unconstrained increment
estimate, not a permitted final steering-vector norm; original projection
would still apply in the prospective method.

The benchmark verifies that these fixed solver cases complete within their
declared bounds. It does NOT establish that216 real forwards fit1200s.

## Storage: numeric budgets fit, complete evidence does not yet have a bound

Proposed allocations sum524,995,016bytes, below512MiB=536,870,912 by11,875,896.
216 compressed-logit bounds total214,616,520bytes; rows226,492,416;
updates67,108,864; auxiliary16,777,216. Preload free guard1GiB.
The arithmetic check observed779,086,725,120 free bytes. No preallocation.

Each mask entry has <=12 pivots +12 residuals +8 metric scalars, no native
vector. Reserving32bytes per finite binary64 number, maximal schema
1,399bytes/mask gives5,738,496bytes for the4096-mask list, including
delimiters/default JSON separators. Eight lists <=45,907,968bytes.
This leaves2,650,112bytes inside each8MiB update allocation for the
selected solution and outer update data.

A1024-number list bound is34,818bytes. Rows have at most7 native vectors;
updates at most9. With256KiB reserved for their remaining fixed fields,
the estimates are505,870bytes/row and6,314,002bytes/update. These support
the fixed numeric-schema allocations; they do not bound arbitrary logs.

Read-only source audit found three concrete gaps:

1. scripts/shared_comply_two_family.py:578-582 redirects unrestricted worker
   stdout/stderr to worker.log. No byte ceiling or bounded termination overshoot.
2. Exception text is unrestricted at that file's554/598/605, at
   scripts/shared_preserve_two_family.py:119 and
   scripts/envelope_transfer_diagnostic.py:270-281/311.
3. scripts/verify_shared_comply_two_family.py:674-695 checks inventory before
   later verification/report and candidate writes. The complete final
   audit/candidate/report/closeout inventory is not yet enforced.

The backend metadata schema does NOT contain raw chat-template text; that
was checked and is not the identified logging channel.
Disk free space is ample, but free space does not solve an unbounded evidence
budget. A16MiB reserve is a budget, not a proof.

## Explicit remaining work and stop condition

A supervisor-reviewed auxiliary policy must budget complete logs, exception
records, verification, candidate, report and closeout; provide a tested
hard-stop with bounded capture/termination overshoot and a failure-record
reserve; then validate the complete final inventory. Periodic file-size
checks alone cannot bound an arbitrarily large single write. Silently
truncating diagnostics or enlarging512MiB is not an acceptable substitute.

No new logging framework or numeric policy change was implemented here.
The committed machine-readable preparation certificate remains negative,
and freeze/preflight fail closed. No preregistration lock or actual preflight
was attempted. This is the requested explicit blocker, not model infeasibility.

After the missing policy is authorized, implemented and verified, source
must be committed before a separate preregistration-only lock commit.
The latter must remain HEAD directly after its source parent. A read-only
zero-model preflight can then run. A real construction still needs a
SEPARATE supervisor authorization naming that eventual lock SHA256.

Stop here for review. No gate/controller, transfer, new layer search,
generalization, collateral or natural self-preservation claim.

## Provenance

Base commit:ba5ae6b7559f120f7907d60c191297788dcdd095.
The preparation commit contains only the eleven new source/config/test/report
files listed in the handoff; all historical tracked bytes remain unchanged.

Parent lock SHA256:
c3c32fc972541d9b3969525b3edc088372a164e5815f3afe69cb55e7bd40d431.
All114 parent source hashes checked, zero mismatches.
Config SHA256:
70e9ab21b03e3cdbf007c82f92d1400b32579ab0ce26de3f3d147a79983a92d6.
Exact implementation, test and benchmark-source SHA256 values are in the
machine-readable preparation report; the commit supplies the report's own
noncircular identity. Plan revalidates every raw input and protected artifact;
old candidates are hash-only provenance and never a warm start.

No pushes, dependencies, permissions/security/Git repairs, usage resets,
credits, paid compute or model/settings changes. Standard usage42% at final
test batch; explicit below90 guard remained satisfied. Unrelated untracked
user files were preserved.
