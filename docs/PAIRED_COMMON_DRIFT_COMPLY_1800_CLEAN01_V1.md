# Clean01 launcher repair: model-free preparation only

This separately named clean successor repairs the executable dispatch defect in the immutable [failed 1800 attempt](../evidence/paired_common_drift_comply_three_family_1800_v1_qwen35_08b/PILOT_REPORT.md), evidence commit `3397190640d87323b9405c1b4eab523bfe7f2ebd`. That attempt did not enter the worker or load a model: its CLI looked up `_worker` as an attribute, while the frozen runtime exports `worker`. The previous preparation certificate's passing imported-function tests did **not** cover that executable command mapping. It remains historical evidence, not proof of launch correctness.

Clean01 uses an explicit command map: `freeze -> freeze`, `preflight -> preflight`, `run -> run`, **`_worker -> worker`**. Unknown or malformed commands fail closed. No production test-mode switch, authorization bypass, relaxed source pin or alternate loading path is added. Authenticated frozen runtime and independent checker code are reused in isolated modules; only identity/provenance bindings and CLI dispatch differ from the 1800 predecessor.

All scientific and resource settings remain unchanged: pinned Qwen3.5-0.8B CPU float32, same twelve prompts/six pairs, fresh native zero, original layer/hook/casts/own norms, tau/lambda/B-floor and update arithmetic, eight updates,216F/96D,1800 seconds worker including loading, step .05/net .20/path .40, first acceptance and twelve final replays. No warm start, resume, partial-output coordinates, new data, tuning, alternate vector or extra step. Independent offline audit/sealing remain outside the worker timer. This is adaptive completion/reproducibility work, not a success rescue or held-out test.

Recording class, serializers, capture and all limits are unchanged:512MiB complete content,16MiB complete auxiliary allowance,1GiB free-space guard and524,995,016-byte category sum. Quiescence and complete fault-free inventory are required for any candidate; failures remain INCONCLUSIVE. Candidate acceptance, independent recorded-gradient arithmetic/full-vocabulary behavior/geometry/replays and native norm<=.20 without renormalization are unchanged.

Required regression coverage now includes the **actual executable** invoked in subprocesses. A test-only startup observer blocks ML imports and records whether `worker`, `require_authorization` and `load_backend` are reached. With authorization absent, `_worker` must reach the intended worker/authority path and stop before loading; a nonzero exit alone is insufficient. Unknown commands must never enter that path. A separate temporary fake worker receives the supervisor's exact emitted `[python, -u, script, _worker]` command through the real bounded-capture machinery and proves argument routing, one process, no duplicate/retry and no ML imports. Test fixtures do not alter production authorization or source pins.

The new exact-source certificate must affirm those entrypoint checks independently of the historical certificate, while retaining its explicit coverage limitation. Targeted tests alone, maximum120 seconds aggregate; no broad historical repeats. Source commit, separate adjacent preregistration-only lock and clean zero-model preflight are permitted. New namespace: `evidence/paired_common_drift_comply_three_family_1800_clean01_v1_qwen35_08b`.

**STOP before real launch. This job does not authorize a run.** Model/tokenizer loads and real forwards/derivatives must remain zero. Require fresh available usage below90% before batches; no resets, credits, push, transfer/f04, gates/controllers or LoRA. Preserve all historical and unrelated files. Publication remains40%.
