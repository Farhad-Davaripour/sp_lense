# Certified-descent COMPLY: model-free preparation certified

Storage/accounting preparation is certified for the bound source composition. No real model run is authorized. This certifies the runtime adapter, recorded arithmetic, schedule, compact audit and recording/finalization behavior—not model efficacy or real derivative correctness.

## Gate and scientific scope

The sole saved-initial-input diagnostic used authenticated initial zero w, twelve gradients and margins from evidence commit `d3338172b1e4560b74f28f14652c219fe4ac279b`. The unchanged solver/checker completed externally in 0.7427715 seconds under hard10s. It rejected j0 on exact serialized geometry and admitted j1: local objective 0.5182565513 to 0.3762749519, gain 0.1419815994. This is posthoc local-surrogate numerical evidence only. No endpoint vector was exported or reused, and the diagnostic was not repeated.

The fresh runtime still starts from zero and obtains twelve new gradients at every current common w. The pinned model, twelve prompts, own original norms, casts/hooks, signs, scientific gates and immutable four-file numerical method remain unchanged. Final eligibility requires all twelve actual COMPLY full-vocabulary argmaxes, margins >=.05-1e-6, pair mass >=.80, finite KL >=-1e-6, integrity and matching final replays. Future limits are one attempt,1800s,216F/96D/eight updates; no actual F/D were performed here.

## Executed coverage and honest source chronology

The [original prospective lock](certified_descent_comply_model_free_test_lock.json) bound the initial runtime, numerical audit, full fixture and recording bytes. Its ancillary batch passed 94 tests (2 full cases deselected), externally in 9.153464 seconds. The two full cases below each passed exactly once with hard300s limits. Both used native1024 vectors, distinct full248320 logit arrays,216 rows,96 supplied-gradient ledger completions,eight actual dyadic updates and twelve matching final replays. Actual budgeted writes, independent numeric audit, finalizer and sealed reader were used; no fabricated full-case audit callback.

| Full synthetic pipeline | Interior candidate | Finite negative |
|---|---:|---:|
| External seconds | 227.397930 | 231.684036 |
| Construction seconds | 147.806542 | 156.208301 |
| Independent audit/finalizer seconds | 78.457557 | 74.415165 |
| Namespace bytes | 9226483 | 9221728 |
| verification.json bytes | 274107 | 274855 |
| PILOT_REPORT.md bytes | 986 | 988 |
| CLOSEOUT.json bytes | 248 | 249 |
| comply_vector.json bytes | 5786 | Absent |
| candidate_freeze.json bytes | 198 | Absent |
| Candidate eligible | Yes, synthetic only | No |

Both cases passed all per-file,5MiB final-category,16MiB auxiliary,512MiB namespace and free-space checks. The prospective compact verification envelope stays1015808 bytes. Whole-record worst-finite encodings passed at189842 row bytes and85306/85290 update bytes. These are valid synthetic recordings, not model results. All temporary fixtures were retained.

Only after both full cases passed, source review prompted a [metadata-only amendment lock](certified_descent_comply_metadata_test_lock.json): require method-specific named coverage, pin the compact saved-gate identity, bind the original versus amended test chronology and complete shared execution ledger, and qualify report text for zero/no-step outcomes. It does not alter the numerical method, runtime adapter, raw audit or actual recording/finalization algorithms. The later batch passed142 metadata/recording/plan/runtime tests (2 full cases deselected), externally in7.903700 seconds under hard20s. No optimizer or full case was repeated in that batch.

The [results receipt](certified_descent_comply_model_free_results.json) proves the narrow amendment scope: removing only the documented metadata/report/source-closure additions and three-line generic-certificate unit-test isolation in memory exactly recovers each original tested file SHA256. The full cases are attributed only to their original source lock, never retroactively to the metadata amendment. The exact final certificate binds both locks, results, source and this report.

Coverage includes actual CLI/supervisor authorization and wrong-lock rejection before loading, fresh inclusive100% usage validation, fresh-current-w/own-norm/path-history checks, adapter-to-independent-checker round-trip and tampering, first-passing exact serialized certificates, assembly+solver+encoding deadlines, exact-zero versus ordinary-no-step versus technical-failure handling, supervisor INCONCLUSIVE behavior, compact references, preencoding/caps, zero-byte/sticky faults and sealed tampering.

## Budget, failures and handoff boundary

Shared allocation was720 seconds: saved gate10, each full case300, ancillary110. Explicitly measured execution totals 479.291593 seconds. Adding a conservative75-second charge for all remaining root/agent source reads/reviews, format/hash/Git bookkeeping and bounded administrative closeout yields **554.291593 charged seconds**, including **94.466857 ancillary seconds**. The75 seconds are a conservative charge/allowance, not invented measured runtime; the remaining certificate/commit/freeze/preflight administrative execution is capped at20 seconds within that allowance. Agent work was source-only.

Known failures are preserved: two Ruff style findings fixed before their respective test locks; one read-only scope-restoration check initially used CRLF instead of LF and then passed after correcting only its in-memory reconstruction. No numerical, model or full-case retry occurred. Detailed measured dispositions, source and artifact hashes, fixture paths and cap checks are in the results receipt.

All model/tokenizer/real F/D counters are zero, with ML-import sentinels in both pytest phases and both full cases. Latest usage check69%; no reset/credit or push. No gate, LoRA, f04/new data, warmstart or unrelated changes. The full fixtures are axis-supported synthetic vectors and do not prove physical-model performance, dense-problem convergence, model loading time or causal efficacy.

Next administrative sequence: commit tested source/report/certificate, freeze a new untouched namespace, commit only its preregistration as the immediate child, then run exact-source zero-model preflight. Record actual commit hashes and administrative timing separately outside the frozen namespace/source; do not modify this bound report afterward. **STOP BEFORE REAL LAUNCH. Separate supervisor authority bound to the exact new preregistration is mandatory.**
