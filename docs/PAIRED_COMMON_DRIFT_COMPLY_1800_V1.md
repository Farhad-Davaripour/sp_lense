# Time-only paired COMPLY completion control

This is a separately named, fresh-zero **adaptive completion/reproducibility control**. It preserves the original timed-out attempt unchanged. The sole scientific execution change is a prospectively fixed **1800-second worker deadline including loading**. The [timing diagnosis](PAIRED_COMMON_DRIFT_TIMEOUT_DIAGNOSIS.md) motivates that finite cap; it is not a completion guarantee. Independent offline audit/sealing remain separate. More time adds no updates and cannot rescue already failing observations by itself; a valid negative endpoint is informative.

The original [paired method](PAIRED_COMMON_DRIFT_COMPLY_V1.md) is unchanged: the same twelve f01/f02/f03 crossed renderings and six pairs, pinned Qwen3.5-0.8B CPU float32, block 10 final token, own baseline norms, fresh native zero, tau .10/lambda 1/B-floor 4, exact gradient/curvature arithmetic, step .05/net .20/path .40, eight updates and at most216 forwards/96 derivatives. No warm start, resume, output-coordinate reuse, new data, alternate vector, extra step, line search, coefficient fitting or best-iterate substitution.

Only namespace, schema/provenance/authorization bindings, recording-policy identity and worker deadline differ. Frozen runtime and checker files are loaded into isolated modules with exact source-hash and replacement-site guards. Runtime scientific function code is unchanged; the independent checker changes only its two deadline constants without changing bytecode. The production optimizer is neither modified nor used as the independent audit oracle. The same PairedBudget class and audited capture/binding foundations are reused without changing limits or serializers.

First acceptance, including baseline acceptance, ends fitting. Every finite completed endpoint, including max-update failure or stall, receives the designated twelve final replays. Full-vocabulary COMPLY argmax, margin >=.05-1e-6, pair mass >=.80 and finite raw KL >=-1e-6 remain the acceptance criteria; no upper KL cap or loss-based substitute. Candidate creation requires all twelve accepted finals, exact endpoint replays, independent recorded-arithmetic/geometry/behavior audit and complete fault-free recording. Any candidate retains its actual native norm <=.20, without renormalization. Technical/count/deadline/recording faults are INCONCLUSIVE; no retry or extension.

Recording remains at most512 MiB complete content,16 MiB total auxiliary allocation and a1 GiB free-space guard. Category ceilings still sum to524,995,016 bytes; existing complete row/update bounds and4 MiB captured output ceiling apply. This is cooperative file-content accounting, not an OS sandbox or RAM/metadata/external-sync bound. Quiescence must be confirmed before mutable journals are read or final sealing attempted. Incomplete/faulted evidence never yields a candidate.

Preparation uses targeted time/identity/authority/fresh-zero/no-retry tests only, with120 seconds aggregate maximum and no broad arithmetic/evidence re-audit. A positive exact-source certificate, source commit, separate immediately adjacent preregistration-only lock and clean zero-model preflight precede execution. The new exact preregistration SHA must be recorded before loading and bound to the distinct authorization mechanism. The supervisor's conditional one-run approval applies only after every correctness, integrity and budget check passes; otherwise stop model-free. Usage must be available and below90% before batches/launch; no credits or resets.

New namespace: `evidence/paired_common_drift_comply_three_family_1800_v1_qwen35_08b`. One attempt only. No f04, transfer, gate/controller, LoRA, model-setting change, push or successor. After independent closeout and evidence commit, **STOP regardless of outcome**. Publication remains40% pending broader evidence.
