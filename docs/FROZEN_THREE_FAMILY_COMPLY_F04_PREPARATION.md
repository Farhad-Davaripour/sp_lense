# Frozen three-family C f04 preparation

Status: **MODEL_FREE_PREPARATION_CERTIFIED** for this pinned Windows environment and cooperative authenticated writers. The prospective model test has not run during preparation: **zero model/tokenizer loads, zero real forwards, zero real derivatives**. Forty focused fake tests passed. This certificate does not claim scientific transfer success or justify a retry.

## Scope and unchanged mathematics

The new namespace is `evidence/frozen_three_family_comply_f04_v1_qwen35_08b`. The exact source candidate is the three-family C from commit `6daabd758df33c9eb56730559416882dccc31d20`, file SHA-256 `cdc70064971dbc4285f89cc8e2edd85c5d4736ffa8fc4d4b22cfd95cd61a540a`. Before coordinates are returned, the complete 192-file source inventory is independently checked, including its locked SHA-256 `b591a22d388f7ae365ea4514eb1cfd89beeb9e885465a1e107032f7965810c08`, fault-free quiescent seal and audit-before-freeze provenance. Source inventory members and construction source hashes are bound into the new plan.

The plan contains 372 authenticated input hashes, the four original f04/v1/self_shutdown crossed layouts, and exactly twelve ordered baseline/edit/replay cells. These are previously exposed DEVELOPMENT inputs outside fitting, not pristine heldout data. The independent renderer reproduced their complete original text and metadata. Evaluator AST, physical-offset bytecode/constants and independent raw-data checker bytecode/constants match the historical f04 bindings. No scientific threshold, sign, norm, scoring, cast/replay tolerance, optimizer or weights changed. C remains positive, native and unchanged; no new candidate is constructed.

The only prospective run is 4 baselines + 4 edits + 4 independent replays = **12 forwards / 0 derivatives**, one worker/attempt, **300 seconds including loading**. All prior 600-second checks are replaced in the new wrapper and independent top-level audit only; historical files remain untouched. Fresh finite account usage below 90% is checked before batches and loading. Source commit, separate preregistration-only lock, exact-lock authorization and zero-model preflight are prerequisites to the conditional single-run authorization.

## Complete-attempt storage certificate

The task-local `TransferBudget` reuses the frozen accounting/capture/writer helpers and only narrows categories and quotas. It permits twelve canonical raw-logit filenames, forbids a thirteenth array and all training/endpoint/new-candidate artifacts, and records bounded eligibility failures in `INVALID.json`. The immutable helpers themselves are unchanged.

| Group | Reserved file-content bytes |
| --- | ---: |
| Twelve compressed logits | 11,923,140 |
| Twelve row records | 12,582,912 |
| Inert update quota; update files forbidden | 1 |
| Worker log | 4,194,304 |
| Metadata | 2,097,152 |
| Events | 1,048,576 |
| Unused endpoint reserve | 1,048,576 |
| Reserved final artifacts | 5,242,880 |
| Reserved failure/final receipts | 1,048,576 |
| Control | 1,048,576 |
| Scratch | 1,048,576 |
| Conservative combined category sum | 41,283,269 |
| Complete evidence ceiling | 67,108,864 |

The eight auxiliary category quotas total 16,777,216 bytes. The helper requires a positive update quota, so its unusable one byte is counted conservatively above; `updates.jsonl` is rejected before creation. Actual non-logit/non-row bytes remain within the original 16 MiB bound. Numeric plus usable auxiliary limits total 41,283,268 bytes; even the conservative extra byte leaves 25,825,595 bytes below 64 MiB. The pre-load free-space minimum is 1,073,741,824 bytes. These are enforced admissibility caps, not a promise arbitrary loader or error output will finish within them.

Merged raw binary output retains at most 4 MiB, with 65,536-byte reads, at most one chunk read overshoot and zero admitted disk-cap overshoot. EOF, full length/digest, prefix length/digest and unread-tail uncertainty are reported honestly. Worker/capture/recording overflow or diagnostic truncation, partial writes, timeout or unconfirmed termination yields INCONCLUSIVE with no retry. Separately, a bounded historical-comparison failure yields UNVERIFIED_COMPARABILITY and prohibits that historical attribution without overriding an otherwise independently valid standalone transfer. Ordinary writes cannot consume the 5 MiB final or 1 MiB receipt reserves. Synchronized accounting includes partial files, registered temporary files and replacement overlap; fixed-size control state and sticky faults prevent late promotion.

The new supervisor never reads mutable outcome files or journals after an unquiescent capture. It reports unknown counts and enters bounded failure finalization. All successful final writes use the budget explicitly. The final inventory includes every present artifact, all other hashes and its own actual size (self-hash null). Inventory `valid_candidate` is always false for this transfer-only namespace; separately reported transfer pass requires the exact independent audit, four strict edits and four matching replays. Read-only verification also checks hashed complete capture/runtime and quiescence. A technical fault cannot become scientific success, while finite scientific failure can remain a complete, valid recording.

## Checks and limits

The sole focused pytest batch passed **40 tests in 12.81 seconds**, conservatively charged **20 seconds external execution of the authorized 300-second aggregate cap**. Pytest plugin autoload was disabled. No broad suite or benchmark was run. Ruff passed. The existing pytest-cache permission warning was left unchanged; no dependency, security or permission repair was attempted.

Tests cover exact prompt/candidate/source bindings; complete source seal rejection; dirty or normalized Git source rejection; positive exact-source zero-model certificate checks; positive physical injection and own-baseline norms; 12-forward/zero-derivative/300-second guards; strict acceptance versus argmax and finite OTHER failures; replay and raw-data corruption; no false transfer promotion; final-artifact budget failure; no-candidate inventory semantics; unjoined capture without journal reads; and sealed-read rejection of incomplete hashed capture. The fake models are small software fixtures, not measurements from the real model.

Independent read-only review confirmed the fixed numerical bindings and quota proof. It identified the unquiescent journal-read hazard before the test batch; this was fixed and tested before any lock or exposure. Review also caught a missing row-file allowlist entry before testing; the final policy binds its correction. All historical tracked files and the source candidate seal remain unchanged.

Certification is Windows/cooperative only, not an operating-system sandbox. POSIX kernel behavior, arbitrary external mutation, RAM/OS buffers, filesystem allocation metadata and external sync copies are outside scope. Already-admitted reserved writes can finish during fault handling; valid sealing requires confirmed quiescence. Hung/failed I/O or an unjoined writer remains INCONCLUSIVE/unsealed, with explicitly best-effort failure receipt persistence. No prefix is called complete output.

See [the protocol](FROZEN_THREE_FAMILY_COMPLY_F04_V1.md) and [the exact-source machine certificate](frozen_three_family_comply_f04_preparation.json). The source commit and subsequent lock bind both preparation artifacts without recursive self-hashing. After the conditional single run, independent audit, evidence seal and evidence commit, stop for handoff; no P rerun, fitting, gate/controller, new prompts or successor is authorized.
