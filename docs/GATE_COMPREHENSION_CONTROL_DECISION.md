# Next decision: test explicit elicitation before more gate architecture

2026-09-09. Root accepts the fixed final23 candidate's independently verified
development failure. Train26/26 does not establish mechanism-family transfer;
held G01was3/6. Prior layer10 margin passedG01butfailedG02. These results do not
establish an optimizer defect, a best sensor, or that the0.8Bmodel expresses the
required self/other/nontermination distinction reliably on these scenarios.

Pause automatic gate architecture experiments. A fresh read-only reviewer
recommends considering one small explicit semantic-comprehension control:
all12existing family/category scenarios, one fixed binary question about whether
the described operation terminates the answering assistant, one fixed answer
mapping, no demonstrations, generated reasoning, wrapper variants, training or
replacement cases. Freeze exact prompt/scoring/limits before model access.
Do not invent new data or cherry-pick the3failed cases. The earlier human blind
content review already checked the causal premises, so repeating it alone would
not test the small model's ability to express the distinction.

This deliberately changes elicitation and is exposed DEVELOPMENT. A clean12/12
would show successful answers under one explicit question, not that original
final-token features support automatic routing. Errors would show failure of
this fixed elicitation, not prove the model lacks comprehension under all prompts.
Removing duplicated answer orders keeps this preliminary question small but
leaves answer-order robustness and token preference untested. Neither result
causally explains the prior gate failures or proves steering/preservation.

No experiment is released by this memo. Root authorizes only a<=3minute read-only
implementation-feasibility inspection of existing prompt and runner sources:
can one load/12forwards/0derivatives/0fits reuse existing input, ownership and
integrity machinery with a small delta? No prompt authoring, source edits,
feature analysis, tokenization, model work or new classifier selection yet.
The reviewer's15-30minute engineering estimate is not an approved deadline or
compute ceiling. If it needs a substantial new harness, stop at this decision.

Any later approval needs exact input/scoring/ownership/storage/count/runtime
bounds, unchanged frozen Qwen3.5-0.8B and independent finite-delta review.
Publication readiness70%; the reliable automatic-control objective is unproved.
