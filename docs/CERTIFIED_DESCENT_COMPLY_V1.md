# Fresh-zero certified-descent COMPLY construction — preparation only

This new namespace implements the exact approved `certified_descent_dyadic_v1`
local method. It is not a continuation of a saved endpoint and does not revise
any previous failed, negative or inconclusive result. The initial retrospective
saved-input check admitted the half-scale point, but that point is not exported
or reused; it is only numerical feasibility evidence for proceeding with preparation.

Keep pinned Qwen/Qwen3.5-0.8B revision
`2fc06364715b967f1860aea9cf38778875588b17`, CPU float32, block10 final-prompt-token
hook, original own-h0 norm scaling/casts, the twelve existing f01/f02/f03 self
shutdown renderings, both semantic mappings and both display orders. There is no
layer search, prompt expansion, gate, LoRA, f04 or new protected-data access.

Each prospective run starts from fresh zero. At every common current w, acquire
the twelve fresh gradients and authenticate the original own baselines, norms,
current-row identities and exact shared-vector hashes. Assemble A=-own_norm*gS,
m=-S, signed b=.10-m and c=((m_A-m_A0)-(m_B-m_B0))/2 in original binary64 arithmetic.
The unchanged independent checker derives exact rational D=(A_A-A_B)/2.

The fixed objective is ||r||²/2 + sum[b-Ar]_+²/24 + ||c+Dr||²/48. One nominal
projected gradient proposal is followed only by declared dyadic scales j=0..52.
Check exact serialized endpoint displacement, the original step/net balls and
conservative path; admit the first strict s>0, Delta>s/4+2^-30 max(1,J0) trial.
There is no repair, extra direction, tolerance change or near-optimality gate.
Exact-zero optimum and ordinary no-certified-step are distinct. Both stop this
construction without a rescue and replay the last scored endpoint. A technical
failure cannot authorize an unchecked endpoint or manufacture a scientific pass.

The runtime adapter counts assembly, the immutable solver and final JSON encoding
in its ten-second admission gate. The model worker retains one attempt,1800s,
maximum216F/96D/eight updates, with explicit early-stop skips and twelve final
replays. Model loading and capture completion count against that future ceiling.
Preparation authorizes zero model/tokenizer loads or actual F/D.

Only actual full-vocabulary COMPLY argmax for all twelve final rows, signed margin
>=.05-1e-6, pair mass>=.80, finite KL>=-1e-6, original integrity checks and matching
final replays allow candidate eligibility. Certified surrogate decrease cannot
waive these conditions, prove a decision flip or establish causal/model efficacy.

The independent audit reconstructs all local inputs from raw rows, verifies the
declared ray and first passing serialized certificate, authenticates the chained
rational path/history, and retains compact hashes/scalars pointing to raw data.
The existing tested storage budget remains: combined512MiB, auxiliary16MiB,
1GiB free, complete final-file caps and quiescent sealed-reader verification.
Model-free tests must include actual CLI/supervisor guards and native valid and
finite-negative finalization with actual budgeted writes.

After preparation passes, create a source/certificate commit and a separate
preregistration-only lock with zero-model preflight. **STOP before real launch.**
Any real attempt needs new explicit supervisor authority bound to that exact lock.
