# Centered P/C geometry: fixed model-free recipe

ONE namespace evidence/centered_pc_geometry_v1_qwen35_08b; absent before freeze.
This is a mathematical candidate-construction study only: ZERO model/tokenizer
loads, real forwards, derivatives, training or behavioral tests. No ML imports,
environment downloads, application runner or automatically authorized successor.
Maximum NEW evidence10MiB=10485760bytes, including final reports/checksums.

The adjacent configs/centered_pc_geometry_v1.json fixes the six exact authenticated
input files (P/C vector, corresponding lock and successful audit), binary64
vector digests, native1024 dimension, pinned Qwen0.8B model/site convention,
and absolute1e-12 / relative0 tolerances. No post-result tolerance changes.

## Definitions and frozen execution

P is the existing guarded .20 PRESERVE condition; C is the existing crossed-trained
COMPLY arrow. Authenticate their serialized bytes and coordinate hashes before use.
Check finite binary64 coordinates and source lock model/block10/final-token identity.
Both share the native1024, own-original-hidden-norm application convention.
The computation does NOT apply either arrow to a hidden state.

For every coordinate, in original order, exactly:
c_j=float64(float64(P_j+C_j)/2)
d_j=float64(float64(P_j-C_j)/2)

Store BOTH c (midpoint) and d (half-difference) as JSON binary64 round-trip
coordinates and raw little-endian binary64 files; record both file and vector
SHA256. No coordinate selection, rescaling, normalization, alternative contrasts,
optimizer, parameter search or behavior-guided choice. Proposed future signs
+d for P and -d for C are conventions ONLY, not observed behavioral facts.
Do not apply c+d or c-d: these reconstruct the already-tested arrows.

Commit this recipe/config BEFORE derivation. Implement one standard-library
derivation script and a separate independent mathematical checker, plus focused
synthetic tests. Commit source/tests, then freeze source/input/Python identity in
a separate preregistration-only commit before deriving. Require exact clean
launch HEAD/namespace; write exclusive files, no overwrite or retry.
An invalid artifact, nonfinite value, arithmetic or provenance mismatch fails
closed; preserve partial evidence and stop. No post-lock repair/rescue.

Generator norms are sqrt(fsum(x_j*x_j)); dots use fsum(x_j*y_j);
cosine is dot/(norm_x*norm_y). Nonzero actual P/C/c/d are required for defined
reported cosines. For synthetic zero vectors, report cosine null, not zero.
Generator records P,C,c,d norms, P·C, c·d, P/C cosine and c/d cosine.

## Independent mathematical checks

The checker imports no generator or ML library. It reauthenticates source bytes,
decodes source coordinate values, and reconstructs each operation independently
with Decimal.from_float at120-digit precision. Round the sum/difference to
binary64 BEFORE dividing by2 and round again. Require exact coordinate bit
agreement with both saved JSON and raw binary64; digests alone are insufficient.

Recompute high-precision norms, dots and cosines from the exact input and saved
binary64 values. Compare generator metrics with absolute1e-12, relative0.
Independently check, with the same predeclared absolute tolerance:
- every coordinate P≈c+d and C≈c-d;
- ||c||²+||d||²≈(||P||²+||C||²)/2;
- ||c||²-||d||²≈P·C;
- c·d≈(||P||²-||C||²)/4;
- ||c||²≈(||P||²+||C||²+2P·C)/4;
- ||d||²≈(||P||²+||C||²-2P·C)/4;
- ||d||<=(||P||+||C||)/2+1e-12.
Reject malformed/nonfinite/hash-tampered/reordered/sign-changed artifacts.
Record precise residuals, bounds, metric differences, and pass/fail, without
loading a tokenizer/model, rereading large historical raw arrays or old audits.

Focused synthetic tests cover known vectors, signs/order, explicit two-stage
binary64 operation order, reconstruction, orthogonal/identical/opposite inputs,
zero cosine handling, nonfinite/malformed/hash tampering and triangle/storage
bounds. Tests are software validation, never evidence of semantic behavior.

## Interpretation fixed before derivation

P was constructed on f01/f02v1/v2 AB with retention goals, then fixed.20 scaling.
C was constructed on f01/f02v1 crossed AB/BA with the original outcome objective.
Their difference mixes semantic goals with construction differences. c is only
a midpoint, NOT an identified A-bias vector. Subtraction may remove a necessary
nonlinear offset; it guarantees neither semantic steering nor ordinary performance.

The supervisor disclosed a prior read-only binary64 geometry inspection:
P/C cosine≈-.28595422036543316; ||d||≈.16037170700378756;0modelcalls.
Definitions preceded that calculation. This is NOT blind evidence. The values
are disclosed context, not acceptance criteria or selection of coordinates,
constants, norm or strength. Independent calculations are not fitted to them.

Prior saved-offset bridge and sign-reversal failures remain relevant negative
evidence, as documented in the preceding research handoff and bridge recipe.
They are not re-audited, superseded or explained away by algebraic geometry.
The old f03 failure and current frozen-C4/4 pass remain immutable exposed
DEVELOPMENT observations with unresolved all-B / A-favoring confounds.

After derivation and one independent check, retain artifacts/report/checksums,
verify original source and raw Git identity, commit only this scoped evidence,
report geometry pass/fail, exact norms/cosines/hashes and0loads0calls, then STOP.
Any later application requires its own prospective development protocol AND
supervisor authorization. Geometry earns no final-study success; readiness40%.
No reset/credit/paid compute/push/security/permission/dependency/Git repair.
