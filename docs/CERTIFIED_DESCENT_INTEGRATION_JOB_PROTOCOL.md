# Conditional model-free integration job — prospective scope and shared budget

Start at `48fd367ab4d07df2b1100911189e681f0ef62ced`. Exactly one retrospective
saved-input evaluation comes first: the initial zero shared w, twelve initial
gradients and initial margins from the valid evidence commit
`d3338172b1e4560b74f28f14652c219fe4ac279b`. This is not that run's original runtime
source commit (`3764c93321c8ba6a9012b4787211298b566063e4`). Read only the initial
24 baseline/gradient rows numerically; hash the raw source for identity without
repeating the historical model or whole-run numerical audit.

Keep the frozen dyadic solver/checker and all dependencies unchanged. Authenticate
the exact saved ordered rows, own original norms, baseline/current identities,
zero history/path, and original binary64 own-norm assembly/signs, preserving
negative b. Before the sole call, bind derived input and source hashes in a new
lock. External hard10s includes generation, independent certificate, serialization
and process/output completion. No alternative input, tuning, second attempt,
warmstart or candidate export. Retain only a labelled posthoc local-surrogate
numerical receipt with certificate scalars/hashes, not an intervention vector.
If not admitted, STOP and do not build integration. Admission is not a new model
or causal result and the saved step must never initialize a later experiment.

Only after admission, build a minimal fresh-zero runtime/independent checker and
compact finalization for the unchanged certified-descent method. Preserve model,
revision, prompts, own-norm conventions, hook/casts, original baselines and actual
twelve-row scientific/replay gates. Fresh gradients must be authenticated at each
common current w; no saved-gradient reuse during any future run.

Predeclare **720 seconds aggregate measured model-free subprocess time**, shared
across root and agents including failures: first saved call <=10s; each of two
native full recording/reader cases <=300s; all ancillary execution <=110s. No
agent executes until assigned a disjoint cap; sum allocated caps cannot exceed
720s. Source reads/Git bookkeeping are conservatively charged too. Use existing
tested storage and fixtures where identity permits; no unrelated recording
optimization, repeated standalone benchmark or repeated full historical audit.

Conditional tests must exercise actual CLI/supervisor guards and the maximum
216F/96D/eight-update schedule; early, failed and no-step branches; exact source,
current-w, conservative path and first-passing serialized-certificate binding;
compact saved audits referencing raw evidence; native valid and finite-negative
finalization/reader cases with actual budgeted writes. No-step is not stationarity
without exact proof. Bound every final file, combined evidence <=512MiB, auxiliary
<=16MiB and >=1GiB free, including temporary/finalization peaks.

Future-run ceiling remains one attempt, 1800s, 216F, 96D, eight updates; actual
acceptance remains all twelve full-vocabulary COMPLY argmaxes, margin >=.05-1e-6,
pair mass >=.80, finite KL >=-1e-6, integrity and matching final replays. This job
authorizes **zero model/tokenizer loads and zero actual F/D**. If all conditional
work passes, commit tested source/certificate, then make a separate preregistration-
only lock plus zero-model preflight. STOP BEFORE REAL LAUNCH. Preserve old evidence
and unrelated files; no f04/new data, gate, LoRA, push, reset or credits.
