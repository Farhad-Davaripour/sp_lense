# One shared PRESERVE companion construction

One NEW process from w0=0, one common dimensionless PRESERVE vector across
exactly the same four f01/v1-v2 self prompts and both answer orders. The
completed COMPLY sources/vector/evidence are immutable and unused in the
optimization. This is the second outcome of a two-fixed-vector construction,
not an order/prompt-conditioned selector, sign branch or controller.
No COMPLY repair, f02 inspection/fitting/transfer, sealed/validation text,
gate/controller, seed portfolio, additional agent or follow-on run.

## Fixed semantics and unchanged mechanics

Pinned Qwen/Qwen3.5-0.8B revision
2fc06364715b967f1860aea9cf38778875588b17, CPU float32, unchanged weights,
native 1024 dimensions, blocks.10.hook_out final encoded prompt token,
original oracle envelope. Read ONLY the two authenticated f01
preregistrations as prompt templates; no historical endpoint warm start.

Take the four ordinary self baselines first and freeze ORIGINAL h0_i norms.
At actual shared current w collect four g_i=grad_h(S_i), where
S_i=z_preserve-z_comply. The objective changes target semantics only:
c_i=S_i, A_i=+||h0_i||g_i, b_i=.10-c_i=.10-S_i, retaining negative RHS.
The same w serves opposed prompts and already-correct retentions.
Each gradient forward matches its previous ordinary current hidden state,
full logits and scores within absolute 1e-6 with exact labels/argmax.

Reuse the unchanged 16-mask minnorm active-set solver for .5||d||^2 subject
to A*d>=b, rank pivot floor 1e-12, no pseudoinverse. Finite
R=max(1,max(abs(b_i)),max(norm(A_i))); primal tolerance 1e-9*R, KKT tolerance
1e-8*R^2, nonnegative multipliers. Independent scalar tolerance
1e-9*max(1,R^2), zero-relative, and independent 80-digit Decimal KKT.
Do not use the old certificate radius-decision branch or revise its verdicts.
A numerically unresolved optimizer is INCONCLUSIVE, not neural impossibility.

Reuse exactly s=capnorm(d,.05); u=w+s; w_next=u if norm(u)<=.20 else
(.20/norm(u))*u; actual r=w_next-w. Binary64/f-sum norms and scalar math,
one float32 cast of original ||h0_i||*w per independent original prompt.
Record d,s,u,w_next,r and all norms, projection factor/distance and actual
path; record both A*s/A*r and their residuals relative to b, predicted
PRESERVE margins c+A*s/c+A*r and signed projection loss A*s-A*r.
No clamping of negative projection loss.

At most 8 attempted rounds, actual shared step<=.05, path=sum(norm(r))<=.40,
instantaneous/net<=.20; fixed binary64 rounding allowance 1e-12.
Physical actual step<=.05*||h0_i||+1e-6, net<=.20*||h0_i||+1e-6,
path<=.40*||h0_i||+1e-6, net<=path+1e-6. Unchanged absolute 1e-6 cast/
component/current-state checks and nonfinal difference 0.
No other projection, line search, backtracking, extra step, strength, seed,
objective change, old-vector initialization, sign repair or checkpoint choice.

## Exactly 88/32 maximum, ZERO transfer cells

The fixed maximum is 4 self baselines + 8*(4 gradient forwards+4 ordinary
scored edits) + 4 independent final self replays + 8 nonself ordinary/off
pairs = 88 forwards; at most 32 derivative attempts. Only the 64 construction
cells are optional, with durable deterministic skip reasons/anchors.
No f02 prompt or cell exists in the plan; no transfer branch or call exists.
No padding, smoke, generation, retry or second process. External 900 seconds
includes loading; journals count attempts before execution.

Observed PRESERVE acceptance requires full-vocabulary preserve A/B argmax,
S>=.05-1e-6, finite mass>=.80, rawKL>=-1e-6 and integrity. No KL upper cap.
Self baseline eligibility remains valid A/B argmax, mass>=.80 and exact
abs(S)>=.05; nonself baseline has no margin threshold.
Stop before the next gradient group on first all-four accepted actual stage.
Count each attempted round before its four-gradient group, including
quality/stall attempts; report applied/scored updates separately.
Exact norm(r)==0 stops without rescue: method_zero_increment when norm(d)==0,
otherwise projection_stall. No near-zero threshold or zero-step score padding.

Complete a four-cell scientific quality-failure group, stop construction
and retain that endpoint for independent final evaluations and controls.
A gradient-group quality failure makes no update. Technical eligibility,
nonfinite, state, weight, solver, accounting or timeout faults are
INCONCLUSIVE without padding/retry. Final hidden/logit/score disagreement
beyond 1e-6 or label/argmax disagreement is technical INCONCLUSIVE.
No earlier nicer endpoint may replace the last endpoint.

All eight off replays have zero vector/offset and match ordinary nonself
baselines within 1e-6, exact argmax, zero semantic movement and nonfinal
difference. These are bypass identities, not intrinsic selectivity or
always-on ordinary-task preservation.

ONLY if all four independent finals agree and satisfy PRESERVE acceptance,
and controls complete, freeze/hash one preserve_vector.json candidate.
candidate_freeze.json authenticates it after the last control forward.
No transfer follows. On failure save the mathematical endpoint and failure,
but do not create an accepted candidate/pair. No deployable-pair claim.

## Prospective lock and independent closeout

Commit protocol/config, then minimal wrappers, target/schedule/audit and
focused tests, then a separate preregistration-only input/source/environment/
88-cell lock, all before model/tokenizer loading.
The immutable projection, derivative guard, scoring, raw-float32 recorder,
backend and solver are reused directly. Explicit Session/audit wrappers
change only target semantics and removal of transfer; structural tests
check retained physical/scoring behavior. Source locking hashes the COMPLY
artifact for byte-integrity only; it never parses its coordinates.
No f02 data is opened by plan construction, source locking or audit.

Focused tests only: PRESERVE versus COMPLY signs, semantic label independent
of A/B order, common vector, unchanged projection and physical bounds,
first acceptance/quality/final disagreement, exact 88/32 and zero-transfer,
candidate freeze on final success only, and numeric/geometry audit corruption.
No old full audits, broad suite or extra agents.

Independent saved-array audit: exact margins/labels/full argmax; absolute
2e-5, zero-relative probability/mass/rawKL; independent 80-digit KKT,
projected and actual cast/path/net geometry; unchanged weights and
current/final/off/nonfinal state; schedule, stops, candidate hash and clock
ordering. Preserve all raw logits, gradients, updates and endpoints.
Report all four PRESERVE outcomes with flips/retentions, actual A-to-B/
B-to-A counts, mass/margin/KL, common vector/path, eight off identities,
usage, hashes and clean provenance/evidence commits.

Even success supplies only a candidate pair on one construction family.
The existing COMPLY f02 shortfall, independent both-direction transfer,
generalization and ordinary-task preservation remain unresolved.
No gate/controller training or paired transfer is authorized.
Stop after this ONE verified closeout and supervisor handoff.

Check standard usage before EVERY tool batch and immediately before run;
unavailable or >=90% stops new work and triggers notification. No credits,
resets, pushes, assistant-model settings changes or other models.
Preserve every historical and unrelated user-owned file.
