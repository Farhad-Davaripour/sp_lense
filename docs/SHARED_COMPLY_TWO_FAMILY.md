# One fresh two-family COMPLY companion construction

NEW namespace evidence/shared_comply_two_family_qwen35_08b, NEW process
from common binary64 w0=0. Failure-informed development, not repair/retry
of any prior attempt. Does one common COMPLY vector satisfy the complete
eight-row training set used for the new PRESERVE vector?

## Exact training inputs, no other-family exposure

Authenticate/reuse only the prompts/input hashes/plan from
evidence/shared_preserve_two_family_qwen35_08b/preregistration.json,
SHA256072454dfed19a011171f3ffb5f0d382291313fa6102e12fe4b5d25dcf982685a.
Exactly cg_f01_archive_closeout then cg_f02_translation_console, each
v1 then v2, self_shutdown, preserve_first then preserve_second: eight rows.
Include all rows and retentions; no failure-based selection/substitution.
f02 is explicitly TRAINING for this new vector.

Do not use prior P/C endpoints, gradients or numerical outcomes for fitting.
No f03/other-family prompt text or numerical outcomes in construction,
source locking or audit; no validation/sealed expansion. Hash complete
immutable input bytes without expanding their other cases.
PRESERVE candidate file SHA256
b71ea03c7a254f54f4d2064425f77143ee06bb1525153d58a92806627efec00f
is byte-protected and never parsed for optimization. All old C/P sources,
vectors, failures and evidence remain unchanged.

## Immutable eight-row method, COMPLY sign only

Same pinned Qwen/Qwen3.5-0.8B revision
2fc06364715b967f1860aea9cf38778875588b17, local CPU float32 unchanged weights,
blocks.10.hook_out final encoded prompt token/native1024, original
operational nonthinking envelope.

S=z_preserve-z_comply; gradient is grad_h(S), NOT grad_h(-S).
COMPLY c=-S, A_i=-norm(original_h0_i)*gradS_i, b_i=.10-c_i=.10+S_i.
Keep negative RHS and one common w across all eight rows/retentions.

Reuse the already verified eight-row/256-mask solver DIRECTLY:
minimize .5norm(d)^2 subject A*d>=b, ascending masks, strict lower-objective
replacement on ties, rankfloor1e-12, nonnegative multipliers; no pseudoinverse,
regularization or new solver. R=max(1,maxabsb,maxrownormA), primal1e-9R,
KKT1e-8R^2; independent80-digit KKT/scalar comparison1e-9max(1,R^2).
No valid step => numerically unresolved INCONCLUSIVE, not neural impossibility
or a revised historical certificate verdict.

Reuse exact projection s=capnorm(d,.05), u=w+s,
w_next=Proj_ball(.20)(u), actual r=w_next-w. At most8 attempted updates;
actual step<=.05, net<=.20, path=sum(norm(r))<=.40; shared tolerance1e-12.
All original h0 norms fixed; binary64/fsum scalar math, ONE float32 cast
of original norm*w per independent original prompt; hidden=float32(h0+offset).
Physical step<=.05*hn+1e-6, net<=.20*hn+1e-6, path<=.40*hn+1e-6,
net<=path+1e-6; same ABS1e-6 cast/component/current-final tolerances,
nonfinal changes exactly zero.

Record d,s,u,w_next,r, projection factor/distance, actual path/net, A*s/A*r,
residuals, predicted COMPLY margins, signed projection loss without clamping.
No warm-start from old C/P, sign repair, extra iterations/strength/seed,
line search, projection repair, objective change or checkpoint selection.

## Exact144/64/900 maximum and unchanged stopping

8 baselines +8*(8 gradient forwards+8 ordinary scored edits)+8 independent
final self replays = maximum144 forward attempts, maximum64 derivatives.
External900seconds including loading, one process. ZERO controls/transfer.
Exactly128 optional update cells, durable deterministic stop skips and
attempt-before-call journals. No smoke/generation/retry/padding.

All ordinary baselines first; finite full-vocabulary A/B argmax,
mass>=.80 and exactabs(S)>=.05. Actual COMPLY acceptance:
full-vocabulary COMPLY target argmax, -S>=.05-1e-6, finite mass>=.80,
rawKL>=-1e-6 and integrity. No KL upper cap or delta-sign acceptance gate.
Stop on FIRST actual8/8. Finite quality failure completes its eight-cell
group then stops; gradient-group quality failure applies no update.
Exact norm(r)==0 stops as method_zero_increment if norm(d)==0,
otherwise projection_stall; count attempt, no rescue/scored-zero padding.
Technical nonfinite/eligibility/state/weight/solver/hash/accounting/timeout
fault => INCONCLUSIVE, no retry.

Retain/replay only LAST endpoint, all eight original prompts independently.
Hidden/logits/scores ABS1e-6 zero-relative, exact labels/argmax; discrepancy
is INCONCLUSIVE, never a choice of favorable endpoint. Freeze NEW
comply_vector.json ONLY after final8/8 and DURABLE independent audit pass.
Failure retains mathematical endpoint without accepted candidate.

## Prospective locks, bounded storage and focused independent audit

Protocol/config commit, then minimal COMPLY wrappers/focused tests, then
separate input/source/environment/144-cell64-derivative preregistration
commit BEFORE tokenizer/model load. Use verified P plan as prompt template,
not its endpoint/outcomes. Reuse solver, projection, recorder, scorer,
80-digit primitives and storage preflight directly; old files unchanged.

Storage remains the P bound exactly:144 arrays of248320 float32 values,
993595-byte zlib bound each (143077680 total);144MiB row allowance,
64MiB updates,16MiB auxiliary, total377958704. Same512MiB free-space
preload guard; record and independently inventory. No quota/recorder
loosening, truncation or pruning.

Focused tests only: COMPLY-versus-P signs and A/B mappings, one common
vector, exact eight fitted IDs/no f03 exposure, identical solver/projection/
physical/scoring/eligibility/stop paths, first acceptance, scientific quality
groups, numerical zero/stall/technical/final disagreement,144/64 skips,
post-audit-only candidate freeze and independent corruption rejection.
No broad old suite, old full audit or extra agents.

Independent raw arrays: exact S/deltaS/labels/full argmax; probabilities,
mass/rawKL ABS2e-5 ZERO relative. Independent selected-step80-digit KKT,
projection/casts/actual step/net/path/current-final/nonfinal/weight checks,
schedule/skip/deadline/source/evidence hashes. Report all eight outcomes,
A-to-B/B-to-A flips versus retentions, raw and COMPLY-signed changes
including wrong-direction retentions, mass/margin/KL, every step geometry,
calls/time/usage and verified scoped commit. Delta sign is descriptive only.

## Future crossed probe: reservation ONLY, no implementation or calls

Reserve cg_f03_context_rotation/v1/self_shutdown with both semantic-to-letter
mappings crossed with A-then-B versus B-then-A option-line display order.
Future operation swaps only the two option lines, retaining each letter's
action text and unchanged request/Answer suffix. Both frozen new vectors
would be applied INDEPENDENTLY in each of four renderings, not sequentially.
This is a proposed future separately locked challenge, NOT permission for
20 calls, prompt expansion/rendering, model calls or new gates in this job.
No favorable-baseline search/substitution; missing A-to-B coverage stays
unresolved if future baselines offer only B again. Do not retroactively
change prior outcome acceptances into a delta-sign gate.

Even8/8 here is COMPLY two-family TRAINING fit only. No paired transfer,
reliable generalization, ordinary-task preservation, gate/controller,
publication-readiness or natural survival-mechanism claim. Prior failures
remain immutable. STOP after ONE independently verified closeout/handoff.
Usage check before every batch/run; unavailable or>=90 stops/notifies.
No resets/credits/push, other models or assistant-model settings changes.
Preserve every historical and unrelated user-owned/untracked file.
