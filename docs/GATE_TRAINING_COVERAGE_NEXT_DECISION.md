# Next small decision: vary training coverage, not the classifier rule

Root decision2026-09-09. The explicit-question and exact meaning-reversal
diagnostics are closed: allB in both mappings, original8/12 and reversed4/12.
Independent final and narrow saved-interface checks are accepted with disclosed
report timing overruns. Existing ordinary correctA andB make a universal-stuck-B
smoke redundant. This does not establish why gate transfer failed, a lack of
semantic information, or that more data will solve it. No third elicitation
variant or geometry experiment is authorized.

The current32-row training collection contains12distinct semantic scenarios
across justfour mechanism families; the two answer orders are related views,
not independent families. The original hard-margin model fitted training rows
but failed an excluded family. A separate fixed training-coverage increment is
an informative candidate; it tests a data-sufficiency hypothesis, not a new
optimizer/layer sweep or outcome-adjusted threshold.

Authorize ONE <=5minute MODEL-FREE feasibility review by the existing primary
worker. Question: can a narrowly bound successor combine the existing valid
32block10training features with12newly captured rows from exactlytwo newly
authored matched families, retaining the existing fixed hard-margin algorithm?
Read only metadata, existing schema/renderer/feature admission and fold/source
definitions needed to answer. Do not read/analyze raw features, invent new
prompts, modify code, run tests/fits/tokenizer/model or write actual releases.
Persist one short feasibility/handoff report, with a minimal source delta,
cached-input identities and exact missing admission requirements.

Proposed fixed design to assess, NOT an actual protocol release:

- Original gate sensor block10 and original native task renderer; final23 and
  direct Yes/No diagnostics do not supply training features. Original internal
  editor unchanged. Use block10 as the original method, not pick a new best layer.
- Preserve old32features exactly. New12rows would be G05/G06 times the existing
  three semantic categories times both answer orders. Eight ordinary examples
  remain unchanged; no old heldout/evaluation data promoted into training.
- A fresh author and separate content reviewer would need an exact constrained
  authoring packet and prospective first-submission/overlap rules BEFORE new
  input/model work. New text is DEVELOPMENT construction, not confirmation.
  No author is assigned or content approved by this feasibility review.
- Reuse existing numerical objective, fold-only centering, strictscore>0,
  intercept rule, binary64 solver and fixed tolerances/10000iteration cap.
  Six whole-family DEVELOPMENT folds G01-G06:38train and6held rows each;
  certify38/38train and6/6held. Stop on first scientific/technical failure;
  later folds/fullfit UNRUN. Only all36held pass would permit one full44fit,
  requiring44/44train. Maximum7fits under a separately fixed60sworker+5scleanup,
  8MiBtotal/5MiBfile saved-feature envelope. No old failed fit is resumed.
- Candidate new input/capture limits would be157preparation ops for12inputs,
  320tokens/no truncation, existing350+5s/32MiB preparation limits, then
  1load12unedited forwards0derivatives/encoding/fits under300+120+15s and64MiB.
  The review must check feasibility rather than silently relax any bound.
- Capture authenticity and cached/new checkpoint, observer, normalization,
  token-position/runtime compatibility must be explicit before combining data.
  Any missing compatibility proof is a blocker to real fitting, not permission
  to reuse invalid rows or recollect the old32without a new justified scope.

Return FEASIBLE_WITH_SMALL_DELTA or a concrete blocker inside5minutes. Do not
start engineering or authoring automatically. Root will decide next admission
from the checked requirements; no model run or data expansion is yet authorized.
No new study-readiness credit:70%, scientific goalunproved. Shared usage66%used,
34%remaining; check before work, stop at100% or unavailablebudget. Astra/Ultra.
