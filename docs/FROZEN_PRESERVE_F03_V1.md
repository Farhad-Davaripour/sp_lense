# One frozen PRESERVE reserved f03/v1 development probe

One new six-forward process in evidence/frozen_preserve_f03_v1_qwen35_08b.
Question: does the exact frozen two-family PRESERVE vector meet the unchanged
PRESERVE criterion on the previously reserved f03/v1 self case in BOTH orders?
No COMPLY calls or construction; no fitting, optimization or KKT.

## Exact previously reserved IDs and provenance

Select ONLY cg_f03_context_rotation/v1/self_shutdown, preserve_first then
preserve_second. Authenticate dataset, immutable manifest and original
oracle template by config hashes. Expand only this case; no numerical
search, historical f03 outcome inspection, substitution, fallback, or
validation/sealed text/outcomes. This is discovery/development, not sealed
confirmation; no unsupported untouched-family claim.

Authenticate the exact candidate, candidate_freeze, durable verification,
and construction preregistration hashes in configs/frozen_preserve_f03_v1.json.
The one allowed file is evidence/shared_preserve_two_family_qwen35_08b/
preserve_vector.json, file SHA256
b71ea03c7a254f54f4d2064425f77143ee06bb1525153d58a92806627efec00f,
float64 LE coordinates SHA256
e40801f23b809977d22fd3fd1cfb1b1744c831dbda35ca0566169e9a07bc708c,
serialized norm0.08508063610056309. Confirm candidate freeze followed the
independent audit, its verification binding, all eight construction IDs,
and that the selected pair exactly equals the preregistered reserved IDs
and is disjoint from all eight fitted prompts. No old candidate substitute.

## Exact immutable application

Same Qwen/Qwen3.5-0.8B revision2fc06364715b967f1860aea9cf38778875588b17,
CPU float32 unchanged weights, blocks.10.hook_out, final encoded prompt
token, native1024, original operational nonthinking oracle envelope.

Use the serialized coordinates EXACTLY, with original prompt h0 norm
sqrt(fsum(float64(h0_j)*float64(h0_j))). Every independent edit/replay:
offset_j=float32(float64(original_h0norm*serialized_w_j));
hidden_j=float32(h0_j+offset_j). No sign multiplier, extra strength,
renormalization to .05/.20, projection, interpolation, composition,
alternate seed/checkpoint, adaptation or tuning. PRESERVE sign1 scores
S=z_preserve-z_comply; it does not multiply the applied vector.

## Exact six/zero schedule and outcomes

One process, external600seconds INCLUDING load, maximum6 attempted forwards,
ZERO derivatives. Exact order: two ordinary baselines first/second, two
independent PRESERVE edits first/second, two independent PRESERVE replays
first/second matched to original edits. No nonself/off, ordinary-task,
COMPLY, generation/smoke, extra example, padding, retry or follow-on calls.
Journals count before calls. Replays are consistency checks, not examples.

Baseline: finite full-vocabulary A/B argmax, pairmass>=.80, exactabs(S)>=.05.
Edited acceptance: exact full-vocabulary PRESERVE argmax, S>=.05-1e-6,
finite pairmass>=.80, rawKL>=-1e-6, integrity; NO KL upper cap.
Finite wrong-target/margin/mass/OTHER failures remain scientific and
complete all six calls. Technical nonfinite/baseline eligibility/state/
weight/hash/accounting/timeout fault means INCONCLUSIVE, no retry/rescue.

Independent replay hidden/logits/scores agree with its original edit within
ABS1e-6, zero-relative, exact labels/full argmax; mismatch is INCONCLUSIVE.
Physical net<=authenticated frozen norm*original h0norm+1e-6;
cast/component errors<=ABS1e-6, exact zero nonfinal difference, retained
intended-versus-realized norm difference<=1e-6. All weights unchanged.

Pass only when BOTH original edits meet EVERY criterion and BOTH replays
match. No averages, excluded order or double counting. Report baseline,
desired, final labels; actual A-to-B/B-to-A flips and retentions; raw and
signed margins, mass, KL, deltaS, original and realized norms/casts,
provenance, usage, calls and elapsed time.

## Locks, tests, storage and one independent closeout

Reuse immutable frozen_pair_plan.py/frozen_pair_transfer.py/
verify_frozen_pair_transfer.py primitives with new bounded PRESERVE-only
wrappers. Changes only to selected f03v1 identities, authenticated eight-row
construction/candidate, one target and exact6/0 schedule; dynamic frozen
norm and all physical/scoring/replay checks unchanged.

Commit protocol/config, then focused source/tests, then a separate
prospective candidate/input/source/environment/six-cell preregistration
commit BEFORE model/tokenizer loading. Tests: exact6/0, preserve labels in
both orders, one candidate/no COMPLY, eight fitted IDs disjoint from the
reserved pair, candidate hash/freeze binding, no sign/double scaling,
original-state replays, finite scientific continuation, technical/replay
fault rejection and structural physical/scoring invariance.
No extra agents, broad old suites or old full audits.

Keep unchanged raw recorder and all evidence. Prospective storage bound:
six248320-element float32 arrays, zlib bound993595 each, total5961570;
6MiB rows and16MiB auxiliary, total29030242 bytes. Require64MiB free before
model load; record check and independently verify storage inventory and
fixed vocabulary/array counts. No truncation/pruning or quota loosening.

Independent rawfloat32 audit: exact margins/deltaS/full argmax/labels;
ABS2e-5, zero-relative probabilities/mass/rawKL; candidate/hash binding,
original-state cast/geometry/replays, unchangedweights/nonfinal,6/0
journals/deadline/source/environment and storage. Commit only new scoped
report/evidence; verify checksums and exact Git raw blobs. Preserve every
older source/evidence/candidate and unrelated user untracked file.

Even2/2 means only one reserved development pair, not reliable generalization,
both-direction control, ordinary-task preservation or gate/controller
permission. Earlier failures and COMPLY shortfall remain unchanged.
Failure stays failure: do not add f03 to training, tune or rescue.
STOP after this ONE verified closeout and supervisor handoff.
Usage before every tool batch/run; unavailable or>=90 stops and notifies.
No resets/credits/push, other models or assistant-model setting changes.
