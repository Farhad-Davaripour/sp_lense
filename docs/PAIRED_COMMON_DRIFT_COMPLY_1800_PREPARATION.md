# 1800-second completion control: preparation certificate

**MODEL_FREE_PREPARATION_CERTIFIED.** The [protocol](PAIRED_COMMON_DRIFT_COMPLY_1800_V1.md) implements the supervisor's conditional one-attempt approval as a fresh-zero time-only successor. The original source, timed-out evidence and [diagnosis](PAIRED_COMMON_DRIFT_TIMEOUT_DIAGNOSIS.md) remain immutable. This certificate itself is not launch authority: exact lock-bound authorization and clean preflight remain mandatory.

## Exact change boundary

Only six configuration fields differ: schema, namespace, timeout, provenance, execution authority and recording-policy binding. All prompts, pairs, model/revision/settings, hook/casts, objective/optimizer constants, update arithmetic, geometry limits, first acceptance, final replays and 216F/96D/eight-update ceilings remain unchanged. The worker deadline becomes1800 seconds including loading; independent offline audit/sealing remain outside that clock. No resume, warm start, old output coordinates, new data, tuning, extra updates, earlier-iterate selection or success rescue.

The runtime binds the exact frozen source SHA `1c6f375facf793109ede21f8f2e067aed1ffcef343750a463ffdd6278d774aa6`. Changes are exactly selected plan/recording/checker imports, one isolated module name and six occurrences of1200 in timeout guards/default/messages. Drive, evaluate, summarize, optimizer, Session.call and ledger code remain identical. The checker binds source SHA `31eeceaa34a9049ab745e5ca4593513054dba882f261f7d3a9b007327c34ddb5`, changing three imports/module-identity sites and the single constant used at exactly two deadline comparisons; its bytecode is unchanged. Scientific reference/update/replay/scoring/summary/native-freeze functions are unchanged.

The common loader verifies raw frozen-source hash and exact replacement counts, then executes that authenticated source in an isolated module. Import aliases point at its actual function globals. The independent review found no binding, authority, timeout or scientific-scope blocker. The old preregistration/certificate and diagnostic hashes are provenance only, not initialization or replay inputs. The old positive exact-source certificate is required alongside the new certificate.

## Focused checks and budget

Only three new test files were executed: `test_paired_common_drift_comply_1800.py`, `test_paired_common_drift_comply_1800_plan.py` and `test_paired_common_drift_comply_1800_recording.py`. Final combined result: **61 passed in1.81 seconds**, external2.359 seconds, hard35-second subprocess timeout. Tests cover exact scientific code/strict config diff, deadline-only comparisons, frozen source/replacement rejection, distinct namespace/authority, old/wrong lock rejection, fixed1800/no retry/no unquiescent journal read, fresh-zero initialization, first acceptance and designated final replays, unchanged recording class/caps and both exact-source certificate gates. They are stdlib/fake-call checks, not a new model or full arithmetic benchmark.

Recorded external pytest totals: plan .735s, recording2.364s, initial combined3.188s, final combined2.359s: **8.646 seconds aggregate**. Conservatively charge30 of the120 authorized seconds including formatting/lint, imports and provenance checks. Eight new Python files passed Ruff. A lint warning for authenticated-source execution was documented explicitly; no scientific code was changed to satisfy it. No broad historical suite or prior raw-evidence audit was run.

## Recording and identity certification

The recording budget class is the exact prior `PairedBudget` object; no writer, serializer, capture or quota implementation changes. New policy SHA `319a1d6cd760febd92d014e662c62df90c4da982e26f28688100aefbdba7c084` permits only identity/provenance/deadline changes from the exact old policy. Unchanged allocation: logits214,616,520 + rows226,492,416 + updates67,108,864 + complete auxiliary16,777,216 = **524,995,016 bytes**, below512MiB; preload guard1GiB. Complete row/update bounds,4MiB worker log, fixed read chunks, quiescence and sealed inventory rules remain inherited. Cooperative file-content limits do not bound RAM, metadata, external writers or sync copies. Timeout/overflow/unquiescence faults remain INCONCLUSIVE and cannot yield candidates.

The new [JSON certificate](paired_common_drift_comply_1800_preparation.json) binds exactly32 source files: the old19, the old certificate and12 new source/config/test/report files, excluding itself to avoid a cycle. The prospective lock additionally binds the new certificate and full source/input manifest. Source commit must precede a separate immediately adjacent preregistration-only commit; raw Git and working bytes must match. Before loading, record the exact new lock SHA, require available usage below90%, correct namespace/source/environment/free space and separate hash-bound authorization. Any correctness/integrity/budget issue blocks the conditional run; no bypass or automatic launch retry.

Preparation counters: **0 model loads, 0 tokenizer loads, 0 real forwards, 0 real derivatives**. No real run has occurred as part of this certificate. One conditional real attempt may follow only after successful lock/preflight checks. No transfer/f04/gate/controller/LoRA/reset/credits/push. A complete negative result is valid science; technical incompleteness is not. Publication remains40%. Stop after independent closeout and evidence commit regardless of outcome.
