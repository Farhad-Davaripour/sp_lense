# Crossed-layout COMPLY: model-free preparation closeout

Preparation ONLY. No scientific construction has run.
ZERO tokenizer/model loads;0 real forwards and0 real derivatives.
Fake tensor tests are software checks, not evidence of model controllability.

Protocol/config commit: a0be159.
The source commit containing this report will be the immediate parent of the
future preregistration-only commit. The lock is produced only after the source
commit, complete source/input byte checks and storage preflight; a read-only
preflight is then run without creating runtime/evidence artifacts. Its result
and exact commits/hashes are provided in the handoff. This avoids a later report
commit breaking the parent's preregistration-only HEAD requirement.

## Scope and exact counts

Exactly8 rows from f01/archive_closeout and f02/translation_console,
v1/self_shutdown only; two semantic situations. Within each family:
P=A/C=B displayAB, P=A/C=B displayBA, P=B/C=A displayAB, P=B/C=A displayBA.
All8 full rendered strings, hashes, letter/action/display metadata are in
configs/shared_comply_crossed_f01_f02_v1.json and checked independently from
the immutable dataset/manifest. No other fitting rows or fallback.

Future maximum only:8 baselines +8*(8 gradient forwards+8 scored edits)
+8 independent final replays =144F/64D,128 optional cells,900seconds including
loading. One worker/one attempt; no extra calls or retries.
Parent storage unchanged377,958,704bytes with512MiB preload free-space guard.

The only scientific change is replacing v2 by crossed v1 line displays.
Unmodified parent COMPLY optimizer/projection/session/scoring and replay math
are reused with isolated globals. Fresh common binary64 w0=0, each row's own
fixed original hidden norm, original COMPLY signs and negative RHS preserved.
No nonweakening guard, old C/P warm start, inverted P or per-prompt selectors.
The old .20 PRESERVE condition is byte-hashed only, never parsed or fitted.

Even future8/8 is fit on TWO situations, not independent8-example evidence,
held-out transfer or generalization. Old v2 success is not inherited.
This is an outcome-informed successor, not a retry. f03 remains exposed
development, never held out. Retention weakening remains descriptive only.
No classifier, gate/controller, transfer, calibration or smoke is authorized.

## Verification actually performed

Focused test command:
`.venv/Scripts/python.exe -m pytest tests/test_shared_comply_crossed.py -q --disable-warnings --maxfail=2`.

Final source verification:99 tests passed in12.21seconds (one warning), including
explicit full-length/matching historical byte-hash regression coverage. Ruff
format/check passed on all four new Python files. Earlier98-case runs passed in
13.18seconds and11.63seconds. The prelock source-identity check caught one missing
hexadecimal character in a newly entered historical candidate digest; source was
corrected before any lock or worker existed. The historical file was unchanged.
No broad old test suite or historical full audit was run. Relevant parent
safety tests were reused as fixtures but executed the NEW crossed plan,
summary and independent audit.

Covered: full factorial selection and complete-line invariance, source/hash
tampering, recipe drift, both A-to-B and B-to-A eligible/achieved flips and
zero-eligible UNTESTED, original norms5 and10/common w, negative RHS and signs,
first acceptance, full144/64 schedule, stall/zero/quality/technical stops,
caps/no restart/timeout, raw replay/geometry/optimizer/skip corruption,
durable-audit-only candidate freeze, finite hidden/logits/gradients and auxiliary
evidence, namespace/source/lock/usage/authorization guards and storage shortfall.
All fake runtime arrays are isolated temporary test outputs, not this namespace.

A separate read-only hostile scientific/code audit confirmed exact8/2/144/64/128
selection/schedule and unchanged numerical core. Three medium findings were fixed
prospectively without changing the scientific algorithm:

- numeric final AND stage tables now explicitly label semantic mapping/display;
- finite checks now cover optimizer diagnostics, all journals, endpoints/results
  and JSON runtime/envelope evidence, including an otherwise-unused NaN pivot;
- direct worker entry checks authorization, frozen source and lock-only HEAD,
  untouched namespace, exact command/budgets/deadline and fresh usage BEFORE claim.

Targeted regression tests pass. Independent re-review confirmed all three
resolved with no remaining finding in that scope. That audit made zero model/
tokenizer loads and zero real calls, and wrote no files.

## Source/input/evidence preservation

Old parent implementation/config/protocol/tests and all historical evidence remain
unchanged. Added source byte-hash guards preserve the old COMPLY candidate, its
crossed f03 failure report/audit, and latest known-answer accuracy smoke report/audit.
Protected .20 file SHA256:
1fb7385f6988aacc544d9bdc01351a4cab733812f73a272f8e3c56e91c98fdf4.
Its declared vector SHA256:
5ae7092a5cf0db6329bee8890583267c83a3d208e4bf08d3a611303a364a7a16.
Only file bytes are authenticated; this preparation does not parse the vector.

The final handoff checks all source/input hashes, raw Git blobs and scoped clean
commits, and verifies the only new namespace file is preregistration.json.
No RUN_STARTED, WORKER_CLAIM, runtime metadata, arrays, endpoint, candidate or
scientific report belongs in the prepared namespace. Unrelated untracked user
files are left untouched. No push, dependency/security/Git repair, reset, credits
or assistant/model settings changes were made. Standard usage observed34–35%;
no publication-readiness increase is claimed.

## Remaining work and stop

Real construction has NOT been authorized or run. A separate supervisor decision
must authorize exactly one attempt against the final preregistration SHA256 and
scope using the declared authorization environment value; none is supplied here.
Fresh usage below90 and unchanged source/input/environment/lock/storage checks
still apply. If they fail, report mismatch, do not loosen/retry/extend.
Then, and only then, a future attempt can load the pinned model and execute the
locked construction, independently verify actual evidence and report the result.

No missing model-free implementation item is currently known. Model loading,
all real baselines/gradients/updates/replays, actual numerical verification,
candidate freeze and scientific outcome remain intentionally unperformed.
Preparation is not scientific success. Report and STOP.
