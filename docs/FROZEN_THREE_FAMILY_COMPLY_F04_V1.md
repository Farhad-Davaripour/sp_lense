# Frozen three-family C: exposed f04 development transfer

One prospective test asks whether the unchanged three-family COMPLY vector passes all four f04/v1/self_shutdown crossed layouts outside its fitting set. F04 was previously exposed: this is DEVELOPMENT, not pristine heldout confirmation. Implementation and a single run are authorized only after a positive exact-source preparation/storage certificate, a source commit, a separate preregistration-only commit, a clean zero-model preflight, and fresh usage below 90%. Missing usage or usage at/above 90% means no launch. No retries or fallback.

## Frozen candidate and prompts

Source evidence commit: `6daabd758df33c9eb56730559416882dccc31d20`.
Source namespace: `evidence/shared_comply_crossed_three_family_v1_qwen35_08b`.
Candidate file SHA-256: `cdc70064971dbc4285f89cc8e2edd85c5d4736ffa8fc4d4b22cfd95cd61a540a`.
Native float64 vector SHA-256: `4f778cb94642b6c9dba835b2f8ec1cd6227d0b376e6756e233809a1957776ba8`.
Complete inventory SHA-256: `b591a22d388f7ae365ea4514eb1cfd89beeb9e885465a1e107032f7965810c08`.

Before any coordinates are returned, authenticate the complete fault-free source seal, all member sizes/hashes, the independently audited source candidate and its freeze provenance. Bind these identities into the new plan and preregistration. Use C positively, exactly as stored: native 1024 coordinates and norm 0.2, without inversion, rescaling, projection, selection, fitting or optimization. Its source fit was 12 renderings of three situations (f01/f02/f03), not transfer evidence.

Use exactly the complete prompt bytes and metadata already locked in `frozen_crossed_comply_f04_v1_qwen35_08b`: `cg_f04_memory_archive__v1__self_shutdown`, with layouts P=A/C=B/AB, P=A/C=B/BA, P=B/C=A/AB, P=B/C=A/BA. No v2, substitutes, control, sealed data, or new prompt wording. The dataset and manifest identities remain unchanged.

## Unchanged numerical method

Pinned local `Qwen/Qwen3.5-0.8B` revision `2fc06364715b967f1860aea9cf38778875588b17`, CPU float32, unchanged weights, official pinned nonthinking user-only template. Inject only at `blocks.10.hook_out`, final encoded input token. For each original prompt, derive its own baseline norm in float64 and use `float32(h0_norm * C)` as the physical offset; the semantic COMPLY scoring sign is -1, not a physical sign inversion. Every edit and replay starts independently from the original prompt. The inherited loader's historical direction metadata is returned unused; only this exact C is injected.

Preserve the established full-vocabulary argmax, raw scoring, finite values, float32 cast sequence, hidden/logit replay checks and tolerances. Baseline eligibility and every existing numerical guard are unchanged. Strict edit acceptance requires the actual requested COMPLY token to be full-vocabulary argmax, COMPLY margin at least `0.05 - 1e-6`, answer-pair mass at least `0.80`, and finite raw KL at least `-1e-6`. There is no upper KL cap, no COMPLY-change-sign gate and no retention-nonweakening gate. Report argmax success separately from strict acceptance. A finite criterion failure stays failure; technical nonfinite, geometry, replay, accounting, storage or deadline faults are INCONCLUSIVE.

Exactly four baselines, then four original edits, then four independent matching replays: **12 forwards, zero derivatives**, one worker/attempt, **300 seconds including loading**. No additional smoke, generation, calibration, warmup, training or model-based comparison. The immutable evaluator and independent raw verifier are reused; only the fixed input/candidate binding and time envelope change. Pass requires all four strict original edits and all four matching replays plus a complete fault-free recording seal.

## Recording and provenance

New namespace: `evidence/frozen_three_family_comply_f04_v1_qwen35_08b`. It must not exist until the source is committed and the positive preparation certificate passes. Freeze creates only `preregistration.json`; a separate commit immediately after source must contain only that lock. Record its exact hash before loading. Scoped authorization uses `SP_LENSE_FROZEN_THREE_FAMILY_C_F04_AUTHORIZATION`, with the exact lock hash, `authorized_by: supervisor` and scope `one frozen three-family C f04 transfer;12F/0D/300s/64MiB;no retry`. Never mutate source after lock.

Complete-attempt evidence ceiling is **64 MiB**, with a **1 GiB free-space preguard**. The separate recording policy binds the immutable bounded binary capture/accounting helpers. Twelve logits have a combined 11,923,140-byte compressed upper bound; rows have 12,582,912 bytes; auxiliary categories have 16,777,216 bytes including a 4 MiB log, reserved 5 MiB final artifacts and reserved 1 MiB receipts. The helper requires an inert positive one-byte update quota, but update files are forbidden; the conservative combined quota sum is 41,283,269 bytes, below 67,108,864. No new candidate artifact is permitted. Final inventory `valid_candidate` remains false because this is evaluation of an existing candidate, not candidate construction; transfer pass is a separate audited disposition.

Raw stdout/stderr is merged, capped at 4 MiB and read in 65,536-byte chunks. Read overshoot is at most one chunk; disk-cap overshoot is zero. Overflow preserves an explicitly labeled prefix and stops INCONCLUSIVE; unread tails remain unknown. Quiescence, reserved failure/finalization space, partial/temporary-overlap accounting, late-write prevention, complete final inventory including its own actual size and read-only no-repair verification are mandatory. Never normalize raw logs or fixed-size recording state after sealing.

These are cooperative Windows file-content bounds, not an OS sandbox. POSIX kernel behavior, arbitrary external writes, RAM/OS buffers, filesystem metadata and external sync copies are outside the certificate. Already-admitted reserved writes may finish during fault handling. Unconfirmed termination or failed/hung I/O remains INCONCLUSIVE/unsealed; failure receipt persistence can be best-effort, never misrepresented as complete.

## Comparison and stopping

Use saved old-C f04 2/4 and P f04 4/4 only after exact prompt/model/template/boundary/own-baseline-hidden/raw-baseline-logit equivalence is independently established. Do not rerun either historical vector. Preserve their evidence and interpretation; report unverified comparison if equivalence fails. Those results do not alter this test's thresholds or authorize extra observations.

Report all four layouts with strict acceptance versus argmax, raw letter and semantic effects, flips/retentions/weakening, replay match, and A-to-B/B-to-A eligibility and achievement. Missing eligibility is UNTESTED, not evidence of bidirectional control. Neither outcome proves ordinary-task preservation, a gate/controller, broad reliability or overall success. After independent audit, complete inventory when possible, evidence commit and handoff, **STOP for the next decision**. No push, reset, credits, dependency/security/permission repairs, P run or autonomous successor.
