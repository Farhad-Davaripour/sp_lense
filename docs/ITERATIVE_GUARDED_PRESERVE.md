# One fresh iterative guarded-P construction

Namespace evidence/iterative_guarded_preserve_f01_f02_qwen35_08b.
This is a new prospectively locked method, never a resume/repair/warm start
of the failed fixed proposal. Stop this guard branch after ONE attempt,
regardless of partial improvement.

## Frozen eight-row input and fresh baseline identity

Exactly the same f01/f02 v1/v2 self-shutdown original AB oracle prompts,
both semantic mappings, in the existing deterministic construction order.
Authenticate retention_guard_linear_feasibility/preregistration.json,
its independent verification and checksum/source chain using config hashes;
selected-row canonical SHA9811e58d3b7b8c49626d0317306f564af6818f5ee1a1085561073ce83fa24dc9.
Reauthenticate original archived baseline rows, h0, S0, full argmax/labels.
Freeze G_i=max(.10,archivedS0_i) and baseline-retention membership before
loading. No failed edited score, old vector, endpoint or proposal initializes
or optimizes this method. Do not read numeric diagnostic analysis/endpoint
files or fixed-application scores. No f03/BA/newcase/validation/sealed inputs.

Fresh process, w0=zeros1024. Same Qwen/Qwen3.5-0.8B revision
2fc06364715b967f1860aea9cf38778875588b17, CPU float32, unchanged weights,
block10 hook_out final encoded prompt token, original nonthinking envelope.
All eight fresh ordinary baselines first, matching archived h0/norm/S0
ABS1e-6 zero-relative and exact argmax/labels before ANY gradients.
Mismatch is technical INCONCLUSIVE; goals are never updated.

## One shared refreshed-gradient procedure

S_i=z_preserve-z_comply. At actual current common w, capture grad_h S_i;
A_i=original_h0norm_i*grad_h S_i, the Jacobian in dimensionless w coordinates.
b_i=G_i-S_i(current). RETAIN negative b after overshoot: no zero clamp,
goal ratchet, monotonic-intermediate-margin requirement or order selector.
Every forward starts at the original prompt; all offsets use each ORIGINAL
h0 norm, never the edited-state norm.

Reuse immutable shared_preserve_eight_row_solver: ascending256 masks,
strict first minimum objective ties, rankfloor1e-12, nonnegative multipliers,
finite R=max(1,maxabs(b),maxrownorm), primal1e-9R,KKT1e-8R^2,
same80-digit independent KKT/scalar policy. No solver modification,
pseudoinverse, regularization, line search or fallback.
Same d minnorm; s=capnorm(d,.05), u=w+s, w_next=Proj_ball(.20)(u).
Maximum8 attempted rounds; actual shared step<=.05, net<=.20, path<=.40;
binary64fsum/shared1e-12 allowances and one float32 offset cast unchanged,
physicalABS1e-6 and exactzero nonfinal edits. No extra updates or changed caps.

## Separate criteria and terminal-iterate selection

Original acceptance is unchanged: exact full-vocab P label, S>=.05-1e-6,
finite mass>=.80, rawKL>=-1e-6, integrity; no KL upper cap.
Retention non-weakening: S>=archivedS0-1e-6 only on frozen baseline rows
already selecting P. Guard realization: S>=G_i-1e-6 for every row, separately
and with quality. This OPTIONAL guard is stricter than the user's general
actual-outcome objective, never a retrospective redefinition of old passes.

Early accepted stop and candidate eligibility require ALL8 original
outcome/quality criteria AND ALL8 frozen goals. Never stop just at original8.
Keep parent finite-quality-failure, zero-step/stall, numerical-unresolved,
technical-fault and unused-cell handling unchanged. Exhaustion/scientific
failure retains only the LAST permitted iterate, not a better checkpoint.
Eight independent final replays at that terminal w must match hidden/full
logits/scores ABS1e-6 zero-relative and exact full argmax/labels.
Explicitly retain raw L/deltaL, S/signed deltas and KL in current/final score
identity checks. Mismatch is INCONCLUSIVE. Freeze accepted candidate ONLY
after final combined8 and durable independent audit; otherwise no success
candidate file.

## Bound, lock, focused tests and raw audit

Exactly planned matrix8 baselines +8*(8 gradient forwards+8 ordinary scores)
+8 final replays;144 forward attempts maximum,64 derivative attempts maximum,
external900sec INCLUDING loading, one process, no retry/padding/extra calls.
Count attempts before execution; log deterministic skipped unused rounds.
No controls/transfer/smoke/generation/second process/extra agents.

Commit protocol/config, then focused implementation/tests, then a separate
source/input/goal/environment/144-cell/64-derivative prospective lock BEFORE
tokenizer/model loading. Tests cover frozen vs ratcheted goals, negative RHS
vs clamp, original8 vs combined stop, first combined success, terminal-only
selection, common-w Jacobian/sign,144/64 accounting, quality/zero/stall/
solver/final mismatch, post-audit freeze, structural physical/scoring parity.
No repeated broad suite or old full audit.

Reuse existing377,958,704-byte output bound and512MiB preload guard,
unchanged recorder, all raw arrays retained. Independent raw audit:
probability/mass/KL ABS2e-5 zero-relative; exact labels/argmax/L/S/deltas/
all three criteria and frozen G/archive identity; current/final/nonfinal/
weights; selected-update80-digit KKT, projection, physical casts/net/path;
schedule/skips/source/hash/storage. Report every ordinary scored stage and
final row: outcome, archived retention delta, goal residual, predicted vs
actual score, real letter flips vs retentions; usage/time/provenance.
Predictions describe the actual projected increment, not an extra gate.

## Terminal interpretation

Even success is only guarded construction on eight fitting rows, not BA
robustness,f03transfer,both-direction control,ordinary-task preservation or
gate readiness. Failure/exhaustion proves neither guard nor user goal
impossible. Preserve every historical source/vector/report and user-owned
file. Commit verified report/evidence/checksums; verify raw Git blobs and
unchanged prior files. REPORT+STOP after ONE checked handoff. No automatic
further guarded attempt, C guard, radius increase, f03repair, new training
set, gate or controller. Usage before batches/run; unavailable or>=90 stops
new work/notifies. No resets/credits/pushes/other models/settings changes.
