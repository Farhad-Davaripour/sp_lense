# Clean01 model-free launcher preparation

**MODEL_FREE_PREPARATION_CERTIFIED; no real launch authorized.** This certificate concerns the isolated [clean01 repair](PAIRED_COMMON_DRIFT_COMPLY_1800_CLEAN01_V1.md). The failed predecessor and all historical sources remain immutable. The previous certificate recorded passing imported-function tests but missed actual `_worker` CLI dispatch; that coverage limitation is retained, not converted into new launch approval.

## Repair and unchanged scope

The new executable explicitly maps `_worker` to the frozen `worker` function; unknown/malformed commands fail closed. Apart from that mapping, changes are namespace/schema/provenance/authorization/recording-policy identity. Exactly five config fields differ from the 1800 predecessor. Runtime scientific functions, worker and run function code, optimizer, Session.call and independent reference/update/replay/summary/freeze code are unchanged. The checker reuses the exact frozen 1800 wrapper and its deadline guards. Source-hash and exact replacement-site pinning remain enforced; no production bypass or test switch was added.

All original 1800s/216F/96D/eight-update settings, twelve prompts/six pairs, model/revision/CPU float32, hook/casts, fresh zero, tau/lambda/B-floor, step/net/path caps, first acceptance and final replays remain unchanged. No prior output coordinates, resume, tuning, alternate vectors, scientific successor or model-setting changes.

## Regression evidence

Final targeted suite: **75 passed in6.63 seconds**, external7.063 seconds with a35-second subprocess timeout. Only the three new clean01 test files ran. Independently run plan tests took .719s and recording tests1.728s externally; total recorded pytest subprocess time is approximately **9.510 seconds**. Conservatively charge30 of the120-second aggregate allowance including lint/import/provenance checks. Seven new Python files passed Ruff; no broad historical suite or raw-evidence re-audit.

The new evidence is stronger specifically at the previous missed boundary:

- The actual new script is executed in a subprocess with `_worker`, with all task authorization variables removed. Test-only startup instrumentation records exactly `worker`, then `require_authorization`, with no `load_backend` call, no ML module and no blocked ML import attempt. The expected authority-path exception is asserted; a nonzero exit alone is not accepted. Before a lock exists it can be a missing-lock exception in that path; after a lock it is missing authorization.
- Four actual-script malformed/unknown argument cases fail closed without entering worker or authorization.
- A separate fixture calls the unchanged supervisor `run()` path with a temporary root and fake worker. The exact emitted `[python, -u, script, _worker]` command traverses real bounded capture. The fake child verifies its arguments and no ML imports; the receipt proves exactly one process, exit0, EOF and quiescence. With no scientific records the overall fixture is INCONCLUSIVE and cannot produce a candidate. This is not a real experiment or reuse of the production namespace.
- Frozen scientific-code equality, strict identity/provenance-only config diff, unchanged limits and new exact-source/certificate coverage requirements are tested.

The test-only observer blocks ML imports but does not modify production functions or authorize anything. Parent guard substitutions occur only inside the temporary pytest capture fixture; production source has no corresponding bypass. Independent source review found no blocker and explicitly limits the claim: **unauthorized worker entry and fake-worker capture are covered, not successful execution of the fully authorized real model path**. Model efficacy/completion are not certified.

## Recording and source bindings

The budget class, serializers, capture and quotas are direct immutable reuse. Complete category allocation remains524,995,016 bytes below512MiB, including16MiB auxiliary; preload free guard1GiB. Whole-record caps, captured output ceiling, quiescence-before-journal-read, no post-seal writes and candidate prohibition after faults remain unchanged. Cooperative file-content limits do not bound RAM, filesystem metadata, external writers or sync copies. Offline audit/sealing remain outside the1800-second worker clock.

The new [JSON certificate](paired_common_drift_comply_1800_clean01_preparation.json) binds exactly44 source files: historical32, the prior certificate and11 new files, excluding itself. Historical certificate SHA `fb25e4a26b51e4838cb726e9864bad4e953a38fe993dcac7f10c38b9424d7f80` and its source bytes are authenticated only as provenance with the known CLI gap; its positive-readiness gate is not used to assert repaired coverage. All six new entrypoint-coverage attestations must be literal true. The complete prospective source/input lock additionally binds this new certificate and all inherited inputs.

Before handoff: commit tested source, create a separate immediately adjacent preregistration-only lock, and run clean zero-model preflight with raw Git/source/environment/namespace/free-space checks. No authorization is set and no worker is launched. Usage was available below90% before testing; no credits/reset/push. All historical and unrelated files are preserved.

Real-work counters in this preparation: **0 model loads, 0 tokenizer loads, 0 forwards, 0 derivatives**. Unauthorized subprocesses and the temporary fake-worker capture are tests only. No production RUN_STARTED/WORKER_CLAIM or scientific output is permitted in the new namespace. **STOP after lock/preflight handoff, before any real launch.** Publication remains40%.
