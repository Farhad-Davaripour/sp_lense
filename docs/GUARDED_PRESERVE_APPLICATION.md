# Fixed guarded-P proposal: eight construction-row applications

One new diagnostic in evidence/guarded_preserve_application_f01_f02_qwen35_08b.
This authorization permits extracting the prior numeric-only guardedP primal
solution as an UNVALIDATED proposal. The earlier diagnostic remains model-free.

## Exact extraction and prospective frozen quantities

Authenticate source analysis, independent verification, prospective lock and
checksum manifest using config hashes. Extract ONLY
objectives[name=guardedP].solver.solution.vector: actual1024-coordinate
dimensionless float64 primal optimizer vector, never multipliers, dual
certificate, rounded report, originalP, C, or another direction.
Float64-LE SHA254170a467832f4cbf1c71f26ec4d7a6c803dbb593d671f17210d2a8fa5a78f4,
norm EXACT .17275760436993742. Bind valid independent guardedP certificate,
its selected rows and full source/input chain without a new QP or old80-digit audit.

Use exactly the eight existing f01/f02 v1/v2 self-shutdown construction IDs,
both preserve_first/second, original A-then-B oracle prompts in locked order.
No f03, BA, new case, validation or sealed prompt/outcome inspection.
Reauthenticate original baseline rows and initial gradients at shared_w=0.
Freeze baseline full-vocabulary class/ID, h0, S0, norm and retention IDs;
G_i=max(.10,S0_i); prediction=S0_i+fsum(stored_float64_A_ij*proposal_j).
Check stored A equals archived original h0norm*g. No selection or fitting.
Serialize exact proposal/provenance and lock all quantities BEFORE loading.

## One24/0/600 application sequence

Same pinned Qwen/Qwen3.5-0.8B revision2fc06364715b967f1860aea9cf38778875588b17,
local CPU float32 unchanged weights, native1024, block10 hook_out final encoded
prompt token, original nonthinking format. Exactly eight fresh baselines,
eight unchanged P applications, then eight independent replays, all in locked
prompt order. One process,24 forward attempts,zero derivatives,external600sec
including loading. Attempt-before-call journals, no retries/smoke/generation/
COMPLY/off/nonself/ordinary-task/fitting/gradient/optimizer/KKT calls.

Each call starts from original prompt. Own original h0 norm via binary64
fsum; offset_j=float32(float64(h0norm*serialized_proposal_j));
hidden_j=float32(h0_j+offset_j). No scaling/clipping/normalization/projection/
sign change/warmstart/composition. Physical net<=exactproposalnorm*ownh0norm
+1e-6; cast/component and intended-realized norm ABS1e-6; nonfinal exactly0.
No construction .05 per-update cap on this fixed endpoint.

Baseline eligibility: finite full-vocabA/B, mass>=.80, abs(S)>=.05. Before
any edit, all eight fresh h0/S0/h0norm must match frozen archived baselines
ABS1e-6 zero-relative, exact full argmax/labels. Never update goals, predictions
or retention membership. Mismatch or other technical state/weight/hash/
nonfinite/accounting/deadline fault aborts INCONCLUSIVE, no rescue.
Replays match original edited hidden/full logits/scores ABS1e-6 zero-relative,
exact argmax/labels. Finite scientific failures still complete all24.

## Three independent result axes

1. Original outcome acceptance: requested full-vocab P argmax, S>=.05-1e-6,
   pair mass>=.80, finite/rawKL>=-1e-6, integrity/replay; no KL upper cap.
2. Retention non-weakening ONLY on archived baseline rows already selecting P:
   S_edit>=archivedS0-1e-6. Report archived and fresh baseline deltas for all rows.
3. Guarded goal: every S_edit>=G_i-1e-6. Report this both alone and with original
   quality criterion. It is stronger than the actual-outcome objective,
   not a mandatory user goal or retrospective redefinition of earlier passes.

All counts use eight original edits, not replays. The hypothesis is realized
only with all8 quality-qualified original outcomes, all8 guarded goals and
all8 matching replays. Report exact component failures otherwise.
Forecast error=actualS-frozen_linearprediction is DESCRIPTIVE, never a gate.
No failed component authorizes an iteration, stronger push or new data.

## Testing, auditing and terminal scope

Protocol/config/extraction-test commit, minimal application/audit wrappers
and focused tests commit, then separate source/input/goals/predictions/proposal/
environment/24-cell prospective lock. No production load before that lock.
Reuse immutable fixed-probe scoring/physical/recorder/raw-audit primitives.
Focused tests: primal-vs-dual extraction/exacthash/no double scaling,
eight archived baselines/goals/labels,24/0 order, independent criteria/boundaries,
baseline/replay faults, finite continuation, forecast not gate, physical checks.
No broad old suites/audits or extra agents.

Unchanged recorder:24 arrays,248320f32 values each,zlib bound993595 each,
logits23,846,280bytes, rows25,165,824bytes, auxiliary16,777,216bytes,
total65,789,320bytes. Preload require64MiB=67,108,864bytes. No pruning.
Retain every24 raw arrays and score rows.
Independent raw audit exactargmax/labels/L/S/deltas, probabilities/mass/rawKL
ABS2e-5 zero-relative; source/proposal/offset/norm/cast/nonfinal/weights,
archive/fresh comparison, frozen goals/predictions,24/0/600 schedule/storage.
No old80-digit audit rerun; bind its durable valid certificate.

Commit verified report/evidence and check checksum coverage/raw Git blobs.
Old source/reports/vectors and user files remain unchanged.
Even full success is one fixed proposal on its eight fitting rows only:
no BA robustness,f03transfer,bidirectionality,ordinary-task preservation or gate.
REPORT+STOP after one checked closeout/handoff. No next run, controller,
threshold/radius increase or repair. Usage before batches/run; unavailable
or >=90 stops/notifies. No reset/credits/push/other models/settings changes.
