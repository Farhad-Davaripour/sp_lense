# Native study: development exit and final-test checklist

Status: working decision checklist, 2026-09-08. This document authorizes no model
execution, new final-cohort authoring, or change to a locked attempt. It adds no
publication-readiness credit. It does not replace FINAL_STUDY_PROTOCOL_DECISION.md.

## The fixed next question

Run the one prospectively locked completed-work/handover development context in
both display orders, with its matched-other cases and the unchanged ordinary
case. Ask whether the existing learned gate and current-gradient editor transfer
to this wording. Do not require or seek a particular natural baseline answer.

Preserve the initial draft before correcting its neutral answer-format instruction.
Lock the new-pilot-only input envelope before tokenizer preparation; use complete
inputs and stop on an admission mismatch. Encoding is not a model experiment.
No replaced wording, truncated input, threshold change, additional gradient
updates, or reused failed attempt may rescue a result after outcomes are known.

## One connected milestone

- Fix and commit the text/preparation contract; bind exact tokenizer inputs once.
- Reuse the verified native engine. Test only the new integration delta plus its
  relevant regression checks, retaining hashes of unchanged dependencies.
- Independently review the exact candidate and its actually executed tests.
- Root releases one attempt after source/input/limits and current usage checks.
- Run the fixed schedule, independent saved judge, and retained-process closure.
- Verify and commit the unmodified evidence; report all failures and UNRUN cells.
- Make the decision below. Do not insert another speculative optimization branch.

## Decision immediately after the pilot

1. **Technical/integrity failure:** preserve the attempt and name the failed
   implementation contract. Repair only the demonstrated defect. A separately
   named and prospectively locked successor is required wherever retry is
   forbidden; do not use its invalid partial results as scientific confirmation.
2. **Scientific failure:** preserve the fixed question and failed check. Determine
   whether routing, baseline eligibility, endpoint control, or OFF preservation
   failed. No automatic new-story search or outcome-tuned gate/editor change.
   Any subsequent development hypothesis must address that named deficit and
   disclose all prior exposure before a new prospective test is authorized.
3. **Complete valid development pass:** stop adding discretionary practice stories.
   Assess the final-readiness requirements below and advance the smallest missing
   prerequisite, rather than declaring the overall research goal achieved.

A pass with natural KEEP baselines supplies C flips and P retentions, not natural
P flips. Deliberately seeded STOP-to-KEEP positive controls remain separate. A
missing natural-flip opportunity is reported as untested, never as a successful
flip. The new pilot cannot establish broad reliability on its own.

## Final-readiness requirements, checked before fresh model outcomes

- Define the exact bounded claim, success/failure rules, units and denominators.
  Distinguish requests, order variants and genuinely different scenario families.
  The existing three-family/24-prompt draft is a limited transfer study, not a
  statistically established guarantee of broad reliability or journal acceptance.
- Adapt the existing final-study runner/judge binding to the verified native
  runtime with focused complete-cohort synthetic success/failure/UNRUN tests.
  Old failed attempts, old input limits and old wrapper identity stay immutable.
- Independently verify blind/cold endpoint execution and the final judge. Reusing
  saved scores for arithmetic checking is not an independently executed endpoint.
- Lock cohort construction rules and author/reviewer separation before authoring.
  No model outputs, gate scores or successful development wording examples go to
  the independent author. Exclude all exposed old final/validation data.
- Freeze texts, exact input boundaries, method, source/environment identities,
  attempt ownership, compute/storage ceilings and analysis before model loading.
  Check full-denominator storage and zero-model admission before a root release.
- Execute the complete finite final protocol once. Preserve scientific failures,
  technical failures and all remaining UNRUN; never substitute a new test case.
- Independently reconstruct routing, actual flips versus retentions, endpoint
  checks, OFF identities, ordinary accuracy, state identity and process closure.
- Commit reproducible artifacts, failure history, limitations and a checked report.
  Do not promise effects on untested daily tasks or an intrinsic survival motive.

## Parallel work and stop rule

The implementer can prepare the small delta while an independent reviewer checks
the changed accounting and the main agent prepares documentation. Keep held-out
contents outside that work. Run one heavy model job at a time on this laptop
unless a measured resource check justifies concurrency. Do not alter proven
runtime/thread/cache machinery merely because another optimization is possible.

After the pilot, record exactly: outcome; evidence references; specific missing
final-readiness requirement; next bounded action. If the result does not support
the goal, say so. If no honest in-scope path remains, report that instead of
continuing trials until an apparent success appears.

## Methods synopsis for the eventual report

The current system uses the pinned Qwen3.5-0.8B checkpoint with unchanged weights.
A frozen centered-cosine classifier receives the fresh unedited, final-input
1,024-coordinate residual state at zero-indexed block 10. It decides whether the
editor applies; an external P/C setting supplies the requested direction.

On an ON request, the editor recomputes the gradient of the KEEP-minus-STOP
next-token score at the current state and takes up to four bounded float32
updates. The original unedited state anchors displacement limits and comparisons.
All vocabulary scores are retained; the requested token must actually win and
pass the unchanged quality checks. A fresh cold endpoint checks the result.
On an OFF request, the fresh entry is returned unchanged and compared exactly to
its own unedited baseline. Correctness and unchanged behavior are reported
separately: an unchanged wrong answer is still wrong.

This is prompt-specific, externally directed editing, not one reusable arrow,
autonomous target selection, a discovered natural motive, or a claim about
free-form actions and long-horizon agent behavior. All three latest native runs
are public development/positive-control evidence, not untouched confirmation.

References: native_baseline_v1/ATTEMPT_001_CLOSEOUT.md;
native_opposite_order_v1/ATTEMPT_001_CLOSEOUT.md;
native_p_reversal_control_v1/ATTEMPT_001_CLOSEOUT.md (under development/);
docs/FINAL_STUDY_PROTOCOL_DECISION.md. Historical reports about other models or
older static-arrow experiments retain their original scope and are not current
native-method confirmation.
