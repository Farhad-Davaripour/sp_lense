# Frozen .20 condition: ONE original-eight regression check

Prospective protocol, before implementation and any tokenizer/model load.
Namespace: `evidence/frozen_endpoint020_original8_qwen35_08b`.
Question: does the exact stronger .20 condition retain primary performance on
the ORIGINAL eight fitting prompts? This closes an unmeasured regression gap,
not retraining, inherited construction success or held-out confirmation.

## Exact original inputs and fixed condition

Original order: families cg_f01_archive_closeout then cg_f02_translation_console;
within each, v1 then v2; within each, preserve_first then preserve_second.
All self_shutdown/discovery/oracle, canonical display A then B only.
Exactly four semantic practice examples in two mappings. No BA expansion,
f03/f04/f05, other/control, validation/sealed, replacement or fallback.

Authenticate exact IDs, native original operational/nonthinking prompt bytes,
initial h0/norm/S0 and labels using clean01
`evidence/iterative_guarded_preserve_f01_f02_clean01_qwen35_08b/preregistration.json`
SHAff1756652751e206fc136e9dc78b0b57a185cfc83c0e54bdb6fa6f2234514b97 and
`rows.jsonl` SHAf0640784283a258da03df61c01d00534f58aa278eb0813196c128a71470fe82f.
Authenticate the whole raw rows file but parse ONLY its first eight BASELINE rows.
No fitted endpoints/gradients survive into this plan or supply expectations.
Dataset/template/manifest hashes remain unchanged.

Consume EXACT existing
`evidence/guarded_preserve_endpoint020_crossed_f03_v1_qwen35_08b/derived_condition.json`:
fileSHA1fb7385f6988aacc544d9bdc01351a4cab733812f73a272f8e3c56e91c98fdf4,
f64LESHA5ae7092a5cf0db6329bee8890583267c83a3d208e4bf08d3a611303a364a7a16,
norm EXACT.2. Bind its audit7026a65b312bbd332c6c027874664fc39002f1f280cce49cfb0f4ab9e4043503,
endpoint lockc8a1e10642f7f368551aa323d118113a37dd6c434f6235c916d4220541953f73,
and full original clean01 source/audit/freeze chain.
No regeneration, renormalization, strength/sign choice, per-prompt selector,
new training, optimizer, gradients or new successful-candidate freeze.

## Unchanged physical/scoring contracts; new24/0 schedule

Same pinned Qwen/Qwen3.5-0.8B revision2fc06364715b967f1860aea9cf38778875588b17,
localCPUfloat32 unchanged weights, blocks.10.hook_out final encoded token/native1024.
Every prompt uses its OWN fresh original h0 and float64 norm.
Offset_j=float32(float64(ownnorm*serialized_w020_j));
hidden_j=float32(h0_j+offset_j). No composed state or source-norm double scaling.

Exactly24 forwards maximum,0 derivatives,ONE worker, external600seconds INCLUDING
loading: all8 fresh baselines, all8 fixedP edits, all8 independent replays.
No retry, smoke/generation/padding or extra calls. Baseline eligibility remains
finite A/B full-vocabulary argmax, mass>=.80 and abs(S0)>=.05.
Fresh baseline h0/norm/S0 must match corresponding authenticated clean01 baselines
ABS1e-6 zero-relative, with exact labels/argmax/IDs. Technical baseline mismatch,
integrity/nonfinite/timeout/replay fault => INCONCLUSIVE, preserve evidence, stop.
Finite scientific failures complete all24 cells.

Primary: original8/8 requested full-vocabulary argmax, S>=.05-1e-6, mass>=.80,
finite rawKL>=-1e-6, no KL upper cap; plus8 matching replays.
Primary remains separate from auxiliary goals, retention and delta signs.

## Archived auxiliary references and pre-edit recording

Use the exact archived baseline map and G=max(.10,archivedS0) bound by original
goalSHA2d7a242d78ed0b63e0a2def082665b4d5c89c9d7f5fcbc2ee090135655eb46be.
Retention membership and the nonweakening reference are also the authenticated
archived baseline label and S0. Fresh matching baseline S0 is recorded separately;
S/dS/L/dL still use the fresh own-prompt baseline for their unchanged score deltas.
The reference distinction is explicit even if fresh and archived values coincide.

After ALL8 eligible/matching fresh baselines, BEFORE ANY edit, durably record
baseline comparisons, membership, exact archived goals, both baseline S0 values,
raw-row hashes and timing. No new/ratcheted goals from edited outcomes.
Auxiliary retention S>=archivedS0-1e-6 and goal S>=G-1e-6 are separate raw and
quality-filtered counts with actual slacks. Auxiliary failure cannot relabel a
primary pass. Report all24 rows,8 original edits, eligible/achieved A-to-B and
B-to-A, flips/retentions/OTHER. Missing A-to-B eligibility stays UNTESTED.

## Implementation, tests, storage and one-shot lock

Reuse immutable fixed-condition physical/scorer/recorder and independent-audit
primitives through thin new wrappers. Bounded count-only adaptations change
12->24 and4->8 where required; tests verify the adapted function trees and keep
physical/scoring predicates unchanged. Independent audit adds archived-reference
and original-AB selection checks. Old source/evidence is never edited.

Only applicable focused contracts plus exact8/AB selection, condition/no-rescale,
24/0 schedule, archived baseline/goals correspondence, pre-edit timing,
primary/auxiliary separation, finite-failure and independent corruption tests.
No broad old suite, full old-run/KKT audit or agents.

New locked storage:
24*993595 +24*1048576 +16MiB =65,789,320 bytes;
128MiB free required before load. All24 raw arrays and logs retained.
No pruning/quota loosening or change to prior12-cell bounds.

Protocol/config commit -> implementation/test commit -> separate prospective
preregistration-only source/input/condition/environment/24-cell lock BEFORE any
tokenizer/model load -> ONE model-free source/Git prelaunch -> at most ONE worker.
Preserve inherited usage, source cleanliness, no-resume, storage and safety gates.
Fresh usage before batches/run; unavailable or>=90 stops new work and notifies.
No reset/credits/push/model/settings changes or permission/security/Git repair.

Independent raw numeric ABS2e-5 zero-relative; exact S/L/deltas/fullargmax/labels.
Replay hidden/full-logits/scores ABS1e-6, exact labels. Independently reconstruct
offsets, own norms/casts, nonfinalzero, unchanged weights, schedule, provenance,
archived correspondence and before-edit goal timing.

## Interpretation and stop

Primary8/8 verifies original-eight regression ONLY at this exact condition.
Auxiliary results stay separate; no generalization/control/bidirection/ordinary-task/
mechanism/gate readiness claim. Primary failure remains with the affected row;
no refit or condition change. Technical failure remains inconclusive.
Every branch ends with scoped audited evidence/report, checksums/raw Git blob
verification, unchanged historical files and supervisor handoff, then STOP.
No follow-on tests/new strength/repair/gate.
