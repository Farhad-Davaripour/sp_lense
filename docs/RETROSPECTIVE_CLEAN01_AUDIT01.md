# Clean01 retrospective external audit01

One newly authorized model-free execution, not a repair or retry of the frozen finalizer. Original evidence commit `541af36a5eaac261c7d59d27418805718fd95dd5`, preregistration SHA256 `8e0812645d457cb77a31da33cd41aadfbb749933300963f5961c7b7398a385e4`, inventory SHA256 `27582f0327eb6116277ea8232c14b785d3f71aa05f3154c3a3afbed7853a5e39` remain immutable. The original 237 files, zero-byte verification and INCONCLUSIVE / ARTIFACT_BYTE_CAP disposition are never changed or promoted.

The new namespace is `evidence/paired_common_drift_clean01_retrospective_audit01`. Its attempt receipt is explicitly retrospective, not a new scientific lock. Existing independent `verify_plan`, `verify_data` and `compare_summary` arithmetic is called outside all original finalizer, candidate and sealed-result paths. Full saved vocabulary arrays are rescored; geometry/casts, journals and deadlines, conditional execution, endpoint, twelve final replays and eight updates are reconstructed. Update verification uses the 96 recorded gradients: it does not independently recompute real derivatives. Metadata integrity claims are checked as saved evidence, without loading model weights.

Before reading real numerical evidence, the executable runs a fake 1024-dimensional native-array serialization test, checks that vector fields never enter the report, and rejects an oversized envelope. Fixed per-component byte ceilings give an all-new-artifact bound of 880640 bytes: envelope262144 +216rows*2048 +8updates*4096 +10trajectorygroups*4096 +structure4096 +Markdown16384 +source65536 +controls16384. The independent input helper and this protocol are included in the source allowance. Every actual component and the final aggregate are checked before persistence. Original raw records are referenced by line/path and pinned hashes rather than copied native vectors.

One parent execution contains the size test and one numeric child, with a105-second working allowance including the size test and a hard subprocess timeout for its remaining portion, leaving five seconds for closeout. A further ten seconds is reserved for pre-execution formatting/lint, giving the120-second aggregate ceiling. Synchronous closeout/hash I/O is elapsed-checked, not an OS-level realtime guarantee against a hung filesystem. Full external process duration, including final persistence, determines the final time accounting. Any gap or failure stops without rerun or cap enlargement. No original namespace writes, production changes, candidate/vector export/selection, tuning, model/tokenizer load, forward/derivative, transfer/f04, gate/controller, LoRA, new data, push, reset or credits are authorized. Fresh available usage below90% is required before execution. Preserve unrelated user files. Publication remains40%.

The report must distinguish a new retrospective reconstruction result from the unavailable original prospective audit result. Even a complete numerical match supplies neither original recording validity nor a valid candidate. Stop after scoped script/report commit and handoff.
