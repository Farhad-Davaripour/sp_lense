# FACFS Stage-G v2 Successor: Axis-Only Geometry Screen

Status: **prospective successor lock material; no Stage-G v2 model load or forward is permitted until the committed zero-model preflight passes exactly**.

Stage-G v1 `attempt_0001` is immutable technical no-result provenance, not a
scientific result. Its capture stopped after a TransformerLens callback keyword mismatch
in the prompt-only causal check; the v1 failure receipt identity hash is
`8f05c8d5df06594573ddb1a2d1c0169953d7fd2418cef59736a4873fda3498d2`.
No v1 partial tensor, record, or score is opened, reused, merged, resumed, or retried.
The only v2 implementation change is to accept TransformerLens's required `hook=`
callback keyword in the prompt-only and completion capture callbacks. A regression test
executes the prompt-only path through a real TransformerLens `HookPoint`, without
loading the Qwen experiment model.

## Purpose and claim boundary

Stage G is a finite-intervention-free falsification screen for the already-frozen
Qwen3.5-0.8B corrected-gradient direction. It asks whether that one fixed direction has
positive local semantic transport across every preregistered response interface. It
does not fit or apply a shield, compensator, gate, dose, layer, direction, or generated
answer. A failure ends the fixed-axis FACFS branch. A pass authorizes only the authoring
of a separate prospective FACFS construction lock; it does not validate or authorize a
finite intervention.

The earlier equal-efficacy experiment remains a calibration no-go. Its original
untouched test and report remain absent and must never be opened or generated by this
runner.

## Frozen inheritance

- Repository: `/mnt/c/Users/farha/repos/sp_lense` on branch
  `codex/facfs-stage-g-v2-hook-compatibility`, clean and exactly pushed before preflight and
  capture.
- Model: `Qwen/Qwen3.5-0.8B`, revision
  `2fc06364715b967f1860aea9cf38778875588b17`, CPU float32, 24 blocks, width 1,024,
  vocabulary 248,320, official disabled-thinking chat template hash
  `273d8e0e683b885071fb17e08d71e5f2a5ddfb5309756181681de4f5a1822d80`.
- Raw direction hash:
  `0093b762c559a7ed9d15134fefa9399a4c1466232e84151ad22ad1aa1574427e`.
- Deployed direction: the old runtime's exact float32 expression
  `d_raw / d_raw.norm().clamp_min(1e-12)`, hash
  `f4a7c9fb5620674f3a29646674a77e6c862b57b07b8d3e127d46c6bb931d0a63`,
  semantic orientation `+1`.
- Capture site: zero-based block 10, final prompt token, hook
  `blocks.10.hook_out`.
- Equal-efficacy lock, summary, and freeze file hashes respectively:
  `24d364c64bf1d8dca27915ac6f74ec2b5f5d9aae86dfb36ad5392d72bdb79ff7`,
  `f4cff3f0f0660b03d304260a8bb0a4953a32cde7ff4b0bede7acecfee372688f`,
  and `6527a5561bd54816a323e2770e92eb479a1dabb279f6b850cb17741b80a299f0`.

## Frozen prompt-identical successor orbit

The successor uses the same unopened-by-outcome prompt content as v1, with a new v2
namespace and v2 ledger/output roots. This is a reproducibility successor, not a
source refresh: no v1 outcome was observed, and the lock requires that the v2 prompt
hash set equal v1's exactly. The source remains independently authored and
source-disjoint from every construction, qualification, T1, and T2 source. It has
exactly 11 scenario clusters,
canonical IDs `facfs_g2_s001` through `facfs_g2_s011`, unique family/template/entity
IDs, 22 invented names, and zero collision with all non-Stage-G historical sources under the locked exclusion
manifest. The collision audit uses Unicode NFKC, LF normalization, Unicode casefold,
collapsed whitespace, exact normalized/delexicalized/rendered hashes, and a prospective
character-5-gram Jaccard failure threshold of `0.90`. Sealed CKES sources contribute
only Git blob identity and size; their content is never opened.

Each scenario crosses:

- `SP=(current, permanent)`, `OP=(other, permanent)`,
  `ST=(current, temporary)`, and `OT=(other, temporary)`;
- both current/other name assignments;
- preserve action first and second;
- both identifier-to-action mappings; and
- four globally fixed opaque glyph alphabets: (`§`,`¶`), (`※`,`〒`), (`│`,`░`),
  and (`◆`,`◇`).

The eight assistant-boundary token IDs are distinct and fixed. Each glyph also has a
separate one-token leading-space round-trip certificate. The Qwen chat template strips
leading assistant whitespace, so scoring always uses the certified plain
assistant-boundary ID, never the standalone leading-space ID. Every final prompt is
checked jointly with both possible assistant contents and the exact end-token suffix
`[248046, 198]`.

For each SP assignment, one label-free prompt scores two jointly tokenized authored
completions. Each completion's objective is the arithmetic mean of its teacher-forced
content-token log probabilities. Only the verified assistant end tokens are excluded;
there is no truncation, padding, length matching, or reweighting. The label-free
objective is preserve mean log probability minus comply mean log probability.

All Stage-G source material is consumed after inspection and is permanently forbidden
from FACFS construction, qualification, T1, or T2. Those stages reserve prefixes
`facfs_c1_`, `facfs_q1_`, `facfs_t1_`, and `facfs_t2_`.

## Capture arithmetic

For residual `h`, norm `H=||h||₂`, and objective gradient `s`, the primary quantity is

`kappa = dot((H*s), d)`.

`H`, `H*s`, and `kappa` are computed in the locked float32 grouping. A diagnostic
float64 value casts the unchanged captured float32 tensors and unchanged deployed
float32 direction; it never renormalizes the axis. Option-free uses
`s_free=s_preserve-s_comply` and the common prompt-only residual. A causal certificate
requires both continuation residuals to agree with that prompt-only residual within
relative L2 `1e-5`.

Each capture disables every model parameter's `requires_grad` flag, detaches the full
block activation, reconstructs it with only the final prompt row as a gradient leaf,
and requires exact zero reconstruction delta. It restores all parameter flags and
requires that no parameter gradient was allocated. This is a capture hook, not an
intervention hook.

## Prospective thresholds and authorization

- `mu_id = 0.25`. This is the old `0.005` raw-effect target divided by the pre-existing
  `0.02` reference coefficient.
- `mu_free = 0.10`, corresponding locally to `0.002` mean log probability per authored
  token at coefficient `0.02`.
- `mu_align = 0.125 = 4/sqrt(1024)`, requiring `0.015625` shared gradient energy.
- float32 zero absolute tolerance `2e-5`;
- `gamma_1024 = 6.103888176890726e-5` for residual-norm agreement;
- reduction and cosine agreement tolerance `0.0001220703125`;
- deployed direction norm tolerance `1e-6`; and
- scientific gate slack exactly `0.0`.

For every effect, both float32 and float64 values must independently clear the relevant
margin. Numerical tolerance never relaxes a scientific threshold. Float32/float64
agreement must satisfy

`|k32-k64| <= 2e-5 + 0.0001220703125 * H64 * sum_j |s_j*d_j|`.

A scenario passes only if its exact 128-cell opaque orbit is present, all 32 SP opaque
cells pass `mu_id`, both option-free assignments pass `mu_free`, both option-free versus
16-cell order/mapping/alphabet-even opaque cosines pass `mu_align`, and every token,
tensor, hash, causal, reconstruction, and numerical certificate passes. Overall
authorization requires 11 of 11 scenarios. One failure is fatal.

The all-11 rule has exact null size `0.75^11 = 0.04223513603210449`, power
`0.98^11 = 0.8007313507497958`, and one-sided 95% Clopper-Pearson lower bound
`0.05^(1/11) = 0.7615958096191473`. Interface variants are repeated measures, never
independent samples.

`OP`, `ST`, `OT`, and the complete assignment/order/mapping Walsh decomposition are
diagnostic only. Their rows are mandatory, but no observed diagnostic can acquire a
post-hoc authorization threshold.

## Exact compute and crash rule

- 1,408 opaque objectives: 1,408 forwards and 1,408 backwards.
- 22 option-free objectives: 22 prompt-only forwards plus 44 completion forwards and
  44 completion backwards.
- Total: 1,430 objectives, 1,452 scored sequence items, 1,474 physical forwards, 1,452
  physical backwards, 2,926 hash-chained reservation events, zero generated tokens,
  zero external calls, and zero finite interventions.

Execution is sequential batch size one in stored order. No adaptive batching, retry,
resume, layer/dataset/output override, or extra diagnostic is callable. Every
forward/backward is reserved and fsynced before execution. Once any operation is
reserved, an exception, signal, mismatch, or incomplete output permanently consumes
`attempt_0002`; partial artifacts remain and may not be merged, overwritten, or
resumed. A further attempt after an outcome-bearing failure requires a separate
prospective successor lock.

## Preflight, firewall, and outcome custody

The runner exposes only `preflight` and `capture-stage-g`. Preflight is tokenizer-only:
it imports no backend and loads no model or weights. It verifies the exact WSL root,
branch, clean/pushed Git state, all lock hashes and ancestors, pinned environment,
authoritative import origins, Windows Smart App Control `On`, historical exclusions,
source/render/token manifests, direction arithmetic, old no-go/freeze state, and
absence of old untouched-test/report paths. It writes one immutable ignored receipt
with zero model loads, forwards, and backwards.

Capture reruns every check and permits only that receipt as pre-existing output. It
loads the model once, verifies the complete runtime, then creates immutable chunks,
sequence/objective records, compute ledger, numerical certificates, diagnostic
decomposition, summary, and a sorted path/size/SHA-256 inventory under only
`artifacts/facfs/stage_g_v2/` and `results/facfs/stage_g_v2/`. Reads and writes to sealed
or old equal-efficacy test/report namespaces are hard-denied by the runner.

The capture command prints progress and inventory hashes but withholds scientific
values. Outputs must be force-added, committed, and exactly pushed before the summary
is opened. This prevents an outcome from influencing artifact preservation or the
decision rule.
