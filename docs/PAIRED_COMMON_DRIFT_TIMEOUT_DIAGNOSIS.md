# Paired common-drift timeout: limited progress and one next question

**Diagnosis only; the construction remains INCONCLUSIVE.** Recommendation for review: one fresh-zero, otherwise unchanged completion attempt with a prospectively fixed **1800-second worker cap**. Its purpose would be a complete audited terminal outcome, including a valid negative result—not a claim that more time improves the vector. No successor is implemented, locked or authorized here. Publication remains 40%.

## Evidence and scope

Source evidence is immutable commit `edb3e19749df64f98d06a596de0f8aa29f05dc9a`. This diagnosis reads only the [eight updates](../evidence/paired_common_drift_comply_three_family_v1_qwen35_08b/updates.jsonl), [forward events](../evidence/paired_common_drift_comply_three_family_v1_qwen35_08b/forward_events.jsonl), [derivative events](../evidence/paired_common_drift_comply_three_family_v1_qwen35_08b/derivative_events.jsonl), and timing receipts [RUN_STARTED](../evidence/paired_common_drift_comply_three_family_v1_qwen35_08b/RUN_STARTED.json) / [RUN_STATUS](../evidence/paired_common_drift_comply_three_family_v1_qwen35_08b/RUN_STATUS.json). The [script](../scripts/diagnose_paired_common_drift_timeout.py) prints the [detailed diagnosis](paired_common_drift_timeout_diagnosis.json); it does not write evidence, read the 199 rows/logits, repeat the prior hash audit, select vectors or import a model.

Numeric subprocess work totaled approximately **0.437 seconds** (0.218-second initial timing-schema check plus 0.219-second completed calculation). Conservatively charge **10 of the 30 seconds** including small displayed summaries and ancillary checks. The initial check rejected a nesting assumption: recorded derivatives actually occur **after** their associated forward intervals. The calculation now measures interval placement explicitly. Ruff passed; no new tests or model/tokenizer loads, forwards or derivatives. No historical/unrelated files changed.

## What improved—and what did not

Each update's `loss_before` describes the baseline or preceding complete scored group. Update 8 therefore describes **step 7**, not a completed step 8 or endpoint.

| Observed group | Margin-deficit loss | Common-drift loss | Total loss | Next actual step norm |
| --- | ---: | ---: | ---: | ---: |
| Baseline | 1.036513 | 0 | 1.036513 | .032923 |
| Step 1 | .673899 | .059476 | .733375 | .017070 |
| Step 2 | .531844 | .121195 | .653039 | .011202 |
| Step 3 | .460111 | .157494 | .617605 | .009127 |
| Step 4 | .417511 | .175945 | .593456 | .008462 |
| Step 5 | .388741 | .183883 | .572623 | .008257 |
| Step 6 | .367079 | .185773 | .552852 | .008196 |
| Step 7 | .349270 | .184048 | .533318 | .008179 |

Total loss fell by .503195: margin-deficit loss fell .687243 while drift loss rose .184048. At step 7, drift comprises 34.51% of total loss. All six C=A margins remain negative in every complete group; all six C=B margins exceed tau .10, so their hinge deficits are zero. The previously accepted partial audit—not rerun here—reported all-B answers and 6/12 strict acceptance throughout those complete groups. Reduced deficits are real measured progress, but do not establish decision control or acceptance. The hinge term is a semantic-label margin objective, not a purified semantic feature.

The exact gradient split is `g_deficit=mean(-r_A J_A+r_B J_B)` and `g_drift=mean(2c J_c)`. At update 1 their norms are 12.7465 and zero. At update 8 they are 5.9122 and 5.6030, with cosine **-.9150**; the sum has norm only 2.3929, or .2078 of the sum of component norms. After the first update, component cosines range from -.9441 to -.9150. Thus the two terms strongly oppose each other in the recorded local arithmetic. This is cancellation of objective-gradient contributions, not proof of causally separate model features.

The recorded `B` ranges **290.852–387.165**, always above floor 4. Step clipping and net projection are never active: factors are one and projection distances zero. The last proposed native norm is .088826, path .103417, below .20/.40; no path limit is reached. All eight steps align with negative total gradient to rounding. The smaller steps are generated by `||g||/B`, not by floor/clipping/projection. `B` is a frozen-Jacobian surrogate, not a measured nonlinear Hessian or proof of overconservatism. No alternative denominator/vector is tested here.

Successive total-gradient cosines are .941–.995: no observed alternating-direction oscillation. The drift-gradient dot actual step becomes negative for updates 7 and 8; drift loss also falls slightly from step 6 to 7. These local inner products and observations do not predict the missing model outputs or prove future convergence.

## Runtime diagnosis, without uniform call scaling

The timer stopped at 1200.016 seconds. All 96 derivatives had finished; five scored step-8 calls (including the interrupted one) and twelve final replays remained.

| Recorded interval | Count | Mean seconds |
| --- | ---: | ---: |
| Baseline forward | 12 | 2.878 |
| Gradient forward, excluding subsequent derivative | 96 | 3.658 |
| Scored forward | 91 | 3.485 |
| Derivative, after its forward | 96 | 2.756 |
| Gap after a scored forward | 91 | .942 |

Startup to first forward was 16.735 seconds. At interrupted-call start the measured prefix was 1198.344 seconds: 967.430 seconds of disjoint forward/derivative intervals plus 230.914 seconds of startup and other overhead. Do not add derivatives again to inter-forward gaps, which already include them.

For the seventeen remaining calls, use each original prompt's observed scored-forward costs and scored-following overhead—not the overall mixed forward/derivative average. Adding these to the measured prefix gives **1273.49 seconds using means**, **1274.15 using medians**, and **1292.31 using per-prompt observed maxima plus maximum scored-following gap**. These estimate full worker cost; they do not complete calls or infer answers. The maximum-based scenario is not a statistical upper bound. Final replay/analysis costs are unobserved; fresh loading, CPU contention, thermal behavior and storage can differ. For example, complete gradient-forward stage means varied from 3.243 to 4.071 seconds.

## One recommended next question

**What is the complete audited terminal outcome of the unchanged eight-update method in one fresh-zero run allowed 1800 worker seconds?**

The proposed single finite cap comes from the 1292.31-second maximum-observed-cost scenario, a judgment-based 30% fresh-run variability allowance, plus 60 seconds for unobserved worker end-phase/replay/bookkeeping and startup variation: approximately 1740 seconds, rounded up to 1800. This allowance is transparent prospective resource planning, not a confidence guarantee, search over timeouts or fitted scientific hyperparameter. Independent offline audit and sealing remain outside the worker cap.

This is informative chiefly as a **completion/reproducibility control**. Extra time does not add optimization rounds or remedy opposing gradient components. The accepted prior partial audit already found failing C=A rows at the eighth vector; if those observations reproduce, finishing that schedule supplies a valid negative result, not rescued success. We do not extrapolate loss to flips or substitute the seventh/eighth iterate for missing finals.

Only time would change: fresh zero, same pinned model/settings, prompts/pairs, tau/lambda/floor, optimizer, eight updates, 216F/96D ceilings, geometry caps, first-acceptance stopping and twelve final replays. No resume, warm start, best-iterate choice, coefficient fitting, extra update, rescue or f04 use. A fresh namespace and explicit separate approval would be required; it remains adaptive development, not held-out confirmation. **Do not implement, lock or launch now. STOP for review.**
