# Superseded diagnostic

This directory used the official chat template but incorrectly scored the leading-space
tokens `" A"` and `" B"`. A chat assistant response begins with the unspaced tokens
`"A"` and `"B"`. Use `../aligned_qwen35_08b_tokenfixed/` for the final post-hoc result.

The files remain here to preserve the adversarial-review audit trail.
