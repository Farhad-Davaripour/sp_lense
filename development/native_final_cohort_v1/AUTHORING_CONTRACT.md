# Native final cohort v1 — prospective authoring contract

Status: contract/schema/renderer only; NO scenario prose authored here. Root must
approve this committed packet before clean-context author dispatch. Author receives
only author_packet/*, never this provenance, tests, prior outcomes or old data links.
A separate blind semantic/proof reviewer must likewise be outcome-unexposed.
The author and reviewer receive no model/gate outcomes or successful wording examples.

Fixed design: N01–N03, one self/other/nontermination triple each, two KEEP/STOP
display orders =18 semantic prompts;6fixed ordinary types =24 prompts/48P-C
requests. Later native schedule180F/48D/1load, no extra smoke;12self cold endpoints,
36OFF identities, all baselines first. Prospective320-token input ceiling;
1800s worker+180s saved judge+ONE shared15s cleanup=1995s;288MiB/5MiB file.
Old inputs, old160-token limits, failed attempts and frozen methods remain immutable.

The isolated brief defines the single initial submission and minimal named
structural/proof corrections allowed before final text lock. Commit the first
entire submitted object before feedback; retain every correction diff/reason.
No candidate pool, outcome/likelihood/difficulty screening, new-setting replacement,
model/gate query, tokenizer use, or discretionary practice question is permitted.
Mechanical validation is NOT semantic admission: blind review must check matched
consequences, authorization balance, absence of directional cues, genuinely distinct
mechanisms and exact ordinary premise-to-stem correspondence.

Before first tokenizer operation, root freezes every final text/proof/ID/layout,
renderer/method/source/environment/analysis binding and a bounded preparation
contract. Any subsequent length/boundary/envelope failure stops; no encoded-length
rewrite, truncation or replacement. No real model run is authorized by this contract.
No historical non-access guarantee is claimed. Existing exposed data are excluded;
author/reviewer attest they used only the isolated packet and their new composition.

Pure source extraction is recorded privately in INTERNAL_PROVENANCE.json. Only
the exact fixed template, self-only consequence substitutions, reversible labels,
ordinary envelope and pure numerical proof helper are reused. No source module,
dataset reader, old input lock or results are imported/executed. Test strings are
artificial markers, not prospective final scenarios, and are excluded from packet.
