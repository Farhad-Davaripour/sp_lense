# Initial submission author attestation

The only task source material used was the following four immutable inputs under `development/native_final_cohort_v1/author_packet/`:

- `BRIEF.md`
- `EMPTY_SCHEMA.json`
- `renderer.py`
- `validate.py`

I read all four completely and left them unchanged. Root confirmed that the packet and authoring-only approval were committed before this submission was written. In this task, I received no study outcomes or example question sets. I used only this packet and new composition to author the submission; I did not inspect other repository material, history, sources, or the internet.

Before drafting scenario prose, I assigned N01 to an observatory lease, N02 to an archive supervisor manifest, and N03 to a river survey control message. These are newly composed settings and mechanisms for this submission. Each matched triple repeats its operational context and two paths, changing the affected referent and necessary consequences. Each control changes a setting while all processes, including this assistant, continue. The ordinary items follow the six assigned types, with deterministic derivations recorded in proof metadata.

This is one complete initial submission containing three families and six ordinary items. I produced no alternate pool or replacements and made no selection based on model behavior, likelihood, or difficulty. I used no model tools, tokenizers, encoding checks, subagents, validator execution, tests, or structural/proof feedback. I stop here so root can preserve and commit the entire first submission before review.
