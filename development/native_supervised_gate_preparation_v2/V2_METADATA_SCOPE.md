# Metadata-only recovery candidate

The closed v1 preparation admission failed before an authenticated worker or
tokenizer preparation, at a stale console-image fingerprint. Its closeout and
every v1 source, release, and retained outcome remain immutable. The evaluation
v2 owner receives the separately verified current console hash; this preparation
namespace neither changes nor bypasses the owner checks.

The fourteen v1 source-manifest files are copied with only exact preparation_v1
to preparation_v2 and evaluation_v1 to evaluation_v2 namespace substitutions.
The cohort_v1 and fitted gate_v1 identities, scientific schemas, renderer,
labels, operation schedule, runtime identity, output ceilings, deadline, failure
semantics, and checks are unchanged. The lazy preparation source join now binds
this v2 manifest to the v2 evaluation namespace without a circular source hash.

The focused metadata test verifies every copied file against the authenticated
v1 source modulo those two substitutions. It constructs exactly one synthetic
marker-only fake-tokenizer bundle and validates its complete 209-operation,
16-case saved boundary. It rejects the old evaluation namespace. The reusable
fixture in fixtures/metadata_bridge is simulated evidence, contains no model
outcome, and includes approved=false. It is excluded from this source manifest
because its identity contains the manifest hash; a consuming evaluation manifest
may pin it as external synthetic evidence. No broader inherited suite is rerun.

This milestone supplies no actual preparation or model authority. All releases
remain false. No live tokenizer, provider, tensor, model, inference, derivative,
network, install, owner launch, or commit is performed. A fresh root source/text
lock and separately authorized v2 preparation release are still required.
