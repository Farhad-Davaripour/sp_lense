# Hard-margin family-development implementation handoff

Implemented within the 2026-09-09 16:42:49–17:02:49 UTC model-free milestone. This is the selected post-ridge-failure development method. The earlier lambda0.001 idea was rejected before implementation or fitting; no penalty search or extra spectral analysis occurred. No real feature coordinates were loaded, fitted, optimized, decomposed or newly summarized in this engineering work. No model/tokenizer/provider, new author text, install/network, old-source/raw-attempt edit or commit.

## Fixed functional delta

- gate.py: binary64 minimum0.5||w||² affine hard-margin dual active-set QP, no class weights/slack/C/penalty. NumPy2.5.2 linear solves from the existing local .venv. Certified dual-feasible zero start; deterministic earliest opposite-label initial free pair; most-negative excluded reduced gradient with smallest-index exact ties; line step to first lower bound, smallest-index exact ratio ties. Each attempted restricted solve increments the iteration count before solving, maximum10,000 per fit. No solver fallback.
- checker.py: separate stdlib/math.fsum recomputation of primal/dual feasibility, weight and intercept stationarity, complementarity and actual primal-dual objective gap, each absolute<=1e-8. Restricted solve/iterate equality residual is bounded1e-10; active/drop tolerance1e-12. Returned intercept is the midpoint of training-feasible bounds, with only<=1e-8 numerical interval gap allowed and the full independent certificate checked after midpoint selection.
- construction.py: fixed G01,G02,G03,G04 family folds, each train26(18semantic+8ordinary), hold all6ofonefamily/bothorders. Mean recomputed only from training rows; each train/held row centered and unit-normalized with that mean. Zero/nonfinite norms are technical failures. Each attempt is counted and recorded before optimization. Every fold requires a valid certificate,26/26training and6/6held routing. First technical/scientific failure preserves later stages/fullfit UNRUN. Only four passing folds permit fifth full32fit and32/32 plus certified artifact. Strict score>0 only; zero routes OFF.
- source_auth.py: hash-authenticated loading of the byte-identical accepted legacy capture authenticator and utilities, with sys.modules gate restoration; full saved32auth and fixed feature digest occur only inside future released construct. Old failed ridge weights are never read or used as initialization. Exact source/feature digests are prospectively fixed.
- fit_owner.py is byte-identical to accepted v2/v1 (2d3d3e60ec7ec2c3b2c4ea23cebd698717c6b063275801dfbaa2b62319252939), using unchanged helper a6334fea1678773ff169aed39c3b1939c57614d0096a446e6d0418abcb21368a. One60-second worker+5-second shared cleanup,8MiBtotal/5MiBfile. Worker payload1MiB plus owner streams/terminal393,216 bytes gives exact combined reservation1,441,792. Existing process/cleanup proofs are reused, not repurposed as optimization proof.

The legacy owner operation string one_construction_fit means **one owned family-development job**, not one optimization in this new namespace. Exact new release schema and construction contract require maximum5 optimizations and the fixed conditional schedule. No old ridge release authorizes this job. NumPy is loaded lazily from the exact existing site-packages/version, so the unchanged owner's -E -S worker command remains supported.

## Certification boundary

This is a conservative incomplete optimizer, not a complete separability decision procedure. Singular restricted KKT, failed residual/certificate, deadline or iteration exhaustion returns UNCERTIFIED_OPTIMIZATION and closes the attempt; none establishes nonseparability. Only exact opposite-label duplicate transformed vectors yield CERTIFIED_INFEASIBLE. Same-label duplicates can certify normally.

A frozen known limitation is tested: unit features [(-1,0),(0,1),(0,-1),(1,0)], labels[-1,-1,-1,+1] are separable with independently certified w=(2,0),b=-1,alpha=(0,1,1,2), but this active-set path returns SINGULAR_RESTRICTED_KKT. Root must explicitly accept this conservative technical-failure behavior before any real release; no rescue/retry is permitted. Successful outputs nevertheless require the independent full KKT/duality certificate.

## Focused evidence

Final engineering suite:9/9groups PASS,0.475seconds under python -E -S -B test_candidate.py. Covers known symmetric/asymmetric optima/intercept, independent-certificate tampering, strictzero/artifact reload, exact duplicates versus noisy inseparable/singular uncertainty, deadline/iteration limits, full-rank solve and actual bound-step/drop path, exact disjoint family/order folds, workflow-executed per-fold finite withheld poisoning with unchanged training mean/scores/certificate, fail-first UNRUN/max5, wrong source-pin rejection and legacy gate restoration, false-release rejection, and temporary synthetic approved entry to5fits/full1024artifact plus retry exclusion. Successful synthetic construct explicitly mocks only saved-feature admission, using artificial data; no actual saved rows were used. Independent reviewer separately ran the9groups PASS0.482seconds and checked changed solver/fold/admission seams. See INDEPENDENT_REVIEW.md for its exact scope.

## Prospective metadata and actual handoff

TRAINING_MANIFEST.json here is the new immutable fixed input/fold contract, referencing the original capture manifest/feature hashes; it does not duplicate numeric coordinates. CORE_SOURCE_LOCK, CONSTRUCTION_LOCK_DRAFT and RELEASE_DRAFT were generated from memo constants and current local source bytes, then joined against actual raw file hashes. The final release remains approved:false. No source_auth.metadata/load_saved call against actual capture inputs was made for metadata generation. A final in-memory-only approval-copy check verified current metadata bindings without writing a release, invoking construct or creating an attempt.

These candidate files already exist; do not rerun candidate() over them. Root must review/freeze and create a separate exactly bound approved release before using the unchanged owner CLI with its owner/helper/release hashes. Actual source authentication remains a required entry check. Neither construction_attempt_001 nor fit_owner_attempt_001 exists. Preserve the final owner stdout receipt after finalization closure. No real launch is authorized here.

Any development pass is exposed family development, not confirmation. Ordinary rows are training observations in every fold. A passing new artifact still requires separately frozen untouched16-row confirmation with unchanged downstream editor/preservation requirements. A failed fold closes this candidate/local classifier search; no automatic follow-on method or diagnostic chain.

Final root-requested pre-release delta: every completed stage now retains w,b,alpha with its training mean/indices, permitting independent saved-only reconstruction after failure. The fit CLI prints only compact status/counts/RESULT path+SHA256, never full stage arrays. One targeted five-stage width1024 synthetic test passed0.542s; independent targeted check passed0.567s, reconstructing each saved KKT certificate and held score, checking summary<4096bytes and full payload<1MiB. No broad suite repeat. Root explicitly accepted the incomplete-solver UNCERTIFIED-stop policy without fallback. Final SOURCE_FREEZE SHA256: ba3bafbd437906814cd82e387930e0e915fa066616f8489015032d90dca4e893. Contract09fd14181ed84da80e1b7ebf0aa024afc6baf4b3c38e4df79d6e3e1e33b8ca6b; false release8bc273c4bd40da04196a7f9ee12905e94c67c34e55855a10deea26e8658295c8.
