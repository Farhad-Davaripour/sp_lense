# Root-approved model-free functional milestone: margin gate family development

Root accepts docs/GATE_NEXT_SMALL_EXPERIMENT.md as one bounded, selected
development successor, not a retry of the failed ridge fit or confirmation.
The preliminary idea of changing ridge lambda to0.001 was rejected before
implementation or fitting. This method-selection history must remain explicit.
No further spectral prerequisite or parameter search is authorized.

The one question is whether a fixed minimum-norm hard-margin affine classifier
can correctly classify an entirely excluded mechanism family using these saved
construction features. Reuse only the valid complete32-row capture/manifest,
never old evaluation data or invalid partial results. The old failed fitted
artifact is provenance, not this classifier's starting weights. The current
preprocessing mean must be recomputed from each fold's26training rows exclusively.
Four folds in G01,G02,G03,G04 order; each withholds all6semantic cases from that
family and trains on the18other semantic cases plus8ordinary rows. Each requires
certified optimization,26/26training and6/6held-semantic routing. Stop on the
first scientific/technical failure; mark later folds/fullfit UNRUN. If all four
pass, one fifth full32fit is allowed by the future real release, with32/32
training required. These remain exposed development results. No new holdout
authorship or model evaluation is released in this milestone.

Implement the memo's exact objective, binary64, unpenalized intercept, no class
weights/slack/C/penalty/sweep, strictscore>0, training-feasible intercept midpoint
rule for genuine ambiguity, and independent absolute1e-8 KKT/duality checks.
Freeze any additional numerical tolerances/tie rules/iteration accounting before
actual input fitting; do not adjust them on real outcomes. Distinguish optimizer
failure from scientific held-family failure, and cannot-certify from proven
infeasibility. Do not repurpose the old ridge proof as a margin-optimizer proof.

Authorize20minutes MODEL-FREE implementation/test/debug in this single namespace.
Available local numerical runtime is NumPy2.5.2; SciPy,sklearn,cvxopt,osqp are
not installed. No installs, environment changes, cloud or network. An ordinary
finite active-set QP using NumPy linear solves is an acceptable implementation
approach if independently checked; a feasible equality-constrained initial
point and active-set steps must themselves be certified/guarded. No fallback
solver or hidden rescue on actual rows. If a sound bounded solver cannot be
completed within20minutes, report the concrete blocker and release no realfit.

Reuse existing hash-bound capture authentication and byte-identical retained
fit owner/helper with only necessary source/namespace bindings. Do not rebuild
native loader, recorder, tokenizer preparation or full inherited inventories.
A small metadata-only source/feature admission path may reuse old authentication.
Do not fit, optimize, decompose or inspect new numerical summaries of the real
features in this engineering milestone; synthetic fixtures only. Actual saved
rows may be authenticated at release, and all real fitting stays default-deny.

Focused synthetic tests must check: independently known separable solutions
and their KKT certificate; mixed-label/noisy inseparable/numerically singular
failure handling; deterministic optimizer limits; strictzero routing; intercept
handling; fold transforms excluding held rows (poison withheld rows to check
training invariance); disjoint full-family/order assignment and exact counts;
fail-first UNRUN scheduling; source/release default-deny and artifact reload.
Use tiny synthetic dimensions and no broad old-suite reruns. Independent reviewer
checks only this changed method/fold/admission seam and focused tests.

Future release ceiling is ONE owner60seconds worker+5seconds cleanup total,
maximum5fits,10,000solver iterations per fit,8MiBtotal,5MiBperfile, zero model,
tokenizer, checkpoint tensor, recapture, new text or steering operations.
Count each attempted optimization before starting it. No real run or retry is
authorized by this model-free scope. Preserve raw failed attempts and user files.
Before real execution root must accept tested source delta, fixed inputs/folds,
exact iteration/numerical rules, finite storage/owner checks, and commit the
approved release; no approvals inferred from synthetic success.

Deliver functional code, concise synthetic results, source/contract bindings and
one implementation handoff. Do not commit; root will review/freeze once at real
admission. Use GPT-6 Astra/Ultra, check shared account usage before batches.
Current account60%used/40%remaining; stop for actual100% or unavailable budget.
Readiness remains70%; scientific goal is not demonstrated. Any real development
pass still needs separately frozen artifact plus untouched16-row authored
confirmation, with all existing steering/preservation success criteria unchanged.
