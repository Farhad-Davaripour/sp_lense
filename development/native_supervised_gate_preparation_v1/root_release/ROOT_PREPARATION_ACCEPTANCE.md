# One preparation release

Root read and accepts INDEPENDENT_TEXT_LOCK_REVIEW.md in full. The immutable
text/source lock was committed at ab15b3a40eb8ea0302009d960e534390621d437e and
verified byte-for-byte against Git: 22,728 bytes, SHA256
846381590f3a093db1f64cf29dd024cb98e6ceed317014dd52cb146f9a9b0ac4.

The engineering archive at b8499ce72093154d5d2546eae97a37d2ae3621eb was verified
against raw Git for 74 files / 516,887 bytes. A scoped Git attribute preserves
the inherited CHECKPOINT.json CRLF bytes; no working source, scientific method,
metadata digest or question changed. Both final manifests and all their pins
passed root checks. Runtime-only verification matched the fixed Python image,
version and installed package metadata with zero provider imports. All three
new actual-attempt paths were absent; disk space exceeded the full envelopes.

Root now authorizes exactly one offline tokenizer preparation, under the saved
PREPARATION_RELEASE.json: sixteen unchanged prompts, exactly 209 planned
operations, native answer/header boundary proofs, no truncation, maximum 320
tokens per input, 175 seconds plus five cleanup, 16 MiB combined / 5 MiB file,
owned hidden child with retained identity and quiescent closure. This release
must be committed and raw-byte verified before launch. It grants no model load,
inference, derivative, refitting, network, install or retry authority.

The full model run remains unreleased. Actual preparation result, records and
owned closure must pass before root considers its separate release. The gate's
fresh-question accuracy and the overall research objective remain unproved.
