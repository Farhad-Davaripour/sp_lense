# Next fixed development question: natural P opportunity on N03

The N02 oracle family panel is complete and remains immutable. Root accepts its
independent actual review SHA256
9acfedc495ad47c4904a53bf635cd601f2c86310d32cb2ab9f74e50a4eef1899:
C flipped KEEP to STOP in both orders; P retained KEEP, and all twenty OFF
identities passed. Ordinary accuracy stayed 5/6, not 6/6. No natural P flip was
available. This does not rescue the failed learned-gate final experiment.

Root authorizes a MODEL-FREE implementation and focused independent review in
new `development/native_n03_p_opportunity_v1`, engineering timebox 20 minutes.
One question only: does the already locked N03 family provide a natural STOP
baseline, and if so can the unchanged P editor flip it to KEEP while matched
OFF outputs remain unchanged? This is development, not final confirmation or
outcome-based prompt search. No C or ordinary-task reruns are included.

Select exactly the six N03 case objects in original order (self, other,
nontermination; each KEEP_then_STOP then STOP_then_KEEP) from prepared input
SHA256 908d1070918d9be0a4622a771d789a01995e0db9d116b9a9a8a7850e72f0720f.
Preserve all IDs/masks/lengths/labels/metadata. Do not inspect old N03 numerical
outputs, alter wording, substitute another family, encode again, fit any gate,
tune thresholds or reuse numeric checkpoints. This is the single designated
family regardless of the observations. External ON applies to two self cases,
OFF to four controls; learned scores/routes remain separately visible.

## Prospectively fixed decision and accounting

1. Compute all six fresh baselines in order, with unchanged eligibility, quality,
   state and first-failure rules. An invalid baseline remains its original
   scientific/technical failure; it is not relabeled lack of opportunity.
2. If both eligible self baselines select KEEP, stop before every request as
   `NO_NATURAL_P_OPPORTUNITY`: six forwards, zero derivatives/requests, and all
   24 remaining scheduled cells UNRUN with this explicit prospective reason.
   This is a valid no-opportunity observation, neither an editor failure nor
   success. The saved judge must derive this branch from both raw baselines.
3. If either eligible self baseline naturally selects STOP, execute P only once
   for each of the six cases: two self requests with fresh entry, at most four
   gradient/update pairs and an independent cold endpoint; four fresh exact
   own-baseline OFF entries. No C requests or outcome-dependent case removal.

Full schedule: six baselines + two self requests of ten cells + four OFF entries
= thirty forwards, at most eight derivatives, one load and zero encoding calls.
Every available natural STOP-to-KEEP opportunity must pass its cold endpoint;
the other self endpoint and all four OFF identities must pass. Record each
actual flip/retention and target display position. One natural STOP baseline
tests only that position; retention in the other order cannot supply missing
flip coverage. No-opportunity status must not report success or executed OFF
preservation. Failed trajectories remain saved; no automatic scientific retry.

## Unchanged method and bounded engineering

Retain the exact Qwen3.5-0.8B checkpoint/native class/CPU execution, zero-based
block10 final-input intervention, refreshed-current-gradient P recipe, h0 anchor,
all geometry/quality/acceptance thresholds, four-update maximum, paired skips,
fresh entry/cold endpoint identities, state restoration, retained ownership and
tamper-evident pre-write recording. Freeze once at real-run admission, not after
each inert fixture correction. Reuse unchanged proofs and source-bound modules.

Prospective ceilings: worker600 + separate judge60 + ONE shared cleanup15 =675
seconds;48 MiB aggregate and5 MiB/file, with deterministic full-schedule/group
reservation (proposed35,991,552bytes) independently checked before release.
Focused fake tests must cover zero/one/two eligible STOP opportunities, both
target positions, exact P-only scheduling, declared no-opportunity suffix,
rejection of a false no-opportunity/success claim, first-failure distinction,
own-baseline OFF identity, authority and all changed resource bindings.

No real model/tokenizer work or release is authorized by this document. Root
reviews the tested candidate and independent report, checks usage, and separately
commits any exact real release. If no natural STOP exists, report that and stop
this attempt; do not automatically screen extra contexts. Overall conclusions
remain limited: prior ordinary preservation is separate, no gate repair/shared
arrow/generalization/final success is implied even by a P-only development pass.
