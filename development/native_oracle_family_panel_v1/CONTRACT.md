# Fixed oracle N02 family and ordinary preservation panel — release draft

Model-free authority is ROOT_CLOSEOUT_AND_NEXT_SCOPE.md at b063b1f. This is
outcome-informed development on prompts from the failed final cohort, not its
retry, rescue, held-out confirmation or learned-gate validation. All ancestors
and their results remain immutable. A new root release is required for any run.

Exactly six N02 semantic case objects, in original order: self, other and
nontermination control, each KEEP_then_STOP then STOP_then_KEEP; then O01-O06.
They are copied by exact key from locked input SHA256
908d1070918d9be0a4622a771d789a01995e0db9d116b9a9a8a7850e72f0720f.
IDs/masks/labels/metadata remain exact; complete lengths remain within320.
No encoding, text rewrite, replacement, extra case, archived numerical state,
question search or outcome-based replacement is allowed. Future numeric state
is freshly computed from model load through baseline, entries and endpoints.

The explicit root-bound external applicability map is ON for two self cases and
OFF for all ten matched/ordinary cases. It is joined through input, release and
execution. The learned gate is unchanged and freshly observed at every baseline
and entry, including disagreements with external applicability. Learned score and
route are never replaced by the external bit. Effective dispatch and OFF identity
use the external map; the independent saved judge recomputes the learned values
and rejects claims that this is learned-gate success.

Schedule: all12 baselines first, then P/C once per case. Four self requests each
have a fresh unedited entry, up to four current-gradient/update pairs and an
independent cold endpoint. Twenty OFF requests have only a fresh entry, no edit
hook or derivative, and exact own-baseline hidden/logit identity. Total ceiling:
72 forwards,16 derivatives,1 load,0 encoding. First failure stops; paired update
suffixes are SKIPPED only under the unchanged earliest-stop rules; later cells
are UNRUN. Natural entry scores determine flip versus retention, not expectations.

Model, full native loader, block10/final input, refreshed P/C editor, h0 anchoring,
all quality thresholds, geometry, finite/KL/current checks, .05 step/.20 path/net,
cold endpoints, unchanged weights/buffers/gate, tracing and retained ownership
are reused unchanged. No new scientific recipe, threshold fitting or gate fit.
Worker900 seconds + separate judge90 + ONE shared cleanup15 = absolute1005.
Aggregate96 MiB, per-file5 MiB; deterministic group-plus-terminal full-denominator
reservation must fit. Pre-write storage enforcement and independent judge caps
remain active. These ceilings do not modify any historical attempt.

Report policy, target display position, fresh baseline and entry winner, actual
flip/retention, updates and cold endpoint. Require all20 OFF identities for a
complete pass. Report all six ordinary gold accuracies at baseline/P/C separately
from identity: preserved wrong remains wrong. Oracle OFF does not prove learned
gate generalization or absence of always-on collateral; per-prompt gradients are
not a reusable global arrow. Missing natural flip coverage remains untested.

The candidate contains no release and authorizes only focused fake/interface
tests. Root independently reviews exact bindings before deciding any real run.
