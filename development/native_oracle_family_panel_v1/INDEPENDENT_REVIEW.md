# Independent oracle family-panel candidate review

PASS for the bounded model-free candidate. No blocker was found in the changed multi-case applicability, input/schedule, OFF identity, ordinary reporting, authority or resource bindings. This review does not authorize model execution or confer held-out, learned-gate or final-success status.

## Scope and source findings

I read `development/native_oracle_applicability_control_v1/ROOT_CLOSEOUT_AND_NEXT_SCOPE.md` completely. Root's scope at `b063b1f`, SHA256 `28eaeb172aeb6525bc7a8561e78ec3542a882f927239b02e4d5af228df04f4f8`, authorizes this explicitly outcome-informed development panel only. The failed final experiment and completed one-case control remain immutable. No inherited prohibition is being bypassed by relabeling a retry: the scope prospectively names the expanded external-applicability treatment, exact already exposed inputs and separate development claims, without revising either prior result.

Input validation requires exactly the six N02 case objects in original self/other/nontermination and display order, followed by O01–O06, copied from locked source inputs SHA256 `908d1070918d9be0a4622a771d789a01995e0db9d116b9a9a8a7850e72f0720f`. IDs, masks, labels, lengths, metadata and per-item gold remain exact. N02 lengths are 160/164/167 for self/other/control in both orders; ordinary lengths are 66/70/42/46/63/65. Ordinary gold IDs remain 32/33/32/33/32/33. No tokenizer, prompt replacement or old hidden/logit/gradient checkpoint is used. Any future run recomputes numerical state from a new load through baseline, fresh entries and cold endpoints.

The root-bound applicability map contains strict booleans: ON for the two self cases, OFF for all ten controls/ordinary cases. Inputs, release and execution must agree. Learned gate score/route are still computed and independently checked at every baseline/entry. Dispatch and OFF preservation use the separate effective route, so a learned-ON/oracle-OFF control does not become an editing request. False learned-gate-success claims remain rejected.

The schedule has 12 baselines first, four self requests with mandatory fresh entries and independent cold endpoints, and 20 OFF entries. Complete success requires the exact case/policy request set. Every OFF request is bound to its own baseline, exact hidden state/logit bytes, unchanged edit/derivative counts and no additional forwards. The judge separately reports ordinary gold, actual token and correctness for baseline/P/C; preservation never changes a wrong answer into a correct one. Self outcomes derive flip/retention from fresh observations and report policy and target display position from the locked layout.

Diff review found only the authorized binding/schedule/report/cap changes. Eleven copied scientific, native-loader, tracing, ownership and configuration files are byte-identical to the accepted one-case ancestor. The editor, learned gate/threshold, quality and geometry checks, first-failure and paired-SKIP/UNRUN rules, source/path security, native state checks and retained-process machinery are reused, not broadly retested. Counts, release and saved judge consistently cap 72 forwards, 16 derivatives, one load and zero encoding. Wall budget is 900 worker + 90 judge + one shared 15 cleanup = 1005 seconds. Pre-write storage has a full-schedule/group-plus-terminal reservation of 84,295,680 bytes within 96 MiB, with 5 MiB/file ceiling. Source, scope, input and ancestor pins are acyclic; no real release or attempt exists.

## Independent focused test

Ran once with shell `login:false`:

```powershell
.venv/Scripts/python.exe -B development/native_oracle_family_panel_v1/test_panel.py
```

Exit 0; all 25 focused groups PASS. The complete tiny-autograd panel and separate model-free judge produced 44 forwards, two derivatives, two flips/two retentions, four cold endpoints, 20 OFF identities, 28 legitimate skips and zero UNRUN. Learned routes deliberately disagree with external applicability in both directions. All four policy/target-position reporting combinations were checked. Eighteen ordinary checks include six preserved-wrong O02/O05 rows with their correct A/B gold identities. These are artificial software-test observations, not natural model opportunities or study results. An ineligible-first-baseline fixture stops at one forward with 71 UNRUN.

Focused negatives rejected missing/reordered cases, changed gold/length, missing/reversed/non-boolean applicability, absent release/loader authority, counter overages, coherently rehashed learned-success/route claims, changed OFF effective route, foreign baseline, wrong gold/correctness claims and incorrect stopping reasons. An actual artificial file-size set above 96 MiB was rejected by the saved checker; an oversized individual publication was refused before creation. Provider/network/checkpoint guards reported zero real Qwen calls, tokenizer calls, old numeric-state reads or real release creation.

| Exact artifact | SHA256 |
| --- | --- |
| Reviewed/tested `SOURCE_FREEZE.json` | `629fe50d46f45169dc547ed40a0354057dbf2ccc1a150010ff3c98f99089abd7` |
| `inputs.json` | `f87fa5f36c50fd4319bf2b538a083c8d4ab42a99c402ce40e0a8ba1296aba664` |
| `test_panel.py` | `e00fbfe05e6e30e3948b1846406d3aa4878d5142543ca707dec7d5cb0ad87093` |
| Independently reproduced `TEST_RESULTS.json` | `b119b445472492d1f270b6c3358e7c5522a0c7359b7cd7bb2f2fd221b4b2a61e` |

All 23 local and 134 external pins verified after the run; source hash remained unchanged. Independent evidence is under `test_evidence/panel_1788921865202552300/`. Git comparison against `b063b1f` returned 0 for the final execution namespace and the one-case control's raw evidence/source freeze. Earlier tests and actual attempts are preserved.

I made no production source/input/threshold edit, gate fit, real model/tokenizer call, scientific retry, old sealed-data inspection, broad inherited-suite rerun, commit or release. Only this report was added outside the frozen inventory. Any real panel still needs root's exact prospective release and separate authorization. Even a future panel PASS would not establish learned-gate generalization, always-on safety, a reusable global arrow, unobserved natural flip coverage or final confirmation.
