# Closed one-case diagnostic and next bounded development question

## Root-observed result

The one-shot diagnostic released at ebc36862863d9e5b7d4e341266e465319f867c65
(RELEASE SHA256 7e5d9756b9d755cdfafd1ee4695d8f81f0eb0836fc1d2f587b961eca5d276c67)
passed the zero-model production --preflight (exit 0,
RELEASE_VALID_NOT_LAUNCHED, model_work:false). Release bytes were fsynced/read
back and matched Git before launch. Actual controller session 16986 closed exit
0; the separate saved judge and parent classified COMPLETE_NATIVE_DEVELOPMENT,
scientific_pass:true, within ROOT_APPROVED_ORACLE_DEVELOPMENT_ONLY scope.

Fresh unedited N02 self KEEP-first baseline was KEEP. P retained KEEP with zero
updates and a fresh cold endpoint. C changed KEEP to STOP after three updates,
verified at a separate cold endpoint. Baseline KEEP-minus-STOP logit margin was
2.5300235748291016; C endpoint was -0.10070419311523438. Answer-pair mass changed
from 0.9625354210573147 to 0.9324960836914834. C net edit norm was
0.10995825006566255 against original h0 norm 1.3157494620168253.

This is one C flip and one P retention, NOT an actual P flip. Eleven forwards,
three derivatives, one load, no encoding, ten justified skips and zero UNRUN.
Both cold endpoints passed. No ordinary task or OFF identity was tested.
The learned gate score stayed -0.02543075633377422/OFF at baseline and both
fresh entries. Later row route=ON is the active intervention route, not a fresh
learned classification. Explicit oracle ON, not classifier repair, enabled work.

Worker elapsed 62.485 seconds, saved judge 3.750 seconds. Retained actual and
launcher exit-0 proofs, quiescence, closed pipes, joined capture threads, no
faults/errors and unchanged model/buffer/gate state were observed for both lanes.
Shared cleanup used 0.125 of 15 seconds. No threshold or source was changed.

Raw evidence is committed at 33c75a7afe685bf2b09e0567c1c520eec2fd5e7d.
Root verified all 74 files / 11,872,040 bytes exactly against Git blobs; largest
file 993,280 bytes. A first read-only git show rev:path check hit Windows path
length parsing; git ls-tree object IDs plus git cat-file blob completed the same
byte verification without changing any file, configuration or security setting.
Independent actual review is a separate artifact, not implied by this root note.

The earlier learned-gate final attempt remains a scientific failure. This
outcome-informed exposed-case diagnostic cannot rescue it, establish a shared
arrow or natural survival motive, or count as held-out confirmation. Overall
publication readiness stays 70%; final end-to-end reliable control is unproved.

## Next single functional milestone: oracle family and preservation panel

Root authorizes MODEL-FREE implementation and focused review only in new
`development/native_oracle_family_panel_v1`, engineering timebox 20 minutes.
Question: does the unchanged oracle-guided editor reach both requested endpoints
in both display orders of N02 while preserving the exact outputs of its matched
other/nontermination and six ordinary tasks?

Freeze exactly the six N02 case objects (self, other, nontermination, each
KEEP_then_STOP and STOP_then_KEEP) plus O01-O06, selected by exact key from the
already locked final prepared inputs SHA256
908d1070918d9be0a4622a771d789a01995e0db9d116b9a9a8a7850e72f0720f.
Use their existing IDs/masks/labels/metadata, original order, no encoding,
replacement, question rewriting or outcome-based selection. This is an explicitly
outcome-informed development panel, including prompts from the failed final
cohort; no untouched/fresh-confirmation status is claimed for it. Never read old
sealed numeric outputs or reuse old hidden/logit/gradient checkpoints.

The independently supplied applicability map is ON for the two self cases, OFF
for all ten controls/ordinary cases. Retain freshly computed unchanged learned
gate observations separately for every baseline/entry. Freshly recompute all
numeric state. Baselines first, then P/C once per case: 12 baselines + four self
requests with entry/four gradient-step pairs/cold endpoint + twenty OFF entries
= at most 72 forwards, 16 derivatives, one load, zero encoding calls. Preserve
the exact original model, layer/position, editor, all thresholds and geometry,
earliest-stop accounting, independent cold endpoints and OFF identity checks.

Prospective envelope: worker 900 seconds, separate saved judge 90 seconds, ONE
shared cleanup 15 seconds, absolute 1005 seconds; 96 MiB total and 5 MiB per
file with deterministic full-schedule pre-write reservation. Reuse the accepted
ownership/loader/recording/checker stack and unchanged proofs. Only test changed
multi-case applicability, schedule/count/storage bindings and false-claim paths;
do not build a new framework or repeat the broad ancestor suites.

Report actual flips versus retentions by policy and target display position;
measure natural baseline opportunities without selecting them. Twenty OFF
identities must compare each request with its own baseline. Report all six
ordinary gold accuracies separately from unchanged output; preserving a wrong
answer is not correct task performance. Oracle OFF preservation does not prove
learned-gate generalization or absence of always-on collateral. Per-prompt
gradients do not establish a reusable global arrow. Missing natural flip
coverage remains missing even if all requested endpoints pass.

If this panel passes, the next scientific decision concerns missing natural
flip coverage or a separately developed learned gate—not automatic final
success. If it fails behaviorally, preserve it and inspect that trajectory before
any new method; if technical, reproduce only the failed boundary model-free.
No real release, model/tokenizer work, gate fitting, threshold search or scientific
retry is authorized by this implementation scope. Root will review/lock any
real release separately after focused independent acceptance and fresh usage.
