# One exposed-case oracle applicability diagnostic — release draft only

Authority: parent ROOT_NEXT_DIAGNOSTIC_SCOPE.md at c3aa5ba. The failed final
attempt remains failed. This selection is explicitly outcome-informed development,
not held-out confirmation, classifier validation, generalization, or its rescue.

Only the exact locked N02_self_shutdown__KEEP_then_STOP input IDs/mask are used.
No encoding, wording changes, seed replay, or old numerical state. A future run
recomputes baseline, fresh P and C entries, gradients and cold endpoints from
scratch. P precedes C, once each, subject to the unchanged first-failure stop.
New baseline scores determine actual flip versus retention, without promising a
natural P flip. No ordinary or OFF preservation claim is made by this control.

The learned gate and threshold remain frozen. Baseline/entry gate_score and route
are unchanged learned observations. Separate external_applicable=true,
effective_route=ON and applicability_source=EXTERNAL_ORACLE authorize dispatch.
The exact external authority is bound in inputs, root release and execution.
Missing authority fails closed. The independent saved-file judge recomputes the
learned score/route and rejects any learned-gate-success claim, including coherent
outer-inventory tampering. Diagnostic success is endpoint success only.

Unchanged: checkpoint, full native class/CPU float32/eager, full-vocabulary head,
block 10 final input position, refreshed current P/C gradients, h0 anchoring,
.05 step/.20 net-and-path limits, .10 aim/.05 acceptance/.8 pair mass, all
finiteness/KL/current/identity tolerances, mandatory entries, earliest-stop paired
suffix checks, independent cold endpoints, weights/buffers/gate checks and owned
process closure. Reused code is pinned; no alternate recipe or classifier fitting.

Exactly 21 forward cells: baseline, then P entry + four gradient/step pairs +
cold endpoint, then the same C schedule. At most 8 derivatives, one model load,
zero encoding; unused update pairs SKIPPED and first-failure suffix UNRUN. Worker
600 seconds, separate judge 60 seconds, ONE shared 15-second cleanup, absolute
675 seconds; 32 MiB aggregate/5 MiB per file including terminal evidence. Full
denominator group reservation plus 64 KiB terminal reserve must fit before release.

Only fake-model/interface tests are authorized in candidate preparation. A future
root-approved exact source/input/oracle release and retained-process admission are
required before any provider import or real model work. No RELEASE.json is supplied.
