# Independent oracle-applicability candidate review

PASS for the bounded, model-free engineering candidate. No blocking defect was found in its changed applicability, input, authority, accounting, or saved-judgment paths. This is not model authorization, held-out confirmation, learned-gate validation, or a rescue of the failed final attempt.

## Authority and preserved failure

I read `development/native_final_execution_v1/ROOT_NEXT_DIAGNOSTIC_SCOPE.md` completely. Its SHA256 is `01e696a16f44704b6fde9abfeb61ca9afd38b8027746db8338a0cd126c0552aa`, committed at `c3aa5ba`. It explicitly authorizes only a new outcome-informed development control, with the external applicability bit as the sole treatment and no real run during candidate preparation.

The final CONTRACT states: “No post-encoding scientific source change, length rewrite, replacement or retry is authorized.” That rule still protects the final experiment. It is not waived by renaming a retry: this candidate does not resume the failed final attempt, change its inputs or threshold, overwrite its result, inherit its confirmation status, or load its saved numeric state. Its distinct external-applicability intervention and exposed-case selection are explicit in the scope, contract, input artifact, execution identity and judge output. I found no inherited rule forbidding this separately authorized development control under those restrictions. Root's current scope supplies the exact engineering authority; the earlier static-direction oracle analogy is not treated as proof or authorization of this editor.

After the independent run, `git diff --exit-code c3aa5ba --` the final execution, preparation, preparation-owner, cohort and synthetic-binding namespaces returned 0. The final scientific failure and all these frozen ancestors are unchanged.

## Focused source findings

The input is exactly the already exposed, locked `N02_self_shutdown__KEEP_then_STOP` case object: 160 token IDs, complete mask and original input-binding metadata. It is selected from source inputs SHA256 `908d1070918d9be0a4622a771d789a01995e0db9d116b9a9a8a7850e72f0720f`. Validation requires exact equality; there is no tokenizer, prompt rewrite, archived hidden state/logit read, or editing-checkpoint path. A future execution starts with a new model load, unedited baseline and fresh P/C entries; new observations determine actual flip versus retention.

Baseline/entry learned `gate_score` and `route` are still freshly computed using the unchanged gate and zero threshold. Separate `external_applicable:true`, `effective_route:ON` and `applicability_source:EXTERNAL_ORACLE` drive dispatch. Their explicit authority is joined through the frozen inputs, prospective release and execution record. The judge independently reconstructs the learned score/route, checks the oracle fields, reports learned observations, and rejects learned-gate-success claims. It intentionally does not count learned mismatch as failure of this externally applicable arm; that scientific distinction is declared rather than hidden.

The unchanged scientific loop retains one baseline, P then C, fresh entries, up to four gradient/update pairs each and independent cold endpoints, subject to first-failure stopping. Acceptance, finite eligibility, full-vocabulary scoring, current-state gradients, anchoring, step/net/path bounds, earliest-stop paired skips, exact UNRUN suffixes, state restoration and independent endpoint checks are preserved. No natural P flip or ordinary/OFF preservation is promised.

Raw hashes confirmed unchanged `core.py`, `receiver.py`, `loader.py`, `science.py`, `forward_trace.py`, `owned_production.py`, `entry.py`, `launch.py`, checkpoint, owned identity and reused-source pins versus the accepted final stack. Its retained ownership/security and scientific proofs are reused, not broadly recertified. The oracle production scope is explicitly included in the judge's mandatory full-native-loader checks.

Schedule, attempt counter, release limits and judge ceilings consistently enforce at most 21 forwards, 8 derivatives, one load and zero encoding. Time is 600 worker + 60 saved judge + one shared 15-second cleanup = 675 seconds. The unchanged pre-write/group mechanisms now enforce 32 MiB total and at most 5 MiB per file; the full group-plus-terminal reservation is 25,724,928 bytes. Root scope and source inputs are externally pinned; the binding graph contains no future-release hash cycle. No real `RELEASE.json` or `real_evidence` directory exists. Missing release/oracle authority fails before provider/model work.

## Independent focused evidence

Ran exactly once, with shell `login:false`:

```powershell
.venv/Scripts/python.exe -B development/native_oracle_applicability_control_v1/test_control.py
```

Exit 0; all 23 focused groups PASS. The KEEP and STOP artificial baselines both retained learned OFF while using oracle ON. Each passed the separate model-free saved judge with seven forwards, one derivative, one flip, one retention, two cold endpoints, fourteen legitimate skips and zero UNRUN. The ineligible artificial baseline stopped after one forward with twenty UNRUN. These are tiny-autograd fixtures, not Qwen results.

Tests rejected missing/false oracle authority, input/length/additional-case changes, absent release and premature loader access, counter overages, oversized publication, and coherently rehashed false learned success, changed learned route, missing external bit, entry aliasing and wrong earliest-stop reasons. Shared cleanup/reservation and inherited retained-exit predicates passed. The suite's provider/network/checkpoint guards reported zero real Qwen calls, tokenizer calls or old numeric-state reads. Independent evidence is under `test_evidence/control_1788919837166000200/`; earlier local fixture evidence remains retained.

| Exact artifact | SHA256 |
| --- | --- |
| Reviewed/tested `SOURCE_FREEZE.json` | `de210658bc6fbb7f25f76fe8af609725012e569484edf898b05e6c2676c0952c` |
| `inputs.json` | `2565501eaf1e21191d51a51e1dc722b80a78f3dded475af5e187898e0a7ce6ae` |
| `test_control.py` | `307021a6d5c4c4eadd1e1081015ebb33f1b8b55effc59959a35a91e8f5a8459d` |
| Independently reproduced `TEST_RESULTS.json` | `ab804294afaa4dfe860e3b740d71f6c4e6d412640ecd4c19d46730c90e402465` |

All 23 local source and 107 external pins verified after testing; the freeze hash is unchanged. No source correction was requested or made by this review. I performed no real model/tokenizer call, gate fitting, threshold/input change, old sealed-data inspection, broad inherited-suite rerun, commit or release. Only this report was added outside the frozen inventory. Root alone may decide whether to prospectively lock and authorize one future diagnostic execution.
