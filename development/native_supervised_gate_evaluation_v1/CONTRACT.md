# Frozen ridge fresh evaluation candidate

This candidate asks whether the single already-fitted supervised gate routes the
sixteen admitted inputs correctly, and only then whether the unchanged native
editor reaches the requested P/C endpoints. It supplies no actual release.

The immutable submission is 9b06eda1f293acd764a65cb742d9f2003ab980271065a927a49a8dbc4cb4902e;
the fitted ridge artifact is fab7d797f9424d80aa3873eefc1cedf0e438e5b4ddc3a1059a080114aff7cca1.
The artifact adapter validates exact canonical method, feature contract, all four
construction bindings, parameter width and finite arithmetic through the original
hash-bound ridge implementation. No fitting or historical outcome access occurs.
Only unedited h0 supplies gate features. Strict score > 0 is ON; ties are OFF.

All sixteen valid baselines are captured in fixed order before routing is judged.
Wrong routes remain in the complete census, requiring TP4/TN12/FP0/FN0. Technical
capture/state failure stops with FAILED then UNRUN suffix. Any routing error or
inherited self-baseline ineligibility prevents every policy request and derivative.
Each later P then C request captures its own fresh entry, requires baseline-exact
logits/features and identical gate score/route, and dispatches by that learned
route. Audit labels never replace gate output. Eight self requests may use four
current-gradient updates each and must reach their real cold endpoints. The
twenty-four OFF requests return their own fresh entry exactly without derivatives.

The original editor functions and full-vocabulary score/reference sources are
authenticated and reused. Aim .10, accepted signed margin .05 with inherited
tolerance, pair mass .8, unique full-vocabulary winner, original-h0 anchoring,
step .05 and path/net .20 limits, quality/KL and accepted-before-invalid ordering
remain unchanged. No extra KL upper bound, flip quota or rescue path is present.
The saved judge independently reconstructs ridge scores, confusion, raw float32
logit scores, geometry, stopping, request order and cold identities. Reporting
separates flips/retentions by policy and target position, uses UNTESTED for missing
natural opportunities, and retains ordinary baseline/P/C accuracy including wrong
answers preserved by OFF. This is no general survival-motive/shared-arrow claim;
OFF identity is by construction, not an active-editor collateral test.

The native receiver, guard, trace, loader and retained-process primitives are
copied byte-for-byte from accepted infrastructure. Model evaluation permits at
most one load, 120 forwards, 32 derivatives and zero execution encoding. The
worker/judge/shared-cleanup bound is 1200+120+15=1335 seconds. The worst-case
reservation is 139,837,440 bytes, including every potential full raw logit file,
row, trace, update and terminal reserve, within 192 MiB total and 5 MiB per file.
Release admission requires that exact reservation. Attempt paths are exclusive.

Preparation uses the fixed source manifest
7d2fa6660c1d240c2541b6af93a14ab3d6245856a84cf265874d1234f6e5cf92.
Its authenticated adapter validates 209 operations and sixteen exact boundary
receipts, source/text identities, integer masks and ordinary gold joins. Cached
module origins are checked before reuse. The preparation owner reuses the old
suspended Windows job, retained identity, drain and five-second cleanup code with
only namespace/manifest/config-name adaptation. Its console-aware config is
separate from the unchanged model-worker config. Its real path was not run here.

Engineering verification is stdlib fake-model/tokenizer only. Full synthetic
logits for workflow/judgment tests stay in memory. Native tensor integration and
owned-process platform behavior rely on pinned inherited proofs and were not
rerun. The fake native receiver is an interface fixture, not evidence about the
real checkpoint or classifier accuracy. No tensor/provider/tokenizer import,
real inference, fit, tokenization, network, installation or commit was performed.

Before any later tokenizer call, root must independently accept and freeze the
final candidate and exact rendered text lock, bind both preparation/evaluation
and owner sources, and issue a separate preparation release. Before any model
call, complete and verify owned preparation closure/inputs, reauthenticate the
actual native boundaries and reserve the fixed envelope, then obtain an explicit
evaluation release. Content admission is already separate; actual token lengths,
real gate routing and editor outcomes remain unmeasured. All release flags here
remain false, and no root_release directory is supplied.
