# One functional fresh-gate evaluation candidate

2026-09-09. Root authorizes model-free implementation and focused fake-model
verification only, within one twenty-minute engineering milestone. No real
tokenizer/model/provider import, checkpoint tensor read, fitting, inference,
derivative or experiment launch is authorized. All actual releases stay false.

## One question and exact inputs

Can the single frozen supervised gate route the sixteen prospectively specified
fresh inputs correctly, then allow the unchanged editor to reach its requested
endpoints while leaving OFF cases unchanged? Implement this conditional test,
not another optimizer, classifier candidate or tuning loop.

Use only the Qwen/Qwen3.5-0.8B revision, native CPU float32/eager block-10 final
input receiver and unchanged refreshed-gradient editor already used in the
accepted study. The fitted gate is
`development/native_supervised_gate_v1/construction_attempt_001/FITTED_GATE.json`,
SHA256 `fab7d797f9424d80aa3873eefc1cedf0e438e5b4ddc3a1059a080114aff7cca1`.
Bind its existing method/feature/source/training/construction hashes; no refit,
feature augmentation, threshold change or alternate gate.

The exact cohort is
`development/native_supervised_gate_cohort_v1/SUBMISSION.json`, SHA256
`9b06eda1f293acd764a65cb742d9f2003ab980271065a927a49a8dbc4cb4902e`,
first complete submission committed at 60e7ec2. Read its immutable author packet
for fixed sixteen-case ordering, shared category-neutral option wording and
ordinary proof types. Content/overlap admission and final source/text locking
must pass before any later tokenization, independently of this engineering scope.
Do not edit any question or the packet. Never load historical evaluation numeric
payloads; existing source code and public interface contracts may be reused.

## Small coherent deliverable

Build one source-bound candidate spanning the pure renderer/validation, offline
preparation adapter, learned-gate artifact adapter, sixteen-baseline census,
conditional editor workflow and saved-evidence judgment. Reuse authenticated
native source/receiver/scorer/owned-process infrastructure and tests wherever
unchanged; adapt only the relevant changed counts, bindings and routing seams.
Do not create a new generic framework, broad inventory or repeat full inherited
audits. A finite-source manifest plus focused changed-interface tests is enough
for engineering review; real admission still checks the actual locked boundaries.

Preparation has exactly 1 + 16*13 = 209 planned top-level operations, within
the proposal's 256-operation ceiling. Preserve offline tokenizer identity,
thinking-off full generation header, joint answer boundary proofs, exact masks,
KEEP/STOP IDs 50057/48964 and A/B IDs 32/33, maximum 320 full tokens without
truncation, one attempt, 175 seconds plus five cleanup, 16 MiB total/5 MiB file.
No preparation call is allowed until separately released by root.

Model evaluation first captures all sixteen valid native baselines in order,
using only unedited h0 for the gate. Audit labels may judge routing, but cannot
override it. A wrong route does not stop this baseline census early; retain all
sixteen routing outcomes. Invalid capture/state/ownership stops technically with
an explicit UNRUN suffix. Census success requires TP4/TN12/FP0/FN0. Any routing
error prevents all 32 policy requests and all derivatives. Report it honestly.

Only after the full census and inherited self-baseline eligibility pass, execute
P then C for each case: eight self requests with fresh entries, up to four
unchanged gradient/update pairs and real cold endpoints; twenty-four OFF requests
return their own fresh unedited entry exactly. Recompute the gate on each entry,
requiring baseline identity and the same score/route. A correct census does not
permit use of oracle labels to force those later routes. First integrated failure
stops subsequent requests; preserve SKIPPED and UNRUN distinctions.

Preserve aim0.10, signed acceptance margin0.05 with inherited tolerance, pair
mass0.8, full-vocabulary unique winner, original-h0 anchoring, step0.05 and
path/net0.20 limits, at most four current-gradient updates, validity/quality/KL,
current/cold identity checks and accepted-before-invalid stopping order.
No additional KL upper gate, natural-flip-position quota or rescue path is added.
Report flips and retentions separately by policy and target position; missing
natural opportunities remain UNTESTED. OFF identity is by construction, not an
active-editor collateral test. Report ordinary baseline/edited accuracy including
preserved wrong answers. No general survival-motive or shared-arrow claim.

The evaluation ceiling remains one model load, 120 forwards, 32 derivatives,
zero execution encoding, 1200-second worker, 120-second saved judge and one
shared fifteen-second cleanup. Aggregate 192 MiB/5 MiB per file. Reserve all
possible full raw logits plus rows/traces and terminal evidence before release,
not based on observed usage. No actual launch, retry, added smoke or cap extension.

## Ownership and verification

Write only within this new evaluation namespace and
`development/native_supervised_gate_preparation_v1`. Preserve every closed
namespace, raw result, source lock, gate artifact and user-owned file. Combined
new engineering output is at most 16 MiB, 5 MiB per file. Ordinary edit/test/debug
within the twenty-minute milestone is permitted; freeze the final tested candidate
once, at prospective real admission. Stop and report a precise missing part if
the useful milestone cannot be completed in that budget; do not create a successor
namespace for a fixture bug or claim written-only code verified.

Use fake-model/tokenizer fixtures for a full success path, a wrong-route census
that completes all sixteen then performs zero interventions, invalid capture
stopping, entry-route mismatch, tie OFF, ordering/count/resource boundaries,
artifact/source/input tampering and first-failure accounting. Reuse unchanged
hash-bound proofs without rerunning them. Deliver exact final hashes, observed
test commands/results, reusable-source pins and remaining release prerequisites.
No commit or actual execution is authorized by this engineering assignment.
