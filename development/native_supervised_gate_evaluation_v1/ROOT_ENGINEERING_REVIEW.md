# Root changed-interface acceptance

2026-09-09. Root accepts the final model-free engineering candidate, not a real
classifier result or authorization to load a tokenizer/model. The exact manifests
verified from disk are:

- Preparation: 7d2fa6660c1d240c2541b6af93a14ab3d6245856a84cf265874d1234f6e5cf92;
  14 local files / 57,757 bytes and 9 external source pins.
- Evaluation: 7bde258987d4ee1e2df5f4f5264d25babc04dd7792f787773fe02fcb93a713db;
  50 local files / 381,416 bytes and 43 external source/metadata pins.

Root read the independent changed-interface review completely and accepts its
11-group fake-workflow/saved-judge proof, adversarial reconstructed-verdict checks,
invalid-feature checks and separate final-authority reservation predicate checks.
The independent review's test_candidate hash covers the original eleven tests;
it does not cover the subsequently appended preparation bridge. The original
reviewed scientific source hashes remain unchanged.

Root additionally read the preparation renderer, validator, plan, core, reader,
offline entry, dependencies, contract and synthetic tests, and the evaluation
input adapter, preparation owner, admission tests and changed bridge. All sixteen
rendered UTF-8 hashes matched the independent blind review in exact order.
An initial root comparison wrongly expected canonical IDs in a human-label table;
the corrected ordered-hash check passed without changing any question.

Root reproduced the reload/dataclass-identity defect before correction. The final
adapter reauthenticates source/artifact bytes and compares immutable parameters.
Root's independent regression accepted the identical reloaded artifact and
rejected changes separately to mu, w and b. No actual feature was scored or fit.

Root ran three admission tests with the workspace Python and -E -S -B: all passed
in 0.036 seconds, exit 0. They verify disabled real entry/release, the separate
console-aware preparation config/command, unchanged native sources and parsing.
The preparation-to-evaluation bridge test passed independently in 11.135 seconds,
exit 0. Root then identified its dependence on an ignored temporary bundle. The
worker preserved exactly that artificial bundle under fixtures/preparation_bundle,
added FIXTURE_INVENTORY.json and source pins, and reran only the bridge: PASS in
11.136 seconds. Root read the final path/authentication-only delta and verified
all twenty-one fixture hashes via the final manifest. Final test_candidate SHA256:
ef9975fe8680a32ab7800d10b7eb872486bdd30df8643a026a6fca4528ad1a56.

Root independently compared the inherited receiver, guard, loader, trace, entry,
launcher, process controller, checkpoint metadata and model-worker identity with
the accepted oracle candidate: byte-identical. Preparation windows_job is also
byte-identical. The preparation-owner adaptation is limited to namespace,
manifest field and separately pinned console-aware config. Existing platform
proofs are reused; no unnecessary native process/model rerun was performed.
Counts, reservation and deadlines use the prospectively specified smaller
envelope. All 120 possible raw-logit records and supporting evidence are reserved:
139,837,440 bytes within 192 MiB, 5 MiB per file. Preparation remains 209 planned
operations, one attempt, 175+5 seconds, 16 MiB combined.

The worker reports twelve final workflow groups, three admission groups and five
preparation groups / eighteen synthetic runs passing. The earlier nineteen-run
message was corrected; it is not the accepted count. Engineering edits/debugging
occurred before actual release, without new experiment namespaces or real calls.
An interim root manifest check overlapped the requested fixture edit and detected
the expected changing test file; the final complete manifest check above passed.

No new Qwen behavior, gate generalization, actual encoded length, real steering
endpoint, active-editor ordinary preservation, broad reliability or publication
completion is established here. Exact source/text commitment and prospective
preparation approval remain next; actual prepared inputs/closure must pass before
the separately bounded model evaluation can be approved.

Before archive, root corrected one test-report provenance field: the worker
associated its reported twelve-test intermediate run with the earlier reviewed
eleven-test source hash. The exact intermediate hash was not retained, so that
field is now explicitly unknown, with the reported value preserved separately.
Acceptance relies on the independent eleven-test proof plus the checked final
bridge delta, not an invented twelve-test source identity. The original
preparation test report is also preserved at a tracked stable path. No scientific
source, artifact, question, method or final source-manifest hash changed.
