# Offline final preparation — prospective implementation only

No real text lock, preparation release, tokenization or model work is authorized
or supplied here. The blind author works elsewhere; this worker has not read any
submission, author attestation or actual new cohort text. Tests use artificial
markers only. Root must first separately admit the whole cohort after blind
semantic/proof review and freeze the final text. No new real runner adaptation.

Future single path: root commits a self-contained TEXT_LOCK.json containing the
cohort object, its canonical SHA256, exact24 rendered prompt records, scope
ROOT_ADMITTED_NEW_FINAL_TEXT, blind_semantic_review_approved=true,
final_text_locked=true, and exact model/STUDY metadata. Its raw bytes must then
be bound in a separate root PREPARATION_RELEASE.json with approved=true, scope
ONE_OFFLINE_FINAL_PREPARATION, exact PREPARATION limits and SOURCE_FREEZE SHA.
Neither file exists in this candidate. The entry needs the explicit release SHA.

Before tokenizers, check root/source/environment/model-metadata/renderer/analysis
pins and mechanically re-render the frozen object using the approved pure packet.
All24 texts/IDs/order/hashes must exactly match. This is identity verification,
not renewed authoring or semantic admission. Author/template files remain untouched.
No source module containing historical data readers is executed; only the isolated
pure packet is imported. No old final/validation content or new submission is read
outside the future self-contained, explicitly admitted text lock.

Exactly313 scheduled top-level tokenizer operations:one native tokenizer load
and13 per each of24 fixed prompts. Per prompt:render+encode generation;render+
encode no-header;render+encode empty assistant;render+encode+decode each of two
answer words;decode final input. KEEP/STOP for18 semantic slots;A/B for6 ordinary.
Every operation is reserved and journaled STARTED before invocation, then COMPLETE
or FAILED, including exceptions. Native library internals, source/asset hashes,
pure24-prompt renderer checks and list/string boundary arithmetic are not extra
top-level tokenizer operations. No opaque boundary helper or alternative call
schedule. 313 is both exact expected count and hard ceiling.

Preparation-only envelope:180s entire process, root retained-process external
wait175000ms plus at most5000ms termination/exit confirmation;one process, no retry.
Use hidden Windows launch, retained handle, bounded stdout/stderr capture and
authoritative closure from the existing reviewed ownership workflow. This worker
entry supplies its own180s watchdog. Root must retain the process/closure receipt;
in-process success alone is not process-closure proof. No launch is performed here.

Storage-only revision authorized by root commit3da6385: combined preparation plus
owner output remains16MiB. Exactly32768bytes is reserved solely for owner evidence,
so ALL preparation writes are checked before publication against16744448bytes,
including ADMISSION, critical RESULT and entry-failure receipts. The separate64KiB
terminal reserve is inside that preparation allowance. RESULT is at most8192bytes;
exception type is at most64ASCII characters and error code at most128ASCII
characters, otherwise an honest fixed fallback is used. Every file remains<=5MiB.
The single stdlib storage publisher is used by both bootstrap and core paths.
An entry failure after a journal exists reports operation counts unknown rather
than incorrectly claiming zero operations; its retained journal remains evidence.
An exclusive
preparation_attempt_001 directory prevents retry. Preserve operation journal and
completed case records on failure; publish final inputs.json only after all313
operations and24 cases pass. Report failed and remaining UNRUN operations honestly.
A watchdog may leave only a STARTED journal prefix: never relabel that PASS.

Pinned offline tokenizer/checkpoint revision, template, Python/provider versions
and approved native method/analysis/renderer source hashes are in DEPENDENCIES
and TOKENIZER_PINS. Thinking disabled; no padding/special insertion/truncation.
Require every full input1..320 tokens, all-one mask, exact full generation header
bytes/IDs, same full prefix under empty/each completed answer, exactly one appended
content token with fixed answer ID, assistant end IDs and final-input decode.
No tokenizer probing, alternative wording, outcome screening, shortening, partial
input, retry or repair after a locked preparation fails. All sources/methods/old
attempts remain immutable. No weights or checkpoint tensors are read by preparation;
deny legacy wrappers/PyArrow/datasets/model-class imports and network.

The later native study remains24inputs/48requests/180F48D1load, all24baselines
first,12independently executed self cold endpoints,36OFF identities, no smoke,
320tokens,1800worker+180judge+ONE shared15cleanup=1995s,288MiB/5MiB. Preparation
budgets are separate and confer no model authorization. Output bindings include
all exact IDs/masks/lengths/rendered hashes, ordinary scoring-only gold and source
identities; the current synthetic final runner still rejects real model admission.

The historical20-group TEST_RESULTS.json belongs to the original committed source
and is preserved unchanged. Revised storage-only observations are separately saved
in TEST_STORAGE_RESULTS.json; prior results are not relabeled as later-source tests.
