# Metadata-bound v2 successor

The closed v1 preparation failed its exact console-helper fingerprint before any
preparation directory or accepted actual worker. Its source, attempt, release,
and closeout at commit6599a5b remain untouched. This v2 candidate is a separate
prospective namespace, not a retry of that attempt.

The only runtime/configuration changes from accepted v1 are the preparation and
evaluation namespace bindings, unique evaluation attempt name, the current
console-helper SHA256, and their dependent source/fixture joins. The scientific
workflow, independent saved judge, ridge adapter, editor/scoring adapter, native
receiver/guard/loader, counts, budgets and model ownership code are byte-identical.
All cohort/gate/earlier scientific source references remain v1. CHECKPOINT.json
retains its exact bytes and has an explicit -text attribute.

Current console SHA256 is
e449bce01f275cd08f3d4e64bb73b3b43ae845a0dbdb3e6131426e66537705e5.
Read-only PowerShell inspection independently reproduced that hash, Valid
Authenticode status and the Microsoft Windows signer in
CONHOST_IDENTITY_REVIEW.json. The focused test also recomputed the executable's
hash. The signature receipt is admission metadata for these exact bytes; no live
owner security check is removed, bypassed or replaced by the receipt. The
retained handle/job/parent/creation/image/hash predicates remain byte unchanged.

The admitted sixteen texts and frozen ridge artifact are still unmeasured. Their
hashes remain 9b06eda1f293acd764a65cb742d9f2003ab980271065a927a49a8dbc4cb4902e and
fab7d797f9424d80aa3873eefc1cedf0e438e5b4ddc3a1059a080114aff7cca1. Strict score>0,
sixteen-baseline census before every policy, TP4/TN12/FP0/FN0, unchanged editor
thresholds/geometry, 120F/32D/one load,209 preparation operations, and all existing
time/storage bounds are preserved. Full reservation remains139,837,440 bytes.

No actual preparation/model/owned child, real tokenizer/provider import, fit,
inference, tensor or historical numeric outcome access, installation, network,
commit or actual release is part of this recovery. The only regenerated fixture
uses artificial marker text. It is tracked and authenticated. Focused metadata
and adapter tests passed; unchanged scientific suites were not repeated.

Root owns final source/text admission, current machine admission, separate v2
preparation release and closure, and any later model release. Every supplied
release/draft authorization flag is false. No actual attempts or releases were
copied from v1 or created in v2.
