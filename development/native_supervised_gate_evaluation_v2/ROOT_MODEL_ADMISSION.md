# Root admission for one bounded fresh learned-gate evaluation

Root read and accepts ROOT_ACTUAL_PREPARATION_REVIEW.md completely. Actual
offline preparation passed 209/209 operations and 16/16 inputs, lengths 45-167,
without model work. All three retained process exits were zero; quiescent and
within-deadline passed, with no termination or capture/cleanup error. Root
retained its actual launch and final CLI exit zero. The failed v1 is unchanged.

Raw preparation archive 38ed46baa6ef119a83ec52a241ac5453aa577b8d was verified
against Git for 25 files / 125,003 bytes. Bound input archive
6dd3925ba6019224ae7962b70a0e33d5a0ded73d was verified for 22 files / 147,460
bytes. The independent review compared every one of the 21 source copies and
validated the real, unpatched input-reader interface. Root independently ran
the same actual saved-bundle validation and the final source manifest check.

The exact scientific program and success/stop rules remain those reviewed and
prospectively locked before preparation. Run every valid one of the 16 unedited
baselines first. All gate routes come only from the immutable ridge gate and
unedited features; labels may judge but not force a route. Any wrong route ends
the scientific attempt before policy requests or derivatives. Only a fully
correct census and eligible self baselines permit the fixed 32-request schedule,
with unchanged per-prompt gradients, real cold endpoints and fresh OFF returns.
Report all failure, skip, unrun and absent-natural-opportunity distinctions.

Root authorizes exactly one new Qwen/Qwen3.5-0.8B native CPU float32/eager
evaluation, same revision and frozen parameters, under root_release/RELEASE.json.
Maximum one model load, 120 forwards, 32 derivatives, zero encoding; 1200-second
worker, 120-second saved judge, 15 shared cleanup seconds; 192 MiB/5 MiB file.
Worst-case reserved evidence is exactly 139,837,440 bytes. No refit, tuning,
replacement text, extra smoke, retry, external compute or publication is allowed.

Commit and raw-Git verify that exact release, then require the existing zero-model
preflight to pass before launching once. The native loader/receiver/ownership
and immutable model-state checks remain active and will reject a runtime mismatch.
Real gate routing, conditional control and the overall scientific objective are
still unproved at admission; a preparation or preflight pass is not model success.
