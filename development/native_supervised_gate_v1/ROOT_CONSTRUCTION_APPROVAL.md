# One prospectively approved construction-only fit

2026-09-09. Under the user's permission for small incremental research steps,
root accepts the independently reviewed core and separately reviewed finite
owner for exactly one nine-row construction fit. No fit has occurred before
this approval. Commit and raw-byte verify this approval, release and all bound
source before launch; check current usage again and do not launch if unavailable
or exhausted.

## Exact approved scope and implementation

The fixed method and all source/label/ordering/stop rules are unchanged from
the accepted proposal and sequence amendment. Use the existing nine native
development baseline residuals, one fixed fit and one independent reconstruction.
The actual construction release is ROOT_CONSTRUCTION_RELEASE.json; the old
RELEASE_DRAFT.json remains false and unchanged. Its exact raw SHA256 must be
supplied to the owner and child. The construction lock is
`c9b933dacdde736e1dc88a9f21cc987bc45e51789f9d93aeb7749397a593e397`,
training manifest
`1970f752b394349bf7b926e46a7cc70fd90647bc197f9a52e008f9477ca598d4`,
and four-file core source lock
`58712c7a3939650e2c9c04ed752d411a4927b36314435092abf582d8c2ae7693`.

Parent owner fit_owner.py SHA256:
`2d3d3e60ec7ec2c3b2c4ea23cebd698717c6b063275801dfbaa2b62319252939`.
Reused unchanged windows_job.py helper SHA256:
`a6334fea1678773ff169aed39c3b1939c57614d0096a446e6d0418abcb21368a`.
Root must pass both exact hashes through the fixed owner interface. Use the
workspace .venv/Scripts/python.exe with -E -S -B; the child likewise excludes
Python environment/site initialization and performs no model/tokenizer work.

Root read the entire final owner, tests, helper and handoff. The five focused
fake-child tests independently passed in 0.955 seconds, including timeout with
descendants, output overflow and quiescent closure. A separate root-injected
terminal-fsync fault raised without returning success, left the job/readers
closed, stored no final technical PASS, and preserved the scientific fixture.
No real feature extraction or fit occurred in those checks.

## Bounds, retention and acceptance

There is one whole sixty-second computation envelope and one shared five-second
cleanup allowance. Keep the one-shot construction_attempt_001 and
fit_owner_attempt_001 claims; no retry, deletion, substitution or fit resumption.
The child reserves at most 1 MiB including its 64 KiB fault allowance. The parent
adds at most two 64 KiB streams and a 256 KiB terminal/finalization allowance,
well below the aggregate 8 MiB construction and 5 MiB per-file caps. Engineering
artifacts remain within their separate 16 MiB namespace allowance.

This small owner is not a real-time OS or hostile-filesystem security boundary:
OS calls may block. Do not convert such a failure into success. Root retains the
final returned CLI receipt after actual close, verifies its elapsed/cleanup
times and technical completion, and cross-checks saved terminal/finalization,
child exit and scientific result. A file cannot attest its own future close;
FINALIZATION.json explicitly leaves that attestation to the retained root caller.
Preserve partial files and scientific failures alongside any technical fault.

Construction acceptance requires exactly one fit, the fixed nine rows correctly
classified, successful independent math/route checks, immutable artifact/source
bindings, complete retained process/receipt evidence and all caps satisfied.
An in-sample miss is a recorded CONSTRUCTION_FIT_FAIL, not permission to tune.
A construction pass is only training adequacy, not fresh-case generalization.

No checkpoint tensor read, tokenizer, model/provider load, model inference,
evaluation, threshold adjustment, alternative method, network/cloud service or
publication is authorized. After an accepted pass, freeze the artifact/result
before isolated authorship of the separately proposed fresh sixteen-input
evaluation. That author receives no features, coefficients, scores or outcomes.
Any future tokenization or evaluation still needs its own prospective lock and
root release. The overall goal and journal-readiness claim remain unproved.
