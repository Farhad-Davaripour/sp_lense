# Observed one-shot construction pass, not generalization

2026-09-09. The source/release package was committed at
674fdf5a9b45346a598ca0a5d28148d26a11729b before the one retained launch. Root
raw-Git verified all twenty-one package/helper files, 106,355 bytes, all scope
joins and the absence of prior attempt directories before launching. Actual
release SHA256 is
`ba494cef9a2379cb8885d8bee7dd6b4d12b8611b5e0ea07d46313cb5c52bfc5d`.
Usage was 51% used at launch. No reset, purchase or external service was used.

The fixed construction ran exactly once and returned CONSTRUCTION_PASS: all nine
practice rows classified correctly. The independently implemented checker agreed
within maximum parameter difference 5.551115123125783e-17 and normal-equation
residual 2.7755575615628914e-17, below the fixed 1e-10 tolerance. No second fit,
parameter search, residual extraction run, model/tokenizer operation or checkpoint
tensor read was performed. This is an in-sample result on two semantic families
and one ordinary question, not nine independent semantic families.

FITTED_GATE.json SHA256:
`fab7d797f9424d80aa3873eefc1cedf0e438e5b4ddc3a1059a080114aff7cca1`.
Keep this artifact and all raw results immutable before any fresh authorship.

The constructor reported 2.25 seconds. The owner returned after finalization in
2.4540000000015425 seconds, and root's retained exec closed with exit 0 in
2.7664947 seconds, without an ongoing session. The complete returned receipt is
preserved in ROOT_RETAINED_CLI_RECEIPT.json. Job membership was established before
resume; the retained job was terminated for cleanup and verified empty and
closed, with readers and process handle closed. The recorded launcher exit is 0;
do not infer separately retained natural exit codes for every former descendant.
No timeout, output overflow, error, finalization deadline or storage fault was
reported. Measured final cleanup elapsed was 0.0 at the available clock resolution,
not a claim that cleanup literally took no time.

Root cross-checked the returned receipt against saved terminal/finalization,
stdout against RESULT.json, the fitted artifact hash, and all size/count/cap
conditions. Six raw child/owner files total 46,356 bytes, comfortably below the
8 MiB cap; stderr is empty. The terminal hash matches finalization. No technical
fault file or uncompleted process was reported. The core source remains frozen.

A separate saved-receipt review will check these joins without another fit or
numeric reconstruction. This closeout accepts only the observed bounded
construction outcome, pending that final independent receipt check. It grants
no model/evaluation launch. After that check, clean authorship of the planned
sixteen fresh inputs is the next step; it must not receive learned coefficients,
feature values, margins, errors or outcome-conditioned example suggestions.

The full automatic-control objective remains incomplete. This arm has not tested
new scenarios, integrated the new gate with the editor, or established preservation
of fresh ordinary tasks. The previous narrow archival checklist remains 90%; no
new journal-readiness credit or general reliability claim follows from training.
