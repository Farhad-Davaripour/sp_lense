# Independent saved-construction receipt review

2026-09-09. **ACCEPT the observed one-shot construction pass. No blocker found.**
This accepts training adequacy and the retained bounded execution only. It grants
no tokenization, model evaluation, integration or generalization claim.

Read `ROOT_CONSTRUCTION_APPROVAL.md` and `ROOT_CONSTRUCTION_CLOSEOUT.md` first.
A standalone standard-library read/hash/join script exited 0 and reported PASS.
It did not import the gate, constructor, extraction or checker modules, rerun
any numerical reconstruction, or calculate anything from real feature values.
Artifact decimal values were decoded as strings; only binding/method/contract
metadata was examined. No coefficient values were displayed.

The actual approved release hash is
`ba494cef9a2379cb8885d8bee7dd6b4d12b8611b5e0ea07d46313cb5c52bfc5d`.
Its exact construction/manifest/source hashes match the accepted locks and
artifact bindings. The four core source hashes remain unchanged. Model,
tokenizer and evaluation permissions remain false. The retained child command
binds that release hash and uses the specified Python with `-B -E -S`.
Owner/helper raw hashes match the prospective approval.

The saved result reports one fit, nine correct of precisely nine construction
rows, independent-check verification and zero model/tokenizer/checkpoint-tensor
calls. Its maximum parameter difference is `5.551115123125783e-17`, with
normal-equation residual `2.7755575615628914e-17`, both below the fixed `1e-10`.
These are saved observations, not a new checker execution. The denominator is
nine renderings of two semantic families and one ordinary question, not nine
independent families.

| Accepted saved artifact | SHA256 |
| --- | --- |
| `FITTED_GATE.json` | `fab7d797f9424d80aa3873eefc1cedf0e438e5b4ddc3a1059a080114aff7cca1` |
| `RESULT.json` | `4553e4b9d5dd8d95fbc31c91fc78ee69b8a58a30162412276369807b5f9a981a` |
| `TERMINAL.json` | `c2da5c84f1d1cde73b1b9423bac867a3fabf2c54e2dcb67318f927e0b26623d4` |
| `FINALIZATION.json` | `f24474575dff04f0916e2cb07a7f92e08ae48755dfc68814a012859464c22ce7` |
| `ROOT_RETAINED_CLI_RECEIPT.json` | `1127209a3d52cef77291cb1209358df21b22080683c6edf73d5e672f886aeaae` |

Child stdout equals the saved result; stderr is empty. Finalization binds the
terminal's raw hash. The returned receipt exactly reproduces saved terminal and
finalization data and reports technical completion. The separately retained
host response exited 0 without an ongoing session, supplying the closure
observation that `FINALIZATION.json` intentionally cannot attest for itself.

Recorded constructor/owner/host times are 2.25/2.454/2.7664947 seconds, within
the 60-second envelope and shared five-second cleanup allowance. No timeout,
overflow, deadline/storage fault, error or technical-fault file was reported.
Job assignment preceded resume; the job was terminated for cleanup and was
reported empty/closed, with readers and process handle closed. The launcher's
recorded exit is zero. This does not establish independently retained natural
zero exits for all former descendants. Cleanup reported 0.0 seconds at the
available clock resolution.

The six raw child/owner files total exactly 46,356 bytes; all satisfy per-file
and aggregate caps. Including the retained receipt and root closeout gives
eight files and exactly 53,996 bytes. Every current raw file exactly matches
its Git blob at corrected commit
`4b46209fae45a49e8b01d1ea3fd0be23c91c13c6`, including stdout's original CRLF.
The scoped binary attributes preserve this archive correction; no computation
rerun is implied or needed.

Keep the fitted artifact and training result frozen before clean authorship.
The clean author must not receive this result, coefficients, feature values,
margins or outcome-guided suggestions. Fresh evaluation remains unperformed.
Only this review document was written; no release, commit or experiment was made.
