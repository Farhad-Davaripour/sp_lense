# Prospective clean authoring after construction

2026-09-09. Root fixes this packet for the one planned sixteen-input evaluation.
The immutable trained artifact is fab7d797f9424d80aa3873eefc1cedf0e438e5b4ddc3a1059a080114aff7cca1,
archived before authoring. Root must accept the separate saved-construction review
before dispatch and commit/raw-verify this packet first.

Give a distinct clean-context author ONLY the two author_packet files and its
output paths. Do not share this scope, fitted coefficients, features, scores,
errors, old results or source-aware successful examples. The initial authoring
scope is five minutes, no model/tokenizer, one whole submission, no candidate pool.
Use GPT-6 Astra / Ultra as requested by the user.

The four ordinary types concretize the proposal before any prose: addition,
uppercase, closed-world age discrimination, and bracket instruction following.
Gold slots A/B/A/B are fixed. Shared semantic option wording implements the
already accepted pre-fit sequence amendment; the old renderer remains unchanged.
The full sixteen inputs, independent semantic/proof/overlap review and source/text
lock are still prerequisites to preparation; authoring is not model permission.

A separate content reviewer checks all renderings and proofs without model
outcomes. Root performs source-aware text-only mechanism/literal overlap checks
against construction and excluded historical cohorts. Do not include historical
numeric outcomes in either author's or content reviewer's context. Reject a
content/mechanism failure without automatic replacement; keep the whole submission.

This packet does not claim a natural-flip-position quota, general reliability,
automatic gating success or ordinary-task preservation. Future census and
conditional editor rules/caps remain as in the proposal; no fitting, threshold
change, model execution or publication is authorized by authorship.
