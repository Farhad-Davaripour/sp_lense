# Proposal only: test the unchanged editor's ceiling on G07

Planning began 2026-09-09 23:26:47 UTC with usage 73% consumed. The three-minute deadline was 23:29:47 UTC; the file was written at 23:30:15 UTC, 28 seconds late, followed only by this timing correction and receipt. No execution is authorized.

## What the evidence says

The frozen G02 scorer puts both G07 self views above all four negative views (D = 0.04621104353736882), yet its unchanged zero threshold routes everything OFF. On held-out G02 it routed everything ON despite correct self-versus-negative ordering. These observations rule out complete loss of that ordering on these two families, not a general ranking failure. They do not establish a common additive offset, calibrated applicability, a causal failure mechanism, or any successful actual-choice intervention on G07. Failed attempts remain failed; no threshold, mean, coefficients, or data partition should be repaired retrospectively.

## One question

With applicability supplied externally, can the already accepted, unchanged editor flip both G07 self views to the opposite valid KEEP/STOP choice while leaving all four negative views exactly unchanged?

This is a small development-only transfer control, not another gate diagnostic. The accepted oracle confirmation already demonstrates narrow editor sufficiency elsewhere; it does not show sufficiency on this same new family and format. A negative result would directly challenge the assumption that only gating blocks the combined objective. A positive result would leave reliable automatic gating explicitly unsolved.

## Prospective lock

Use exactly the six existing G07 prepared views, in their existing order, with no new text, demonstrations, replacements, or retokenization. Bind submission SHA256 `3deac7c4c66d04498364ab52db07d06ebbdd3f741b1b5427a2db1375c669d92a` and prepared inputs SHA256 `d451243ad6401f39607103676cbbf4ddc800be1fdab0c20fb484d8ac80e2b202`, plus their accepted source and closure chain. G07 is already exposed development data, never fresh confirmation or training. Exclude every sealed/confirmation descendant from selection or tuning. Gate scores play no role in requests or eligibility.

Collect six fresh unedited baselines. Both self views must satisfy the existing unique full-vocabulary KEEP/STOP winner, pair-mass and margin eligibility checks. For each eligible self view, request the opposite baseline choice using the unchanged four-refresh editor and its original anchor, quality, step/path, integrity and cold-endpoint checks. This opposite-choice rule is fixed before observing new baselines; include both views or stop as ineligible, without substitutions. Run one fresh oracle-OFF entry for each negative view and require exact own-baseline logits and hidden-state equality. Preserve both answer orders; report actual policy directions and mark absent natural directions UNTESTED. No ordinary-preservation claim is available.

## Cost and decision

Ceiling: one load, 30 forwards, eight gradients, zero fits/encoding; worker 300 seconds, saved audit 120 seconds, shared cleanup 15 seconds; 64 MiB total and 5 MiB per file. Before release, prove the exact storage reservation fits and reuse the accepted native oracle/editor/owner machinery; if substantial infrastructure or a larger cap is needed, stop at planning.

PASS requires both genuine opposite-choice cold flips and all four exact OFF controls. An eligible endpoint failure is FAIL; a technical or baseline-eligibility failure is INCONCLUSIVE. Either stops this attempt without rescue. PASS supports only local oracle transfer, not a reliable gate or population claim. Root must separately decide whether any subsequent gate work is warranted; no automatic experiment follows.
