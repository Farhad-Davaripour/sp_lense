# One fresh-family frozen-scorer falsification

Root accepts docs/GATE_COVERAGE_FAILURE_NEXT_QUESTION.md after independently
verified closure of the coverage failure. One20-minute model-free implementation
milestone is approved, with one focused independent reviewer in parallel and one
separate4-minute clean author. No actual tokenizer/model/scoring run is released.
Geometry, LoRA, direct-logit control and the overall scientific goal stay unchanged.

## Question and frozen scorer

Does the existing failed G02-fold scorer retain self-above-negative ordering on
ONE new G07 family, without fitting or changing its threshold? Use ONLY stage G02
from native_gate_hardmargin_coverage_v1/construction_attempt_001/RESULT.json,
SHA e1827795212abe104c8fcfc663c7fc0b68bef10e10c766653dc140829fbdc38a,
archive75df0d868f8dc6cf726dfe5ba7f96eb75bb5bf02, independently verified by
coverage ACTUAL_FIT_REVIEW.json db766a810718f5fcb344f38647ee5cd014630507ea81d5e33d60748e2884bd5b.
Freeze its training_mean, parameters.w and parameters.b exactly, plus source,
feature contract and result provenance. This is a DIAGNOSTIC_SCORER_FROM_FAILED_STAGE,
not an accepted gate or FULL44 artifact. Reuse the exact binary64 transform/dot/
score rule from coverage gate.py, with no centering on new/held/batch data. Old
scorer parameters may be read for extraction; old44coordinates may not be rescored
or used for new fits. Old failed attempts and UNRUN folds remain final.

The known G02 ordering survives in its six saved views, but a common additive
family offset is only a hypothesis. Its held negative1.601572 exceeds training
positive scores near1, so one global threshold cannot classify all44 correctly
for that fixed scorer. No threshold search is needed or authorized.

## Exact new data and decision

Clean author uses ONLY author_packet/BRIEF.md and EMPTY_SCHEMA.json to supply
one complete DIAGNOSTIC_SUBMISSION.json and AUTHOR_ATTESTATION.md. Exactly one
new mechanism, three matched self/other/nontermination scenarios, six existing
KEEP/STOP-order views,2positive4negative; no ordinary rows or training additions.
First whole submission must be preserved before separate clean content review
and root overlap admission. Reject invalid/overlapping content with no automatic
replacement. Engineering uses empty schema/synthetic prose, not actual submission.

Freeze D=min(two self scores)-max(four negative scores). Primary ordering criterion
D>1e-8; also report all six original strict score>0 decisions. The diagnostic
comparison tolerance is not a changed routing threshold. Report raw scores in
canonical self/other/control each KEEP-first/STOP-first order.
D<=1e-8 fails the prediction; strict inversion contradicts an offset-only account
there, near ties fail the diagnostic but leave cause inconclusive. D>1e-8 with
routing errors preserves ordering but does not identify an additive cause or
supply label-free calibration. D>1e-8 and6/6routing is transfer on ONE family,
not a robust gate. Every branch stops after reporting; no automatic fit, corrective
centering, new family, steering or untouched confirmation follows this scope.

## Finite implementation and future caps

Own only these new engineering paths plus explicitly permitted scorer files in
this namespace: development/native_gate_score_preparation_g07_v1,
development/native_gate_score_capture_g07_v1, and score/schema/freeze/test/handoff
files in development/native_gate_frozen_score_g07_v1. Do not change root scope,
author packet/submission or existing sources. Reuse the reviewed native scaffold,
semantic renderer and retained ownership; only finite6-row/count/source/role joins
and one saved scorer/decision are new. No generic framework or full old suites.
Prefer the scoring decision within the existing saved audit, with its own10s/
64KiB analysis cap, rather than building a new process-ownership framework.

Prospective preparation:79operations(1+6x13),320tokens/no truncation,
350sworker+5scleanup,32MiBcombined/5MiBfile/32768-byte owner reserve. Prospective
capture:1load6uneditedforwards0derivatives/encoding/optimization/edits, native
Qwen/Qwen3.5-0.8B@2fc06364715b967f1860aea9cf38778875588b17 CPUfloat32/eager/eval,
originalblock10output,last-original-input,width1024,h=h0, unchanged weights/buffers,
300sworker+120saudit+15ssharedcleanup,64MiBtotal/5MiBfile. Freeze a sufficient
finite reservation before admission; no cap widening after launch. Primary
scoring6calls only,10s/64KiBoutput; independent saved-only replay may recompute
these6 once for verification, never optimize. Keep all raw records/counts.

Within20minutes ordinary synthetic edit/test/debug is allowed, then freeze once.
Focused tests: exact6views/79ops/320token/fullnativeboundary, label/metadata
isolation, unchanged scorer extraction/reconstruction/source hashes, train-only
mean binding, D branches/ties/finites/order-invariance,6capture/0fit counters,
owner/storage/closure/default-deny. No actual author prose/new features,
tokenizer/model/real scoring/optimization/release/commit during engineering.
Deliver main and independent concise receipts first, sources/contract/inert
release drafts and handoff. Do not rebuild broad unchanged proofs. Root later
admits content, commits exact text/source/scorer, accepts zero-provider preflight,
releases one preparation, accepts closed actual inputs, then separately releases
one diagnostic capture and independent replay. No implicit authority now.

Account70%used30%remaining at assignment; check before every batch, stop100% or
unavailable, no reset/purchase. All agents GPT-6 Astra/Ultra. Readiness70%, overall
scientific objective unproved. No journal-acceptance probability is implied.
