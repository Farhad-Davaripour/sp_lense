# G07 separate capture and frozen-scoring release

Root accepts ACTUAL_PREPARATION_REVIEW.json SHA256
a0f25313c4aa73e0424579ce8914137f5e83524af3b1b1eb0b9dfe6773146b67.
The reviewer authenticated the unchanged reader, 66 source pins, all six exact
renderings, 79 operations/158 journal entries, complete 180-183 token inputs,
11 exact copies, raw Git preservation and clean retained-process closure.
Root separately rechecked all 66 source pins. Prior content-review initialization
qualification remains disclosed and unchanged in ROOT_CONTENT_ADMISSION.md.

The new root_release/RELEASE.json admits only the six exact saved inputs SHA256
d451243ad6401f39607103676cbbf4ddc800be1fdab0c20fb484d8ac80e2b202,
TEXT_LOCK ddd87c7892b2f5b08ca3282a87dd2098a04815d092b4e909ac32b7283e413b94,
and preparation closure 0b26850493459394cfd5d517f9444074f20dcd1197c1037e224d85775a6ed14e.
Original unapproved BUNDLE_BINDINGS and all old evidence remain immutable.

Authorize one retained native capture: Qwen/Qwen3.5-0.8B at the unchanged pinned
revision, CPU float32/eager/eval, unchanged weights and buffers, block10 output,
last original input, width1024, no editing. One load, six forwards, zero
derivatives/encoding/optimization. Existing caps remain 300-second worker,
120-second audit, 15-second shared cleanup, 64MiB total, 5MiB per file, exact
8,220,672-byte reservation. No retries or source/input/limit changes.

The existing embedded audit may evaluate the frozen failed G02 scorer exactly
six times within its own 10-second/64KiB ceiling. Preserve all six raw scores,
strict score>0 routing and D=min(self)-max(negative), with D>1e-8 as the fixed
ordering criterion. Every result stops this diagnostic. No fitting, threshold
adjustment, new mean, new family, steering or final confirmation is authorized.
Collection completion is not scientific success or a validated automatic gate.

Before launch the designated worker must commit this admission, exact RELEASE
and original independent review, verify raw Git equality/fsync and all frozen
bindings, and pass unchanged launch.py --preflight using the actual release SHA.
If a required check fails, do not launch or repair frozen sources; report it.
If preflight passes, execute that exact launch once, archive all actual output,
closure and CLI evidence without editing outcomes, raw Git verify/fsync, and
return compact hashes/results and independent replay inputs. Do not run a second
scoring replay yet; root will separately authorize one independent saved replay.

Account72percent used28remaining; check before batches, stop100 or unavailable,
no reset/purchase. Publication readiness70percent, overall goal unproved.
