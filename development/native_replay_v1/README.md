# Fixed public native replay — ordinary development

Status: saved-only engineering tests PASS; real admission BLOCKED by a source/evidence mismatch. No real replay was authorized, frozen or run. This milestone imported no Torch/HF/TL/provider, accessed no checkpoint, constructed no model/tensor and executed no numerical forward/derivative/encoding.

## Completed slice

`reference.py` authenticates all63 current/raw-Git reference files (5,491,344 bytes) at ac756279e595772733c20c7884bea60c61108a2f. It joins the exact137-token int64 IDs/mask/final index136, runtime, fitted gate, five completed forward rows, one completed derivative at row003, all compressed and decompressed full-vocabulary logits, gradient and setup chunks. No final cohort or new case was opened.

`replay.py` drives only public `[1]` zero offset, then the saved baseline, entry, gradient_1, step_1 and cold endpoint. Offsets come directly from those authenticated rows; there is no optimizer or fitting. It checks all248320 logits at <=1e-6, exact float32 h bytes/nonfinal hash and unchanged centroid score/decision; row003 gradient uses <=1e-6. The unchanged centroid source is hash-authenticated and only reloaded, never fitted. Saved-only audit reconstructs these checks from raw files with separate comparisons. A failure retains the measured row where available and the exact remaining UNRUN suffix.

`engine.py` connects that schedule to the reviewed native receiver interfaces: unedited entry, current-graph derivative, fixed saved update, cold endpoint, initial/final supplied weight check, unconditional guard restoration. The final tested handoff uses inert callbacks, not the real receiver or an actual weight proof. `real_entrypoint` unconditionally rejects before provider or checkpoint work.

Five final ordinary engineering groups PASS in5.718s: complete saved replay/saved audit; fail-stop/UNRUN; coherent outer-hash gradient tamper; actual handoff interfaces with inert receiver; disabled real entry. Two earlier ordinary saved-only iterations also passed before tightening failed-row preservation and adding the handoff checks. Raw synthetic scratch directories remain locally retained and ignored, not historical real evidence. Three iterations total26.4MB including scratch, below32MiB; each file<5MiB. TEST_RESULTS.json is the final compact receipt. The saved fake's reported load/forward/derivative schedule is explicitly simulated; actual counts remain zero.

## Concrete admission blocker

The fixed reference is not evidenced as a text-only graph. Authenticated setup-before raw SHA `5f2dcc90b72d4cfc39cdda0d1b7508598d22d492d2c69fb6e103072526926219` contains root children `vision_encoder` and `vision_projector`. Their original components are HF `Qwen3_5VisionModel` and `Qwen3_5VisionPatchMerger`, with real patch/position/block/merger children. TEST_RESULTS.json retains the exact compact graph/type evidence.

Installed TransformerBridge.parameters/named_parameters delegates to the original HF model (transformer_bridge.py1230–1260). Native Qwen3_5ForCausalLM has only its text model and bias-free head (modeling_qwen3_5.py1581–1583). UnembeddingBridge additionally installs a source-fixed zero head bias (unembedding.py37–55); that is a valid isolated difference, but appending only that zero segment is NOT a complete justification for the historical fingerprint when the saved graph also has vision components. No parameter-count inference from the473-entry progress bar is accepted. No replacement checksum, synthetic visual bytes or weight projection was implemented.

Consequently the mandated text_config/CausalLM loader cannot yet be admitted against fixed legacy fingerprint `6a671f0ae00398453e5b453d13b4ebe9de861eb3483f0e500e07bc9e456d06be`. This is a mapping/provenance boundary, not a measured numerical-parity failure. Earlier native receiver tiny tests remain valid within their stated scope. Root's successful native definitions-only import probe is supervisor-supplied evidence; it was not repeated and does not resolve this graph mismatch.

## Smallest next decision

Independently resolve the exact saved full HF graph/parameter stream before implementing a loader. A potentially smaller protocol-preserving route is the genuinely native full checkpoint class with a text-only forward, selecting its language-model block10, if root approves that change from the requested text_config/CausalLM input. That would avoid TL without pretending visual parameters never existed. It is only a proposal: complete source-fixed order/shape/tied-alias/zero-bias correspondence and fixed old digest still need proof, and no full native model is authorized here.

After that decision, the remaining code is the thin native loader/type/config/source/weight admission plus the retained-owner/counter/finite-trace binding to this tested schedule. No production owner/capture receipt, robust whole-process I/O contract or real native parity is established by these saved-only tests. No freeze or real-release instructions are emitted while that prerequisite is unresolved.
