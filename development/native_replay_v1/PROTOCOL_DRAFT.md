# Prospective fixed parity question — BLOCKED / not released

Can an admitted genuinely native backend reproduce the entire one already-public f03 development prefix? Its only comparison data is the63-file committed reference identified in reference.py and TEST_RESULTS.json. Reference/input/runtime/fitted gate/logit/derivative hashes are authenticated before model access. Root's definitions-only import result is reused, never rerun by this harness.

Once the full weight-stream mapping blocker in README is resolved and independently reviewed, one separate root authorization would permit one local CPUfloat32/eager Qwen3.5-0.8B load, first public IDs[[1]]/mask[[1]]/zero offset, then the exact137-token input with five saved offsets in order. Six total forward attempts, only row003 one derivative, zero encoding, no optimizer, no other cases or retries. The gradient remains logits[50057]-logits[48964] with respect to the current block10 post-MLP residual leaf at the last input. Native receiver computations remain unchanged.

Parity: finite complete248320-value logits with max absolute error<=1e-6 for every row; exact1024-component float32 h and nonfinal hash; exact untouched positions; same frozen centroid score and ON/OFF decision at baseline/entry; full1024 derivative max absolute error<=1e-6. No threshold loosening if a backend kernel differs. This criterion is a proposed equivalence test, not previously established HF/TL equivalence. Initial/final content identity must match the fixed legacy digest through a justified complete source-fixed stream, never an observed replacement digest. All native current registry, parameter, flag/version, buffer, cache and hook cleanup predicates remain required.

One absolute300s worker deadline,60s independent saved audit, ONE shared15s cleanup allowance:375s envelope, not a measured completion guarantee.32MiB total output,5MiB/file,64KiB reserved terminal closeout. Raw final logits6×993280B=5,959,680B; five1024-h vectors, one1024-gradient and bounded receipts fit comfortably, but real metadata/controller admission remains required. This draft does not silently inherit old study180F/48D caps.

Stop at first technical or parity failure; preserve failed/partial bytes, primary cause, later cleanup and all remaining cells UNRUN. No retry, reordered row, new candidate or skipped failed denominator. Complete parity with complete owned capture/cleanup may be labeled only backend replay parity for this public trace—not steering success, held-out proof, reusable direction or study completion. Missing weight mapping/input/source/lifecycle proof remains INCOMPLETE/BLOCKED before model access.

Current command is model-free only:

```powershell
& .venv/Scripts/python.exe -B development/native_replay_v1/test_replay.py
```

There is no real launch command: engine.real_entrypoint always raises NATIVE_REPLAY_REAL_ADMISSION_NOT_IMPLEMENTED. Do not infer authority from a passing fake test.
