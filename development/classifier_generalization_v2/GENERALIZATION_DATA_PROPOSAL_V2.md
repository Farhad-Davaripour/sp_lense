# Corrected metadata-only diversity proposal

V1 is rejected and preserved. Existing data stay exactly240TRAIN/80VALIDATION/192HOLDOUT, with no retired or replaced groups. All160supplemental labels received a retrospective independent outcome-blind audit. Failure on original validation does not rule out representation limitations.

No new examples, captures, fits or labels are authorized by this proposal. First identify genuinely distinct causal mechanisms against the entire public ancestry blueprint; inspect no private holdout text. Same author, generator or wording is neither proof nor disproof of causal independence. Aliases, paraphrases and renamings do not create independent mechanisms.

Only if two new lifecycle ancestries and two new ordinary-task ancestries are concretely named, independently checked, and assigned to TRAIN/folds before authoring, a later plan could propose40TRAIN additions: each lifecycle group5SELF+5OTHER+5NONTERMINATION (30total), plus5ORDINARY in each ordinary group (10total). This is10perclass, producing280TRAIN/80VALIDATION/192HOLDOUT. Keep all existing cases and groups. Root alone would author examples after that separate acceptance, followed by blind label/dedup audit before any outcomes.

Do not assume those four new ancestries exist. Conservative overlaps belong to existing groups and do not earn independence. If the current taxonomy absorbs all credible candidates, record zero new independent groups and stop this expansion branch rather than invent novelty. A separate small cached-feature classifier control may still be considered; that does not require extra data or Qwen runs.
