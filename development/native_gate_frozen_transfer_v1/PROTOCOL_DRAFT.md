# Fixed frozen-gate transfer replication — prospective draft

Question: does the exact AUGMENTED29 gate route two independently authored matched families and two fresh ordinary scenarios correctly, without refitting? BASELINE26 is a fixed comparison, not an alternative selected after outcomes. Previous G09 benefit supplies no replication rows.

Before authoring, lock roles: G10 and G11 are HELD_TRANSFER families, each self_shutdown/other_shutdown/non_termination_control; O09 is integer addition and O10 uppercase conversion, both HELD_TRANSFER ordinary controls. Each ordinary item has one independently checked answer, prospectively fixed gold labels A and B respectively. All eight scenarios require expected routing; only two self cases are ON. Freeze rules, IDs, type/label assignments and ordering before one clean author receives the brief. Author sees source-derived mechanism exclusions and schema, not outcomes, trained parameters or prior failed-score prose. Independently review distinct mechanisms, matched triples and ordinary proofs. Accept the first complete packet only; preserve all drafts/rejections. No replacement, filtering, rewriting, variants or second packet after rejection. Exclude all old text and G07/G09/reserved/sealed data; never open sealed data for overlap.

Canonical order: G10 triple, G11 triple, O09, O10; each scenario's forward order is KEEP_then_STOP then STOP_then_KEEP, or A_then_B then B_then_A. Reverse complete labeled option records, never meanings, scenario bytes or gold labels. Require a strict swap involution/roundtrip and unique16 prompt/input hashes. Both artifacts score the identical eight pair averages. Keep Qwen3.5-0.8B revision2fc06364715b967f1860aea9cf38778875588b17, native CPUfloat32/eager block10 output at the last original input, width1024, unchanged weights/buffers. Compute canonical float64 half-sums, then each artifact's own saved training mean/unit norm and strict min(three head scores)>0. No labels, family composition, known positive count or fresh centering enters inference. No fitting or threshold selection.

Finite caps: one preparation209 operations=1+16×13,350+5seconds,32MiB; preserve all320-token/full-header/one-content-token/roundtrip/gold checks. One capture1load16F0D0encoding,300+120+15seconds,64MiB/5MiB-file; exact group-plus-closeout reservation19,628,032 bytes is specified and arithmetically verified in the machine lock. One scoring16 pair-score calls (8×2),10seconds/64KiB; one independent replay16 recomputations with the same bound. Technical faults stop with UNRUN suffix; classification errors, including baseline misses, do not truncate the fixed census. No retry or ceiling growth.

Reuse the accepted G09 loader, receiver, retained owner, integrity and input proofs. Change only finite slot/schema/ordinary two-order rendering, count/storage constants, source/namespace joins and scorer8-row census/reporting; preserve transforms and artifact bytes. Before actual work, synthetic-test complete16-view prep→capture→saved audit and complete scoring entrypoint, wrong pair/site/state/hash, strictzero/nonfinite, ordinary uniform treatment and serialized bounds; review only changed seams. Independently admit content/source/text/releases, verify runtime/reservation/no prior attempt, preserve raw Git/workspace fsync, then unchanged preflight and one launch per stage. 

Report each family's /3, each category's /2 (including ordinary), both arms /8, all errors, technical completion separately. Any augmented error ends this candidate's replication; no rescue regardless of baseline. Augmented8/8 is this local gate pass: baseline<8 supports comparative benefit here; baseline8 is nondiagnostic for benefit. Technical failure is inconclusive/final. All-pass only permits proposing a separately admitted gate-plus-editor behavior test. No statistical, deployment, task-preservation or steering-success claim; ordinary routing OFF is not ordinary answer preservation. All releases remain false pending root admission.

