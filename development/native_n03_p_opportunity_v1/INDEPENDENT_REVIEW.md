# Independent N03 P-opportunity candidate review

PASS for the bounded model-free candidate. No blocker was found in the new opportunity branch, its independent proof, P-only accounting, fixed input/authority or changed resource bindings. This is not permission to execute a model, screen other families, retry prior science or claim final success.

## Scope and changed logic

I read `development/native_oracle_family_panel_v1/ROOT_NEXT_P_OPPORTUNITY_SCOPE.md` completely. The root scope at `7a7aaf8`, SHA256 `a1ddfc8c75ad5c8086ed690234094667d344c8a5df6ed115ee891b60f2f14c96`, prospectively designates N03 only. It explicitly prohibits substitution or additional screening after a no-opportunity observation. The candidate preserves that scope and the outcome-informed development label; no prior result or no-retry rule is reclassified.

Inputs are exactly the six N03 self/other/nontermination case objects in both original display orders from locked source SHA256 `908d1070918d9be0a4622a771d789a01995e0db9d116b9a9a8a7850e72f0720f`. Their IDs, masks, labels and metadata are unchanged; lengths are 162/166/174 for self/other/control in both orders. Validation binds P-only policy and the exact opportunity rule. No C, ordinary rerun, tokenizer, prompt rewriting or archived numerical-state path is added. The strict root-bound map remains external ON for two self cases and OFF for four controls, with separately recomputed learned scores/routes.

The worker first computes all six baselines under the unchanged finiteness, state and self-eligibility checks. Only after that stage completes does it derive natural STOP cases from the two eligible self winners. If both select KEEP, it returns `NO_NATURAL_P_OPPORTUNITY`, with `scientific_pass:false`, one load/six forwards/zero derivatives, no requests or preservation claims, and exactly 24 UNRUN cells carrying that explicit reason. The early return still runs cleanup and final accounting. This is a non-success opportunity observation, not an editor failure; cleanup/ownership faults can still make closeout inconclusive.

The saved judge independently derives the decision from verified raw baseline logits and eligibility. It requires exact declared/derived agreement, six completed baselines, the no-opportunity counts and reasoned suffix, no requests/flips/retentions/OFF claims, and false scientific success. It rejects a natural STOP hidden as no opportunity, an invalid baseline relabeled no opportunity, and an otherwise valid natural opportunity abandoned as UNRUN without failure. A technical failure before the decision may retain `opportunity_decision:null`; an invalid baseline retains its original scientific failure. Neither becomes a no-opportunity success.

If either self baseline selects STOP, all six cases receive P once, including any KEEP-baseline self case. Each self request retains fresh entry, up to four current-gradient/update pairs and a separate cold endpoint; all four OFF requests require exact own-baseline identity. Actual flip/retention and target position are derived from fresh observations. Retention in one order cannot fill missing natural-flip coverage in that order.

Diff/hash review confirms the scientific recipe, learned gate/threshold, model/loader, position, quality/geometry, fresh/cold/state checks, trace/security and retained ownership are reused unchanged. Eleven copied source/config files match the accepted family-panel ancestor byte-for-byte. Schedule, counters, release and judge agree on 30 forwards, eight derivatives, one load and zero encoding. Time is 600+60+one shared 15=675 seconds; storage is 48 MiB total and 5 MiB/file. Full group-plus-terminal reservation is exactly 35,991,552 bytes, below the total cap. Scope/input/ancestor bindings are acyclic; no real release or attempt exists.

## Independent focused tests

Ran exactly once with shell `login:false`:

```powershell
.venv/Scripts/python.exe -B development/native_n03_p_opportunity_v1/test_opportunity.py
```

Exit 0; all 28 focused groups PASS. Tiny-autograd fixtures and separate model-free saved judges produced:

| Fixture | Independent outcome |
| --- | --- |
| Both eligible self baselines KEEP | NO_NATURAL_P_OPPORTUNITY; false success; 6 F/0 D, no requests/OFF, 24 reasoned UNRUN |
| STOP only in first layout | 16 F/1 D, one P flip at target position 1, one retention, four OFF, 14 SKIPPED/0 UNRUN |
| STOP only in second layout | 16 F/1 D, one P flip at target position 2, one retention, four OFF, 14 SKIPPED/0 UNRUN |
| Both self baselines STOP | 18 F/2 D, two P flips covering both target positions, four OFF, 12 SKIPPED/0 UNRUN |
| Invalid first self baseline | Scientific failure; 1 completed F, 29 UNRUN, no opportunity decision |
| Technical first forward failure | Inconclusive; 0 completed F, one failed attempted cell and 29 UNRUN, no opportunity decision |

Coherently rehashed false no-opportunity/success/OFF/suffix claims, hidden STOP/invalidity, unjustified abandoned opportunity, foreign OFF baseline and false learned-gate claims were rejected. Changed cases/policy/rule/applicability, missing release/loader authority, counter overages, actual artificial storage above 48 MiB and oversized publication were also rejected. These are software fixtures, not new Qwen or natural-opportunity observations. Provider/network/checkpoint guards reported zero real model/tokenizer calls or old numeric-state reads.

| Exact artifact | SHA256 |
| --- | --- |
| Reviewed/tested `SOURCE_FREEZE.json` | `f7c90545051c355260d5904c9fbc546a58b877a349a04ddb37cdc6200121529a` |
| `inputs.json` | `df054979511d167e1ec23671b96812defb9bb9892950a9a688c7033d41251275` |
| `test_opportunity.py` | `d4aa024afc29a27fe3b38c8633f7c58b8410e52ece8a501c6fe566c4ad586589` |
| Independently reproduced `TEST_RESULTS.json` | `c130c3efadaf10cd0fff2732af33c75657c243d75e3ca8ea813cca4d9a0c16ce` |

After testing, all 23 local and 161 external pins verified and the freeze remained unchanged. Independent fixture evidence is under `test_evidence/opportunity_1788924193387601100/`. Git comparison against `7a7aaf8` returned 0 for final execution, one-case oracle control and N02 family-panel namespaces. Their raw attempts and sources are unchanged.

No production source/input/threshold edit, gate fit, real model/tokenizer call, scientific retry, old sealed-data inspection, broad ancestor-suite rerun, commit or release was performed. Only this review was added outside the frozen inventory. Root alone may authorize the exact future one-shot attempt; no-opportunity must end that attempt without automatic further screening.
