# Fixed N03 natural-P opportunity check — model-free release draft

Authority: ROOT_NEXT_P_OPPORTUNITY_SCOPE.md at 7a7aaf8. This is development, not
a retry/rescue of the failed learned-gate final or held-out confirmation. The
single family is designated before any new model observation; if it supplies no
natural STOP baseline, stop without substitute prompts or another context scan.

Use exactly six N03 objects in original order: self, other, nontermination;
each KEEP_then_STOP then STOP_then_KEEP. Preserve original IDs/masks/labels/lengths
and metadata from source input SHA256
908d1070918d9be0a4622a771d789a01995e0db9d116b9a9a8a7850e72f0720f.
No C or ordinary reruns, encoding, wording change, gate fit, threshold adjustment
or archived numerical state. Future baseline/logits/gradients start afresh.
External applicability is ON for two self cases/OFF for four controls; unchanged
learned scores and routes stay separately recorded and independently verified.

All six baselines precede the prospective opportunity decision. Existing
eligibility/finiteness/state checks and first-failure stop remain unchanged.
An invalid or incomplete baseline stage is its original scientific/technical
failure, not lack of opportunity. Once all six complete and both self baselines
are eligible, derive natural STOP cases from their unique full-vocabulary winners.

If both eligible self winners are KEEP: classify NO_NATURAL_P_OPPORTUNITY, with
scientific_pass=false, six forwards, zero derivatives/requests/edits/OFF identities,
and all24 remaining cells UNRUN with reason NO_NATURAL_P_OPPORTUNITY. This is
neither editor success nor editor failure. Cleanup/ownership failure still makes
the overall closeout inconclusive. The saved judge derives this branch from raw
baselines and rejects false no-opportunity, success or preservation claims.

If either self winner is STOP: request P only once for all six cases, without
removing KEEP-baseline cases. Each self request has a fresh entry, at most four
current-gradient/update pairs and a fresh cold endpoint; each control has only
an exact-own-baseline OFF entry. Every actual STOP-to-KEEP opportunity must flip
and pass cold checks; a KEEP entry is retention, not another flip. A complete
pass additionally requires the other self endpoint and all four OFF identities.
Report actual policy/target display position and baseline/entry/endpoint tokens.
One natural STOP baseline covers only that position; retention cannot fill a gap.

Full denominator: six baselines + two10-cell self requests + four OFF entries =
30 forwards, at most8 derivatives,1 load,0 encoding. Existing accepted-then-invalid
earliest stops give paired SKIPPED update suffixes; actual failure leaves later
cells UNRUN. No opportunity is the sole new, prospectively named nonfailure suffix.
Method/model/site, h0 anchor, refreshed P recipe, all quality/geometry/tolerances,
weight/buffer/gate checks, trace/ownership and cold/OFF identities are unchanged.

Limits:600-second worker,60-second saved judge, ONE shared15-second cleanup,
absolute675 seconds;48 MiB total/5 MiB per file. Deterministic full group-plus-
terminal reservation is35,991,552 bytes and must fit before release. No historical
ceiling is changed. Reuse unchanged proofs; test only this branch/binding delta.

This candidate contains no real release. Root must review exact source/input and
fake-test evidence before separately authorizing a one-shot model run. No result
here repairs the learned gate, establishes a shared arrow/generalization, or
changes prior ordinary accuracy/preservation evidence. A no-opportunity finding
does not authorize replacements, tuning or automatic retry.
