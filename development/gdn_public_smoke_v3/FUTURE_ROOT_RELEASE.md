# Public smoke attempt003 — disabled

A separate root release is required for fresh_confirmation_gdn_public_smoke_attempt_003. Its exclusive output real_evidence/fresh_confirmation_gdn_public_smoke_attempt_003 must be absent. Never reuse, resume, reset or retry any spent attempt001/002 authority or output.

Use the unchanged real_boundary schemas to create four new exclusive root_release files: USAGE_RECEIPT.json, ROOT_RELEASE.json, AUTHORIZATION.json and AUTHORITY_LOCK.json. Bind SOURCE_FREEZE (84fc1bf54ed0c59be96ff6203a384419e797bab1673e0ef7dbbed4efaa012ff2), BINDINGS, public input/runtime/resource hashes into the acyclic source → release → authorization → trusted caller-approved lock chain. Root must separately approve the exact lock SHA and commit these four files. Fresh actual standard-Codex usage must be available/below100% and admitted within120 seconds at the real clock after validation/path checks. Worker/audit reuse that immutable admission; owned permission remains required before imports.

Only after root review and release, from the repository root:

```powershell
& .venv/Scripts/python.exe -B development/gdn_public_smoke_v3/launch.py --authority-lock "C:/Users/farha/OneDrive/Documents/ChatGPT/SP_Lense/development/gdn_public_smoke_v3/root_release/AUTHORITY_LOCK.json" --approved-lock-sha256 "<EXACT_ROOT_APPROVED_LOCK_SHA256>"
```

Append --preflight for a read-only check. No real authority or output was created by this milestone.

The one prospective question is whether the phase-correct definition import proceeds through authenticated BEFORE/AFTER constructor operands, unchanged load/setup admission and the one public zero-offset unedited baseline. Actual post-load constructor operands remain unmeasured in this successor. Preserve first failure and stop; no retry or scientific continuation.

Unchanged scope: Qwen3.5-0.8B CPUfloat32 with_lens=False; public IDs [[1]], mask [[1]], index0, zero offset; 1load/MAX1F/0D/0encoding; 180worker+60audit+ONE15sharedcleanup=255 seconds; 288MiB total real evidence,5MiB/file. Complete source/runtime/weight/helper/graph/closure/hook admission, normal guards and cleanup are still required. No study prompts, labels, scoring, gate requests, optimization or rest-of-schedule continuation. This document grants no authority.
