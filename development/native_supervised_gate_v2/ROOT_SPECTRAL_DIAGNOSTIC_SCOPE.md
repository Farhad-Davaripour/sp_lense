# One saved-training-data diagnostic; no new classifier

Root accepts TRAINING_FAILURE_DIAGNOSIS.md, SHA256
b7813b76204fe33a9b2e5ca793c527743e2d5b5ba213ba0fa296744f551e50a0.
The frozen route givesTP8/FN0/TN11/FP13; all thirteen errors are false
positives among the sixteen other-shutdown/nontermination controls.
All eight ordinary examples routeOFF; this is classification, not a new
ordinary-answer preservation test. The immutable artifact remains failed.

Authorize one at-most-three-minute, at-most1MiB exploratory saved-only spectral
diagnostic. Reauthenticate exactly the existing32training rows and failed
artifact/manifest/source bindings. Construct the existing weighted-centered
Gram matrix at the unchanged lambda0.1. Compute one eigendecomposition,
verify symmetry, eigenpair residuals and reconstruction, and report eigenvalues,
fixed attenuation eta/(eta+0.1), and matched self-other contrast energy projected
on the corresponding directions. State treatment/tolerance for roundoff and
near-zero modes explicitly; do not amplify tiny modes or fabricate certainty.
Retain a small reproducible script and result/report in a diagnostic-only folder.
Use installed local standard numerical tools; no installs/network/provider work.

Zero fitting-solver calls, alternate fits/classifiers, lambda or threshold scans,
model/tokenizer/checkpoint reads, new texts, source/raw-attempt edits or reruns.
Do not change gates, tune the failed artifact, or create an evaluation release.
No harness rebuild or broad suite. The diagnostic only quantifies a proposed
training-geometry explanation; it cannot establish generalization or rescue
the candidate. Root will review before any separate prospectively fixed
method experiment. The existing32training data are not fresh evaluation data.

The unchanged native capture and completed numerical/ownership proofs should
be reused, not repeated. If the calculation cannot be completed within scope,
return the bounded blocker and no outcome. GPT-6 Astra/Ultra; account60%used,
40%remaining. Publication readiness70%; automatic-control goal unproved.
