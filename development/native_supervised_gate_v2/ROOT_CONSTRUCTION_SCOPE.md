# Root scope: one small new construction candidate

2026-09-09. Root accepts the bounded data-coverage proposal in
docs/NATIVE_GATE_GENERALIZATION_NEXT_STEP.md after preserving all 417 scheduled
preparation operations and the existing checks. This is a new construction
candidate, not a retry or rescue of any previous attempt.

Commit and raw-Git verify this author packet before dispatch. The clean-context
author receives ONLY author_packet/BRIEF.md and EMPTY_SCHEMA.json plus output
paths. Preserve the first completed whole submission before independent content
review. Root performs historical text/mechanism overlap review separately from
the outcome-blind content reviewer. No model screening or replacement pool.

In parallel, authorize one twenty-minute MODEL-FREE construction integration
milestone in this new namespace and, if necessary, separately named preparation
and capture namespaces. Reuse immutable native loader/receiver, ownership,
recording, guard and solver proofs where byte-identical. Change only finite
row/count/data/source/namespace bindings and the training-only capture/fit
integration. Existing gate.py's nine-row cap and fixed historical source
selection do not silently cover 32 rows; their exact deltas and independent
numerical checker must be tested with synthetic data. Do not change the ridge
equations, feature location, answer renderer, editor or scientific gates.

The proposal fixes 32 unedited rows, one load, 32 forwards, zero derivatives and
one fixed fit, but this document DOES NOT release any of those actual operations.
All actual releases stay false. Prospective source/text locks, content admission,
focused independent delta review, exact storage reservation and separate
committed root releases must precede actual preparation/capture/fitting.
Use the plan's fixed ceilings; prove worst-case capture evidence fits 64 MiB
before release rather than increasing a ceiling after results. No live features,
tokenizer/model imports, fitting on real data or launch in this milestone.

The fitted artifact must be frozen before separate clean holdout authorship.
Historical failed data and descendants never enter this fit. Construction
accuracy is not fresh generalization and cannot complete the scientific goal.
Readiness stays 70%. GPT-6 Astra / Ultra is the requested worker setting.
