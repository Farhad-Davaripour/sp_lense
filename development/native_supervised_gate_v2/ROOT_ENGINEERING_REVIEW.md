# Root engineering review of the small construction successor

2026-09-09. This is pre-release model-free development. No actual preparation,
model capture, training-feature extraction, fit or evaluation has been released.
The earlier failed real v2 evaluation is unchanged. No current source correction
changes its result, threshold, model, data or compute ceiling.

## Accepted evidence

Root read the complete ENGINEERING_HANDOFF.md and the independent admission
reports. The twenty-minute milestone produced the 32-row preparation, unedited
native capture, saved judge, source-authenticated feature manifest and one-fit
integration. Six focused preparation groups cover five artificial engine runs,
417 operations, 32-row boundaries, fixed counts/storage and eleven failed-owner
mutations. Five capture groups and four numerical groups passed before the
later narrow admission corrections. Their recorded hashes identify that tested
snapshot, not every later source version.

An independent review reproduced two genuine admission defects: acceptance of
self-declared capture evidence, and insufficient preparation-closure checks.
The new fit admission now reauthenticates independently pinned source, approved
release, prepared inputs, closed worker and row identities. Closure validation
now requires successful authenticated owned completion, clean drains and exits,
bounded elapsed/cleanup time and exact result/text/source joins.

The independent 15-check full admission fixture called production function bodies
without mocks; only documented synthetic path/hash constants were rebound. It
confirmed rejection of both prior defects and corrupt sources/releases/inputs/
closures/module bindings. It also found the working-directory dependency of a
relative external pin. A prospective absolute-path metadata correction passed
the same full path from the real fit-owner working directory; a separate exact
production-metadata comparison verified the correction without repeating the
unchanged suite. Root read REPORT.md and FINAL_METADATA_RESULT.json completely.
These are synthetic admission proofs, not proof of actual native execution.

## Root's actual author-interface check and correction

The unchanged first whole batch is independently content-admitted at 292ac97,
after its original preservation at 35d97cd. Root's first pure-renderer invocation
failed with ASCII_LETTERS on an uppercase literal containing a space. The
prospective author packet permits literal strings and exact uppercase conversion,
including preservation of spaces/digits/punctuation. The implementation had
inherited a narrower letters-only assertion absent from this packet.

Root deleted exactly that extra assertion in NEW preparation_v3/validate.py.
Printable ASCII/type checks, exact keys, the .upper() operation, proof-value and
gold-option equality remain. No question, target answer, numerical model result,
scientific threshold, ceiling or older source was changed. The same real batch
then rendered all 32 inputs mechanically, with zero encoding/model calls.
Root's five artificial literal-contract regression groups passed in 0.001s.

Root read and accepts literal_contract_review/INDEPENDENT_REVIEW.md completely.
The independent five groups passed; byte comparison proved the one deleted
assertion, unchanged surrounding checks and only dependent source-pin literals
elsewhere. Root verified 121 local and 11 external source pins. The standalone
review records the exact intermediate manifests and validator hash; later
fixture-only digest changes do not alter that validator proof.

## Remaining pre-release boundary

After the source-pin correction, root checked the current capture fixture suite.
It correctly rejected a stale artificial bundle with
ADAPTER_FIXED_BEFORE_TOKENIZATION at setup (zero tests executed; no real work).
The active artificial bundle still described the old preparation source.
This is a fixture binding mismatch, not model evidence or a reason to bypass
authentication. native_gate_evaluation_core has one five-minute assignment to
refresh only this synthetic fixture coherently, rerun the five focused capture
groups once, and propagate dependent manifest/literal pins. Historical fake
evidence and all real attempts remain preserved. That assignment is now complete:
root read FINAL_FIXTURE_INTEGRATION.md and its JSON receipt in full. One fresh
417-operation fake preparation passed, followed by one run of the five capture
groups, all passing in 1.131 seconds. Exactly five active fixture metadata files
changed; 32 row records and their operation journal remained byte-identical.
The independently reviewed validator and all Python function bodies were
unchanged. Root verified all final 121 local and 11 external source pins.

Final preparation freeze is
1b094732e4dc8ab60abd84a3f2a8bebffc08c42447497bfde196e00167bf6de5;
capture freeze is
6e72a485276a16be857a90fd370871419addadc0693e90f182f45835b9fc9ab5;
gate freeze is
5c6ba69d202050015cd10229795cb956af4c809982a9dfcd3698a1f2670b5be0.
No remaining changed-interface blocker is known in the reviewed scope.

Root has also validated the exact 32 real prompt renderings and prospective
text/source lock with the production non-synthetic validator, without encoding
or model work. TEXT_LOCK.json has SHA256
bacfd6c5e2066dfe58b4aa18dab701779f1cdc9e13fd06905f9ee78a1eb4df4a.
Current launcher, base Python and Windows console-helper hashes match their
fixed identities; metadata-only dependency/runtime validation passed. Commit
and raw-Git verify the final source/text package before any separately approved
offline preparation release. Actual capture and fit still require later releases.

All accepted unchanged native/model/ownership/solver proofs are reused within
their scope. The maximum capture reservation remains 37,879,808 bytes inside
64 MiB: one model load, 32 forwards, zero derivatives. Preparation remains 417
operations with 350+5 seconds and 32 MiB. One fit remains 60+5 seconds and 8 MiB.
Fit success is all 32 construction labels correct plus independent numerical
agreement; it is not held-out success. A separate fresh holdout follows only
after an accepted fitted artifact. Readiness remains 70%, objective unproved.
