# One actual offline preparation admission

Root accepts the complete source/text/content and changed-interface proofs in
ROOT_ENGINEERING_REVIEW.md and ROOT_COHORT_ADMISSION.md. The final source/text
package at de84aba, with exact receipt preservation through 9ca5762, was checked
against raw Git: 659 files / 3,551,654 bytes. One non-gating independent JSON
receipt was initially newline-normalized by Git; a scoped -text attribute and
explicit restaging now preserve its exact original bytes. No reviewed source,
input, result or receipt content was altered to manufacture evidence.

Root validated the exact final text lock through the non-synthetic production
validator: 32 prompts, fixed source bindings, no tokenization/model work. Current
launcher/base/console image hashes and metadata-only dependency/runtime checks
pass. The bound helper's security checks remain enabled.

Authorize exactly one offline preparation of the unchanged admitted 32-row
construction batch under preparation_v3/root_release/PREPARATION_RELEASE.json.
Its committed exact hash must be supplied to the retained hidden owner. Maximum
417 operations, 350 seconds plus 5 cleanup seconds, 32 MiB combined preparation
and owner output, 5 MiB per file, no truncation beyond the fixed 320-token input
ceiling, no retry or replacement. All existing header, mask, roundtrip and joint
single-token proofs remain required. Any failure closes this actual attempt.

This authorizes only the pinned offline tokenizer and preparation records. No
model class/tensor, feature capture, derivative, actual fit, evaluation, network,
installation or external publication is authorized. Actual capture and fitting
need separate later releases after this result is accepted.
