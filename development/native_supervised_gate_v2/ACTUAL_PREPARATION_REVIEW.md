# Independent actual preparation review

PASS_ACTUAL_PREPARATION_ADMISSION. Reviewed 2026-09-09, 15:58:58–16:01 UTC,
within the authorized three-minute window. No blocking preparation-admission
defect was found. This report does not release model capture or fitting.

A fresh standard-library Python process was run with `python -B -` and working
directory `development/native_supervised_gate_capture_v1`. It called the actual,
unpatched `input_reader.read_bundle(base, binding, synthetic=False)` against
`root_release/BUNDLE_BINDINGS.json`. No function, module constant, path, source
hash, or artifact was substituted. The adapter accepted all 32 actual prepared
inputs. The focused review command exited 0 in 0.187 seconds internally.

The actual adapter verified fixed cohort identity and submission/text bindings,
exact case order, category and gold metadata, ordinary proof values, rendered
prompt hashes, full generation header, final-input position, complete masks,
single-content-token joint answer boundaries, source identities, preparation
result/journal joins, and successful bounded retained-owner closure. Independent
additional checks confirmed 32 distinct token sequences, eight positive and
24 negative rows, 417/417 completed operations, 834 ordered journal entries,
zero failed/unrun operations, and input lengths 47–193 (ceiling 320).

All 37 bundle copies match original bytes and their declared hashes: 35
preparation artifacts, the fixed text lock, and owner closure. The preparation
archive contains 41 original files totaling 246,544 bytes. One `git cat-file
--batch` invocation independently compared 81 raw Git blobs:

- 41 original attempt/owner/CLI files against
  `ea95a10b109b3d7104425e027b7e9f8fc5cb222b`.
- 37 copied bundle artifacts plus BUNDLE_BINDINGS against
  `4227f5b143722ea373886bd00c2961461e8b5f5c`.
- The approved preparation release and fixed text lock against prelaunch
  commit `fac572f`.

The closure's preparation release, owner source, text lock, and preparation
result hashes join exactly. The retained root CLI receipt reports exit 0 and
matches closure status PASS, quiescence, timeout false, worker exit 0, and
elapsed 8.297 seconds. Closure verifies authenticated, assigned-before-resume
ownership, empty job, closed pipes, clean drains and cleanup, and successful
retained exits for actual worker, launcher, and console helper. Preparation
worker elapsed time is 7.297 seconds. These are below the fixed 350+5-second
limits. The prior unchanged ownership/source/native proofs were reused.

## Exact verified hashes

- Preparation source freeze: `1b094732e4dc8ab60abd84a3f2a8bebffc08c42447497bfde196e00167bf6de5`.
- Capture source freeze: `6e72a485276a16be857a90fd370871419addadc0693e90f182f45835b9fc9ab5`.
- Gate source freeze: `5c6ba69d202050015cd10229795cb956af4c809982a9dfcd3698a1f2670b5be0`.
- Preparation release: `5996c121945363fdbf8824124603db19534f654ea54a7464e42c2b241b7fe487`.
- Fixed text lock: `bacfd6c5e2066dfe58b4aa18dab701779f1cdc9e13fd06905f9ee78a1eb4df4a`.
- Prepared inputs: `b4f1234f2c072dcdf83c5d4c381f3d35f36b3415af27516b2d8075d65e211970`.
- Preparation result: `f7bab766a6f2fc4decca3391e119af5e51b1d7cd1898bfe3b75a9c75d812ec84`.
- Owner closure: `161fd606c5142cc80488b3aa1d8160002eecb683d1be3ae836351cb917acbb3b`.
- Root CLI receipt: `0d8e6058662f30d20216e8fd1db1b13c90074058c1141b2843e60814fadf986b`.
- Bundle bindings: `e1df259ab630474ba3d6bb10283bdf6fb47311aab5336a90551cd0deffc525fb`.

BUNDLE_BINDINGS has `approved:false`, `model_authorized:false`, and
`fit_authorized:false`; it authenticates this prepared bundle and grants no
model release. The reviewed preparation release authorizes only offline
preparation and explicitly denies model/capture/fit authority. No model work
is reported by the saved preparation result.

During this review there were zero tokenizer/model/provider/tensor operations,
fits, new preparation runs, launches, raw-attempt modifications, source edits,
or commits. The process imported none of torch, transformers, tokenizers,
safetensors, or numpy. Actual authored text was accessed only through the
fixed bundle's required authentication/validation. No broad synthetic suite or
prior unmocked fixture was rerun. Root retains subsequent model-release authority.
