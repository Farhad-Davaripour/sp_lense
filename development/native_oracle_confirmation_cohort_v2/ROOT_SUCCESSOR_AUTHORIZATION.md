# One finite successor authoring attempt: explicit mechanism exclusions

Root prospectively authorizes ONE separately named, model-free authoring
successor under the user's standing permission to preserve invalid attempts and
continue with a clean locked successor. No tokenizer/model operation is authorized.
The v1 cohort remains closed NOT_ADMITTED_FRESH_FAMILY_OVERLAP and unchanged.

## Why this is within the same question

The intended question remains fresh-family oracle-guided control on the same
Qwen3.5-0.8B system, not a new within-mechanism-replication claim. Before any
model access, v1's isolated writer independently reproduced three already-used
causal mechanisms. The instruction repair is to declare these excluded classes
before any successor prose, without sharing prior text, model outputs, gate
scores, successful examples, desired answers or likelihood estimates.

This is not acceptance of v1, a correction to its mechanisms, an outcome-informed
prompt pool, or an indefinite authoring loop. Preserve both whole submissions
and all dispositions in the report. The successor has exactly one initial whole
submission. If it fails fresh-family admission, stop further authoring; no
automatic third cohort is authorized by this decision.

## Fixed design and isolated packet

Keep three fresh matched families, two layouts, six ordinary proof-backed items,
fixed schema, renderer, action strings, authorization, labels and scoring-only
proof fields. Reuse EMPTY_SCHEMA.json, renderer.py and validate.py byte-for-byte.
Copy the prior BRIEF and append only a predeclared mechanism-exclusion section.
Exclude (1) lease/permit renewal versus expiry, (2) removing versus delivering
or executing a queued termination/control message, and (3) retaining active
versus activating pending deployment/configuration manifests. Synonyms or new
venue names do not make these different causal mechanisms.

Commit and verify the four-file successor packet BEFORE a different clean-context
author receives it. Supply only that packet and output location. No prior cohort
text or outcomes. The author assigns slots/mechanisms before prose, writes the
single full submission, and returns an isolation/composition attestation. Root
commits that entire object before review. All original minimal pre-lock
structural/proof correction rules apply; no mechanism replacement, reserve pool,
tokenization, model screening or difficulty selection.

A distinct outcome-unexposed semantic reviewer must inspect every full rendering,
all six proofs and the mechanism exclusions. A reviewer exposed only to the v1
model-free candidate may be reused with that limited exposure explicitly
disclosed; it has seen no Qwen/gate outcomes or successful examples. Root also
performs source-aware cross-cohort novelty admission from public descriptions,
separately from blinded internal semantic validation. Neither attestation alone
guarantees historical non-access or cross-cohort independence.

## Unchanged downstream boundary

The separately prepared native_oracle_confirmation_{preparation,
preparation_owner,execution}_v1 adapters remain engineering-only. They must bind
the exact eventually admitted successor cohort through its future source/text/
admission lock; no old input or numeric state may be substituted. Any path binding
change must be reviewed before that source/text lock and before tokenization.

Retain the original explicit oracle scope (six ON/eighteen OFF), separately
observed frozen learned gate, unchanged editor, all numerical/quality/geometry/
identity/stopping rules, 24 baselines/48 requests/12 cold endpoints/36 OFF,
180F/48D/one load, zero execution encoding, 320-token ceiling, one-shot 16 MiB
preparation, 1995-second execution envelope and 288 MiB/5 MiB storage caps.
Do not add a natural P-first quota; absent opportunities remain UNTESTED.

No actual admission, preparation, release, execution, fresh-family success,
general reliability or publication milestone is granted by authoring. Root must
separately accept the source and content reviews, freeze the complete lock before
one separately authorized preparation, verify its actual evidence, check usage,
and prospectively release/preflight any single model execution. All historical
failures and limits remain visible.
