# GDN runtime admission — ordinary engineering, not a real trial

Status: focused engineering checks PASS; the original post-load constructor discrepancy remains unresolved. No production namespace or installed package is changed. No model trial or launch is authorized here.

## What is established

The closed real helper diagnostic attempt_002 is preserved at 08183dc0551c98088de71d7a6f87e2ade2cb5052. REAL_ATTEMPT_INVENTORY SHA256 5f15e0a78495b01abd36b3aebd344f3955260cc3398109197abc65d30cebeb8a covers 223 entries; all 224 working/raw-Git files (1,758,587 bytes) verified. The 31 real files total 632,349 bytes. All 187 original source/test files and four root release files are untouched. INCONCLUSIVE_DIAGNOSTIC, 1 load/0 F/0 D/0 encoding, 180 cells/48 requests UNRUN, 33.437 seconds, 0.890 shared cleanup; owned worker/audit plus launchers exited 0/quiescent, guard restored. Full scientific cleanup and helper admission did not complete.

The saved trace identifies the constructor's compound CALLABLE_CODE condition, not its false operand. Two permitted offline definition-only probes (10.031 and 11.562 seconds) compared actual function type, every material code field, compiler optimization levels 0/1/2, globals and resolved path. The constructor matches pinned source in all comparisons both before and after importing TransformerLens. Therefore none of its acceptance conditions can justifiably be weakened, normalized or replaced. The probes do not prove that model construction is the cause; they delimit the unresolved difference between zero-weight import state and the historical post-load state.

The probes also establish a concrete, separate latent rejection: Qwen3_5GatedDeltaNet.forward is wrapped by pinned transformers.integrations.accelerate.force_accelerate_hooks. That decorator has no __wrapped__ attribute, so inspect.unwrap returns the wrapper. Its code/path correctly differ from the original HF forward. This discrepancy is reproducible before any model construction and would be reached only after the constructor check passes; it is not claimed to explain the earlier constructor rejection.

## Minimal candidate delta

compat_candidate.py copies the immutable compatibility component with only:
1. The same constructor/code acceptance conjunction split into function-type, exact code-equality and resolved-source-path checks. Rejection codes distinguish CALLABLE_FUNCTION_TYPE, CALLABLE_CODE_IDENTITY and CALLABLE_SOURCE_PATH. admission.code_conditions exposes only finite operand booleans and differing code-field names, suitable for a future bounded diagnostic receipt. It does not accept additional constructors or infer trust from metadata.
2. Replace the erroneous forward unwrap expectation with admission.admit_accelerate_forward. It authenticates accelerate.py SHA256 72de9bd04d7827ac83aee81593d0d253768eb7196f01a5f2067aef80a4e80bc5; exact wrapper code/path/globals/defaults; exact two closure names; child names exactly [conv1d]; and the enclosed original forward's exact HF code/path/globals/defaults/closure. No generic wrapper fallback, kernel selection, code normalization or unwrapped invocation is introduced. The actual wrapper remains untouched and remains the only dispatch path.

The rest of the component AST and the entire source_contract.py bytes match the frozen v3 helper component. All original conv/chunk source, selected-base, default, closure, identity and rollback checks remain. No helper semantics, parameters, model inputs, numerical code, hook/weight predicates, scoring or scientific thresholds change. No candidate is integrated into an authorized loader here.

## Focused tests and permitted probes

test_admission.py: 11 checks PASS in ordinary unit testing (initial 0.392 seconds; final 0.522 seconds after restoring the exact original comparison against the already-resolved trusted path). Both results are retained; these are normal engineering iterations, not frozen experiment retries. The accepted exact function and pinned-wrapper chain are never called. Nine negatives reject non-function, changed code, wrong resolved source, altered wrapper code, changed child closure, altered base code, wrapper defaults, base defaults and foreign globals. The wrong-path test explicitly proves code equality first, so it reaches the intended path condition. AST parity and copied source-contract byte equality pass.

probe_candidate.py, the third and final permitted library probe, imports already-installed TL/HF definitions then runs the candidate's full Targets admission on those definitions: PASS in 11.109 seconds. Constructor operands are all true; original helper/default/closure/source checks and the corrected accelerate wrapper check all pass. This probe preceded the final path-comparison tightening: all its supplied trusted paths were already resolved, so the tightened comparison is identical for those observed arguments; final pure tests cover the checked-in source, and no fourth library probe was run. It invokes no model constructor, tensor/parameter operation, numerical helper, model/tokenizer load, forward, derivative or encoding. This is not post-model-construction proof or real authorization.

Each of the three library subprocesses had an independent 30-second subprocess timeout, -B/-E, explicit offline flags, and an audit hook denying network connection and checkpoint-file access. Only definition imports and metadata/source checks were requested. Probe 1/2 retain their harmless co_lnotab deprecation warnings. Raw compact stdout/stderr and outcomes are saved. No earlier frozen suite, six-group batch or model attempt was rerun. Zero-weight engineering tests do not earn scientific milestone credit.

## Single next real question, still requiring root approval

Can a fresh public synthetic runtime smoke retain the three constructor operands immediately before and after the sole load, and—only if all unchanged admissions pass—complete one unedited forward through the existing bridge and approved helper bindings?

First integrate only this candidate/extra source pin and a <=64 KiB finite operand receipt into a separately reviewed future smoke entry. Preserve exact admitted source/runtime/weight/instance/closure/hook checks and record the original failure before cleanup. Constructor rejection must still stop; no observed mismatch becomes an allowlist. The before/after constructor snapshots should include exact code-field equality bits/compiler settings/path-equality and admitted function/source identities, not parameter data or arbitrary exception dumps.

Proposed public synthetic input is prospectively fixed IDs [[1]], mask [[1]], final input index 0 and zero intervention offset; it is not a study/held-out question, does not need a tokenizer, and carries no gold/policy label. Root must approve this explicit synthetic choice before any real work. Bound the fresh attempt to the same local Qwen0.8B CPU float32, 1 load, at most 1 forward, 0 derivatives/encoding, 180-second worker +60-second audit +one shared15-second cleanup, <=288 MiB evidence/5 MiB file. No study continuation, automatic retry or threshold change.

If the constructor still rejects, stop with the distinct operand/field evidence and all forward work UNRUN. If admission and the synthetic forward complete with exact cleanup/receipts, report execution-smoke completion only, not steering reliability. Missing evidence or cleanup failure remains inconclusive. This plan is not a release command or authorization. The exact constructor cause cannot be recovered from the historical trace; resolving it requires a separately authorized post-load observation, not further definition-only speculation.
