# Clean-source startup successor — model-free engineering PASS

The four focused startup cases PASS in2.844s. This is not a model result or real authorization. The entire actual worker/core path executes public input admission, native reservation, the actual filesystem writer and pinned source provider, the unchanged allowlist, Trace creation, helper/operand reservations, actual loader/target-source authentication and native loader-proof reservation. A test-only checked-load sentinel then stops before any candidate research import. The actual saved audit runs afterward. No isolated AST fragments replace this startup path.

The authority dependency and closed-worker capture in these tests are explicitly inert test doubles with production_authorized:false and INERT_STARTUP_TEST provenance. They select the existing diagnostic resource path but do not supply real authority or claim an owned production run. Existing real authority/ownership proofs are reused unchanged. All synthetic audit findings remain INCONCLUSIVE_DIAGNOSTIC; the passing engineering criterion is reaching the sentinel or preserving the expected failure, never scientific success.

The sole executable-source deletion is obsolete8516dd9d13a626c44a4173e31b55f0f1cea1b64d:development/gdn_public_smoke_v1/candidate_loader.py (7b6774…). The current456f096dbd5376dc4a7154113bf10fe412a146ab loader pin (0767e3…) remains exact, as do all216 retained source records. Actual source-read receipts authenticate that current loader and never access the deleted reference. Historical bytes remain in the closed v1 build and Git. The entire diagnostic_support.py, including the no-different-code-per-path predicate, is byte-identical; reinserting the obsolete pin is rejected.

The only new runtime behavior is finite failure retention before Trace exists: explicit startup stage, bounded predicate/category, source/execution identity and an exclusive acknowledged STARTUP_FAILURE.json up to8KiB inside existing native closeout capacity. Worker and saved-audit copies are joined to the existing closed-worker identity. No arbitrary exception text, locals or values are serialized. Native publication failure preserves the finite finding while native_verified remains false; the saved success condition additionally requires no startup failure. No latch, source gate, scientific predicate or cleanup rule is relaxed.

Cases: clean startup reaches only the sentinel; reinserted same-path conflict yields SOURCE_PATH_CONFLICT at ALLOWLIST; source-byte mismatch yields SOURCE_PIN_MISMATCH at WRITER_SOURCE_ADMISSION; partial startup-primary publication preserves19 actual bytes and the original finite cause without claiming complete native evidence. Every child exits0 and closes. All cases execute zero research-library imports, loads, forwards, derivatives, parameter work or encoding. An initial ordinary engineering pass (2.672s) is separately retained before the additional explicit saved AND predicate; the final report is bound to the final source freeze. No real attempt was retried.

Seventeen reused core files are byte-identical, including the corrected real loader/adapter, constructor snapshots, all14 parameter predicates, retained-order checks, original allowlist, trace/cleanup, writer, counter and ownership code. The real boundary differs only by the fresh attempt identity. SOURCE_PARITY.json and NARROW_DIFF.patch show the bounded changes.

The prior public attempt001 is preserved at0afe53e4a1be4a1655e2297585c53614e2aaf60a, INCONCLUSIVE after2.125s with no loader dispatch. The deterministic stale-reference defect is consistent with that stop; its exact unrecorded historical exception is not retroactively asserted. All prior real/frozen evidence remains unchanged.

Future root-reviewed scope is unchanged: public IDs[[1]], mask[[1]], index0, zero1024-float offset; pinned local Qwen3.5-0.8B CPUfloat32,1load/MAX1F/0D/0encoding,180worker+60audit+ONE15cleanup=255s,288MiB/5MiB. No gate, scoring, study prompt, request or continuation. Actual live loader/weight/helper/graph/hook setup and cleanup remain runtime gates. Preflight is disabled because no root_release exists.
