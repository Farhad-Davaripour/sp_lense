# Fresh public smoke release — disabled

Only a separate root release may authorize fresh_confirmation_gdn_public_smoke_attempt_002. The corresponding output real_evidence/fresh_confirmation_gdn_public_smoke_attempt_002 must be absent. Never reuse the spent attempt001 authority/output or resume/reset/retry it.

The exact unchanged real_boundary schemas require four new exclusive root_release files: USAGE_RECEIPT.json, ROOT_RELEASE.json, AUTHORIZATION.json and AUTHORITY_LOCK.json. Root binds the current SOURCE_FREEZE, BINDINGS, public input/runtime/resource hashes into the acyclic source→release→authorization→caller-approved lock chain, separately approves the exact lock SHA and commits the four files. The trusted actual standard-Codex usage receipt must be available/below100% and admitted within120s using the real clock after all validation/path checks. Later roles authenticate that immutable admission. Retained owned permission precedes research imports/load.

Exact future command from the repository root, only after root approval:

```powershell
& .venv/Scripts/python.exe -B development/gdn_public_smoke_v2/launch.py --authority-lock "C:/Users/farha/OneDrive/Documents/ChatGPT/SP_Lense/development/gdn_public_smoke_v2/root_release/AUTHORITY_LOCK.json" --approved-lock-sha256 "<EXACT_ROOT_APPROVED_LOCK_SHA256>"
```

Append --preflight for a read-only check. No root_release or real output is present in this build. All real fixture/clock override denials remain unchanged.

Public[[1]] only; mask[[1]], index0, zero offset; pinned Qwen3.5-0.8B CPUfloat32 with_lens=False;1load/MAX1F/0D/0encoding,180+60+ONE15=255s,288MiB/5MiB. Existing constructor/weight/helper/source/guard/cleanup checks and full setup capacity admission remain required. No study scoring/gate/request or continuation is authorized. Any real failure is preserved and stops the attempt. This document grants no authority.
