# Root authorization: prospective storage-only repair

The owner candidate at e2dec311139bac0769dde2ef9cefd7ff0caae910 and source hash
bd5361f5171b7123b6367598fd3f9b917e35f74d1c5784f63b9a0171d87b6eb1
passed nine independent fake-child checks but remains blocked for real admission.
Root read the complete independent review and contract. The universal combined
16 MiB cap cannot be proved while the preparation terminal writer may consume
all 16 MiB and owner records require additional space. The block is valid.

Root authorizes one bounded, model-free storage-only engineering repair before
any TEXT_LOCK, real preparation release or tokenizer operation:

1. Reserve 32768 bytes of the unchanged 16777216-byte combined ceiling solely
   for owner evidence. Every preparation output write, including critical and
   entry-failure paths, must enforce the remaining preparation allowance before
   writing. Retain a separate terminal reserve within that allowance.
2. Bound failure serialization and terminal result size deterministically,
   including pathological exception messages/types. Preserve an honest compact
   failure code instead of copying unbounded exception text. Keep owner captures
   and records within their 32768-byte allocation with pre-write enforcement.
3. Test exact-boundary acceptance, one-byte overflow refusal, large failure
   text, terminal/entry-failure publication, and the combined owner-plus-prep
   maximum using fake data/children only. Refusal after an overrun is not enough.
4. Rebind only affected preparation, final-adapter and owner candidate source
   identities before the combined text/source lock. Do not alter questions,
   model, scientific method, gate, thresholds, 313 tokenizer operations,
   320-token ceiling, 180-second preparation envelope, or final study budgets.

These are unadmitted model-free software candidates, not running or completed
scientific attempts. Revise them in new commits within the existing engineering
milestone; preserve the earlier exact candidate commits, blocked review, and all
existing attempt/test evidence. Do not rewrite prior results as if a later source
version produced them. No new scientific experiment namespace is necessary for
this ordinary pre-tokenization repair. Separately frozen historical scientific
attempts and their bytes remain untouched.

Keep real admission disabled until the revised storage contract, relevant fake
checks and independent review pass. Root alone can then approve the combined
lock and a separate one-shot preparation launch. This authorization is not that
release and does not permit tokenization or model execution.
