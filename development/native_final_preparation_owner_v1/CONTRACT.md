# One prospective preparation owner

No real release, text lock, tokenizer or model work is created or authorized here. This source is outside both frozen preparation and execution inventories. Root adds `preparation_owner_binding` = {namespace: development/native_final_preparation_owner_v1, source_freeze_sha256: this SOURCE_FREEZE.json hash} to the future admitted TEXT_LOCK before any tokenization. The unchanged preparation hashes the entire lock.

Future command: `.venv/Scripts/python.exe -B development/native_final_preparation_owner_v1/owner.py --owner-source-sha256 HASH --approved-preparation-sha256 ROOT_RELEASE_HASH`. The sole substantive child command is exactly the pinned .venv Python, `-B`, native_final_preparation_v1/prepare_offline.py, `--approved-preparation-sha256`, ROOT_RELEASE_HASH. The owner verifies its own reviewed bytes, the existing preparation release and locked owner identity before the exclusive ownership_attempt_001 claim. No release is supplied here; no root release means no child.

The Windows venv executable is a launcher for the pinned base Python. The launcher starts CREATE_NO_WINDOW|CREATE_SUSPENDED, enters our non-breakaway kill-on-close Job before resume, and is retained by its original Popen handle. The actual interpreter must be captured live with termination/query/synchronize rights and authenticated against owned-job membership, direct launcher parent, creation order and pinned executable path/hash. The reviewed NativeHandle and probe identity reader are reused. No PID-only kill, posthoc transcript identity or inferred child exit is accepted. Job membership is an OS containment proof, not a self-reported child handshake. A child that exits too quickly to retain/authenticate is a failed ownership attempt, not a successful closure.

Fake-child observation established one legitimate optional Windows console helper under the hidden launcher. Only the exact pinned System32/conhost.exe path/hash is additionally allowed, at most once, with the same job/parent/creation authentication. Its retained exit proof is also mandatory for quiescence when present. It is not an arbitrary-descendant exception.

One exclusive owner attempt and one preparation attempt; no retry. Wait at most 175 seconds from ownership claim, then at most one shared 5-second cleanup window; the entire owned process envelope is at most 180 seconds. Cleanup terminates the owned Job once if necessary, then observes actual and launcher exit codes through their retained original handles. An unassigned suspended launcher is terminated only through its original retained handle. Job containment also terminates any unexpected descendants; extra descendants fail admission. Output drains run concurrently, retain at most 8192 raw bytes each, and stop work on capture overflow or read error. Quiescence requires both retained exit proofs, an empty job, EOF, joined drains and closed pipes. Deadline/nonzero/overflow/nonquiescence stays FAIL, even if later exit is observed. The owner does not renew cleanup budgets or rerun the child.

Preparation and owner outputs together must remain within 16 MiB, every file at most 5 MiB. Owner reserves 32 KiB total (two 8 KiB captures, at most 12 KiB closure, compact admission). Preparation's unchanged writer reserves 64 KiB until its terminal result; owner additionally requires its terminal RESULT.json to be at most 8 KiB. Combined actual size is monitored and independently checked before closure publication. Oversized/unpublishable evidence is never reported PASS; raw retained prefixes remain intact. No old preparation/model caps change.

PRE-RELEASE STORAGE BLOCKER: the preceding checks detect/refuse an overrun but do not universally prevent it. The frozen preparation's critical RESULT publication may consume its full 16 MiB, and failure error text has no fixed length bound. Therefore no deterministic combined-write guarantee is claimed yet. Root must resolve this with an explicitly prospective non-scientific storage reservation/serialization change before approving a real owner launch; polling and an 8 KiB postcheck alone are insufficient. This limitation is not permission to enlarge the combined cap or erase failed evidence.

Until that prerequisite is resolved and prospectively re-frozen, COMBINED_STORAGE_ADMISSION_READY is false: even an otherwise supplied release cannot launch the real preparation through this candidate. Fake-child tests remain executable. Ownership mechanics passing is not a production-release approval.

Exact outputs: ADMISSION.json, stdout.bin, stderr.bin, CLOSURE.json. Closure schema root_preparation_closure.v1 binds owner/preparation-release/text-lock hashes, exact command and times, launcher/actual creation identities and images, retained exit proofs, job/drain/pipe state, termination receipt, actual elapsed/cleanup time, preparation RESULT raw hash/status and combined-byte count. Its one_shot, quiescent, exit_code, timed_out, preparation_result_sha256 and text_lock_sha256 fields directly support the already frozen final adapter. Root must inspect status PASS and all closure fields before copying these exact bytes to its later execution bundle; a receipt is never reconstructed after the fact.

Focused tests launch only fake_child.py with fixed success, exit7, timeout, stdout/stderr overflow and exclusive retry rejection. These exercise actual Windows handles/jobs but zero tokenizer/model imports or actual cohort access. The real preparation ownership observation remains future work requiring root release.
