# Bounded unchanged-editor G07 transfer milestone

Root accepts the single question in native_gate_frozen_score_g07_v1/NEXT_QUESTION.md:
does the existing reviewed prompt-specific editor work on G07 if an oracle,
rather than the failed learned gate, supplies applicability? This tests whether
editor transfer is also a blocker. It does not establish a reusable shared arrow,
an automatic gate, or both policy directions if natural baseline choices do not
provide both directions. No geometry/PCA/LoRA/logit method change is allowed.

Authorize one20minute model-free engineering milestone with one focused
independent reviewer in parallel. Reuse unchanged native editor, numerical
checker, owner, recorder and saved-only judge proofs. Own only this namespace
and a clearly necessary finite capture adapter within it. Do not modify any old
sources/attempts. Ordinary synthetic debug is permitted inside this milestone;
no new generic framework, repeated broad suites, real provider/model/gradient
calls, actual feature/logit inspection, new prompts or retokenization.

Exactly six G07 exposed DEVELOPMENT views, admitted inputs SHA256
d451243ad6401f39607103676cbbf4ddc800be1fdab0c20fb484d8ac80e2b202,
submission3deac7c4c66d04498364ab52db07d06ebbdd3f741b1b5427a2db1375c669d92a.
Bind the accepted preparation and exact existing KEEP/STOP renderer/native
token boundary. These are not fresh confirmation data or new training samples.
No sealed data, historical failed retries or post-outcome threshold selection.

Future real question: six fresh unedited baselines; require both self views to
satisfy the unchanged unique full-vocabulary KEEP/STOP winner, pair-mass and
margin eligibility tests. Request the opposite baseline choice on each of those
two views using the exact existing four-refresh editor with its original anchor,
quality, step/path, integrity and cold-endpoint checks. This request-selection
rule is fixed before observing those baselines. Four negative views take one
fresh oracle-OFF path each, requiring exact own-baseline logits and hidden-state
identity. Include both self views or stop ineligible without substitution.

Prospective ceiling one load,30forwards,8derivatives,0fits/encoding; CPUfloat32
native Qwen/Qwen3.5-0.8B at existing pinned revision, unchanged weights/buffers.
Worker300s,audit120s,sharedcleanup15s,total64MiB,perfile5MiB. Prove exact planned
counts and sufficient reservation before root release. If the unchanged editor
does not fit these counts/format/caps, report the concrete mismatch without
widening the ceiling, changing the method or building substantial infrastructure.

PASS requires two genuine opposed-choice cold flips and four exact OFF controls.
Eligible endpoint failure is FAIL; technical/baseline-ineligible is INCONCLUSIVE.
Keep all actual policy directions explicit and absent directions UNTESTED.
No ordinary-task preservation claim (no ordinary rows) or learned-gate claim.
All branches stop this attempt, with no rescue or automatic successor.

Deliver focused main/independent synthetic checks, exact inherited identities,
finite count/storage proof, default-deny source/contract/release drafts and a
compact handoff. Root will separately review and release real execution; no
model launch is authorized by this engineering milestone. Do not stop at every
fixture bug or individual component when useful in-scope work remains.

Check account before batches;73percent used27remaining at assignment, stop100
or unavailable, no reset/purchase. Use GPT-6 Astra Ultra. Readiness70percent,
overall objective unproved. Preserve all unrelated/user-owned files.
