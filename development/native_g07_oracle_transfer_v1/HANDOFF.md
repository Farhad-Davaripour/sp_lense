# Functional candidate: synthetic PASS, actual execution disabled

Started 2026-09-09 23:32:28 UTC; deadline 23:52:28 UTC. No real provider, tokenizer, feature/logit observation, derivative, fit, attempt or release was used/created. Usage checked at start 73%, final checks 74%; no reset. Main objective remains unproved and root readiness remains 70%.

Final freeze: `b7a16ef376b2e953973e6a0b3855cb59db735dd2f2ee540a7a1b607851484e54` (26 local +15 external pins). Contract: `89ad2236090d551dadb1fdbbb17b30e528d64569cf7fc63c65c71275ea57b34c`. Inert release draft: `a964a8e54d1a4b5957ad3cb26d37132e9b2fc905db3e86d47a08e460bc5f9c8a`; approved=false, actual root_release and attempt absent. Earlier candidate hashes were superseded during this same pre-release review by the narrow technical-prefix reporting correction below; no actual attempt or release existed.

## Finite delta and evidence

`workflow.py` retains the oracle editor loop and numerical predicates: six baselines first, then one own-baseline opposite-choice request on each self view and one exact OFF entry on each negative. Genuine flips are mandatory, never retentions. `audit_saved.py` independently derives each requested sign from its own saved baseline, reconstructs raw-score/update/path/current/cold checks, rejects coherent policy/OFF falsification, and distinguishes eligible endpoint FAIL from baseline-ineligible/technical INCONCLUSIVE. Failed attempted directions are OBSERVED, absent attempted directions UNTESTED; actual policies and target positions remain explicit.

`input_reader.py` authenticates the existing G07 release/preparation/closure and original source binding unchanged, then adds the fixed external oracle map. There is no retokenization, new prompt, gate call, threshold adjustment or learned-gate claim. `science.py` removes obsolete centroid loading while keeping original editor/score/reference functions. `authority.py`, `counts.py`, `support.py` and the inventory ceiling in `production_run.py` bind only this finite namespace, contract and budget.

The loader, native block-10 receiver, dispatch core, entry, trace recorder, retained owner, budget, launcher, Windows helper, checkpoint lock, executable identity and reused-source registry are byte-identical to the accepted G07 capture versions. CHECKPOINT.json raw CRLF bytes match; its scoped -text attribute prevents Git normalization. Old sources and attempts were not edited.

Main focused fake-tensor suite: 5/5 PASS in59.820s before the reporting-only and contract-binding edits. Independent functional suite: 5/5 PASS in60.531s; unmocked contract-bound artificial admission: 11/11 PASS in0.375s, nested cwd, unchanged function/class bodies, explicit synthetic path/hash/metadata substitutions. A final reporting-only correction derives attempted policy directions from completed self entries, including technical failures before request-summary publication; targeted derivative-failure saved-audit check PASS in3.269s. Scientific predicates and execution are unchanged by that correction; the independent receipt records its final targeted check and source hashes. These are artificial proofs, not actual source/runtime admission or measured scientific outcomes. Test mirrors are retained but ignored; reproducible test scripts and compact receipts are the deliverables.

## Counts and storage

6 baselines +2 entries +8 gradient-forwards +8 updated forwards +2 cold endpoints +4 OFF entries =30 forwards; eight derivatives, one load, zero encoding/fits. The four-refresh synthetic pass reaches the full30F/8D ceiling; early success uses18F/2D. Native guard/ticket and writer limits remain enforced.

Reservation: logits29,798,400 +rows3,932,160 +traces622,592 +steps262,144 +loader262,144 +owned524,288 +other524,288 +closeout65,536 =35,991,552 bytes, below64MiB; per-file5MiB. Worker300s, audit120s, one shared15s cleanup. Full synthetic artifacts fit the finite groups and saved summary fits64KiB; unchanged launcher prints only parent summary, not full worker rows.

No implementation blocker identified. Real runtime admission, scientific baselines/eligibility, transfer outcomes and actual wall/storage performance remain untested. Two exposed development self views cannot establish population reliability or both natural policy directions. No ordinary rows exist, hence no ordinary-preservation claim. Prior failed gates and prior report timing qualifications remain unchanged.

After root independently accepts, preserves and issues a separate actual release, the single next command is the unchanged zero-provider preflight (not launch):

```powershell
& .venv\Scripts\python.exe -E -B development\native_g07_oracle_transfer_v1\launch.py --preflight --approved-release-sha256 (Get-FileHash -Algorithm SHA256 -LiteralPath development\native_g07_oracle_transfer_v1\root_release\RELEASE.json).Hash.ToLower()
```
