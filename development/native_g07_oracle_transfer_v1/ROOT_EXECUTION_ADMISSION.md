# One G07 oracle transfer execution authorized conditionally on admission checks

Root read the final independent review, handoff, contract, release draft,
workflow and actual input adapter. Accept final SOURCE
b7a16ef376b2e953973e6a0b3855cb59db735dd2f2ee540a7a1b607851484e54,
CONTRACT89ad2236090d551dadb1fdbbb17b30e528d64569cf7fc63c65c71275ea57b34c,
main receipt8ccb6f554c37df040fbdbfb8a3088012412bab16de12c3f0eb54a7c82694118b,
independent review1b6493166fdd7422a00051043d7f44121836670bae3ec251bd2a2f8b0f3cb9bc.
The original editor, scoring/reference mathematics, eligibility, bounds, cold
endpoint, exact OFF identities, native loader/owner and actual-input checks are
retained. Five functional groups, eleven artificial admission checks and the
final technical-prefix reporting regression passed. The two unsuccessful
ancillary test-script reverse-hash assertions remain disclosed; no test-script
equivalence is claimed. The tested production reporting delta is independently
reconstructed, with unchanged scientific predicates. This is not a real outcome.

Root's RELEASE.json differs from the frozen draft only by approved=true.
Authorize one actual native_g07_oracle_transfer_attempt_001, no retry. Exactly
the six already exposed G07 development inputs and fixed oracle map are used.
Six fresh baselines precede both-self eligibility; each eligible self request
targets its own opposite baseline choice using the unchanged four-refresh
editor. Four negatives receive exact own-baseline OFF controls. No new text,
tokenization, gate fitting/calls, mean, threshold, method or source changes.

Unchanged caps: one load,30forwards,8derivatives,0encoding/fits; worker300s,
audit120s,sharedcleanup15s,total64MiB,file5MiB,reservation35,991,552bytes.
Native Qwen/Qwen3.5-0.8B at the pinned revision, CPUfloat32/eager/eval, block10
last-original-input, unchanged weights/buffers. Actual observed directions must
be reported; absent directions are UNTESTED. No ordinary rows or ordinary-task
preservation claim, shared-arrow claim or learned-gate claim is permitted.

Before launch, the designated worker must authenticate41pins and all exact
prepared-input/closure joins, current signed runtime identities using inherited
read-only checks, no prior actual attempt, and the fixed reservation. Preserve
only the finite source/contract/draft/tests/main+independent receipts/handoff,
root scope/admission and actual release, excluding generated fixtures and all
unrelated files. Commit, verify raw Git equality and fsync before execution.
Use the unchanged launch --preflight with the actual root release SHA. Stop on
any failed required check without modifying frozen sources or widening limits.

If preflight passes, run the identical launch command once without --preflight,
with the same release SHA. Preserve every actual outcome/UNRUN suffix/closure,
run only its built-in saved audit, archive raw evidence/CLI and a compact actual
receipt, verify raw committed bytes and fsync. Return counts, flips/retentions,
OFF identities, policy directions, timing, hashes and remaining independent
review requirement. Root separately authorizes an independent saved replay.
All scientific outcome branches stop this attempt; no automatic successor.

Usage75percent used25remaining; check before every batch and stop at100 or
unavailable. No resets/purchases. Overall goal unproved, publication70percent.
Existing review-isolation/report-timing qualifications remain intact.
