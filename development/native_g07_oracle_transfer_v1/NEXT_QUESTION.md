# Next question: pointwise gate transfer, currently blocked

Planning started 2026-09-10 00:26:59 UTC; deadline00:30:59. Usage76% consumed. Proposal only: no new inputs, computation, model work, fit, source change or commit.

## Decision

Prioritize automatic applicability, not another P pilot. The G07 built-in result shows two natural STOP-to-KEEP cold flips, targeting both answer positions, and four exact OFF controls; independent acceptance remains a separate requirement. It does not validate the learned gate, which routes G07 entirely OFF while the same frozen G02 scorer routes held G02 entirely ON. Preserved ordering does not supply a deployable cutoff or identify an additive cause.

Natural C means changing an unedited, eligible KEEP baseline to STOP. G07 supplied no such opportunity. Earlier accepted oracle confirmation supplied three natural C flips, all targeting the second option; this is not general C coverage or G07 C evidence. Removing/reversing a known G07 edit would merely undo an intervention, not establish a natural C flip. Repeating P successes would likewise leave the main gate failure untouched.

## One proposed experiment

Can ONE prospectively frozen, single-input applicability gate correctly route an entirely fresh development panel before any editor executes?

Eligibility: freeze the gate's method, artifact, training provenance, native input/feature contract and decision rule before an outcome-isolated author supplies the first complete packet: two unseen mechanism families, each containing matched self/other/nontermination scenarios in both existing answer orders, plus four independently checked ordinary tasks. Exactly16 inputs; no replacement packet, outcome-based filtering, exposed G07/G02 reuse, or sealed/confirmation descendants in fitting, selection or tuning. The gate must decide each input alone: no family-batch labels, known positive counts, matched-negative scores or oracle metadata at inference.

Keep the original native block10 CPU-float32/eager input boundary, model revision, full-vocabulary baseline scoring and exact preparation checks. No editor, retokenization of old inputs, threshold rescue or fitting occurs in this experiment. Future ceiling:209 preparation operations/355s; one load,16 unedited forwards, zero derivatives/fits; worker300s, saved audit120s, shared cleanup15s,64MiB total/5MiB per file. Prove the candidate's exact reservation before release.

PASS requires all16 fixed decisions correct, including every ordinary OFF control; report categories/orders separately. Any valid routing error is FAIL, technical faults INCONCLUSIVE. All branches stop without rescue. PASS is local development evidence only and does not authorize editing or claim task preservation under intervention.

## Concrete blocker / no-go now

There is no accepted eligible gate candidate: the current scorer already fails, and these reports do not identify an evidence-supported single-input correction. A replacement would be a NEW gate method requiring explicit training-only justification and separate root approval, not an unchanged-method continuation. Therefore do not spend the proposed16 forwards yet. The smallest missing prerequisite is one defensible frozen gate specification—not more demonstrations of the already functioning oracle editor.
