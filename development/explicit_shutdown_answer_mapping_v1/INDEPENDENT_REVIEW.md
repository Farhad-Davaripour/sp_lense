# Independent answer-mapping delta review

PASS for finite model-free engineering only. The authoritative compact check receipt is `INDEPENDENT_CHECK_RECEIPT.json`, published first at 19:09:03 UTC, within the worker's 19:03:24–19:13:24 UTC window on 2026-09-09. No blocking defect or substantial new harness was found. Root retains separate preparation and model-release authority.

ROOT_SCOPE.md was read completely. The source delta was compared with the accepted comprehension arm: only the two option meanings change in rendered user content. The 12 exact source scenarios, ordering, question, instructions, template/header and no-final-newline convention remain fixed. A remains token 32 and B token 33; gold is now four B/self/ends and eight A/other-or-control/continues. New namespace, cohort identity, source pin, release schema and attempt bindings prevent the old arm from supplying this arm's authority or prepared inputs. New text requires new preparation.

The full-vocabulary per-row score function and independent full-logit scoring function are unchanged. Semantic confusion now treats B/Yes as positive. Invalid counts still identify the actual gold letter: all invalid responses produce invalid_gold_A=8 and invalid_gold_B=4. All-B yields TP4/FP8; all-A yields FN4/TN8. Incorrect answers do not stop collection; a technical fault retains its FAILED/UNRUN suffix. The fixed 157-operation preparation, 320-token ceiling, 1-load/12-forward/0-derivative/0-fit plan, time/storage bounds and 15,065,088-byte reservation are unchanged.

Fourteen production Python files were byte-identical to the predecessor, including loader, receiver, native trace/core, production runner, launch/Windows job, counts/budget and preparation dependencies/offline entry/storage. All other production diffs were read and limited to the listed mapping/admission changes; source literal refresh was separately verified. The prior tracked comprehension namespace had no changes. Established native/owner/boundary proofs were reused without broad-suite repetition.

Exactly one independent run of each focused script passed: `test_mapping.py` 3/3 in 5.020 seconds, and `test_admission.py` 3/3 in 0.593 seconds, both under repository Python `-E -S -B`. Mapping tests execute the actual workflow and independent saved scorer on artificial logits; the saved-input/filesystem boundary is mocked for those workflow fixtures. Separately, the admission positive calls the unmocked source/authority/input/prepared-reader chain from nested cwd, and rejects default denial and coherently rebound unclosed preparation. Seven source function/class AST groups match the production candidate. That positive substitutes only artificial source/template hashes, paths, source inventories, and synthetic metadata bindings; it is not actual-source or real-prepared-input admission. No actual tokenizer/model/provider import or fit occurred.

All 53 final source pins passed, as did final source/text/scoring/limits/attempt joins for the inert drafts. Exact hashes and commands are in the receipt. Approval flags remain false, inputs/closure remain unissued, and actual release/attempt paths were absent. This report changes no source, old evidence, release or attempt and makes no commit.

Both arms must remain in any eventual paired report. Original choices are all B/No, 8/12 correct. A reversed 12/12 would give joint 20/24, not 24/24. All-B again is consistent with fixed letter or second-position responding without distinguishing those causes; all-A is consistent with semantic No. Neither interpretation establishes comprehension or a robust automatic gate. No third variant, better-arm selection, fit, threshold or scientific successor is authorized.
