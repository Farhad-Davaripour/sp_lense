# Native final execution binding — prospective only

This adapter is implemented and frozen before the first new-final tokenizer call. It does not admit text, prepare inputs, or authorize an execution. No actual new cohort text is part of this source candidate. The immutable synthetic predecessor remains disabled.

Before preparation, root includes `final_execution_binding` in the admitted TEXT_LOCK.json: namespace `development/native_final_execution_v1`, this SOURCE_FREEZE.json SHA-256, and `preparation_source_freeze_sha256` equal to bbc9a2587a766e1c1c3260d34add7c621ce32e030ae23b9e141e1b3d52b9feef. The unchanged preparation engine hashes the entire canonical text lock into its output. Root must commit that lock and its adapter/source/environment/renderer/analysis bindings before the first tokenizer call. No post-encoding scientific source change, length rewrite, replacement or retry is authorized. There is no circular hash: the adapter source contains the schema of the future text lock, not its future bytes.

After separately admitted, successful preparation and authoritative process closure, root may separately approve one model execution. The fixed root_release directory must contain TEXT_LOCK.json, PREPARATION_CLOSURE.json, and preparation/{inputs.json,RESULT.json,operations.jsonl,24 exact case receipts}. These are exact copies of admitted/observed bytes, not reconstructed evidence. The closure is a root attestation to retained process evidence, not a replacement for it: schema root_preparation_closure.v1, quiescent true, exit_code 0, timed_out false, one_shot true, and exact preparation_result_sha256/text_lock_sha256. Root retains the underlying authoritative process proof and independently checks it before signing release.

RELEASE.json must have schema native_final_execution_release.v1, approved true, exact attempt native_final_execution_attempt_001, exact output path returned by support.output(), exact authority.LIMITS, source_freeze_sha256, inputs_sha256, checkpoint_lock_sha256, owned_identity_sha256, text_lock_sha256, preparation_closure_sha256, preparation_files (exact 27 relative names to raw hashes), and trace_sources equal to every frozen local Python source hash. Its exact hash is an explicit launch argument, then joined to exclusive admission and both retained-process children. No release is supplied by this candidate. Missing, altered or incomplete release/input/source bytes fail before provider/model loading. Production read() never enables synthetic input scope.

Exactly 24 inputs, 24 original baselines first, 48 fresh P/C requests, 12 self cold endpoints and 36 OFF requests; maximum 180 forwards, 48 derivatives, one load, no smoke. Full inputs are 1–320 tokens with exact declared lengths/masks and KEEP/STOP or A/B boundary receipts. Same checkpoint, CPU float32/eager/full vocabulary, learned gate, layer/position, refreshed gradient, geometry, scores, thresholds and first-failure/SKIPPED/UNRUN semantics. Same 1800 worker + 180 independent saved judge + ONE shared 15 cleanup = 1995 seconds; 288 MiB total and 5 MiB per file, full 209,068,032-byte worst-case group reservation including terminal reserve.

The worker, editor, receiver, counters, process ownership and budget are reused byte-for-byte except support's attempt name. Loader removes only the synthetic denial and resolves the owned bootstrap through the new output path. Saved judge adds the final scope to its mandatory native-loader-evidence check. Its historical classification labels ending NATIVE_DEVELOPMENT remain unchanged internal engine labels; a PASS is not by itself a claim of complete final-study success. Natural baselines, flips versus retentions, target display position and ordinary correctness must be reported separately. Preserved wrong ordinary answers remain wrong. Method/scientific claims are not expanded by this adapter.

Tests use artificial schema markers, constructed prepared receipts and tiny autograd only. They exercise new interface/authority refusals and one complete worker→separate saved judge cohort plus closure aggregation. Existing independent 32-group synthetic binding and 20-group preparation proofs are pinned/reused, not rerun as a new broad suite. No real tokenizer, model, scenario, release or process-owner model launch is used. A future actual owned process closure remains an observation required at that execution, not established by synthetic tests.
