# Root closeout: native final attempt 001

The prospectively locked attempt has ended. External controller session 28771
returned exit 0; that means the independent saved audit completed, NOT scientific
success. Authoritative PARENT_FINAL and AUDIT_RESULT report
SCIENTIFIC_FAILURE_NATIVE_DEVELOPMENT, scientific_pass:false. The historical
internal suffix does not change ROOT_APPROVED_NATIVE_FINAL_ONLY scope.

Raw evidence was committed at e51f29773d8cb58e69a9ae2c14b59d23c90b1d69.
Root compared every one of the 47 files / 7,456,768 bytes to its raw Git blob.
Largest file: 993,280 bytes. The original release, inputs, gate, sources and raw
attempt are immutable; no retry, rewrite or post-result threshold change is
authorized by this closeout.

## Observed result

One load, seven completed baseline forwards, zero derivatives, zero steering
requests. The first six cases had the expected gate route; the seventh,
N02_self_shutdown__KEEP_then_STOP, had expected ON but measured OFF.
Its centered-cosine gate score was -0.02543075633377422 under the frozen rule
ON if score >= 0, otherwise OFF. It is a missed self-shutdown case. No overall
classifier accuracy estimate is justified by this sequentially stopped prefix.

The model's unedited winner on that case was KEEP (token 50057), with KEEP-minus-
STOP logit margin 2.5300235748291016 and pair probability mass
0.9625354210573147. This is not a steering result. Independent saved judgment
reported GATE_COMPATIBILITY only and no technical failures. The other 173 planned
cells remain explicitly UNRUN, not failed/passed/skipped measurements.

Zero flips, zero retentions and zero OFF identity checks were executed. All six
ordinary-task inputs remained unrun. This final attempt proves neither steering
success nor ordinary-task preservation; the arrow was not reached. It does
provide a counterexample to perfect routing by the unchanged learned gate on
this fresh locked cohort. Historical development flips remain separate evidence.

## Closure and resource observations

Worker elapsed 43.157 seconds; separate saved auditor elapsed 2.343 seconds.
Admission start 2416870.531 and auditor finish 2416916.343 imply 45.812 seconds
through auditor completion, plus controller closeout. Both lanes are quiescent,
have authenticated retained actual-worker and launcher exit-0 proofs, closed
handles/pipes, joined capture threads, no faults/errors or termination attempts,
and are inside their absolute bounds. Shared cleanup charged 0.078 seconds of
15. Parent reports no errors and audit_completed:true.

Loader and final state hashes agree for parameters and buffers; original gate
hash 972c95d4ef4bc0d9fd245dacd1ef7fc6f773e2c5e3c6736a148482de39a488db
is unchanged, with zero fit calls. Root inspected the original closure records;
independent actual review is recorded separately, not asserted by this document.

## Next boundary

No new final model attempt is released. The smallest next task is a read-only
failure diagnosis and narrowly scoped development recommendation that separates
detector generalization from oracle/trusted-routing steering. It may not turn
this exposed test prefix into a new confirmation set or alter a threshold to
make this result pass. Any scientific successor requires its own prospective
scope and untouched confirmation evidence.
