# Independent final23 bridge engineering review

PASS for engineering admission of this finite bridge. This is not an actual capture/fit release or evidence of scientific performance. The final23 actual capture, fit candidate, and fit attempts remain absent; prospective permissions remain false. Review completed within the same 20-minute milestone ending 2026-09-09 17:34:23 UTC. Usage was available before batches at 21–22% used.

The committed ROOT_SCOPE.md and docs/GATE_READOUT_LOCATION_NEXT_STEP.md were read completely. Review covered only the changed sensor/input/source/manifest/admission bridge and focused synthetic checks. No actual features, model, tokenizer, checkpoint tensors, fresh data, or actual fit were used. No production source, old artifact, or release was edited, and no commit was made. Tests created synthetic temporary/mirror evidence; the reviewer authored only this report.

## Sensor and preserved machinery

The receiver selects the exact decoder at index 23 and observes its forward output at the last complete original input position. Local Qwen3_5TextModel source places the separate final norm after the decoder loop; the unchanged native loader independently requires exactly 24 decoder instances, width 1024, CPU float32, eager execution and the pinned checkpoint. Workflow and saved audit require blocks.23.hook_out, model.language_model.layers.23, one hook call and the exact final-input index, preserving complete IDs/masks, zero offsets, finite float32 features, full logits, and integrity evidence.

The actual receiver constructor, hook_context and forward_inputs bodies were exercised against a fake NumPy tensor/module interface: block10-final=111, block23-nonfinal=24, block23-final=123, postnorm-final=1123. Only 123 was captured, no edit registered, and hooks were removed. Ancillary digest/registry setup was controlled in this synthetic fixture; unchanged integrity proofs are reused, not claimed to be freshly tested against a real model. Coherently rehashed wrong hook, wrong target and wrong position rows were rejected by the saved auditor.

Thirteen capture loader/guard/count/trace/entry/owner/runtime/checkpoint files are byte-identical to the predecessor. All gate/checker function and class source text is unchanged; gate.py changes only the feature target. Eight construction function source bodies, including run, schedule, confusion, authorize, write_new and output_summary, are unchanged. The fit owner is byte-identical. Training-only centering/normalization, whole-family folds, strict-zero routing, saved w/b/alpha, fail-first stopping and conditional FULL32 therefore retain their prior proofs. The documented singular-system UNCERTIFIED/no-fallback policy remains unchanged.

Tracked old capture, layer10 evaluation/editor and hard-margin sources remain unchanged. No future evaluation implementation or global layer10 replacement was introduced.

## Input and capture-to-fit authority

REUSED_INPUTS binds the original immutable bundle base, source identity and accepted input/result/closure digests, with both record and inherited bundle metadata approved=false. The new adapter authenticates the original reader and passes the original bindings to its unchanged prepared-input validator. It never invokes old capture launch authority. New release schema, attempt, execution scope and reuse-record hash establish separate authority.

The new fit authenticator independently requires the new capture source pin, exact reused preparation digests, valid closed capture, complete 32-row selection, new namespace/feature contract, immutable row/input hashes and exact block23 hook/position. It does not delegate feature authentication to the old layer10 gate. Candidate creation is deferred until a valid closed new capture supplies its own manifest/content hashes. Those hashes must be frozen into new input/source/contract metadata before a separate fit release.

One complete unmocked synthetic authority chain was independently reproduced from the fit-owner working directory: new source_auth.authenticate_capture → new authority.read_release → new reuse adapter → original adapter → original prepared reader. All ten checked function/class AST collections matched production. The artificial 32-row positive passed; corrupted source, coherently hashed unapproved release, old execution scope and coherently hashed failed preparation closure were rejected at the intended boundaries.

This mirror substitutes only synthetic filesystem bindings and constants: original adapter PREPARATION_SOURCE_SHA256; validate.SCHEMA_SHA256; new adapter ORIGINAL_READER_SHA, INPUT_SHA, RESULT_SHA and CLOSURE_SHA; new source_auth capture/prepared-input/result/closure hash constants. Source inventories, absolute paths, synthetic text/proof hashes and release metadata are correspondingly rebound inside the mirror. No production authentication or validation function is mocked in that chain. These substituted fixtures are not actual-source admission or a real approved release. Exact reproducible setup and substitutions are in test_admission.py; this review's receipt is admission_mirror_k87_sc0m/RESULT.json.

## Focused verification

Each command ran once independently from its corresponding new namespace:

```text
..\..\.venv\Scripts\python.exe -E -S -B test_bridge.py
capture: Ran 4 tests in 0.712s — OK
fit:     Ran 3 tests in 1.412s — OK

..\..\.venv\Scripts\python.exe -E -S -B test_admission.py
5/5 passed in 0.516s; production_function_mocks=0
```

The fit fixture uses artificial width-1024 rows, substitutes its isolated input-binding/load interfaces, and executes five synthetic optimizations through the real candidate/admission/artifact path. Every retained certificate and held score reconstructs exactly; the saved payload stays below 1 MiB and compact hash summary below 4 KiB. Default denial, altered permissions, wrong sensor/namespace and one-shot exclusion pass. The separate unmocked chain supplies the authentication proof those isolated fixtures intentionally omit. No old broad native/owner/solver suite was rerun. NumPy was used only for synthetic tensors/optimization; the unmocked admission process imported none of numpy/torch/transformers/tokenizers/safetensors.

Storage/count bounds remain fixed: capture 1 load / 32 forwards / 0 derivatives / 0 tokenizer operations, 1200+120+15 seconds, 64 MiB total / 5 MiB-file, reservation 37,879,808 bytes; fit max5 × 10,000 iterations, 60+5 seconds, 8 MiB / 5 MiB-file, 1,441,792-byte combined reservation. New metadata uses existing bounded groups without enlarging caps.

Final join-only checks passed at 17:30 UTC: all 22 local/41 external capture pins and 11 local/7 external fit pins match; scientific contract equals current method/folds/limits; capture draft binds the exact source, reuse, checkpoint, owner and trace inventory; all actual permissions remain false. No feature authentication was invoked by these final static checks.

## Exact final bindings

- Capture SOURCE_FREEZE: a8b0faee7e1fb776937f48508ff932a95aef5976f156c4bd0ffa441fd9d80178.
- Fit SOURCE_FREEZE: cb3039123fa11c97e8db1ac0476588db275ea307348e88292e76902648655c71.
- SCIENTIFIC_CONTRACT: 53ed32832bf55431345f471ac4add780ef3293ae71b67c30dfed611dd6621b39.
- Capture false RELEASE_DRAFT: 0d1f6f7cc50e46f423122ca4e4dcbd709c1c555cea58e8ad1d7e14346513aa64.
- REUSED_INPUTS: 8f0fbab22353863a8860a14858c2750ea897c05efd7c8e297db7278846ca3871.
- gate.py: eec77b7b38ce04466580aff571193a6076ba20f4abf88c08864b0508c231af6e.
- checker.py: 7cf81c280592f51d9d8024d6baca9dec4fdc7e6b607045f07165aed86749c4b9.
- construction.py: 30789fee2c89d1c18cef20341310ac58f4d40e29dd670284e1f84db77da8afb7.
- source_auth.py: c52bc3e4cd25aa3fb941153c4263426432f096bb8e39095b5c6259b605050731.
- test_admission.py: e440de73b5ee8de88315fa2130f73f23402258e3a4088c074c327cd1381d6fc4.

No blocking bridge defect remains in the tested state. Root acceptance, committed/fsynced prospective release and current runtime/preflight checks remain mandatory before any actual stage. Scientific failure or uncertified optimization must close this final-block candidate without another sensor/method search; even a development pass would not establish fresh generalization or steering/preservation.
