# Root-approved functional milestone: one final-block gate sensor

Root accepts docs/GATE_READOUT_LOCATION_NEXT_STEP.md as one selected,
outcome-informed DEVELOPMENT measurement experiment. The prior hard-margin
candidate stays failed and immutable; this is not its retry or a classifier
parameter search. Root accepts its independent saved review SHA256
72930b1b24539c9e10faa3303025c9d579ed7ea18dabe1345548707ac8575c8d.
G01held6/6 and G02held4/6 were reconstructed exactly from retained certificates,
with later stagesUNRUN. No final artifact or confirmation permission exists.

The sole new question is whether the SAME fixed margin rule can transfer
across the same development families when its feature is read from output of
model.language_model.layers.23, before finalnorm, at the final ORIGINAL input
token. Checkpoint remains Qwen3.5-0.8B@2fc06364715b967f1860aea9cf38778875588b17,
nativeCPUfloat32/eager,width1024. The pinned loader/config assert24decoderblocks.
This final block is a structural choice made before its features/results are
seen. No layer/token sweep, concatenation, penalty, alternate solver or fallback.

Authorize one20-minute MODEL-FREE capture-and-fit bridge milestone, with ordinary
synthetic debugging in these new scientific namespaces only:
development/native_supervised_gate_capture_final23_v1 and
development/native_gate_hardmargin_final23_familydev.
No actual model/capture/tokenizer/feature fit or coordinate analysis during
engineering. No installs/cloud/newdata. Do not implement later full steering
evaluation yet; the present functional milestone is capture+fixed development
fit admission and verification only.

Reuse the exact already prepared32training inputs and proofs via explicit
hash-bound read-only reuse of the old accepted bundle. Do not retokenize or
rewrite its originals/old releases. InputsSHA
b4f1234f2c072dcdf83c5d4c381f3d35f36b3415af27516b2d8075d65e211970;
preparationresultf7bab766a6f2fc4decca3391e119af5e51b1d7cd1898bfe3b75a9c75d812ec84;
closure161fd606c5142cc80488b3aa1d8160002eecb683d1be3ae836351cb917acbb3b.
The immutable417input checks passed earlier; reuse that proof, not another run.
All32rows and labels are exposed construction/development data, never fresh
confirmation. Newly captured coordinates require newfeature/source/result hashes.

Implement only the fixed23observation target and necessary module/shape/hook,
capture metadata, auditor/source-auth/feature-contract bindings. Reuse
byte-identical native loader/dispatch guards, input checks, ownership/cleanup
and storage proofs. A construction-only receiver may observe23 as its declared
feature; it must not pretend those coordinates satisfy the original10contract.
Do not change any old receiver or global layer10 references. Later evaluation
will need a DISTINCT baseline/entry gate23feature while retaining layer10h/h0,
gradient leaf, offset and editor; do not implement or authorize that stage now.
The eventual gate is computed from the completed UNEDITED pass, never rerouted
from edited layer23activations.

Preserve the exact margin solver/checker function bodies, NumPy2.5.2,
hard-margin objective, strictzero rule, tolerances/ties/intercept and known
UNCERTIFIED singular stop without fallback. Only feature identity/newsource
authentication/namespace bindings change. Preserve G01-G04 train26/hold6,
training-only transforms, fail-first UNRUN, conditionalFULL32 and saved
w/b/alpha+compactCLI. Fit success still requires24/24held semantic and32/32full
training; ordinary8stay training-only. No second classifier or parameter change.

Future REAL release caps, not authorization in this scope:
- onecaptureload,32unedited forwards,0derivatives/tokenizer operations;
  1200sworker+120saudit+15ssharedcleanup,64MiBtotal/5MiBfile.
  Recheck the prior37,879,808-byte worst-case reservation against newmetadata,
  retaining fullvocabulary logits, all32finitefeatures/nativeidentity/closed
  inventory and independent saved audit. Do not enlarge caps after launch.
- onlyvalidclosedcapture permits ONEseparatelyreleased fit owner:
  60sworker+5scleanup,8MiBtotal/5MiBfile,maximum5optimizations and10,000iterations
  each, samefail-first schedule and no retry. No freshholdout/modelsteering.

Focused synthetic checks cover exactlayer23only/position/width and unedited
feature recording; distinct layer10vs23sentinels to detect wrong-site capture;
fixedinput/closedcapture/newfeature/source joins; unchanged numeric solver
bodies/certificates and training-onlyfold handling; default-deny releases,
complete saved-reconstruction inputs/compactoutput, and finite worst-case
storage. No repetition of entire unchanged native or solver suites. One
independent reviewer checks the finite changed bridge and relevant synthetic
proofs while the implementation worker builds it. Any defect is debugged within
this one20-minute window; don't invent sub-experiments for fixture fixes.

Deliver code+focusedresults+source/contract pins+concisehandoff, no commits.
Allactual permissions remainfalse. Root must read and accept the final tested
delta, commit/fsync prospective release, check currentruntime/preflight and
limits, then separately authorize each actual stage. If not ready within20min,
stop and state the exact blocker; don't expand the deadline or scientific scope.
No automatic layer/method continuation follows scientific or technical failure.

Account currently21%used/79%remaining; checkbeforebatches and stoponactual100%
or unavailableusage. GPT-6 Astra/Ultra. Readiness70%, fullgoalstillunproved.
A future development pass only admits afrozenartifact and still requires
separate untouched16-row confirmation and alloriginalsteering/preservation gates.
