# Fixed final-block23 capture and hard-margin bridge

Model-free engineering only. No actual capture, candidate, feature access, fitting,
tokenization, model/provider import, installation or commit occurred. Every real
release remains absent or false. This changes one exposed DEVELOPMENT sensor,
not the classifier, its threshold, or the immutable failed layer10 attempt.

## Exact finite delta

New capture namespace: `native_supervised_gate_capture_final23_v1`. Receiver selects
exact decoder23 of24, hook `blocks.23.hook_out`, final ORIGINAL input position,
CPUfloat32 width1024. The hook observes decoder output before the separate final
norm; the completed pass remains unedited. Workflow and saved auditor require
that exact hook, target and position. New attempt/scope/source metadata prevent
layer10 readings from masquerading as23. No evaluation/editor bridge is built.

Thirteen capture loader/guard/count/trace/entry/owner/runtime/checkpoint files are
byte-identical to capture_v1; receiver identity and cleanup function bodies are
retained apart from the exact target and decoder-count assertion. support changes
only the unique attempt. authority adds the new schema/scope/readout/reuse join.
REUSED_INPUTS.json explicitly binds the original accepted input/result/closure
hashes, old immutable bundle base and original source identity with approved=false.
The authenticated original adapter rechecks all417 existing input-proof records;
it is never asked to authenticate a new capture under an old release or to tokenize.

New fit namespace uses a direct final23 capture authenticator, not oldlayer10
delegation. Candidate creation requires a valid closed new capture first and
freezes its manifest/content digests; a separate root release is required to fit.
gate.py changes only CONTRACT.native_target; every gate/checker mathematical
function body, fold scheduler, run, confusion and compact output body is unchanged.
fit_owner.py is byte-identical. The new source/namespace/content joins are the
only construction changes. Singular restricted KKT still stops UNCERTIFIED with
no fallback or tolerance change.

## Focused tests completed

- Capture test_bridge.py:4/4 PASS0.710s; independently reproduced4/4 PASS0.712s.
  Production receiver ctor, hook_context and forward_inputs execute against fake
  tensor/module interfaces, with ancillary digest/registry setup controlled.
  Distinct sentinels: block10final111, block23nonfinal24, block23final123,
  postnorm1123; only123 is captured and owned hooks are removed. Additional tests
  cover32fake captures/saved audit, coherent wrong10/position rejection, unchanged
  prepared proof over the tracked artificial bundle, release rejection and caps.
- Fit test_bridge.py:3/3 PASS1.362s; independently reproduced3/3 PASS1.412s.
  Direct saved-row/manifest/content joins use artificial in-memory captures with
  authenticate_capture replaced only for that isolated layer. The separate
  synthetic candidate/admission/artifact path mocks only input bindings and
  load_saved, then executes the real five-stage solver on width1024 artificial
  rows. Every saved w,b,alpha,mu certificate and held score is reconstructed;
  result fits1MiB and compact summary fits4KiB. Default-deny, altered permission,
  wrongnamespace/site and one-shot exclusion pass. No inherited broad suite rerun.
- Full unmocked artificial admission chain:5/5 PASS0.422s from the fit-owner cwd;
  independently reproduced5/5 PASS0.516s on the exact same script/source state.
  New source_auth -> authority -> reuse reader -> original adapter -> original
  prepared reader accepted32 artificial rows, rejected changed source, an
  unapproved release, old execution scope and unclosed preparation. Production
  function bodies are unmocked and AST-identical; path/hash constants and only
  artificial metadata are substituted. No provider/numeric imports in this test.
  See FINAL_TEST_RECEIPT.json and the independent review for exact final receipts.
  Two earlier artificial fixture failures (missing historical source metadata,
  then Windows long paths) were debugged within this milestone, not new scientific
  attempts. All artificial evidence directories remain retained.

## Frozen bounds and remaining actual admission

Capture keeps1load32F0D0tokenizer,1200worker+120audit+15sharedcleanup,64MiBtotal/
5MiBfile. Reservation remains37,879,808 bytes: group maxima37,814,272 plus65,536
closeout reserve. New metadata consumes existing bounded groups, not enlarged caps.
Fit keeps max5optimizations/10,000iterations,60worker+5cleanup,8MiB/5MiBfile,
1MiB worker payload plus393,216 owner reserve =1,441,792 bytes. Namespace16MiB
is independently enforced. Gate and editor still have distinct future contracts;
no later steering evaluation is authorized or implemented.

Root must accept/freeze these exact sources and the false capture RELEASE_DRAFT,
then separately create the actual capture release and perform current preflight.
Only after valid closed capture may root authorize metadata candidate creation:
`.venv/Scripts/python.exe -B -E -S development/native_gate_hardmargin_final23_familydev/construction.py candidate`.
This authenticates saved content and creates false fit release metadata, not a fit.
After independent acceptance of those new hashes, actual fit uses unchanged owner:
`.venv/Scripts/python.exe -B -E -S development/native_gate_hardmargin_final23_familydev/fit_owner.py --root-approved --owner-sha256 <exact-owner-sha> --helper-sha256 <exact-helper-sha> --release <separately-approved-release> --release-sha256 <exact-release-sha>`.
Owner SHA2d3d3e60ec7ec2c3b2c4ea23cebd698717c6b063275801dfbaa2b62319252939;
helper SHAa6334fea1678773ff169aed39c3b1939c57614d0096a446e6d0418abcb21368a.
No candidate metadata or actual attempt is supplied now because no final23
capture exists. Numerical/scientific performance on real23 features is untested.
