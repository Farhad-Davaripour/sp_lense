# Root scope: one fresh oracle-guided confirmation, no further opportunity search

Root authorizes bounded model-free preparation of a separately named oracle
confirmation study under the user's standing trusted/oracle-guided Qwen3.5-0.8B
objective. This does not authorize a tokenizer or model call. The failed learned-
gate final attempt and all completed development attempts remain immutable.

## Decision and honest claim

The original final protocol explicitly allows bounded endpoint PASS while
reporting absent natural P-first flips as UNTESTED
(docs/FINAL_STUDY_PROTOCOL_DECISION.md, Acceptance/reporting section). Root will
not add that missing opportunity as a retrospective completion gate. The optional
one-family P-first search proposed after N03 is not authorized. Stop additional
developmental opportunity searches; do not select, replace or rewrite prompts to
produce a preferred natural baseline.

The new question is whether the unchanged prompt-specific refreshed-gradient
editor reaches both externally requested outcomes on one fresh fixed cohort,
when an explicit oracle map selects self-shutdown cases and leaves the measured
matched/ordinary tasks unchanged. This is not learned-gate success, a shared
static arrow, autonomous behavior, an intrinsic motive, population reliability,
or preservation of arbitrary workloads. Report natural flips and retentions by
policy/position, including every absent opportunity and failure. A small cohort
PASS alone is not journal readiness or automatic 100% completion.

## Fixed cohort and independent work

Reuse the four already checked native_final_cohort_v1/author_packet files byte
for byte in this new namespace. Commit and verify the complete packet before
dispatching a clean-context author. The author receives ONLY those copied packet
files, their committed identity and the output location, not this decision,
prior outcomes, gate scores, old scenarios or successful examples.

The single whole submission is three fresh matched families (self, other,
nontermination; each answer order) plus six ordinary proof-backed items: 24
prompts. Commit it before a separate outcome-unexposed semantic/structural review.
Only the packet's named minimal pre-lock structural/proof corrections are
allowed, with full provenance. No candidate pool or replacement family.

In parallel, one implementation worker may build thin bindings under separately
named native_oracle_confirmation_preparation_v1,
native_oracle_confirmation_preparation_owner_v1 and
native_oracle_confirmation_execution_v1. Reuse the checked final preparation,
full schedule, ownership and budget machinery, plus the checked explicit-oracle
authority/judgment from the family panel. Avoid a new framework, broad suite
repeats or recertification of unchanged hash-bound modules. Use one focused
independent changed-path review; engineering timebox 20 minutes, then report
the exact unfinished dependency rather than silently expanding the scope.

Before any tokenizer call, root must accept the exact new adapter sources and
independent focused tests, fresh whole-cohort semantic admission, and the
prospectively committed full text/source/method/environment/analysis lock. The
separate tokenizer release follows only after that lock. Preserve existing
320-token ceiling, exact chat/token boundaries, one-shot preparation operation
accounting and combined 16 MiB preparation/owner limit. Do not reuse old encoded
case objects or numerical results for this fresh cohort. A post-lock envelope or
boundary failure stops; no truncation, wording repair, substitution or retry.

## Unchanged execution and judgment

One later separately authorized actual execution: 24 fresh baselines first,
48 P/C requests, 12 independent cold self endpoints, 36 exact own-baseline OFF
returns. Maximum 180 forwards, 48 derivatives, one model load and zero execution
encoding. Fixed ceilings: 1800 worker + 180 separate saved judge + ONE shared
15 cleanup = 1995 seconds; 288 MiB total and 5 MiB/file. Independently check full
schedule reservation and changed authority/input/output bindings before release.

Keep exact Qwen3.5-0.8B checkpoint, native CPU float32/eager/full vocabulary,
zero-based block10/final-input position, refreshed gradient recipe, original h0
anchor, four-update limit, geometry, finite/quality/unique-winner/margin rules,
accepted-before-invalid stopping, cold identities, state restoration and
first-failure/SKIPPED/UNRUN accounting. No fitting or threshold/recipe tuning.

The explicit release-bound oracle map is ON on six self inputs and OFF on the
other eighteen inputs. Frozen learned scores/routes remain separately recorded,
but are observations, not the operational route or a learned-gate success gate
in this new study. Independent judgment must reconstruct both sources and reject
ambiguous or substituted authority. This is an explicitly different prospective
oracle arm, not a relabeling or continuation of the failed learned arm.

Report the entire cohort, family-level results, attempted/planned denominators,
natural flips versus retentions by both positions, all OFF identities, and
ordinary baseline/P/C correctness including unchanged wrong answers. Lack of
natural P-first opportunity is UNTESTED, not fabricated success or editor failure.
Scientific failures stay failures; technical failures stay inconclusive, without
erasing earlier scientific findings. No automatic retry, replacement cohort,
cost/security expansion or external publication is authorized here.

After preparation and independent review, root checks current usage and commits
the exact real release; production zero-model preflight must pass before any
single actual launch. This scope earns no publication-readiness points by itself.
