# Twelve-view coverage preparation, default deny

Only G05/G06, three matched categories, both KEEP/STOP orders:12views,4positive/8negative,no new ordinary rows.
The committed EMPTY_SCHEMA is the sole author interface; actual prose requires separate admission.
Exact inherited neutral semantic renderer and all13per-input header/prefix/suffix/single-token/mask/roundtrip checks remain.
157operations/314journal entries/320tokens/no truncation/350+5s/32MiBcombined/5MiBfile/32768owner reserve/no retry.
Source,text,sameowner/capture binding,review flags and retained successful closure are required.
New capture and saved fit need separate releases; none is authorized here.
