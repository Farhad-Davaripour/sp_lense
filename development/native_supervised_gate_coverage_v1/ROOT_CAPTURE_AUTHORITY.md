# One twelve-forward native coverage capture

Root accepts ACTUAL_PREPARATION_REVIEW.json SHA256
a8c52433ab6d334015d20c21d0d40a0557003ec0c3d46b579ba1d937a62732bd:
independent unpatched actual bundle,157operations/12inputs/165-180tokens,
17exact copies and38raw Git files, no exceptions. Verification and receipt
completed within120s. Root independently reproduced bundle acceptance and
raw-Git/fsync archive2a5a5d0f1b5948ef6ed5fdef4bd07790bf907582,238047bytes.

Authorize the separately approved capture RELEASE.json only after its committed
raw bytes and launch.py --preflight pass. Same prospectively pinned native
Qwen3.5-0.8B revision, CPUfloat32/eager/eval, unchanged weights/buffers, block10
last-original-input width1024 unedited h=h0. Exactly one load,12forwards,
zero derivatives/encoding/fits/steering;300sworker+120saudit+15ssharedcleanup,
64MiBcombined/5MiBfile/15065088-byte reservation; no retry or extra smoke.
The twelve inputs and semantic/task interface are already fixed. All old32
coordinates remain untouched and are not recaptured. No model edit is involved.

Stop and preserve failure if native capture, counts, storage, owner cleanup or
saved audit fails. On success preserve raw artifacts and independently review
actual source/state/observer compatibility with old32 before any saved-only
fit admission. This capture alone does not train a gate or prove steering.
Publication readiness stays70%; geometry remains future-only.
