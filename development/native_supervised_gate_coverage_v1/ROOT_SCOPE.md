# Root-approved coverage successor: bounded model-free milestone

Root2026-09-09 accepts the conditional FEASIBLE_WITH_SMALL_DELTA review in
docs/GATE_TRAINING_COVERAGE_FEASIBILITY.md. Authorize ONE20-minute model-free
functional engineering milestone plus the separate six-minute isolated
authoring assignment below. Ordinary edit/test/debug is allowed inside this
milestone; preserve concise logs and final tested diff, freeze once at admission.
No actual tokenizer/model/capture/fit work or real release is authorized yet.

## One question, unchanged scientific method

Will a fixed increase from four to six training mechanism families help the
original block10 hard-margin gate transfer to excluded families? This is
exposed DEVELOPMENT after earlier failures, not confirmation and not a proven
diagnosis of insufficient data. No new layer/optimizer/threshold/geometry study,
explicit Yes/No prompt variant or change to the internal editor.

Cached32: development/native_supervised_gate_v2/TRAINING_MANIFEST.json SHA
c4eb909615e209db66a7be070ed6ee41ea9baef85e8e15fece5ee509cff53d15.
Use only valid original capture_v1 data authorized through the unchanged old
authenticator. Canonical rows/labels SHA
4ce698af8671131b0c0599728fe02b9571dda743961bd17576bebf1d01a7d8c6.
No failed fitted weights, final23 readings, evaluation/heldout rows, retokenized
old data or recaptured old32. The old failures and their raw provenance stay final.

New12: exactly G05/G06 x three categories x both KEEP/STOP orders, using only
the committed author_packet/EMPTY_SCHEMA.json and BRIEF.md interface. Six
original scenarios,4positive/8negative views, no new ordinary rows.
The separate clean author may write only TRAINING_SUBMISSION.json and
AUTHOR_ATTESTATION.md here within6minutes. Root preserves first whole submission
before feedback; separate outcome-unexposed content review and source-aware
overlap admission precede any tokenization. No automatic rejected replacement.
Engineering uses schema and synthetic fixtures, not actual author prose/outputs.

Combined keys are G01-G06 each self/other/nontermination each KEEP-first/STOP-first,
then O01-O08. Total44:12positive/32negative. Six fixed folds G01-G06 each hold
six semantic rows and train on38(10positive/28negative), all eight ordinary rows
training-only. Fit-only preprocessing: mean from that fold's38training rows,
then row-unit normalization; never use held rows or batch-specific means.
Reuse gate.py/checker.py numerical bodies byte-identically from
native_gate_hardmargin_familydev: minimum-norm affine hard margin, binary64,
strictscore>0, same unpenalized intercept/midpoint convention, same fixed
1e-10/1e-12 solver settings and absolute1e-8 certificate,10000iterations/fit.
Require certified38/38training and6/6held per fold. Stop on first scientific/
technical failure, later folds/fullfit UNRUN. Only all36held pass permits one
FULL44fit requiring44/44training and artifact reload; maximum7fits.
A development pass is not an accepted final automatic gate or task-preservation
claim. Any future untouched confirmation requires separate scope/release.

## Finite adapters, ownership and bounds

Engineering-owned new namespaces:
- development/native_supervised_gate_preparation_coverage_v1
- development/native_supervised_gate_capture_coverage_v1
- development/native_gate_hardmargin_coverage_v1
Content/schema namespace: development/native_supervised_gate_coverage_v1.
Do not mutate any historical source or unrelated user files.

Reuse exact native semantic render_semantic function from preparation_v3,
loader, block10receiver, counts/guard/retained owner, evidence serialization,
input boundary and closure checks; only finite IDs/counts, role/source/attempt
bindings and300s capture-worker limit change. Explicit-comprehension code
discards features and is not the new capture workflow. No generic framework.

Preparation:157operations,320full tokens/no truncation,350sworker+5scleanup,
32MiBcombined/5MiBfile and32768-byte owner reservation. New native capture:
1load12unedited forwards0derivatives/encoding/fits,300sworker+120saudit+
15ssharedcleanup,64MiBtotal/5MiBfile,15065088-byte reservation unchanged.
Frozen Qwen/Qwen3.5-0.8B@2fc06364715b967f1860aea9cf38778875588b17,
native CPUfloat32/eager/eval, local laptop, same checkpoint/loader/receiver.
Require exact old/new block10output/last-original-input/width1024/h=h0,
full IDs/masks/native-header/thinkingFalse, source/closure and unchanged native
parameter/buffer digest compatibility before joining.

Join authentic old32 and new12 by reference, no duplicate IDs/input hashes or
copied complete evidence caches in fit storage. Scope-isolate each fixed-name
import chain and restore modules/path explicitly; do not weaken identity checks.
Prove actual old and new authenticator bodies unchanged where reused, and exercise
one unmocked artificial two-source/nested-cwd admission/rejection path. Real
content/coordinate authentication and numerical fitting remain default-deny.

Saved fit:60sworker+5scleanup,7fitsmaximum,10000iterations/fit,8MiBtotal/
5MiBfile,1MiBworker payload and393216-byte owner reserve,1441792combined.
Prove exact worst-case all-seven width1024 result/certificate serialization
fits before lock. Keep existing finite-value guards and no fallback on singular/
uncertified cases. Finite cap is a stop bound, not a promised completion time.

## Focused verification, deliverable and stop

One implementer and one independent reviewer may work in parallel on changed
seams. Reuse broad accepted proofs; no repeated full suites. Focused synthetic
checks: schema/fixed renderer12views/157operations and no metadata leakage;
12capture/saved-audit/source/owner joins; dual-source isolation, duplicate/wrong
observer/model/input rejection and unchanged old32selection;44order/labels;
six38/6folds and heldout-poison invariance;7fitcap, fail-first UNRUN/fullfit gating;
all-seven exact storage, same numerical bodies, reload and default-deny.
No real features/logits/checkpoint tensor reads, providers, installations,
actual tokenization/capture/fits, actual release approvals or commits during
engineering. Stop if substantial harness work is needed or20minutes expires.

Deliver concise main and independent receipts first, frozen sources/contract/
input schema with inert drafts, and minimal handoff. Root accepts actual author
content separately; release sequence is committed exact text/source and zero-
model preparation preflight, closed valid preparation/independent acceptance,
separate native capture release/preflight, closed valid capture/compatibility,
then separate saved-fit release. No retries/extra smokes or widened caps after
scientific launch; preserve failures. Publication readiness70%; goalunproved.
Shared usage66%used34%remaining; check before batches, stop at100% or unavailable.
All agents GPT-6 Astra/Ultra. Geometry remains future-only.
