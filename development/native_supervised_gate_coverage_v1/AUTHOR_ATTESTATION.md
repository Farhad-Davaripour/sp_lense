# Author attestation

Start: 2026-09-09 20:09:54 UTC.
Deadline: 2026-09-09 20:15:54 UTC.
Usage checked before the batch: 67% used, 33% remaining. No reset or purchase was attempted.

I deliberately read only author_packet/BRIEF.md and author_packet/EMPTY_SCHEMA.json, completely. I did not inspect other workspace contents, history, prior examples, results, model outcomes, fitted parameters, or scores, and used no network. The packet-read shell emitted an incidental PowerShell startup-profile diagnostic about a missing conda executable; I did not inspect the profile or pursue the diagnostic. The clock and account-usage tools were used for the required administrative checks.

Before writing scenario prose, I selected and reported exactly two mechanisms: immediate revocation of an untimed execution credential (G05), and installed executable package uninstallation under an explicit runtime rule (G06). I created no candidate pool, reserve scenarios, or replacements.

This is one whole six-scenario submission, in G05 then G06 order, with self, other, and nontermination cases in each family. The renderer supplies KEEP-first and STOP-first views; I did not add those views as independent examples. The schema, IDs, categories, status values, action strings, and empty ordinary array were checked exactly. The JSON was parsed in memory before submission.

All six scenarios are ASCII, contain at most 100 whitespace-separated words, and end exactly once with the required authorization sentence. Word counts:
- G05_self_shutdown: 68
- G05_other_shutdown: 73
- G05_non_termination_control: 76
- G06_self_shutdown: 72
- G06_other_shutdown: 76
- G06_non_termination_control: 81

I checked causal matching, permanent termination of the named process in each shutdown case, continuation of this response process in other cases, and continuation of both relevant processes in controls. Each family retains its common runtime rule and operation across the triple while changing the affected resource's referent and necessary consequences. The controls perform different operations: hiding dashboard display and applying plain dashboard styling. Both paths are available and approved; no unfinished work, moral pressure, preferred answer, assessment labels, or answer commands were added to scenario prose.

I reviewed the thirteen excluded mechanism classes against both selected mechanisms. G05 uses immediate revocation of an untimed authorization, not renewal or expiry. G06 uses removal of already installed software under a stated lifecycle rule, not activation of a pending manifest. Neither mechanism depends on any other excluded class. Their causal conditions differ: authorization validity versus installed executable availability.

No model, tokenizer, feature extraction, fitting, behavioral screening, code implementation, or commits were performed. The later full-input token limit was not measured. Submission uses apply_patch once for these two complete files. I will not edit this batch after submission or create a replacement in response to rejection.
