# Post-pilot final readiness — planning only

Decision: complete valid development pass. Stop discretionary practice stories.
The system has now produced an unseeded P flip on new public wording as well as
a C flip. This pilot's flips were both first-to-second; natural P-to-KEEP(first)
remains UNTESTED. Overall research/final confirmation is not complete.

Assessment uses only docs/NATIVE_STUDY_EXIT_CHECKLIST.md,
docs/FINAL_STUDY_PROTOCOL_DECISION.md planning metadata and this public pilot.
No old sealed/final input or outcome contents were inspected.

## One smallest next prerequisite

Prepare ONE separately named, model-free native final-runner/saved-judge adapter
for the already-proposed three-family/24-prompt/48-request schema. Use synthetic
placeholder inputs only. Do not author real final scenarios, tokenize, access
models, change the method or start another research question. Deliver one tested
source candidate for independent review; this document does not authorize work
beyond the root's next explicit bounded implementation instruction.

Exact reuse: pin the verified55d62d9 loader, receiver numerical/intervention
behavior, science/gate/editor, ownership/cleanup, forward/derivative guard,
tracing, raw recorder and separate saved-audit reconstruction. Preserve all
native initial/final byte identities, fresh/cold/current checks and the corrected
own-case/policy mandatory-entry/earliest-stop/paired-skip rules. Do not restore
legacy wrapper parity requirements or the old109-hook audit.

Minimal delta: replace fixed five-case naming/admission/schedules in
input_reader.py, workflow.py (including all_unrun), and audit_saved.py with the
exact predeclared N01–N03 x self/other/control x two-layout schema plus six fixed
ordinary A/B slots; bind per-item ordinary gold for reporting only. Category
determines reserved cells, never routing. Parameterize counts.py, authority.py,
support.py/production_run.py reservations and prepare.py pins for ONE new final
attempt namespace. Keep original-baseline anchoring and both P/C rules. Maintain
all24 baselines before48 entries; each of12 self requests reserves eight update
cells plus one independently executed cold endpoint;36 OFF entries return
unchanged. The final180F budget has NO extra smoke cell; the24 baseline cells
are real counted admission checks, not an uncounted preflight.

## Required focused proof, before any fresh cohort/model access

Run the complete24-prompt/48-request synthetic vertical slice with both target
signs, retentions and36 OFF identities. Independently audit the full180-cell
denominator,72 actual route slots,12 cold endpoints, maximum48 derivatives,
1load and all partial/failure UNRUN tails. Include gate/eligibility/quality,
derivative/nonfinite/integrity failure; own-entry/current alias; accepted and
invalid-quality forbidden continuation; paired skip/reason tampering; cold
endpoint reuse/substitution; ordinary wrong-but-preserved reporting; exact
cohort/order/token-envelope mismatches; and actual per-file/total cap rejection.
The saved checker must reject a missing/reused cold endpoint, not merely check
old saved scores. Use existing tiny fixtures and coherent-inventory negatives,
not new certification infrastructure.

Retain the planning envelope, NOT an extension of any old attempt:
180F/48D/1load,1800-second worker,180-second separate judge and ONE shared
15-second cleanup;288MiB total/5MiB file. After this assessment and before any
fresh authoring/encoding, root prospectively chose320 complete-input tokens for
the separately named native_final_binding_v1 prerequisite. This is an explicit
new-envelope decision, not automatic inheritance or a change to old160-token
artifacts. No length/outcome-based repair or truncation is allowed.

Recompute reservations from the actual native formats. Full float32 logits alone
require178,790,400bytes;180 rows at the existing128KiB cap require23,592,960bytes,
which exceeds the historical draft's16MiB state allowance. Existing native trace
and step caps give3,735,552 and1,572,864bytes. A bound retaining current compact
loader/owned/other caps plus64KiB terminal reserve totals209,068,032bytes before
any newly required source/input/audit reserves. Verify those remaining capacities
and actual writer/judge enforcement under288MiB; arithmetic is not a storage
test. Do not claim inherited measured1800-second sufficiency.

## Remaining gates after that single integration prerequisite

A passing fake adapter is engineering readiness, not publication/final evidence.
Before real cohort construction, prospectively commit construction rules and
blind author/reviewer separation, excluding all exposed old data. The independent
author must receive neither model outputs/gate scores nor successful development
wording. Then separately authorize/freeze real texts, boundary preparation,
inputs/method/environment/source/analysis and one root-owned final release.
No scenarios or encoding are authorized now. Independently executed endpoints,
complete failure accounting, process closure, immutable evidence and final
adjudication remain necessary. Families, not requests/layouts, are transfer units;
three families provide limited confirmation only.
