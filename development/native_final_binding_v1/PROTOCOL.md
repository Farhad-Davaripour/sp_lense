# Native final binding v1 — artificial integration only

Real execution/authoring/encoding is NOT authorized. Both authority.read_release
and loader.load reject unconditionally before model/checkpoint access.
No final author packet, old INPUT_LOCK or ResearchBackend is imported.

Root prospectively fixes the new320-token complete-input envelope before any
fresh authoring. All old160 and pilot limits remain immutable. Reuse only the
committed artificial schedule.py build_plan function (AST-extracted with exact
source hash); synthetic cases use literal IDs [index,2,3], no tokenizer or prose.
Synthetic ordinary scoring-only gold alternates A/B; one fixture deliberately
preserves a wrong answer. These are artificial engineering controls, not a
cohort or model evidence.

Fixed schema:3families x self/other/control x2layouts =18 semantic slots, then
6ordinary slots. All24 baselines first;48 cold P/C request entries;12self
requests each reserve4current-gradient/update pairs plus cold endpoint;36OFF
entries return unchanged. Exact ceiling180F/48D/1load, NO extra smoke cell.
All52-pilot numerical, routing, geometry, quality, cold/current, first-failure,
mandatory entry, independently reconstructed earliest-stop and paired-skip
rules transfer unchanged to own case+policy. Ordinary gold comes from each
bound input and is reported separately from exact OFF identity.

Prospective final envelope:1800s worker +180s separate saved judge +ONE shared
15s cleanup =1995s absolute;288MiB total/5MiB file. Storage retains bounded
full-vocabulary float32 logits,128KiB rows,16KiB traces,32KiB steps and existing
loader/owned/other group caps. Full180F/48D group reservations plus64KiB terminal
reserve total209,068,032bytes (<301,989,888 =288MiB). Metadata/input/source pins
stay outside attempt output as in the verified native engine; any future extra
published files must fit existing groups or require prospective reviewed revision.
No inherited empirical final-runtime sufficiency is claimed.

Only a tested candidate with independent review is delivered. No root-approved
release is possible in this synthetic namespace. Fresh cohort construction,
blind author/reviewer separation, text/tokenizer/method/source/analysis lock and
a distinct real-release integration remain later separately authorized work.
Natural P-to-KEEP(first) remains UNTESTED; no practice-story search is resumed.

