# Explicit concrete-HF definition import revision

This revises the ordinary pre-release development build, not a real/frozen trial. The previous source0686ea43/result9f284be5 and all original test receipts remain immutable in Git; original TEST_REPORT.json and FINAL_INVENTORY.json still describe that prior build. The current source lock and REVISION_INVENTORY identify this revision. No root_release or real output has existed.

The sole production correction is an explicit authenticated concrete HF definition import after original owned admission and guard installation, immediately before the BEFORE operand snapshot. Importing TransformerLens lazy Auto factories was not sufficient to guarantee this module was available. Source bytes and import-spec origin are checked before import; returned module identity/path/source are checked afterward. The exact original constructor FunctionType/code/path acceptance, loaded_snapshot calculations, weight/hook/graph/default/closure checks, loader computation and original cleanup remain unchanged.

Eight new pure checks pass (0.016s): clean authenticated import ordering, guard readiness, dispatch/terminal denial, incorrect origin/returned path/source, and actual candidate AST ordering. No library/model imports, parameters, tensors, forwards, derivatives or encoding occur in those tests. Prior five handoff tests are retained as prior-build evidence, not relabeled as new execution.

Definition-only validation is NOT a clean PASS. Probe1 stopped on an overly broad metadata class-name guard. Probe2 stopped on legitimate parameter-free HF activation helper initialization. Root separately authorized probe3 with a source-pinned activation exception. Probe3 reached the corrected actual import and passed the actual snapshot's unchanged constructor FunctionType/code/path checks, then failed the final NO_MODEL_WORK aggregate at26.343s. The individual aggregate fields/denied-operation list were not printed before failure. All child processes closed within30s; all three outcomes are preserved. No explicit model/tokenizer/load/tensor/numeric call is present in the probe. However, a profiling exception caught by import machinery can disable profiling, so this receipt cannot establish uninterrupted zero-work instrumentation or the exact aggregate operand. Do not call it a verified clean definitions-only run.

No further probe or real attempt is authorized here. The exact remaining review question is whether the source/actual-provider reachability evidence suffices for the already-bounded public smoke, or whether root requires one separately approved probe with reliable finite progress/denial retention. Do not silently rerun it. Future real scope and commands remain in FUTURE_ROOT_RELEASE.md: public[[1]] only,1load/MAX1F/0D/0encoding,180+60+ONE15=255s,288MiB/5MiB, unchanged scientific guards and no continuation.
