# Public synthetic operand-capture smoke — disabled

This is an engineering admission boundary, not a study result. The only future input is the prospectively approved public token ID batch [[1]], attention mask [[1]], final index 0 and zero float32 offset of width 1024. No study prompt, labels, gate, score, P/C request, tokenizer call or continuation is admitted. The one-cell public plan reserves 109 UNUSED normal hook-check records to retain the existing recorder's capacity contract; it never executes or claims the scientific 109-check finalizer.

The approved development candidate is 964f76340d3530e67a640e1e821dc5a84c541b23. Its constructor acceptance remains exactly FunctionType AND original compiled-code equality AND resolved-source-path equality. The separately corrected forward admission authenticates the installed accelerate wrapper, exact closure and enclosed HF base while preserving wrapper dispatch. The historical post-load constructor mismatch is still unexplained; it is not relabeled as the wrapper defect.

The loader records finite constructor operands immediately before and after its unchanged single ResearchBackend.load call. These source/execution-bound native publications are exclusive, acknowledged and independently joined to worker/diagnostic terminal status. Failed admission retains the first finite predicate before original cleanup; missing or partial evidence remains unknown/inconclusive. No error text, locals, tensors or weights are recorded. The four operand files share an exact 65,536-byte reserve inside the original 786,432-byte other-closeout allocation: 172,032 helper bytes + 65,536 operand bytes + 548,864 other bytes. Parent closeout retains its existing 32 KiB headroom.

The scientific adapter (including all 14 parameter predicates), retained-order diagnostic/digest helpers, original cleanup operations, counter, native writer, guard trace, ownership and shared budget sources are byte-identical to the checked diagnostic. SOURCE_PARITY.json and NARROW_DIFF.patch document the changes. The authority boundary changes only attempt identity. Runtime/weights/config and upper contract remain pinned; the public input lock is deliberately distinct from the study lock.

Focused inert tests exercise the exact AST-extracted load-call observation window, native publisher and independent saved reader, public counter/admission, preserved first cause, partial native output, and coherent phase tamper. The final five checks pass in 0.312 seconds with zero model/library imports, weight access, loads, forwards, derivatives or encoding. Two ordinary fixture-debug iterations are recorded; no historical or real trial was retried or modified. These tests do not prove live helper selection, actual constructor operands, full hook capacity, model loading or a successful forward. Existing component/owned-controller proofs are reused, not rerun.

Real release remains absent and preflight reports DISABLED_NO_APPROVED_REAL_RELEASE. Independent/root assembly review is required before any launch. The next real question is: what are the exact constructor-admission operands before and after the one load, and if admitted, can the public single-token zero-offset forward complete with original cleanup? No scientific success follows from either outcome.
