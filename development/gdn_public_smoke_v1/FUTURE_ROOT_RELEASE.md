# Separate root release required

Future unique attempt: fresh_confirmation_gdn_public_smoke_attempt_001. Output must be absent: real_evidence/fresh_confirmation_gdn_public_smoke_attempt_001. There is no reuse, reset, resume, retry or automatic scientific continuation. No root_release files exist in this preparation.

After independent/root approval, root must create four new exclusive root_release files with the exact unchanged real_boundary.Boundary.verify_chain schemas: USAGE_RECEIPT.json, ROOT_RELEASE.json, AUTHORIZATION.json and AUTHORITY_LOCK.json. The chain is SOURCE_FREEZE/BINDINGS/public input/runtime/resource -> ROOT_RELEASE -> AUTHORIZATION -> caller-pinned AUTHORITY_LOCK. Root separately approves the exact final lock SHA and commits the four bytes. Tool-derived actual standard-Codex usage must be available and below100%, admitted within120seconds using the real clock after all chain/input/path validation. Later roles reauthenticate the original immutable admission; no usage-refresh deadline reset is permitted.

From repository root, only after that separate authorization:

```powershell
& .venv/Scripts/python.exe -B development/gdn_public_smoke_v1/launch.py --authority-lock "C:/Users/farha/OneDrive/Documents/ChatGPT/SP_Lense/development/gdn_public_smoke_v1/root_release/AUTHORITY_LOCK.json" --approved-lock-sha256 "<EXACT_ROOT_APPROVED_LOCK_SHA256>"
```

Append --preflight to check without reservation or model work. Existing fixture/clock overrides remain forbidden for a real launch. Retained owned permission precedes model imports and the sole load. Keep the pinned local Qwen3.5-0.8B CPUfloat32 loader with_lens=False, fixed expected weight digest, original helper/type/code/closure/graph/current-registry/hook/cleanup predicates and complete setup admission before any forward.

Only public IDs [[1]], mask [[1]], final index0, zero offset are permitted; 1load/MAX1F/0D/0encoding. Worker180s + saved audit60s + ONE shared cleanup15s =255s;288MiB evidence and5MiB/file. Original upper ceilings remain unmodified. A rejection is preserved as final/inconclusive for this attempt. Startup-only success is not forward success, and neither is steering/study success. This document grants no authority.
