# Coverage successor — model-free engineering PASS

Window: 2026-09-09 20:09:31–20:29:31 UTC. Receipt published first:
FINAL_TEST_RECEIPT.json SHA256
e0206226c5e81d16fea353bcc648626a538f7942c4fc8fb383d3f52662e9d325.
No actual author submission, features or logits were read; no real tokenizer,
model, capture, fit, release, installation or commit occurred. Only the three
engineering namespaces changed. Old attempts and sources remain immutable.

## Frozen joins and finite delta

- preparation_coverage_v1 SOURCE_FREEZE:
  9f7b76d774a7b6429d197c3e9219e4390a6d12695e1048fa2eeb57fcb7a85f76
- capture_coverage_v1 SOURCE_FREEZE:
  9ea25b3cefa5576d4a18594b7547d0f438db4ea10b62cb9201da25ef2843fa96
- hardmargin_coverage_v1 SOURCE_FREEZE:
  4211d673a15436f5002fa4dfb6b4828994b31ca40f9ca9c9fa73512f42ac1425

The preparation adapter changes only finite schema/source/count joins to G05/G06,
12 semantic views, no ordinary rows and157 operations. The original semantic
renderer remains byte-identical. The old thirteen per-input checks,320-token
bound,350+5s,32MiB/5MiBfile and32,768-byte owner reserve remain.

Capture reuses the original block10 loader/receiver/core/guard/trace/owner/run
substrate byte-identically. Workflow/auditor/count/namespace joins become12 rows;
the envelope is300+120+15s,1load12F0D0encoding and64MiB/5MiBfile. Exact reservation
15,065,088 bytes remains enforced. Unlike the explicit-question diagnostic, this
workflow retains unedited finite float32 h=h0 at the last original input token.

The finite new fit_source_auth.py lives in capture and is authenticated by its
source freeze. Fit source_auth.py isolates just the two fixed-name import chains,
restoring modules and sys.path on every exit. The original32 authenticator body
and cached32 manifest/content digests are unchanged. Both closed captures must
have the original feature contract and identical recorded native state; loader
state is bound through closed inventory and unchanged worker cleanup. Selected
rows recheck actual IDs/masks, hook10, target, final/nonfinal index and width.
Duplicate IDs/input hashes or incompatible sources/observers/states reject.

Combined order is G01–G06 semantic views then O01–O08:44 rows,12positive/32negative.
The new metadata digest is explicitly the ordered per-row feature hashes, not a
misnamed raw-matrix digest. The old raw32 rows/labels digest is still mandatory.
gate.py,checker.py andfit_owner.py are byte-identical. Only construction's finite
schedule/contract/admission joins change: six38/6 folds, then conditional FULL44,
max7 fits. Fold-only centering, row-unit normalization, strictscore>0, fixed
binary64 solver/intercept/tolerances,10,000 iterations and no fallback remain.
All failures retain later UNRUN stages; no old fitted weights are starting values.

## Focused proof and limits

Main capture3/3 passed0.539s; independent3/3,0.553s. This includes one fake157-op
preparation at exactly320 tokens, bound reader, no metadata leakage,12-row saved
audit, wrong-site rejection and FAILED/UNRUN suffix. Main fit3/3 passed1.392s;
independent3/3,1.374s:44-order/folds, held-row exclusion, fail-first/fullfit gating,
actual synthetic solver, independent saved certificates, artifact reload and
compact stdout. Review-only six-fold workflow poison checks also passed.

Independent test_two_chain.py passed10/10 in5.125s: actual old and new production
function bodies, unmocked artificial two-chain build/extract44 from nested cwd,
module/path restoration and source/observer/position/state/input/duplicate/contract
negatives. Synthetic constants, paths, metadata and fake prepared boundaries are
substituted only in its isolated mirrors; this is not actual-source admission.
All65 source pins pass. Broad old suites were not repeated.

All-seven width1024 RESULT is68,064 bytes. Serializing the fixed successful
schemas with maximum-width finite binary64/int fields, artifact and seven1KiB
ticket reserves gives455,940 bytes<1MiB. Saved w,b,alpha,means and certificates
are retained. Owner393,216 + worker1MiB =1,441,792 combined reservation;8MiBtotal,
5MiBfile,60+5s and64KiBstdout remain. Completion inside60s is not promised.
Retained artificial mirrors occupy about3MiB in this namespace; no old complete
capture archives were copied. Source scope and raw-byte attributes remain pinned.

Review caught finite pre-freeze integration defects: broad count substitution
temporarily narrowed capture320 to120 and changed an unreachable ordinary ASCII
bound; both were restored. The dual-source guard was completed with closed-loader/
cleanup and row observer/position checks before final unmocked testing. No known
blocking engineering defect remains; see independent receipt/review for limits.

## Root-only next admission

The text template deliberately contains null cohort, empty prompts and UNADMITTED
submission/hash fields, with every review/text flag false. Actual author prose was
not read or silently admitted. Root must bind its accepted first submission and
exact rendered12 text to this frozen source, finalize the text lock, and separately
release preparation using the unchanged retained preparation_owner CLI in capture.
Then accept closed preparation and bind its15 prepared artifacts/text/closure into
the separate capture release before preflight and the single native capture.

Only after successful compatible closed capture may root run the fit namespace's
`construction.py candidate` metadata operation, which issues four default-deny
candidate files; none was created here. A separate approved fit release and
unchanged fit_owner CLI are then required. RELEASE_TEMPLATE and INPUT_SCHEMA_DRAFT
are intentionally inert, not substitutes for authenticated actual inputs.

No retries, new text, geometry, layer sweep, optimizer change, later evaluation or
automatic-control acceptance follows. Earlier failures remain final; even a pass
would be exposed development, not untouched confirmation or task preservation.
