# Root conditional integration authority

The parent/root authorized this single functional window at03:02:05 UTC on
2026-09-10, ending03:22:05 UTC. Preserve the historical partial source freeze
63723023d6fa4226e5da20f61ae2648c6e8ae53b4667b461692422d77348d62a
and its one-second administrative overrun unchanged.

Integrate only the six already-admitted G08 TRAIN views; no new authoring,
held prose/features, paired fits, averaging actual features, gate calibration,
editor work or additional model runs. Same native block10/final original input,
CPUfloat32/eager model/revision and unchanged native loader/receiver/ownership.

Only after independently accepted current sources, text, provenance,
reservations and release; owned commit/raw-Git equality/workspace r+b fsync;
and required preflight may the existing conditional authority activate exactly
one TRAIN preparation (79 ops,350+5s,32MiB) and then one TRAIN capture
(1load6F0D0encoding,300+120+15s,64MiB,5MiB/file). Capture reservation8220672
bytes. Every failed actual attempt is final, no retries or cap changes.

Preparation may precede capture only after its own prospective admission.
Capture needs the exact closed preparation bundle and a separate exact release
and successful unchanged preflight. Stop at closed TRAIN capture, independent
saved-only review and old52+new6 metadata join; no construction authorization.
If the window ends, preserve completed stages and leave unstarted stages UNRUN.
Readiness70% unchanged.
