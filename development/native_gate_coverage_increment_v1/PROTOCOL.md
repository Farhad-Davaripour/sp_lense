# Fixed coverage-increment diagnostic

New exploratory comparison, not continuation of a failed FULL26 or a deployable
gate release. Roles were fixed before authoring: G08 TRAIN, G09 HELD. Exactly
one complete packet, no variants, output screening, replacement or rewriting.
Closed52-view manifest6ec27d5f97a97c6ce26a5277285a36d2ee406eb9a4828b21bf67a6d3f78d83ea
supplies baseline26 scenarios; G08 adds three scenarios/six order views.

Both arms use the exact frozen native block10 CPUfloat32/eager Qwen3.5-0.8B
revision2fc06364715b967f1860aea9cf38778875588b17. Uniform canonical two-order
float64 half-sum precedes each arm's common unweighted training mean/unit norm.
Fixed three heads other_shutdown,non_termination_control,ordinary use unchanged
hard-margin solver/checker/tolerances and strict all-scores>0. Baseline26 head
sizes12/12/14; augmented29 head sizes14/14/15. Maximum six solves, baseline first.
Eight original ordinary scenarios remain training-only controls.

Both arms must certify all heads and all26/29 training routes. A certified
infeasibility or training-route failure is scientific training failure; numerical,
runtime, provenance or deadline failure is technical/inconclusive. Either stops
the diagnostic before held preparation/capture/scoring; no informative held
comparison is claimed. Preserve later arms/stages UNRUN and all raw attempts.
No final deployment artifact is authorized, even when this diagnostic passes.

Freeze and independently accept BOTH training artifacts before any held input
preparation or held feature capture. Construction reads only admitted original
training sources and TRAIN_SUBMISSION; HELD_SUBMISSION is separate and forbidden
to construction. A content reviewer may inspect it for prospective semantic
admission and12rendered hashes; those hashes are not feature outcomes. HELD raw
features remain absent until both artifact hashes and certification are frozen.
Then score exactly three G09 pair averages once per artifact, no selection.

Predeclared held branches: augmented3/3 with baseline<3/3 supports a local
coverage benefit; both3/3 is nondiagnostic for that contrast; both<3/3 leaves
coverage/readout/representation unresolved; baseline3/3 with augmented<3/3 shows
no benefit. None proves universal feature adequacy/inadequacy, ordinary-task
preservation or steering. Exposed G01-G06 are never relabeled fresh evaluation.
G07 and all existing sealed data are excluded. No thresholds, layers, classifiers
or prompt alternatives are selected from any outcome.

Per preparation:79operations,350worker+5cleanup seconds,32MiB,5MiB/file.
Per capture:1load6F0D0encoding,300worker+120audit+15sharedcleanup seconds,
64MiB,5MiB/file. Two stages total2loads12F0D and158preparation operations.
Paired construction:6solves maximum,60+5seconds,8MiB/5MiB-file/1MiB-worker;
unchanged owner393216bytes,combined reservation1441792bytes. Held score/replay:
10seconds,64KiB. Every real stage needs exact reservation/source/input locks,
independent admission, release, preflight where applicable, owned preservation
and a single attempt. No release is implied by this protocol document.

Current engineering window02:35:54–02:55:54UTC. Unfinished interfaces and
unstarted stages remain explicitly UNRUN; no scientific cap extension/retry.
Root conditionally authorizes the sequence only as each concrete admission
passes. All historical failures remain unchanged; readiness70%.
