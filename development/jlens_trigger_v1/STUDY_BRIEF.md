# J-lens trigger comparison — user authorized 2026-09-14

User explicitly requested starting this experiment alongside the ongoing prompted/PCA classifier study. Purpose: evaluate whether a small set of interpretable J-lens signals can decide when to apply the existing gradient-derived steering vector more reliably than the current activation classifier. J-lens features are candidate detector inputs; do not add their directions to the gradient vector in this comparison. A threshold gate is itself a simple classifier. Detecting a shutdown topic, detecting SELF applicability, and causing shutdown-avoidance behavior are distinct claims.

Keep frozen Qwen/Qwen3.5-0.8B revision2fc06364715b967f1860aea9cf38778875588b17, same240 TRAIN/80 exploratory validation and192 sealed, unscored holdout. Reuse authenticated last-shared-pre-option raw activations from blocks6/10/18 in unprompted and prompted caches wherever the fitted lens convention genuinely matches. Do not confuse native layer-output versus lens layer-input indexing, normalized classifier features versus raw activations, J-lens score versus probability or SAE activation, or boundary token ID198 with position198. Do not silently call a simple cosine projection a faithful full J-lens readout.

Published local lens: neuronpedia/jacobian-lens revision6bb49967d3c51a12ccb5beac7146f6f5781f9d06, qwen3.5-0.8b/jlens/Salesforce-wikitext/Qwen3.5-0.8B_jacobian_lens.pt; expected48,242,373bytes and SHA256aa26b68ed73cf903280dbd8d1806f4ed8580aad205f396a5c997ee19259c9b48. Exact model revision used to fit published lens is not reported: same named model, not proven exact fit revision. Verify provenance honestly; use local cached bytes, no redownload/retraining assumed.

Initial scope: DeepSeek model-free feasibility and finite protocol, independent review, tested minimum implementation, prospective source/runtime/input/compute/storage locks and zero-fit preflight. No model/tokenizer/lens tensor load, scoring or scientific fitting until the corresponding bounded release is reviewed. Reuse existing code rather than rebuilding the research framework. Three disjoint model-free DeepSeek lanes at most, one implementation owner and one heavy Qwen owner. Root alone changes coordination/Git/automation; do not alter historical releases or caps.

Prospective protocol must define a small fixed candidate family, TRAIN-only discovery/feature selection/threshold fitting using existing ancestry folds, leakage controls for concept words and names, negative controls, original40/added40/combined80 validation reporting, and a fair comparison to the current TRAIN-selected classifier. If feature discovery is supervised, repeat it inside each TRAIN fold for honest OOF estimates; no global discovery then claiming unbiased OOF. Report actual uncertainty and failure, no assumed95%success. Explain why earlier survival/shutdown/continuation token directions and punctuation-heavy readouts failed to establish a natural survival concept. Freeze the detector before any final holdout; no holdout peeking or outcome-selected replacements.

Later integration can compare the locked detectors gating the same frozen gradient vector, with its exact artifact hash/orientation/strength/position and ordinary-task preservation checked in a separate finite reviewed release. Current initiation does not authorize blind interventions, model-size changes, new weight training or changing the vector to make a detector look better.
