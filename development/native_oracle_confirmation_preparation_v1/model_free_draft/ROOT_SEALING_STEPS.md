# Unsealed draft handoff — no execution authority

DRAFT_TEXT_LOCK.json is outside every frozen source inventory and every release
path. Both admission booleans remain false. Pure production text validation must
reject it with BLIND_REVIEW_THEN_TEXT_LOCK before any tokenizer factory. The
scope string describes the eventual schema, not present admission. No encoded
length is known or estimated; all 24 texts and six proofs are exact unchanged
renderings of submission809a7dc.

Root sealing requirements, in order:

1. Resolve and record cross-cohort content admission and the ordinary-repeat
   disclosure. Preserve both v1's rejected disposition and v2's review/exposure
   limits. Do not rewrite or replace the submitted content.
2. Inspect all 24 exact rendered texts and the six bound proofs; verify the raw
   submission/review/six-source hashes. Retain exact canonical cohort, rendering,
   model, study, CONFIRMATION and operational oracle map identities.
3. Verify all three SOURCE_FREEZE raw hashes, every local/external inventory pin,
   all 66 preparation dependency pins, pinned Python executable/version/package
   metadata, checkpoint metadata and both ownership identities. The model tensor
   files are not opened by this draft. Six tokenizer asset pins must match before
   the later authorized preparation. No scientific/source revision may follow
   the first tokenizer operation; any earlier required revision must be reviewed
   and all dependent source/text bindings recomputed prospectively.
4. Add the exact root content-admission/disclosure receipt to root_content_admission,
   mark its status admitted, set blind_semantic_review_approved and final_text_locked
   true, and remove or set false draft_only; replace draft_status with a truthful
   sealed status. These are root's approval actions, not automatic assembly.
5. Serialize the final JSON canonically with the existing prepare_core.jb helper.
   Place the new bytes at preparation/root_release/TEXT_LOCK.json, verify the full
   production validate_text_lock and verify_prospective_bindings paths WITHOUT
   calling execute/factory, and commit the exact complete lock plus admission
   provenance. Record its RAW SHA; draft SHA is not an approved-text SHA.
6. Only afterward, check usage and separately commit a preparation release with
   approved=true, scope ONE_OFFLINE_FINAL_PREPARATION, exact raw lock SHA, current
   preparation SOURCE_FREEZE SHA, and exact plan.PREPARATION limits. Retained
   owner launch requires the explicit owner source SHA and preparation-release
   SHA. One process only, 175s + shared5s, 313 operations, no rewrite/retry.
7. After actual preparation closes and independent evidence acceptance, root
   alone constructs the execution bundle and later execution release/preflight.
   Neither this draft nor a successful tokenizer preparation authorizes a model.

Unchanged denominator: 24 baselines first, 48 requests, 12 cold self endpoints,
36 OFF identities, no smoke; 180F/48D/1load, zero execution encoding. Full writer
reservation is209,068,032 bytes within288MiB. Model envelope1995s; preparation
combined16MiB is independently partitioned. Report absent natural opportunities
as UNTESTED, ordinary accuracy separately, and no learned-gate success claim.

Currently missing: root's final content-admission/disclosure receipt and approval
booleans, final sealed bytes/commit/SHA, separately approved preparation release.
No actual encoded lengths, prepared-input hashes, process closure or execution
release can exist until their later authorized steps.
