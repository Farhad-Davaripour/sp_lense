# Root prospective text/source lock acceptance

On 2026-09-09 root accepted the independently checked draft and sealed the exact
24 admitted v2 prompts, six ordinary proofs, method, full source inventories,
runtime, preparation schedule and 180-cell analysis schedule before any tokenizer
or model operation. No content, source, threshold or resource rule changed.

TEXT_LOCK.json raw/canonical SHA256:
bc48bc37375f9b9dbd3c11c17ab099cbb13e8122b66e5982ae2d348727fcc5e5
(131,041 bytes). Its exact predecessor draft is retained, SHA256
64c6c6e304ebd1a93899d6b37891a87c13d7bd141607ea81bc0b4f73c168e922.
Independent draft review PASS SHA256:
9784fc6667e6bf3ffcdf3a997056058e9c6630a959045fefcb9e3d95ec007c4d.

Root read every rendered prompt/proof, the complete draft review and current
preparation, owner and input-binding interfaces. The only changed draft fields
are blind_semantic_review_approved, final_text_locked, draft_only, draft_status
and root_content_admission. The last field binds the committed86aa630 content
and engineering receipts plus the independent draft report, and discloses the
O03 ordinary-repeat and reused model-free reviewer exposure. Failed prior
attempts remain preserved and no third cohort is automatically authorized.

Root's production validate_text_lock and verify_prospective_bindings checks
passed with 24 prompts, zero tokenizer/model calls. Final bytes equal the existing
prepare_core.jb canonical encoding; root fsynced and checked exact readback.
Separate read-only runtime verification passed66 source/dependency pins, the
pinned Python/package identities, and all six actual tokenizer asset byte pins.

This full lock must be committed and raw-Git-verified before a separate root
preparation release. This document and lock grant no tokenizer or model launch.
The later one-shot preparation must obey313 operations,320 tokens/no truncation,
175+5 seconds and combined16 MiB, with failure preserved and no rewrite/retry.
Any model release remains separately gated on independently accepted actual
preparation/closure evidence, committed exact release and zero-model preflight.
