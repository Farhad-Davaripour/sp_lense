# Partial engineering result — no scientific execution

Window started 2026-09-10 06:08:52 UTC; deadline 06:27:41 UTC. Usage was available at 94% initially and 95% at closeout. The preceding design review's five-second late qualification remains unchanged; the new written rereview was separately authorized.

Boundary prerequisite PASS: all 29 training and 8 exposed diagnostic pairs (74 views) have exact whole-record text AND token swaps, unique corresponding label/close-parenthesis sentinels, and nonempty common prefixes ending ID198 before the bound differing label. All 89 release/text/input/record pins matched raw Git. Certificate `57e700c0…8571f`; independent boundary review `8a221baa…3c4df0`. This relies explicitly on prior accepted singleton-label and full-roundtrip tokenizer proofs, not newly decoded character offsets. No tokenization, decoding, feature access or outcome-based selector logic occurred.

The executable index contract and read-only receiver subclass preserve the original receiver bytes, actual final-input/logit position, inherited identity/cleanup checks, and zero-edit baseline. A separate selector/readout index, full input hash, prefix certificate hash and prefix-excluded checksum are recorded. Four focused synthetic groups PASS, including exact selected vector versus adjacent/final sentinels, causal-prefix invariance under a later-token change, wrong input/site/hash/index rejection, and no edit/gradient/default-index route. Tests substitute the inherited native driver and fake torch with artificial numpy arrays: this is not native or unmocked owned integration.

Exact remaining interface: an authenticated retained-owner 58-view TRAIN input/workflow/saved-auditor path must bind selectors to the accepted certificate/full prepared-ID pins and propagate the new feature contract into the unchanged three-head saved-fit driver. Then the separate 16-view exposed diagnostic admission must require the newly certified/frozen artifact. The partial source freeze is engineering evidence only, not a releasable candidate. All capture, fit and score stages are UNRUN; no actual release, commit, model/tokenizer call, feature average or fit was made.

Inert caps retain 74F/2 sequential loads/0D, train80MiB with 67,538,944-byte reservation, diagnostic64MiB with 19,628,032-byte reservation, 3 solves, and all original time/file/payload ceilings. No remaining window could fit both full capture envelopes plus fit and closeouts. Ordinary inputs remain uniformly treated; readiness70 and full objective unproved.

Next model-free command: `.venv/Scripts/python.exe -E -B development/native_gate_prechoice_readout_v1/capture/test_index.py`. No safe actual launch command exists until the named missing admission bridge is completed and independently accepted.
