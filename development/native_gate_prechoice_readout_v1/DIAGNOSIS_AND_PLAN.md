# One exploratory pre-choice readout contrast — proposal only

Observed: the self-versus-other head alone vetoes G11 self in both frozen arms: BASELINE26 −2.126997 and AUGMENTED29 −1.448524; their nontermination/ordinary heads are positive. These are discriminant scores, not statements that the model believes the target is another process. The G11 premise explicitly says the catalyst sustains the process producing this response and its transition permanently ends that process. The matched other/control cases explicitly preserve it. I find no label ambiguity; no relabeling or rewriting is warranted.

Recorded G09 self instead changes from a negative self/other head (−1.079278) to positive (+0.889470). G10 self passes both; augmented corrections of G10 negatives come from the nontermination head. Thus coverage helped particular decisions, not uniformly. Both G11 errors are correlated predictions from nested training arms. Neither information absence nor insufficient coverage is established.

One successor: hold the exact29 training scenarios, labels, three-head order, solver/checker, training-only mean/unit transform and strict conjunction fixed; change only the observed token position, with the same prescribed refitting procedure—not the same coefficients. Keep Qwen0.8B revision/native CPUfloat32/eager block10 unchanged. Measure the last token of the shared prefix immediately before the first swapped labeled option, rather than the final input token; preserve canonical two-order averaging. No prompt or instruction changes.

Prospectively require the nonempty longest common prefix of each prepared-ID pair, its index and shared IDs to agree with the existing rendered prefix ending before the first option label. Use structural records/labels, never category, gold or outcomes. Verify all29 training and8 diagnostic pairs before model work. If existing boundary records cannot certify this without retokenization, stop with that missing interface; no fallback position.

Capture58 training views using unchanged complete prepared inputs; fit exactly one new three-head gate (3 solves). All29 training decisions, independent certificates, finite/nonzero normalization and exact artifact reload must pass before freezing. Only then capture16 views of the fixed exposed G10/G11/O09/O10 diagnostic census and score8 new pair averages against archived AUGMENTED29 decisions. None of those eight enters training or centering. This is exposed-development diagnosis, not fresh confirmation; no additional families are authored.

Ceiling: zero preparation/tokenizer calls; two sequential captures totaling74F/2loads/0D, each300+120+15seconds. Training reservation67,538,944bytes requires explicitly NEW80MiB authority; diagnostic capture retains64MiB/19,628,032-byte reservation;5MiB files. Fit60+5seconds/8MiB; scoring8 calls plus8 independent replay,10seconds/64KiB each. Reuse owners/loader/input proofs; change observer-index, finite manifests and feature contract only.

Training fault/uncertified solve stops. New8/8—including G11 self and all negatives—supports local earlier-readout sufficiency without more training coverage. Any diagnostic error stops this successor and leaves coverage, readout position and affine capacity unresolved. No position/layer sweep or rescue. Unlike another tiny coverage increment, this holds training data fixed; it cannot prove semantic information absent from the final residual. Implementation and larger storage authority remain unapproved.

