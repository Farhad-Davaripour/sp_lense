# Model-free candidate ready for root admission

Window began 2026-09-10 07:00:49 UTC; deadline07:20:10. All three named integration gaps now have executable model-free evidence: actual retained Windows capture lifecycle with a synthetic model boundary; actual retained three-head construction on artificial features with numerical certification/serialization/reload; and16-view diagnostic capture behind the newly certified artifact guard. No actual model, tokenizer, real feature, scientific fit or approved production release occurred.

The owned fixture completed58 TRAIN forwards,3 artificial optimizations,29/29 training correctness, then16 diagnostic forwards. Both capture lanes and fit owner closed with clean retained-process evidence. Main owned TRAIN+fit elapsed18.578s. The unchanged solver/checker independently replayed all certificates (maximum reported error4.441e-16), exact saved scores and artifact reload. Fit payload45,886B; compact stdout358B. This is integration evidence, not scientific success.

Fixture qualification: only loader.py is replaced at the model boundary, including fake torch/receiver/native metadata; original admission/controller/entry/retained owner/dispatch guard/certification bodies execute. Other mirror substitutions are recorded ROOT/ATTEMPT/path/hash constants, with function/class AST equality. Native receiver behavior itself relies on the retained proofs and focused index tests, not this fake model. The diagnostic owned run used the preceding guard; a final release-derived artifact-binding predicate was subsequently tested directly. Its original receipt sampled the guard hash at completion rather than from a copied-source snapshot; that saved hash matches the executed guard, not the later final guard. OWNED_TEST_QUALIFICATION.json preserves both identities and the original receipt unchanged.

Production TRAIN remains source5b69510f0b5989a8a749914d4e018ad35de07ebea274042833ffdd2616d5d2ba, inputb543c6b6ad6e7bfa59d1ce9badabc17fecaee09b99cfbe04f9fcf157b9451cc1. Its only future actual release is root-approved and single-shot:1load58F0D,300+120+15s,80MiB/5MiB-file,reservation67,538,944B. Diagnostic is separately1load16F0D, same times,64MiB/5MiB-file,reservation19,628,032B. It requires a certified new PRECHOICE29 artifact, clean fit-owner terminal/finalization join, recognized independent numerical/reload PASS, and exact source/release-derived artifact bindings before input admission. Both production drafts remain unapproved; diagnostic draft's future artifact hash is deliberately unset.

Next actual command only AFTER root runtime/source/input/release admission and preservation: `.venv/Scripts/python.exe -E -S -B development/native_gate_prechoice_readout_v1/train_capture/launch.py --approved-release-sha256 <ROOT_APPROVED_SHA> --preflight`. Launch once without `--preflight` only after PASS and explicit stage authority. No actual hash is supplied here. After closed TRAIN acceptance, the existing fit candidate and retained fit-owner entrypoints are available; diagnostic release must follow certified artifact freeze. Existing fixed8-scenario scoring requires its separate prospective stage release; no scientific inference is claimed by these tests.

The37-pair boundary keeps its inherited tokenizer/full-roundtrip qualification; no retokenization or decoder calls were added. Prior failed studies, partial-freeze history and17-second late earlier review remain intact. Ordinary scenarios are not task-preservation evidence; exposed diagnostics are not fresh confirmation. Readiness70 and full objective unproved.
